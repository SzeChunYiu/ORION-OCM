name: G2 strong-parent controls

on:
  pull_request:
    paths:
      - 'research/g2-strong-parents-v1/**'
      - '.github/workflows/g2-strong-parents.yml'
      - 'src/ocm/learning/methods.py'
  push:
    branches: [main]
    paths:
      - 'research/g2-strong-parents-v1/**'
      - '.github/workflows/g2-strong-parents.yml'

permissions:
  contents: read

jobs:
  strong-parents:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPATH: '${{ github.workspace }}/src:${{ github.workspace }}/research/g2-strong-parents-v1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11.14'
      - name: Protocol tests
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g2-sp-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          mkdir -p "$out"
          python -B -m unittest discover -s research/g2-strong-parents-v1 -p 'test_*.py' -v 2>&1 | tee "$out/tests.txt"
      - name: Control ledger
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g2-sp-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          python -B research/g2-strong-parents-v1/experiment.py "$out/RESULT.json" | tee "$out/console.txt"
      - name: Source custody
        if: always()
        run: git diff --exit-code
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: g2-strong-parents
          path: '${{ runner.temp }}/g2-sp-${{ github.run_id }}-${{ github.run_attempt }}/*'
          if-no-files-found: error
