name: G5 factored warrant parent

on:
  pull_request:
    paths:
      - 'research/g5-factored-warrant-v1/**'
      - '.github/workflows/g5-factored-warrant.yml'
      - 'src/ocm/kso/warrant.py'
  push:
    branches: [main]
    paths:
      - 'research/g5-factored-warrant-v1/**'
      - '.github/workflows/g5-factored-warrant.yml'

permissions:
  contents: read

jobs:
  factored-warrant:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPATH: '${{ github.workspace }}/src:${{ github.workspace }}/research/g5-factored-warrant-v1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11.14'
      - name: Protocol tests
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g5-warrant-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          mkdir -p "$out"
          python -B -m unittest discover -s research/g5-factored-warrant-v1 -p 'test_*.py' -v 2>&1 | tee "$out/tests.txt"
      - name: Measure DAG vs antichain
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g5-warrant-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          python -B research/g5-factored-warrant-v1/experiment.py "$out/RESULT.json" | tee "$out/console.txt"
      - name: Source custody
        if: always()
        run: git diff --exit-code
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: g5-factored-warrant
          path: '${{ runner.temp }}/g5-warrant-${{ github.run_id }}-${{ github.run_attempt }}/*'
          if-no-files-found: error
