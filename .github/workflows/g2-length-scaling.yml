name: G2 length-scaling causal reuse

on:
  pull_request:
    paths:
      - 'research/g2-length-scaling-v1/**'
      - '.github/workflows/g2-length-scaling.yml'
      - 'src/ocm/learning/methods.py'
      - 'src/ocm/runtime/**'
      - 'src/ocm/kso/**'
      - 'src/ocm/store/**'
  push:
    branches: [main]
    paths:
      - 'research/g2-length-scaling-v1/**'
      - '.github/workflows/g2-length-scaling.yml'

permissions:
  contents: read

jobs:
  prospective-g2-study:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPATH: '${{ github.workspace }}/src:${{ github.workspace }}/research/g2-length-scaling-v1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11.14'
      - name: Freeze controls
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g2-length-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          mkdir -p "$out"
          python -B -m unittest discover -s research/g2-length-scaling-v1 -p 'test_*.py' -v 2>&1 | tee "$out/tests.txt"
          python -B -O -m unittest discover -s research/g2-length-scaling-v1 -p 'test_*.py' -v 2>&1 | tee "$out/tests-optimized.txt"
      - name: Prospective length-scaling study
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g2-length-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          python -B research/g2-length-scaling-v1/experiment.py --out "$out/result.json" \
            2>&1 | tee "$out/console.txt"
      - name: Source custody
        if: always()
        run: git diff --exit-code
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: g2-length-scaling
          path: '${{ runner.temp }}/g2-length-${{ github.run_id }}-${{ github.run_attempt }}/*'
          if-no-files-found: error
