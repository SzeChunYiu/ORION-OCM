name: G3 representation-insufficiency diagnosis

on:
  pull_request:
    paths:
      - 'research/g3-representation-diagnosis-v1/**'
      - '.github/workflows/g3-representation-diagnosis.yml'
      - 'src/ocm/learning/methods.py'
  push:
    branches: [main]
    paths:
      - 'research/g3-representation-diagnosis-v1/**'
      - '.github/workflows/g3-representation-diagnosis.yml'

permissions:
  contents: read

jobs:
  diagnosis:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    env:
      PYTHONDONTWRITEBYTECODE: '1'
      PYTHONPATH: '${{ github.workspace }}/src:${{ github.workspace }}/research/g3-representation-diagnosis-v1'
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11.14'
      - name: Protocol tests
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g34-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          mkdir -p "$out"
          python -B -m unittest discover -s research/g3-representation-diagnosis-v1 -p 'test_*.py' -v 2>&1 | tee "$out/tests.txt"
      - name: Frozen witnesses
        run: |
          set -euo pipefail
          out="$RUNNER_TEMP/g34-$GITHUB_RUN_ID-$GITHUB_RUN_ATTEMPT"
          python -B research/g3-representation-diagnosis-v1/experiment.py "$out/RESULT.json" | tee "$out/console.txt"
      - name: Source custody
        if: always()
        run: git diff --exit-code
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: g3-representation-diagnosis
          path: '${{ runner.temp }}/g34-${{ github.run_id }}-${{ github.run_attempt }}/*'
          if-no-files-found: error
