# L0 language bootstrap audit

**Decision, 2026-09-08:** the historical language runtime is convenience-heavy English prior, not a minimal substrate. A time-zero substrate with empty lexeme, morphology and construction stores is now registered. English instances are classified convenience and charged or stripped. This does not claim N1 learned understanding. L1 stays locked.

The L0 question is accounting, not capability:

> What language-specific prior exists before protected N1, and what remains after stripping convenience?

[INVENTORY.json](INVENTORY.json) lists 206 items across every required prior class. 29 are constitutionally necessary; 177 are convenience. 149 language-specific items exist in the current runtime and 0 remain in the registered minimal arm. [ABLATION.json](ABLATION.json) records the reduced-bootstrap recoveries and the SOV hostile. [RECEIPT.json](RECEIPT.json) binds the terminal.

## Terminal

```text
MINIMAL_LANGUAGE_SUBSTRATE_REGISTERED
```

Scope: empty object stores at time zero; retain KSO identity/warrant/scope/authority, ambiguity-preserving select, said-record speaker authority, reverse-read gate, MeaningGraph/Lexeme/Construction schemas without English fill, generic version-space and chart/match algorithms, and the exception-override law. Category tags, role registry, tokenizer and per-family semantic target schemas are charged when used. The current runtime is not this start state.

Protected N1 full-versus-minimal acquisition curves remain `CANNOT_CHECK_N1_LOCKED_PROTECTED_CURVES_NOT_RUN`.
