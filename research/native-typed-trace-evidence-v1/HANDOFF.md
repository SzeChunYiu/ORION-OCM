# Typed native structural seam — source tranche

The research module now carries explicit wff/class parameter types and exact native
floating labels through structural reconstruction, proper-chunk extraction and recipe
emission. It checks ordered holes, declared three-parameter contexts and the existing
node/proof limits. It enumerates all proper roots under the fixed two-to-eight semantic
DAG-node rule.

Ordinary alias matching permits composite and repeated substitutions. Unsupported
syntax stays UNKNOWN and noneligible. The Boolean expression parser preserves its
donor token/depth accounting; the class parser rejects undeclared legacy atoms.

The latest pure qualification passed 43 controls with zero native verifier imports
attempted. It includes actual alpha-renamed graphs, exact expected output/proof labels,
a class-constructor regression, generic multiple-root extraction, direct body bounds,
ordinary alias witnesses, DV and exact donor parser boundaries.

These are synthetic engineering fixtures, not native accepted proofs. This tranche
does not establish acquisition, persistence, fresh-process reconstruction, scientific
utility or typed OCM serving. Native source/fixture qualification remains a separate
closed gate. Historical native receipts are provenance only.

All six source/process generations are retained. The first two failed on separate
variable-shadowing defects; the exact expected controls were retained during repair.
Subsequent source review tightened floating/body scope and repaired the two grammar
boundaries. Their aggregate child-process windows were 1.739263248 seconds, excluding
authoring, independent review and packaging; this is custody information, not a timing
comparison.

A separate portable CI job pins Python 3.11.14 and pytest 8.3.5, collects only this
module's structural controls, prohibits native imports and retains receipt/JUnit data.
The module remains outside the installed OCM package.

The old class constructor used as a regression reference is separately pinned and
readback-verified. It is outside the module-only before/after control source map; its
identity is retained in SOURCE-MANIFEST.json and the independent review.
