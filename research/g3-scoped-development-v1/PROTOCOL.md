# G3 scoped developmental pilot v1

Evidence class E1/E2; contribution ceiling controlled mechanism, not protected independent replication. Owner: #165 C/D, with partial H and measured G coordinates. No production or frozen study source changes. Canonical core remains the existing OCMRuntime/KSO/operator registry/solve/check/commit/evidence ledger. This is not a new cognitive core.

## C: scoped failure memory

Import the actually selected #192 macro `square,dec,square` from the retained result (SHA256 896b39429a4fd520cfc7517e73cdd0efb27518a83d7e31f9e6cf99560401db49), retaining its origin and unpaid acquisition cost. The new operator searches only completions of this prefix, with the existing polynomial primitives and exact coefficient checker. A complete failed bounded prefix search is method failure, never task impossibility; the ordinary primitive fallback remains available.

A fixed prior feature menu (degree, value at zero, value at one, leading coefficient) proposes a scoped guard when a failed target's feature value is absent from the complete reachable set. An independently structured enumerator validates completeness and coefficient semantics. The certificate binds the full prefix, ordered grammar, length and attempt budget, environment/version, checker and source identity. No task ID or list of failed target fingerprints appears in serving guard keys. Incomplete resource exhaustion cannot create a general failure certificate.

Compare no failure memory, exact case memory, certified ordinary failure/CEGAR-style memory, analytic degree bound, primitive-only, and OCM with the identical certificate. No OCM novelty from ordinary-parent parity. Test fresh mathematical identities, renamed task labels, successes outside failure scope, larger-budget reopening, changed environment/prefix, live/withdrawn/restored support, and unrelated growth. Retain all acquisition, validation, storage, lookup, revision and process costs.

## D: representation selection and revision

Freeze candidate keys in this order: (length,value-at-zero), (length,value-at-zero,value-at-one), (length,exact-coefficients), (length,exact-coefficients,last-token). The language is an explicitly charged hand-authored prior, not invented by OCM. Require genuine training-fit ambiguity, then actual counterexamples/validation before choosing a representation.

Validate current observations, remaining horizon, ordered action identities and successor-key congruence over the declared finite state/action contract. Serving uses breadth-first search with duplicate elimination, not a table of stored task answers. Compare raw search, the same conventional semantic quotient and OCM. Protect the issued mathematical answer and shortest primitive depth, not identical redundant search traces or physical costs. Exact full coefficients are checked before commitment.

A newly admitted last-token observation distinguishes old coefficient-equivalent states. The old certificate must become inapplicable on the operator/observation inventory change before serving. Retain the separating witness, validate the refined candidate, admit it through the existing external boundary, restart and re-evaluate. No timeout licenses a representation jump.

## One lineage and controls

Use actual fresh OS processes at principal stage boundaries, with one persisted runtime root, exact source/manifest pins and chained expected ledger/state/evidence/operator identities. Revoke and restore actual evidence support. Failure records and representations are first-class payloads of existing field types; ordinary search failures never enter the logical contradiction/nogood store. Current authorization, correctness, acquisition and usefulness remain distinct. Compare control parents with the same imported knowledge and certificates.

G3.4 classifications require an explicit witness or remain CANNOT_CHECK. Missing support, bounded resources, an incomplete frontier, a finite missing-operator witness, representation aliasing and observation-channel indistinguishability are different conditions. Do not use planted root-cause labels as recovered truth.

## Allocation, resources and evidence limits

Use all exact polynomial identities with minimum primitive length 3 through 5 for the main finite development census; training attempts use minimum length at most 2. The #192 acquisition/validation/test strata have minimum lengths 6/7/8, so the census is disjoint by mathematical identity. Keep all cases, including cases favoring primitive-only and misleading guards. Separate explicitly named boundary examples from the census; never backfill or select the best after outcomes.

Measure wall, process CPU, Linux peak RSS, concrete persisted/archive bytes, checked program counts, validation and lookup work. RSS is a peak, not additive; nested timings are not summed. No energy/GPU or whole-lifetime payback claim from missing meters. The finite census shares one authored grammar and is not hundreds of independent lifetimes.

The formal, systems, cognition/library-learning and skeptical causal perspectives are review responsibilities applied by one author; they are not four independent human reviews. Principal intellectual parents are truth maintenance/nogood learning, counterexample-guided abstraction refinement, exact abstract interpretation/congruence, memoization and program-library learning. Their mechanisms receive first right of refusal.

## Admission and kill rules

Refuse a guard from truncated/forged/non-exhaustive evidence; refuse stale scope or altered source; reopen after support or inventory change; fail closed on missing or duplicated populations and incomplete processes. Report parent sufficiency, no utility, harmful transfer, high validation/maintenance cost and insufficient representation as first-class outcomes. Do not promote this authored pilot to E3/E4/E5, language/formal-proof cross-domain learning, three earned self-evolution generations, integrated #73 completion or final programme closure.
