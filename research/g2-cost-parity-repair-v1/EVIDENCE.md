# Source and preservation

[BASE](BASE.json) binds nine exact files at main7926c3: G2/G3 source, tests and
README, their two workflows, and the accepted G2 reconciliation note. No per-task,
validation/test outcome array or scientific result was opened by this repair.
Source reading covered the helper definitions/call sites, index construction and
lookup, G3 parent import/pin, existing control declarations and cost prose.

[Immutable references](UPSTREAM.json) retain those full-source identities without
redistributing the study files. [RAW](RAW.zip) preserves the isolated old helper
bodies, control/observer source and both RED/GREEN process histories. Only the
new scalar/list controls executed; no original study or suite was imported.
[RAW-MEMBERS](RAW-MEMBERS.json) binds each member to its original path.

[The minimal helper delta](patches/minimal-helper.diff) compares the isolated
preimage helpers with the new helper; it is not a patch to either frozen study.
[The additive integration patch](patches/additive-integration.patch) creates only
new helper/control/prose files under research/g2-cost-parity-repair-v1. It changes
no existing source path or workflow trigger. Root owns actual repository integration. The identical CLI control is published
as controls/check_parity.py to avoid accidental test discovery; RAW and receipts
retain the actually exercised test_parity.py name/path. The observer stays in RAW
with its original layout. No renamed-path qualification run is claimed.

[Direct copies](COPY-MANIFEST.json), [qualification receipt](QUALIFICATION.json),
[public inventory](FILES.json) and [readback](READBACK.json) keep the source-bound
control evidence distinct from this packaging step. No independent acceptance or
live adoption is invented; no historical claim is retroactively requalified.
