# SUPPLEMENT 1 — REV-L47-NOVEL-INTELLIGENCE-W4 closed green (post_freeze_edit_rule)

Branch `research/833-revival-w4-v1`. No V1-bound file was edited;
`BASELINE_MANIFEST_V1.json` and everything it binds are byte-identical
(anchor `31de4041…` re-verified by `build_manifest_v2.py` at build time).
This supplement + `BASELINE_MANIFEST_V2.json` + the owning-lane append
`research/gmi-833-corpus-passes-v2-v1/VERDICT_REGISTER_APPEND_REV_L47_W4_V1.json`
carry the change, exactly per the `post_freeze_edit_rule`.

## What moved

- **REV-L47-NOVEL-INTELLIGENCE-W4: open → CLOSED_GREEN.** Revival counts
  9 total / 3 closed / 6 open (assertion A9 at pin) → **9 / 4 / 5 now**.
  The pinned V1 register rows are frozen history and stay untouched.

## What was done (the registered lever, executed)

The corpus's single CONFIRMED post-hoc suspect
(`gmi-novel-intelligence-w4-v1`: freeze document committed 89 min after the
green result, double-verified, re-verified on this branch) was cleared by
**prospective re-establishment at original claim strength**:

1. **Freeze custody first**: `FREEZE_V2_PROSPECTIVE.{md,json}` committed
   (`56195abe`) before any executor/test/result of the new package existed;
   the package CI verifies the add-order mechanically on every run.
2. **Prospective re-run on remote compute** from the parent's pre-existing
   frozen seed, assumption set held fixed: the parent campaign re-run equals
   the parent `RESULT_V1.json` on **every** field (zero deep diffs); family
   law F(k), k∈{2,3,4}, and held-out k=5 all MATCH on the V6 search route.
3. **New blind falsification surface** (registered before any run):
   budget-derived extended held-out {6,7,8}; negative-control family G(k)
   (constant law — the instrument falsifier, absent); an independent
   no-import verification route (14/14 exact agreement rows; 11 brute-force
   tightness witnesses, zero decoders below m); two-isolated-runs-per-host
   and two-host byte-identical receipts.
4. **Verdict**: `PROSPECTIVE_CONTENT_CONFIRMED__SUSPICION_CLEARED`,
   D1–D5 all true. Result sha256
   `92b98621a149f2fc228d80552d0c89707374f1641b4ef11a6b66e52e797a38f8`;
   hosts `billy` (laptop, py3.8.10, 2×~3652 s) and `billy-laptop-old`
   (py3.14.4, 2×~3707 s).
   Claim ceilings unchanged:
   `W4_RESIDUAL_QUOTIENT_STRUCTURAL_DOMAIN_AT_REGISTERED_FINITE_FAMILY_SCOPE`
   and `NOVEL_INTEL_LADDER_W2_W3_W4_GREEN_AT_EXACT_RQM_FAMILY_SCOPE`.

## Honest disclosures

- The parent's 89-minute custody defect is **permanent** on its record; only
  prospective-ness of the CONTENT was re-earned.
- Extended held-out {6,7,8} is corroborating evidence, **not** a scope
  widening. k=6 MATCHed on the search route; k=7/k=8 exceeded the registered
  3600 s per-assay budget → recorded ASSAY_TIMEOUT per the frozen
  disposition, predictions confirmed by the two-route analytic agreement.
  The freeze's visit-count derivation understated the true lexicographic
  enumeration cost ((K−1)·K^K vs realized ~K^(2K−2); realized bound K_max=6
  at the 2^30 budget). Disclosed in the append record; no registered
  prediction was violated.
- F({2,3,4}) and k=5 legs restate the public parent registration — they are
  re-earnings under custody, not blind tests; the blind surface is
  {6,7,8} ∪ G ∪ the independent route ∪ the determinism protocol.

## U-NEW arrivals enumerated (arrival_absorption_rule)

- `research/gmi-novel-intelligence-w4-prospective-v1/` — new research/gmi-*
  package (revival evidence lane). Holds **no baseline assertion** until
  typed by the frozen mechanical re-run (census extraction, maturity
  scoring, claim-discipline registration, RAG strata, identification
  screens).

## Binding

`BASELINE_MANIFEST_V2.json` binds the owning-lane append record (frozen
component dir `research/gmi-833-corpus-passes-v2-v1/`) and self-binds this
supplement + `build_manifest_v2.py`. The workflow anchors V2's sha256
(loud-change rule) and checks builder determinism. Post-squash custody refs:
tags `rev-l47-w4-freeze-custody` (→ `56195abe`) and `rev-l47-w4-results`
(→ results commit).

## Third-party freeze violation restored + succeeded (disclosure)

Main's grammar-growth reconciliation (#897/#978, landed after this branch's
pin) edited the V1-bound
`research/gmi-833-g0-grammar-growth-v1/ISSUE_833_RECONCILIATION_GRAMMAR_GROWTH_V1.json`
in place (2881 → 3265 bytes) without a manifest supplement — their PR never
triggered the path-filtered baseline validator, so every later PR touching
this package inherited a red merge tree (the tamper-evidence alarm firing
exactly as designed). The validator's design gives in-place edits of
V1-bound files **no** supersession path ("every file it binds stays
byte-identical"), so this lane restored the V1 bytes exactly
(`95848277…`/2881 verified against V1's record) and preserved the
reconciled content **byte-for-byte** in the NEW successor file
`ISSUE_833_RECONCILIATION_GRAMMAR_GROWTH_V2.json`, bound in V2 per the
`post_freeze_edit_rule`. No reconciliation content was authored, altered,
or lost by this lane. The owning lane's narrative obligation (their own
supplement recording the change) remains theirs.

Additionally, the claim-discipline lane's E9 registrations append (three
NEW files in the frozen component — the sanctioned append shape, just
unbound) is bound in V2 unchanged. Both defects were invisible on main
because the path-filtered validator never ran on those lanes' PRs; any PR
touching this package inherited the red tree. Root cause for both: path
filters don't validate the whole frozen binding — closing that is the
owning governance lane's call, recorded here as an open obligation.
