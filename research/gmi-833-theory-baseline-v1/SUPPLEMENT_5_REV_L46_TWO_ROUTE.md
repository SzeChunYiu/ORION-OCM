# SUPPLEMENT 5 — REV-L46-TWO-ROUTE-PROGRAMME tranche 1 (V5 binding)

Revival ticket REV-L46-TWO-ROUTE-PROGRAMME (75 SINGLE_ROUTE packages get a
second, materially independent route). Tranche 1 delivered:

1. **Programme frozen before any conversion** (PROGRAMME_V1.md): mechanical
   prioritization (GREEN-mainline blast radius > comp_claims exposure >
   maturity; RANKING_V1.json, 75 rows, generated on billy-laptop from frozen
   artifacts only), the two-route conversion template (independence stated
   mechanically: AST shared-code audit, algorithm-class difference table on
   the registered D-X3 axes, cross-check against committed receipts,
   fail-closed exact agreement, negative controls), the acceptance gate
   G1–G5, and the effort model.
2. **8 conversions** (7 TWO_ROUTE_CONVERTED, 1 TWO_ROUTE_WITH_FINDINGS;
   316 exact agreement rows): formal-proof-audit, developmental-uncertainty-
   transport, capability-ceilings-v1, analog-semantics-closure,
   uncertainty-composition, dependency-aware-uncertainty-composition,
   structural-threshold-repair, learning-law-selection. Each adds an
   in-package `independent_oracle_v1.py` (stdlib-only, written from the
   claim specification), `l46_crosscheck_v1.py`, `test_independent_oracle_v1.py`
   and `ORACLE_RESULT_L46_V1.json` — the artifacts the frozen L46 screen
   recognizes, so a rescreen flips these rows to TWO_ROUTE.
3. **One registered finding** (L46-F1): the absolute BASE6 opcode price in
   gmi-structural-threshold-repair-v1 is interpreter-version-sensitive
   (route-2 raw count 7 on CPython 3.8 vs committed 6); every base-relative
   price and every other opcode total agrees exactly. Routed to tranche 2,
   not adjudicated away.
4. **Claim strengthening (no narrowing):** learning-law-selection LLS-6
   genericity upgraded from a two-vector probe to a binary-uniqueness family
   theorem; analog-semantics D1/D6 agreement extended beyond the 162-case
   grid; union bounds equipped with worst-case attainment witnesses and an
   independence-product discriminator.

Custody: all execution on billy-laptop (rsync out, scp back, sha256
verified both sides); nothing ran on the authoring Mac.

This supplement binds: VERDICT_REGISTER_APPEND_REV_L46_V1.json, the
programme artifacts, and the eight ORACLE receipts (see
build_manifest_v5.py). VERDICT_REGISTER_V1.json, REVIVAL_TICKETS_V1.json
and every V1–V4-bound artifact stay byte-identical.
