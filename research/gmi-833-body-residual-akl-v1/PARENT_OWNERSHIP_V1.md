# Parent ownership — `gmi-833-body-residual-akl-v1`

Assimilation first. Every mechanism this package leans on is owned by a parent,
and the parent is named before the delta. **Nothing in the machinery below is
claimed novel here.** The residual contribution of this tranche is stated at the
end and is deliberately small.

## Row A

| parent | what it owns |
|---|---|
| `research/gmi-833-tranche-ab-ac-lit` | the banned-term list, the crosswalk, the scanner and its compiled patterns. Route A of this package **imports that gate's own pattern** rather than writing a new one, and refuses to run if the pattern has drifted. |
| `research/gmi-833-ab-terminology-harness-v1` | `ABH-1`/`ABH-2`/`ABH-3`: the coverage audit, the separation of the audit clause from the corpus-state clause, the frozen baseline and the blocking repo-wide ratchet. **`ABH-2` already established that the corpus-state clause is not discharged** and already recorded the comment-side twin of this row as not earned. |
| `research/gmi-833-terminology-migration-v1` | the executed migration over the non-`aj` `gmi-833-*` corpus and its logs. The repair of the residual measured here **belongs to that lane**, not to this one. |

External parents of the terminology itself: Rice 1976 (*Advances in Computers*
15, 65–118, doi:10.1016/S0065-2458(08)60520-3) for the algorithm-choice
vocabulary; Alur et al. 2013 (*FMCAD*, doi:10.1109/FMCAD.2013.6679385) for
syntax-guided specification; Wolpert & Macready 1997 (*IEEE TEC* 1(1), 67–82,
doi:10.1109/4235.585893) and Wagner & Altenberg 1996 (*Evolution* 50(3),
967–976, doi:10.1111/j.1558-5646.1996.tb02339.x) for the surrounding terms.
These are cited as the parents of the vocabulary, not re-verified here; the
parent crosswalk self-flags most of its citations `CITE-TF`.

**Delta.** `ABH-2` measured the whole banned list over the flagship set (9,703
sites in 1,400 of 2,572 files). That is a different quantity from this row's,
which names **one** term. This package measures the row's own term, under three
declared scopes, and separates the part the row's preservation clause requires
to remain from the part that is debt.

## Row K

| parent | what it owns |
|---|---|
| `research/gmi-833-capability-predictor-v1` (#1012) | `F = C_hat(M,E,R,H,D,U)` itself, `KP-1A`…`KP-1D`, the failure taxonomy, the typed uncertainty and the single `emit` funnel. `F` is **not modified, re-derived or re-fitted** here; this package only rebinds the registered universe through the parent's own `install_universe`, which proves in-process that `F`'s code is unchanged. |
| `research/gmi-833-capability-predictor-evaluation-v1` | the three real populations, the torch training protocol, the external evaluator, `KE-1`…`KE-7`, the positive fact `KE-3`, the boundary `KE-3D`, and the `FREEZE_V3_ADDENDUM` §6 terminal that forbids a fourth fitted registration law. **All three real-system receipts and every trained system are theirs.** |
| `research/gmi-833-real-transition-receipts-v1` (#903) | the banded-classification discipline the parent imported for its V3 measurement predicate. |

**Delta.** The parent asked whether a *particular* bridge is truthful and
measured 19/32, 28/32, 26/32. This package asks the prior question — what any
bridge that is truthful **by construction** can deliver — and answers it
exactly, with the resolution curve as the cost of the obstruction. It also
states, as `BR-3`, the conditioning property of `KE-3` that the parent's own
wording implies but does not name.

## Row L

| parent | what it owns |
|---|---|
| `research/gmi-833-real-developmental-validation-v1` | the developmental and continual-learning validation, `RV-*`, `HIST-1`, the three registered revivals, and the recorded reason this row is open. |
| `research/gmi-833-developmental-reuse-v1` | the reuse and grammar-growth results, the frozen fixtures, and the rows of section L that are closed. |

**Delta.** The parent recorded the obstruction in prose — *futurity cannot be
manufactured in-session* — and did not attempt the row. This package turns that
sentence into a checkable criterion with a validated checker, and reports the
measured count of admissible candidates, so the next lane inherits a gate rather
than a sentence.

## What is NOT claimed novel

The predictor, the universes, the real receipts, the terminology gate, the
crosswalk, the migration, the reuse results, the developmental results, the
banded predicate, and every external reference above. Also not claimed: any
assessment of whether those parent results are correct.

## The residual contribution

Three row **dispositions**, each decided by a rule fixed before the evidence
existed, each supported by two materially independent routes, and each
accompanied by the exact quantity that keeps its row open.
