# AJ10 — prospective UNKNOWN channel and strongest-parent reduction

The discovery protocol was frozen before the held-out truth vector, outcomes, taxonomy matching and parent review. Search sees only a generic Boolean substrate, protected I/O, raw two-coordinate resources and two generic representation/search families.

## Pre-taxonomy outcome

Held-out `H1` is solved exactly by two Pareto-incomparable candidates:

- `T232`: a complete 8-row response table, raw resource `(evaluation_operations=1, persistent_description_cells=8)`;
- `E4`: `OR(AND(x0,x1),AND(x2,OR(x0,x1)))`, raw resource `(4,0)`.

Both are exact on all eight protected cases. With raw Pareto resources primary and no frozen scalar prices, neither dominates the other and no unique morphology is selected. Capability/resource profiles are committed before taxonomy access.

## Post-hoc taxonomy

Only afterward is AJ9a consulted. `E4` fits registered K10 at this scope because the returned artifact is an executable expression produced from a declared expression language under a complete behavioral specification.

`T232` has no forced registered-family label and enters the explicit `UNKNOWN_MORPHOLOGY` channel. Strongest-parent review then identifies a classical finite truth-table/lookup-table representation. It is therefore `PARENT_REDUCED_KNOWN`, not a novel MI form. The pre-taxonomy UNKNOWN record is preserved rather than retroactively rewritten.

This distinction is intentional:

`not in registered taxonomy != novel`.

## Novelty levels

Every candidate is separately assessed for implementation novelty, morphology/architecture novelty, algorithmic-mechanism novelty, computational-class novelty and capability-profile novelty. No level is promoted to novel in this microscope.

## Replication rule

Any future `NOVEL_AT_REGISTERED_SCOPE` claim requires independent replication. No novel form is claimed here, so that gate is not triggered. The protected behavior itself is independently reproduced through two materially different representations/search procedures.

## Parent context

Novelty search (Lehman & Stanley 2011), quality-diversity/MAP-Elites (Mouret & Clune 2015), and open-ended-evolution literature are parent approaches for seeking behavioral/diversity novelty and open-ended innovation. AJ10 does not reclaim those ideas. Its contribution is a fail-closed GMI discovery ordering: freeze -> generate/evaluate -> taxonomy -> strongest-parent reduction -> novelty/UNKNOWN disposition.

## Claim ceiling

`AJ10_PROSPECTIVE_UNKNOWN_CHANNEL_AND_POSTHOC_PARENT_REDUCTION_AT_REGISTERED_FINITE_SCOPE`

Forbidden: `NOVEL_MI_DISCOVERED`, `GMI_DISCOVERS_ALL_MI`, or `COMPLETE_GMI`.