{
  "schema": "GMI_833_DEVIATION_DEPENDENCE_V1",
  "package": "gmi-833-update-law-space-v1",
  "issue": 833,
  "why_not_in_RESULT_V1": "RESULT_V1.json is regenerated by the executor and gated byte-exact in CI (git diff --exit-code after a re-run). A hand-added field there would break reproducibility, which is the gate working correctly. This record is a separate committed artifact instead.",
  "deviation": "D1",
  "deviation_description": "composition's quiet-step clause, adopted after the first run returned monoid_identity_ok=false; derived from AX-3, which was registered before this tranche",
  "shape": "the #976 POST_HOC_SUSPECT ordering: the clause postdates the observation it repairs",
  "dependent_results": [
    "IL-1.4 two-sided monoid identity"
  ],
  "deviation_free_fallback": "left identity only",
  "sensitivity_test": {
    "claim_tested": "the supplement's inspection claim that rejecting D1 moves nothing else",
    "method": "re-ran update_law_space_v1.py with the quiet-step clause forced off (quiet = is_noop(...) replaced by quiet = False) and diffed every leaf field of the regenerated RESULT_V1.json against the shipped run",
    "host": "laptop-billy",
    "certificate_leaf_fields": 774,
    "fields_changed": 1,
    "changed_fields": [
      {
        "path": "/IL_1/monoid_identity_ok",
        "with_D1": true,
        "without_D1": false
      }
    ],
    "stdout_summary_diff_lines": 0,
    "most_at_risk_field": "composition closure (49/49) - D1 modifies the composition operator itself, so closure was the field most likely to move with it; it does not move",
    "conclusion": "the D1 exposure is exactly one boolean; the 686/686 mixture census, the 49/49 composition-closure census, the 3450-case IL-2/IL-3 grid and every IL-4 field are invariant to the clause",
    "verdict": "INSPECTION_CLAIM_CONFIRMED_BY_MEASUREMENT"
  }
}
