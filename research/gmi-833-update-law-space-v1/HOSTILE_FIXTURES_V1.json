{
  "schema": "GMI833SectionIHostileFixturesV1",
  "note": "Deliberately planted hostile identifiers that the A1 lexical screen MUST flag, plus clean identifiers on which it MUST NOT alarm. Kept out of every screened source file so that the screen's own recall test does not become a self-hit; this file is a declared exemption of the screen for exactly that reason.",
  "planted_identifiers_that_must_be_caught": [
    "selfAttention", "BackPropStep", "gradient_step", "Conv2DBlock",
    "rag_retriever", "world_model_head", "lstm_gate", "SymbolicReasoner",
    "neuron_bank", "TransformerCore"
  ],
  "clean_identifiers_that_must_not_alarm": [
    "edge_multiply_accumulate", "retention_price", "probe_order",
    "interior_vertex_elimination", "improving_selection_query",
    "point_value_query", "slot_retention", "peak_live_internal",
    "development_closure", "worst_case_charge"
  ]
}
