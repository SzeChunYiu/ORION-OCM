# Ordinary-derived-theorem parent: independent review

Qualified acceptance as a prospective source-only design. No actionable correction
is required in the inspected CORE/CONTRACT generation. This is not implementation,
applicability qualification, experiment approval or evidence of equal performance.

The design gives the conventional parent the same checked acquired sequent as an
ordinary proved $p assertion, with its full independently qualified substitution
interface. It does not artificially restrict that parent to the recipe's six atomic
bijections. A separate common-action diagnostic is correctly labelled narrower.
The boundary-only ordinary action and recipe's internal-bank requirements are explicitly
separated; neither equal meanings nor unit action cost is treated as equal search space.

Library changes require a new exact prefix/index/checker binding, expected theorem
population, unchanged trusted axioms, proof dependencies and current policy eligibility.
No added axiom or inherited native receipt supplies the new theorem's authority.
The method/derived-label alias map and current warrant policy govern service use;
residency alone does not. Discovery provenance and independent correctness remain distinct.

Normalization is an additional validated operation: expand only added learned labels,
supply actual floating syntax proofs and ordered/repeated essential ports, retain original
parent labels, bind the reconstructed target and charge its cost. The current atomic-only
recipe constructor alone cannot qualify the parent's broader substitution domain; the
contract already requires separate qualified normalization and applicability before a run.
Proof equality and speed are correctly left to observations, not inferred from sequents.

The existing producer's nonalias eligibility is preserved. A typed-interface alias witness
can reconstruct independently while keeping serving-eligible IDs empty. Utility retention
of aliases is a separate future policy, not an implicit eligibility change here.

All seven source records and all their recorded excerpt lines match the pinned base or
historical producer bytes. No modules were imported and no learner/native/corpus/target
was run. Typed-wff qualification remains the immediate priority. Implementation details,
fixtures and the eventual execution protocol still require their own review and freeze.
