# Independent science/API review V28

## Result and independence

The reviewed production, independent oracle, driver boundaries and paper agree
at the frozen AB1–AB4 scope. No unresolved scientific or API defect was found.
This reviewer authored the paper and independently inspected implementation,
tests and proofs authored by other lanes; this is not independent paper authorship.

The original five literal statements are narrower than some old theorem names.
The new constructors bind their actual process/evaluator/admission/scalar data.
Only R2-009 is eligible, while R2-003/007 and the V16 qualified authority remain
unchanged. The driver explicitly records those limits.

## Actual production inspection

core_v28 caches actual parent modules and enforces their file identities.
checked_history validates every edge index before calling parent traversal.
Encoded retains actual V15 Context, injective labelled histories and an explicit
Fraction decoder; it does not confuse external values with internal integer codes.

forward_v28 uses actual V11 graph interpretation and singleton-state generator
functions. It keeps the complete two-generator free process fixed. Its separate
history observer admits empty/long words while evaluating only singleton words.
values_view and order_view compute different targets, as required.

reverse_v28 derives actual V19 Table/Typed categories and validates them.
Ambient/local codecs retain missing arrows and fixed four-edge labels.
Evaluation uses actual V16 generated membership and actual V15 illegal-first tags.
The four views differ intentionally. In particular the weak view retains ambient
state values even outside E, while active_domain contains only P∩E histories.

recovery_v28 uses recursive type-sensitive labels, followed by actual V9 attained
lookup. Verification checks exact codebooks and labels before lookup equations.
The empty attained image has an empty decoder. No default total extension appears.
scalar_v28 requires exact Fraction tuples around actual V12 arithmetic.

## Supplementary execution observed by this reviewer

Nine named controls ran on the laptop under normal and optimized Python.
Their JSON receipts were byte-identical under Python 3.12. These are supplementary checks of
frozen named boundaries, not extra primary population counts:

- Actual shared process, opposite rankings, admitted empty/two-step undefined queries.
- Unequal values with an equal induced order.
- Actual thin-category weak collision and unequal stronger views.
- Empty E, unequal failure tags and the pointwise admission decoder.
- Generated singleton recovery, a nongenerated callback and empty graph.
- True/int/Fraction separation and the empty attained decoder.
- Positive reversal, zero-weight loss, negative-weight reversal and equal-profile tie.
- Actual three-history V15 indicator construction and Allowed rejection controls.
- Whole-word validation, including a malformed suffix after an ill-typed prefix.

The script is /tmp/gmi-v28-independent-controls.py; its normal/optimized receipts
are /tmp/gmi-v28-independent-normal.json and -optimized.json.
Their source hashes bind the five reviewed production files.
Receipt SHA256: 8481e86a20e3a1f1cdd89faecd99d2d141a8b9ac7c3eb0d9a4f2440499e0b690.

## Independent primary evidence inspected

The separate oracle enumerates attained-image functions instead of calling
production recovery for expected answers. It independently traverses labelled
paths and constructs category tables, context fields, tags and weighted sums.
Strict recursive equality distinguishes numeric aliases.

The oracle's recorded ten-method normal/-O runs agree. Its observed populations
match the freeze: 9 forward contexts/81 pairs/162 recovery decisions;
64 reverse contexts/1920 tags/120 separate admission checks/4 roundtrips/
1024 recovery decisions; and 32 scalar comparisons.
Values versus order have 9 versus 27 recoverable ordered forward pairs.
Weak/active/tagged/domain reverse views have 64/192/256/256 recoverable pairs.
These counts are read from the oracle evidence, not claimed as a second exhaustive run.

Ledger mutations operate on the actual five-row ledger with a valid baseline.
The coverage guard requires the exact eight-module inventory and exact integer
counter values. Its checks reject Boolean aliases, omitted modules and counters.
Primary evidence is recorded separately from this review's nine controls.

## Custody and practical limits

The driver requires fresh kernel replay before its eleven-test suite and binds
all required documents. Source custody pins the freeze-only two-file commit,
31 direct inputs and the dereferenced inherited receipts, not just their names.
This reviewer ran the actual custody verifier: 677 source bindings passed, with
exactly GMI2-R2-003@r1 and GMI2-R2-007@r1 carried separately.
Qualified revisions are carried from the immutable V16 ledger and bound receipt.
Unavailable source/tool inputs remain distinct from checked failures.

The thirty-history signature separates generated domains because it includes
every singleton. It is not an enumeration of all histories or a certification
of arbitrary callbacks. Likewise two singleton forward histories do not enumerate
the free category. Kernel validity does not certify the Python adapters, prose,
CI or physical-model adequacy. The final canonical RESULT and successor checker
remain the authority for integrated execution and scoped accounting.
