# V28 — falsifiers, repairs and evidence boundaries

## Process and evaluator controls

A process-only collision must use an actual common process, not equal selector
names. The free category retains the distinct two generators, their repeated
words and the empty identity. Singleton evaluation uses the original functions.
Empty and two-step queries are admitted but undefined.

The generic opposite-evaluator construction requires two distinct active
histories, a strict ordered pair and Allowed membership for both actual maps.
Constant-only Allowed, collapsed histories, indiscrete order, unavailable
histories and non-Boolean membership callbacks expose missing premises.
The V15 three-history control distinguishes the registered indicator construction
from merely exchanging two coordinates of an arbitrary evaluator.

Values (1,0) and (2,0) have different value observations but the same induced
order. Recovery decisions for those targets must be separate.

## Admission controls

The missing false→true arrow distinguishes actual original thin categories.
C4/V4 supplies a composition distinction with equal admission and is excluded
as a substitute for this row.

With E empty, the weak and active views can hide admission while complete tags
and the singleton-complete domain signature recover it.
Collapsing ILLEGAL/UNDEFINED destroys the tagged decoder.
An all-defined evaluator makes active domain equal P, a positive converse.

An arbitrary callback accepting every singleton while rejecting a two-step path
fails the generated-domain premise. A singleton extractor cannot establish that
premise. Missing or swapped codecs fail the fixed labelled-interface premise.
Empty graph/domain cases must not require a fabricated default target.

## Scalar and codec controls

Zero weights can erase strict improvements; negative weights can reverse
coordinate dominance; equal profiles cannot strictly reverse under fixed weights.
Only the two registered positive vectors belong to the primary scalar population.

Type-sensitive recovery separates bool, int and Fraction values recursively.
Public adapters validate complete containers and all history indices, including
a malformed suffix after an earlier semantic failure.
Actual V15 integer value codes are distinguished from exact external Fraction
values by explicit decoders. Illegal-but-defined ambient values remain stored.

## Evidence interpretation

The deterministic A/B/C populations are frozen in FREEZE_V28.md.
Their planned sizes are 9 forward contexts, 81 ordered pairs with 162 recovery
decisions; 64 reverse contexts on 30 histories with 1920 tags and 1024 recovery
decisions; 120 separate generated-admission checks and 4 singleton roundtrips;
and 32 scalar comparisons. These are plans until actual replay records them.
Classified recovery outcomes and hostile counts must be measured from execution.

Independent finite expectations enumerate decoders on attained observations.
They must not call production recovery to obtain the expected answer.
Every hostile verifier has an actual unmodified no-alarm case.

Kernel hostile controls replace all sources or one of three strongest leaves.
A successful guard requires mutated SOURCES to compile and the unchanged typed
AUDIT to reject. Hash mismatch alone does not establish semantic registration.

Normal and optimized Python modes must give equal scientific receipts.
Missing prerequisites produce CANNOT_CHECK, distinct from a checked failure.
Review authorship is disclosed: the paper author independently reviews code and
proofs authored by the other lanes; this is not a second independent paper author.

All originals, old inputs and V16 authority remain immutable.
The source-keyed ledger guard checks exact identities, declarations and field
shape. It does not certify the scientific truth of arbitrary ledger prose.
