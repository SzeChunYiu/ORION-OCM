# V28 — original irreducibility witnesses and observation boundaries

This freeze and SOURCE_PINS_V28.json must be committed before implementation or experiments.
Package: research/gmi-1068-irreducibility-witnesses-v28.
Only GMI2-R2-009, "mechanize irreducibility", is eligible under original R2 freeze target9:
mechanize the finite irreducibility witnesses in Lean.
This is faithful finite-witness reconciliation, not proof of every phrase in an old theorem name.
Original R2-003/007 remain UNKNOWN; V16 revisions003@r1/007@r1 are immutable.
Do not substitute C4/V4 composition nonrecovery for differing admission.
If009 requires the false stronger full-source reading itself, leave it unresolved.
After this round passes, accounting is35 fulfilled/187 unresolved originals,
with2 separately qualified replacements/185 active unresolved. Whole-round statuses remain unchanged.
V27 merged as8a10a2a6720ccecab9ad1adaa36e3cc18613b56a after61 successful checks.
The starting main tree and V27 receipt/accounting were independently verified.

## AB1 — literal original source and actual fixed-process bridge

Freshly compile immutable ContextIrreducibility.lean and register all five literal theorem types.
Bind actual Action constructors, processStep, ctx0/ctx1, reach0/1, stateValue and score21/score12 equations.
The original reverse theorem states different reachability but does not mention stateValue.
The original forward inequality does not mention processStep; explicitly repair both semantic bindings.

Use actual V11 free paths on one Unit object and the TWO original Action generators.
Do not identify a0 with the identity, collapse a repeated word or replace the free category by C2.
Both generators interpret to the original singleton-state processStep.
Histories h0/h1 are the distinct one-generator paths; empty path is a separate identity.
Both models share the same complete path category, all typed paths admitted, and evaluation domain E=length1.
Actual V15 partial Context k_m evaluates a singleton by the corresponding original ctx_m.
Outside E the evaluation is undefined; an implementation default is never a defined value.
Prove equal process and evaluation domains, opposite actual singleton rankings, unequal contexts/order views,
and V9 attained-image nonrecoverability of context/order from the unchanged process.
The generic V15 separation theorem retains its explicit Allowed membership and strictness premises.

## AB2 — actual lawful reverse models with an explicitly weaker observer

Construct Bool thin categories directly from actual reach0 equality and reach1 equality-or-false-to-true.
Derive reflexivity/transitivity and actual V11 identities/composition/laws; no supplied category conclusion.
Bind Hom inhabitation to the original relation and prove the two actual category records differ.
Both models retain exactly original stateValue. That ambient state-only observation is identical;
admission differs at false→true and cannot be recovered from that weak observation.
Register a strongest leaf containing SAME stateValue, ACTUAL category/admission difference and nonrecovery.

Use a common labelled ambient Bool graph with one edge per ordered endpoint pair.
Generated raw-history admission is actual V16 Admitted from the corresponding primitive edge predicate.
The endpoint-value rule is unchanged, but the full admitted source domains differ on singleton(false,true).
A Context stored separately from P is not the full (P,k) record. Keep these signatures distinct.
No physical admissibility law or independence under every context signature follows.

## AB3 — positive full-domain and three-tag recovery boundaries

Freshly register actual V16 admitted_single, decode_admission and domain_eq_iff_admission_eq.
These concern the FULL generated admitted history domain on a fixed labelled graph.
The active evaluation domain D=P intersection E alone need not recover P for partial E.
Using actual V15 observe and Outcome constructors, prove for arbitrary H,W,P,k,h:
observe(P,k,h) differs from ILLEGAL iff P(h).
Derive equality of tagged observations implies equality of admission predicates.
Construct the actual attained-image decoder tag→(tag differs from ILLEGAL), with no choice/inhabitation premise.
The reverse Bool pair therefore cannot have equal full tagged observations or equal full raw domains.
With an all-undefined evaluator, active domains coincide while ILLEGAL and UNDEFINED distinguish P.
Explicitly collapsing those two tags revives ambiguity. This does not contradict weak-state nonrecovery.

## AB4 — original scalar witness with complete premises

Bind the original two binary profiles and the positive weight vectors(2,1)/(1,2)
to actual V12 finite sums and its actual Int instance.
Prove coordinate incomparability, strict positivity, exact scalar values, and both original Nat-score reversals.
No universal fixed-linear aggregation or world-probability prior follows.
Zero and negative weights are controls against extending the strict-positive law without its premises.

## Deterministic independent calibration

A. Actual V11 graph has one object and ordered generators a0,a1; two singleton histories form the finite roster.
For every reward vector in{0,1,2}², lexicographic order, construct the actual V15 Context on that roster.
There are9 contexts and81 ordered context pairs. Keep ties and exact source/history encoders.
The literal(1,0)/(0,1) witness is designated. Compare actual process data, values and order observations.
For each pair independently enumerate TWO attained-image decoders: process-to-context-values and
process-to-induced-history-order. The81 pairs yield81+81=162 decisions; different values may share an order.
Count9 contexts,18 singleton tags and81 actual unchanged-process comparisons.
Named empty/two-step queries retain admission but have undefined evaluation under E=length1.
This finite roster is not a claim to enumerate the infinite path category.

B. Ambient graph: vertices0,1; edge IDs ordered(0,0),(0,1),(1,0),(1,1).
Four reflexive Bool relations are all transitive; enumerate optional cross-edge masks0..3.
Build actual V19 Typed/Table thin categories plus explicit ambient/local arrow codecs.
For all4 binary state-value maps and4 endpoint-definedness masks, construct64 actual V15 contexts.
Enumerate typed ambient paths length0..3 by ascending length/start/lex state walks:2+4+8+16=30.
Compare all1920 actual tags with independent edge-membership and endpoint arithmetic.
For each fixed value/E pair compare all16 ordered admission-model pairs:256 cases.
For each pair test four DISTINCT observations: weak state(+E), active domain D with values,
complete ILLEGAL/UNDEFINED/VALUE tags, and the generated-domain signature on the singleton-complete roster.
That finite signature contains every labelled singleton; it is not an enumeration of the infinite domain.
Actual V16 generated-domain equivalence connects this signature to equality of complete raw domains.
Independently enumerate attained-image decoders to the primitive edge-admission target:1024 decisions.
All classified positive/negative totals are measured after freeze, not invented beforehand.
Singleton decoding uses actual V16 source functions and fixed external labels:4 primitive roundtrips.
Independently count4×30=120 raw-admission checks, separately from the1920 evaluation tags.

C. All4 binary profiles, all16 ordered profile pairs, both original positive weights:32 comparisons.
Use exact values, coordinate order and incomparability; distinguish strict from weak improvement.
No random trials or unregistered primary enlargement.

## Named falsifiers, repairs and semantic custody

Forward: constant-only Allowed, collapsed action histories, non-strict value order,
illegal/undefined selected histories, malformed Allowed callback and the actual V15 three-history reversal.
Reverse: empty graph, missing/swapped singleton codec, and an arbitrary callback that accepts all
singletons but rejects a legal two-edge path. A finite callback sample cannot prove generated-domain equality.
Only an actual generated-admission constructor carries the general V16 premise.
Full tagged/domain views must destroy differing-admission collisions; D-only with E empty must retain them.
C4/V4 has the same all-word admission and may not certify differing-admission rows.
Scalar: zero-weight strictness failure, negative-weight domination reversal and equal-profile non-strictness.

Every certificate must accept its actual unmodified source before source/map/domain/tag/weight mutations.
Full input validation includes unused entries and later malformed suffixes.
Use exact tuple containers, int labels excluding bool and exact Fraction external values in Python.
Actual V15 values remain strict integer codes with a fixed explicit external decoder; undefined values are None.
Preserve illegal-but-defined ambient values while observation still yields ILLEGAL first.
A complete whole-word guard validates malformed suffixes before immutable parent traversal can short-circuit.
Recovery codecs distinguish typed values such as True,1 and Fraction(1), with actual V9 attained lookup exposed.
Kernel source-valid mutants: all sources empty; strongest forward leaf→True;
weak-state reverse leaf→True; general tagged-admission decoder leaf→True.
All mutated sources must compile before unchanged typed AUDIT rejects them.
Normal/-O receipts, custody mutation controls, no-alarm cases and strict successor integrity are mandatory.
Unavailable inputs are CANNOT_CHECK, not checked-negative or PASS.

## Fixed witness-ledger and adapter contract

WITNESS_TRANSLATIONS_V28.json has exactly schema,source,rows.
Schema is GMI_1068_IRREDUCIBILITY_WITNESSES_V28.
source has lean and paper objects, each with exact path and sha256 for the original package.
rows has exactly five ordered identities: ctx0_prefers_a0;ctx1_prefers_a1;
same_process_different_context;process_models_differ_under_same_context;
scalarization_can_reverse_incomparables.
Each row has original_id,original_statement,original_source,original_paper_section,
observer,constructor_bridge,omitted_data,positive_converse,premises,
proof_level,strongest_parent,falsifier,details.
original_statement is exact declaration text from theorem <name> through immediately before :=,
including whitespace/newlines. original_source is the fixed repository-relative Lean path.
original_paper_section is the complete exact heading line, with ## prefix:
rows1–3 use Theorem R2-1;row4 Theorem R2-2;row5 Scalarization boundary.
The other nine fields are nonempty strings. Structure validation is not prose-truth certification.
Guard cases: wrong/duplicate/missing/extra rows, changed literal statement/source/path/hash,
changed source bytes, wrong schema/keys, absent or empty/non-string fields; valid no-alarm baseline.

Adapters cache actual parent classes once; public containers are strict tuples.
Forward uses fixed value codes0,1,2 and decoder(Fraction(0),Fraction(1),Fraction(2)).
Its separate infinite-path observer admits every typed path and defines only length1.
ReverseModel retains actual Typed/Table, Graph, primitive mask and bidirectional ambient/local codecs.
Reverse values use fixed codes0,1; missing local arrows map to None; no labels are inferred.
Four view names are weak_state,active_domain,tagged,domain_signature, each retaining external labels.
Recovery exposes exact typed observation/target codecs and actual V9 attained decoder.
Independent expectations enumerate functions on the attained image without calling those helpers.
The empty model domain has an empty attained decoder; no arbitrary total extension is supplied.
Scalar adapter is exact Fraction validation around actual V12 dot/domination functions.
These are finite correspondence adapters, not replacements for general Lean statements.

## Authority, source pins and primary ownership

A source-keyed witness ledger retains each original phrase, literal theorem, actual observer,
constructor bridge, omitted data, positive converse, proof level, strongest parent and falsifier.
Original raw source and all replacement authority stay immutable.
SOURCE_PINS_V28.json binds the reviewed nine-Lean-source closure and actual Python V9/V11/V12/V15/V16/V19 dependencies,
the original freeze/theory/schema/checker/receipt and current predecessor snapshot/receipt before implementation.
Independent inventory:11 test methods across8 modules: forward2,reverse2,scalar1,hostiles1,
custody2,witness-ledger1,coverage1,kernel-guard1. Primary keys and APIs are detailed in
the explicit corpus above; named-control and hostile counts are measured against frozen case lists.
Python closure: V9 semantic; V11 paths; V12 scalarization; V15 context; V16 history+algebra;
V19 partial+categories; original check_r2 plus its schema/receipt reads (nine named Python files).
All31 exact input pins are in SOURCE_PINS_V28.json (SHA256 f676f1c67f608e8478a4600d399666ea22c9833d7a4d1b740cbce9bfffb40ca3).
The file is committed with this freeze; implementations may not change its pinned inputs.

Root read Stacks0013 Definition4.2.1/Example4.2.7 and002Z preorder-category construction:
https://stacks.math.columbia.edu/tag/0013
https://stacks.math.columbia.edu/tag/002Z
Root read Riehl Example4.1.13 printed137 for free paths:
https://emilyriehl.github.io/files/context.pdf
Root read Boyd's primary Lecture7 scalarization transcript:
https://see.stanford.edu/materials/lsocoee364a/transcripts/ConvexOptimizationI-Lecture07.html
Actual V9/V11/V12/V15/V16 proofs provide the mature mechanism and explicit hypotheses.
No new irreducibility mechanism, empirical intelligence result or novelty claim is proposed.
