# Formal scope V28

The new proof package connects the five literal original finite witnesses
to actual process, history, context and observation constructors.
It preserves the stronger full-source refutations and V16 qualifications.
Only original GMI2-R2-009 is eligible under its finite-witness target.
Nothing here closes originals003/007 or promotes a whole round.

## Exact replay contract

The isolated Lean 4.19 replay stages nine immutable imports and eleven new
modules, then elaborates 205 static expected types. Each registration has
its assumptions printed; the checker does not rediscover replacement types
from changed source during replay. Source compilation precedes typed audit.
No unproved declaration or unsafe proof mechanism is used.
Ordinary Lean logical principles, including classical reasoning where used,
remain visible in the assumption reports.

All five original ContextIrreducibility statements are freshly registered:
ctx0_prefers_a0, ctx1_prefers_a1, same_process_different_context,
process_models_differ_under_same_context and
scalarization_can_reverse_incomparables.
OriginalBindings binds their actual Action cases, processStep, context
values, reachability relations, stateValue and scoring functions.
The original names alone are not treated as stronger semantic statements.

## AB1: original actions in an actual common process

ActionContextsV28.Graph has one Unit object and the two original Action
generators. The actual V11 Path.category retains arbitrary finite words,
both distinct singleton histories and a separate empty identity.
category, path identity/composition and generator interpretation are bound.
The terminal interpreter maps a generator to actual processStep; its
single-generator equation is proved using the actual V11 interpretation.

The two models share that complete category and admit every typed path.
Their actual V15 Context records have the same E: path length equals one.
The evaluator reads the original ctx0 or ctx1 of the singleton action.
The helper head has an off-domain default, but Context evaluation accepts
only the defined subtype. Empty and two-step histories are admitted and
observed as UNDEFINED, never as a default VALUE.

Both strict ranking reversals, unequal Context records and unequal
history-order observations are proved from these actual values.
orderView is a relation on the common raw history type, with explicit
definedness witnesses, avoiding equality between dependent subtype types.
The V9 collision theorem derives two separate nonrecoverability results:
fixed process to Context, and fixed process to the induced order.
Different value maps may still induce the same order in other models.

The generic V15 fixed/admitted separation results are freshly registered
with their original Allowed memberships, distinct histories and strict
codomain pair. Constant and collapsed observations retain their exclusions.
No theorem manufactures an objective or an Allowed evaluator.

## AB2: lawful reverse models and their weaker observer

ThinModelsV28 constructs two actual Bool categories from original reach0
and reach1. Reflexivity/transitivity are proved directly; identities,
composition and category laws follow. Hom inhabitation is equivalent to
the original relation, not an unbound selector with a suggestive name.
The categories differ at false-to-true admission.

stateObservation is exactly the unchanged original stateValue function.
Its collision proves nonrecovery of admission and of the actual category
record. This observation deliberately omits the admitted history source.
It is not a full-context nonrecovery theorem.

RawDomainsV28 uses a common labelled graph with one Unit generator per
ordered Bool endpoint pair. Its actual V16 Admitted recursion defines
generated history admission; singleton crossing distinguishes the models.
The common total endpoint evaluator returns original stateValue.
The full (P,Context) records, relative admitted evaluation domains and
complete tagged observations differ. None is incorrectly identified with
the weak state observation.

V16 admitted_single, decode_admission and domain_eq_iff_admission_eq are
freshly registered, with actual nil/cons and singleton decoder equations.
They concern all generated paths. Agreement on a singleton-complete
finite roster is a separating signature only under that generated-domain
premise; no arbitrary callback is certified by finite agreement.

## AB3: a positive explicit admission decoder

TaggedAdmissionV28.not_illegal proves, for arbitrary history/value types:
observe P k h differs from ILLEGAL if and only if P h.
It permits partial evaluation, empty types and illegal-but-defined histories.
admission_eq derives equality of P from equality of full tagged functions.

decode q h is literally q h differs from ILLEGAL.
attainedDecoder applies that function to the value of an attained-image
subtype. Its application equation and correctness are separately registered.
admission_recoverable uses this actual decoder, with no representative
choice, inhabitance assumption or extension to unattained observations.
The underlying V15 observe function itself uses classical proposition
decisions; the decoder construction introduces no additional choice.

The actual all-undefined endpoint Context has empty active domains in both
reverse models. Its crossing responses remain ILLEGAL versus UNDEFINED.
eraseFailure maps both failure tags to none and preserves VALUE as some.
The erased functions coincide, displaying precisely the lost information.

## AB4: actual integer scalarization

ScalarWitnessV28 uses the inherited V12 Int instance, finite sums and dot.
The original binary profiles and weights(2,1)/(1,2) have exact definition
bindings. Coordinate incomparability, strict-positive weights, four scalar
values and opposite rankings are proved.
original_scores ties these actual Int values to casts of the original
Nat score21/score12 values; original_reversals reuses the literal old lemma.

Zero-weight strictness loss and negative-weight domination reversal are
separate actual Int controls. Equal profiles retain a tie.
The generic V12 monotonicity, strictness and incomparable-reversal theorems
are also replayed with their hypotheses.
No Fraction/Real kernel instance, probability prior or claim that every
aggregation is linear is supplied by this finite witness.

## Integration and evidence boundary

ContextBindings, ReverseBindings, ScalarBindings and ParentBindings tie
all relevant constructors, recursions, orders, original values, generated
admission, tags and scalar operations to the actual source functions.
The three independent ProofTargets leaves are full forward, weak reverse
and general admission-decoder contracts; replacing them with True cannot
satisfy the unchanged static expected types.

Lean proves the listed generic bridges and exact named models.
It does not prove correctness of Python adapters, exhaustive corpus loops,
decoder enumeration, ledger prose, custody implementation or CI.
Those have separately registered finite tests and independent expectations.
The infinite free-path source is not enumerated by the two-singleton roster.
All original stronger refutations, replacement authority and parent
ownership remain unchanged.
