# Typed native lifecycle — prospective protocol v1

This is a source-only preparation. It authorizes no native verifier, learner,
registered meaning bridge, corpus scan or timing study. A separate source/fixture
review and explicit root gate must precede one fixed lifecycle attempt.

## Fixed inputs and authority

Use the source-reviewed typed module at PR155 head
935ce5e08f361945476cebadaf9ea1a8650d5ff9 and the unchanged, prospectively reviewed
native-typed-wff-fixtures-v1 packet. Bind exact source files, two ordered task/proof
envelopes, native floating context, PREFIX.mm and all 4,191 ordinary PARENT contracts.
Do not replace the 20/25-label authored proof routes or enlarge their context.
No bank/search compiler is involved in this reconstruction qualification.

Preserve three homogeneous wff parameters, empty source/active/applied DV,
no extra floating leaves, atomic bijective reconstruction, 256-node source/body,
proper 2–8 semantic DAG-node and 4096 expanded-label bounds. The ordinary alias test
retains legal composite/repeated substitutions in its registered typed grammar.
The existing class case receives a bounded reconstruction regression from its
already admitted chunk payload, without rerunning the old producer or training.
The fixed class control is canonical ID
5c8ff07719c450616a56ebe26ab8983473c4ad1d95f208df330d4de3c2270a06 from the
sealed native-ocm-adoption-v1/bundle/METHODS.json projection (SHA-256
755634c8e95573075622a8a582cd8c9edebb917c3247a0e1f9578f957e751e51).
Its exact selected payload and producer/admission binding will be frozen as inputs.

## Producer A

Fresh native verification interprets the exact two authored training proofs under
the pinned prefix. Require exact theorem-population and trusted-assertion identity,
then obtain source-bound traces and only their required native contracts.

Run the unchanged registered Boolean bridge on each complete source task. Preserve
native acceptance separately from bridge correspondence and joint-meaning support.
Enumerate every generic proper candidate, canonicalize the full typed body and group
supports by at least two distinct joint premise/query signatures. Retain every
occurrence and support disposition, sorted deterministically by canonical body ID.

Run the complete ordinary one-step alias check over the unchanged catalogue for every
supported body. Preserve ALIAS, NO_ALIAS_IN_REGISTERED_DOMAIN and UNKNOWN separately.
Only complete nonalias checks can confer pre-admission learned-method eligibility.
An alias is eligible only for a separately named interface-witness qualification;
UNKNOWN is never counted as an alias or as a learned method. If any supported body
remains UNKNOWN after all classifications are recorded, stop before witness admission
with ALIAS_CHECK_UNKNOWN.

Fresh native checking admits all supported, structurally valid interface witnesses
under exact issued holes/target/normal proof. Compare the actual admitted-trace
constructor with the recipe emitter. Persist witness bodies, admitted own traces,
required contracts and source/admission identities. Keep discovery history separate.
A exits cleanly before the supervisor creates the serving projection.

## Projection and fresh consumer B

Create the projection once from exact exited-A state, with an explicit schema allowlist.
Keep each candidate's own admitted trace/proof and required contracts; omit training
wrapper traces/proof bodies, discovery histories and source-task result payloads.
Record eligible_method_ids and noneligible alias_witness_ids separately.

Fresh B receives only the projection, current pinned library/admission identities and
the fixed identity plus all five other atomic permutations. It never reads training
inputs/results. Reconstruct every projected witness by both constructor and emitter;
require exact proof/target/ordered-hole equality, then independently native-check
the exact issued claims. Original receipt status is not fresh proof authority.

## Controls, costs and terminals

Freeze source/request/payload tamper, wrong exact hole, wrong target, unsupported
type/DV/renaming, changed alias eligibility and interrupted producer/state controls
before dispatch. Structural refusals and native wrong-proof refusals are distinct.
Preserve every failed/partial generation; no automatic retry or outcome tuning.

Record complete per-process wall/CPU/RSS, bytes, native calls, bridge work,
candidate/alias work, projection work and all failed-attempt costs. These are custody
costs, not a speed or lifetime-benefit comparison. Charge every cold construction.

Possible terminals include no supported candidate, alias check UNKNOWN, native or
reconstruction refusal, TYPED_INTERFACE_QUALIFIED_ALIAS_ONLY with empty learned
eligibility, and typed interface qualification with separately enumerated nonalias
eligibility. None establishes useful acquisition, search benefit, corpus opportunity,
typed OCM serving, novelty or general language/mathematical ability.
