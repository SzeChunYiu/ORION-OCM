# Dependencies and custody

[Source-input provenance](SOURCE-INPUTS-01.json) binds the original copied files.
[The source freeze](candidate-02/SOURCE-FREEZE.json) binds all 26 executing Python
sources and 22 inputs. [The run request](candidate-02/RUN-REQUEST.json) records the
exact laptop command and fixed cohort. A separate root gate is still required.

| Component | Source and use |
| --- | --- |
| Typed context, constructor, extractor, emitter and alias matcher | Exact PR155 source; main119e35b2 and reviewed935ce5e share treebbd41341. |
| Boolean/class grammar | Existing typed donor parsers; no grammar expansion. |
| Meaning bridge and signature grouping | Existing registered Boolean bridge; no execution in this milestone. |
| Native checker | Pinned mmverify with its [MIT license](candidate-02/authority/mmverify.LICENSE); existing source index and trace adapter. |
| Native wrapper | Fresh exact-claim wrapper; [independent source review](records/review-inputs/NATIVE-WRAPPER-REVIEW.md). |
| Lifecycle glue | Source-bound producer, post-exit projection, direct proof-label transport, consumer and process custody. |
| Fixtures | Exact authored 20/25-label routes; no expected learned body or candidate selector. |

The supervisor checks the complete input freeze. B checks executing source files
and its declared library/metadata/class inputs, plus the exact serving projection
and issued request. Its audit hook refuses teaching input/result reads. Only the
native checker interprets the closed prefix; custody code also hashes its bytes.

The serving projection contains each witness's own newly admitted proof and trace,
required contracts, typed context and opaque provenance identities. It excludes
teaching wrappers and discovery histories. A prior accepted flag supplies no fresh
native authority.

The two reconstruction mechanisms must match the issued proof, target and ordered
holes. The request oracle transports the admitted normal proof labels directly,
without calling either mechanism. All six atomic bijections are requested for
each typed witness, plus one identity regression of the fixed old class payload.

The first data-only control process completed eight checks; its outer observer
then failed because the host system Python lacked a wait-status helper.
[The failure](records/preflight/OBSERVER-FAILURE-01.json), first receipt and source
remain. A pinned-Python observer retained the second clean eight-check process.
No native, bridge or learner execution occurred in either preflight. The first
outer observer's exit status, CPU and complete process window are unavailable;
they are not zero. The completed inner control receipt retains its narrower wall
window.

Common helper I/O and native-wrapper I/O have separate counters. Complete Python
import-byte counts are uninstrumented; B's open-attempt audit is not a byte count.
Fresh-process wall/CPU/RSS have explicit
boundaries. Final receipt writes and interpreter imports are identified where
excluded from a component counter; the complete process window includes them.
These records are qualification costs, not an optimization or lifetime study.

Candidate 02 binds each native-negative refusal to the exact issued claim after
the complete current prefix verifies. Failed B runs retain initialized or
uninitialized audit state and accumulated common/work counters. Discovery records
each completed context and candidate before later hard gates. Source custody after
execution remains anchored to the opening request-bound raw manifest, even if a
replacement manifest would be internally consistent.

The focused repair controls use small receipt data and explicit bridge/extractor
doubles. They do not execute the registered bridge, discover a mathematical method,
or exercise current native authority. The five guard checks bind an intermediate
source whose helper AST is unchanged in candidate 02; the recovered exact source
matches the hash retained by that test.
