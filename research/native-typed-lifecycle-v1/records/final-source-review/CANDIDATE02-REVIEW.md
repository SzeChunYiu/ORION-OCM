# Typed lifecycle candidate02: independent successor review

**SOURCE_AND_APPARATUS_QUALIFIED_FOR_ROOT_GATE.** The four candidate01 findings are resolved in this exact successor. No remaining blocker was established within the reviewed boundary. This is source qualification; native, registered-bridge, acquisition and lifecycle results remain unobserved. Root must authorize the registered execution separately.

## Exact generation

- Source request: `2624b2586b05d85ccabd539793e3d2ad96b2f1edb43db0729a3bb31b1a6c48bf`.
- Raw source freeze: `f60811fd320de4725a1c4edef52e2a77f69b0606f492f305923bcf08416cf5b4`.
- Run request: `3226c08b5d3ca8297ebe2a6777d3b248f814a0e8995f358cd06bdd19d7efe8dc`.
- The 26 current sources equal candidate02. Exactly six glue files changed from candidate01. All native-wrapper, typed-engine, authority, payload, request-oracle and fixed input bytes remain unchanged.
- Candidate01 source, request/freeze and both original data-preflight generations remain intact. This review supplements [candidate01](CANDIDATE01-REVIEW.md), [the wrapper review](NATIVE-WRAPPER-REVIEW.md) and [the prospective boundaries](EXPECTATIONS-01.md).

## Repairs accepted

1. `run_b.require_native_negative` now requires a native-check rejection at the exact issued label, one actual native call and the complete verified prefix from the successful current reconstruction replay. A failure in an earlier prefix theorem cannot satisfy the negative control. The wrapper's unchanged exact-request and prefix authority checks remain necessary.
2. B records attempted opens before refusing a forbidden path. Invocation failure preserves audit state, explicitly distinguishes uninitialized audit, and retains common-helper I/O and active work. Wrapper invocation/time are recorded even if verification does not return; unresolved actual native calls are identified rather than represented as zero.
3. Discovery writes progressive context, bridge, signature and returned-candidate checkpoints before later gates. A subsequent failed context preserves earlier candidates and attempted/completed/accepted work separately. A also saves proposed discovery before alias classification and retains each completed classification. Generic extraction and the support/eligibility rules are unchanged.
4. Invocation, process observer and supervisor anchor final source checks to the opening request's raw freeze identity. A replacement manifest that consistently describes altered sources no longer passes that anchored check. Raw freeze identity and canonical manifest digest have distinct fields. The gate additionally checks the existing pinned executable and isolated/no-site/no-bytecode flags.

## Retained controls and custody

The new evidence is **five pure small-receipt guard checks** (PID 1735658) and **four glue checks with explicit data-returning doubles** (PID 1749543). The latter covers both audit states, a clean anchor followed by self-consistent manifest replacement, and a later meaning-gate refusal after an earlier returned candidate. Actual checkpoints and failure artifacts agree with the summaries. Both processes exited zero and were reaped; neither executed native authority, registered meaning or the learner.

The guard control originally used an intermediate 8,313-byte B source. Its recovered bytes exactly match the result's recorded hash; its guard AST equals candidate02. This establishes the unchanged tested helper, not a claim that the entire later B program was tested by that control. The four glue controls bind the final nine root Python files before and after. The earlier eight envelope/data controls retain their original source binding and were not rerun or relabelled.

[The data readback](CANDIDATE02-BINDINGS.json) contains 122 passing byte/schema/custody assertions and the exact read scope. These assertions are a review reader, not additional qualification experiments. It checks both real preserved source generations, all fixed input hashes, receipt/process bindings, the recovered guard AST and actual retained failure/checkpoint data. PREFIX and PARENT were hash-only reads in this successor review. No reviewed module was imported or executed.

## Execution boundary and reporting limits

The registered entry and A/B argv, fixed fixtures, six atomic reconstructions per admitted witness, one old class regression, candidate disposition policy and five-native-call completed path remain unchanged. A must exit before projection and fresh B. Alias witnesses remain noneligible; supported UNKNOWN stops before admission. A no-supported or UNKNOWN terminal is a valid bounded result, not a completed B qualification. The prepared outer-path record matches the registered argv and is preparation only.

B's open-attempt hook begins after common startup and the opening gate. It monitors scoped reads inside the lifecycle root; it is not a universal sandbox or complete import-byte meter. Source custody hashing reads prefix bytes; only native interpretation supplies proof authority. Discovery code is bound as source, while B's serving data exclude teaching bodies and B does not invoke discovery.

Common-helper and wrapper byte accounting are separate; import bytes remain uninstrumented. Process intervals cover imports. A failed extractor does not expose its unreturned internal list; completed candidates survive. Unreturned wrapper calls remain unresolved until actual evidence resolves them. The first data-preflight observer failed after reap, leaving its full outer exit/CPU/window unavailable; that history remains explicit. Missing or interrupted receipts cannot be promoted to PASS.

This source milestone establishes no actual native acceptance, nonalias discovery, useful search consumption, corpus transfer, speed advantage, complete instrumentation, lifetime benefit or OCM novelty. No source, fixture, native, bridge, learner or outcome rerun was performed for this review.
