# Full lifecycle candidate01 review

Source/apparatus clearance is withheld pending the concrete repairs below.
The exact source request is96d9bc56d689de17411147abccd079281b7329d709676b16fb53ee560f29d492;
source freeze f8da74394b9fbb1984b8d0e6815c69d55e5a1e384334725e074cbf3b8197c6ee;
run request e2fc9d2a610c600be634667e9d6fb31a17a93f09d46457995c4bfb67ee8bd829.
All are preserved as candidate01 review copies. No native gate is opened.

## Accepted mechanism and data flow

A freshly verifies the two fixed training proofs, checks the unchanged registered
bridge and complete joint signatures, enumerates all proper bodies and retains
supported/unsupported groups. It classifies every supported body with the complete
ordinary catalogue, preserves ALIAS/nonalias/UNKNOWN, and stops before admission on
any supported UNKNOWN. Witness admission is distinct from learned eligibility.

The post-exit projection carries only each witness's own admitted proof/trace,
typed body/context, required contracts, alias disposition and identity metadata.
It excludes source-task proof bodies, discovery contexts and teaching histories.
The fixed class payload is separately pinned and converted explicitly to the typed
schema, without rerunning old acquisition.

Fresh B's source/metadata input list excludes teaching fixture/result bodies.
Its permitted source reads include extraction/bridge module bytes for custody; it
does not invoke discovery or the bridge. The current code follows only the permitted
projection, request, original library and selected class-payload paths. The open
audit is scoped after its hook is installed; startup/gate reads occur earlier.

The direct issued-request oracle transports the admitted own uncompressed proof's
floating and essential labels, target and ordered premises. It does not call the
constructor or emitter. Both paths must match that independently formed request for
all six permutations per witness plus the class identity control. Fresh native
checking then decides the exact issued proof. No expected candidate ID is inserted.

The source-bound native wrapper retains fresh authority, exact theorem population,
trusted assertions and empty type/DV boundaries. The earlier unconditional native-call
counter is fixed: returned wrappers contribute result.native_calls, with separate
wrapper invocation/time fields. Completed-path intended five native replays have
separate purposes; they are not five theorem-level verifier calls.

## Findings requiring successor review

1. Native-negative acceptance checks only NATIVE_REJECTED and one native replay.
   run_b.py must also require native_check stage, its exact issued pending label,
   and the exact expected verified prefix. A prefix failure cannot satisfy a
   wrong-target or wrong-hole control. Root/lead accepted this repair.

2. life_common.invocation writes B OPEN-AUDIT and common I/O only on success.
   A B reconstruction/native/control failure loses that process's accumulated read
   evidence. Retain audit and work/I/O on failure, distinguishing an uninitialized
   audit from a checked empty log. Lead accepted this repair.

3. life_discovery stores contexts/candidates only when discover returns. A bridge
   gate or later source failure discards prior completed bridge results, candidates
   and progress. Preserve returned results/checkpoints before hard gates and retain
   attempt counters separately from accepted-world counts. Unreturned partial
   extraction may remain explicitly unavailable. Lead accepted this repair.

4. life_process.observe validates post-run files against the current manifest but
   never compares the manifest itself with the opening RUN-REQUEST-bound freeze.
   C.invocation has the same unanchored final check. Require source-freeze identity
   across the interval before reporting sources_unchanged. Distinguish canonical
   C.digest(manifest) from the raw SOURCE-FREEZE file SHA in receipt names.

A related reporting correction: common_io_scope says import I/O is reported
separately, but no complete import-byte counter exists. B records path opens and
native wrappers count their own bytes; other import byte work must be named
uninstrumented. Child wall/CPU timing still includes imports.

## Retained preflight and limits

CANDIDATE01-BINDINGS.json has70 passing read-only byte/data checks. All26 source
snapshot files, fixed inputs, exact engine copies, source/request maps and the
retained eight-check preflight agree. PID1721311 exited0 with no native, bridge,
learner or prohibited calls. Identity and cyclic proof-label transport controls
use the exact authored envelopes; the cyclic expected labels/tasks are specified
independently. The interrupted-state control refuses before reading absent state.

The first eight-check preflight output is retained with its observer failure after
child reap. Its outer exit/CPU/process window was not recovered; those values are
unavailable, not zero. The second data-only observer used the pinned Python. These
controls establish data/projection boundaries only, not native acceptance.

This review read frozen source and retained records; it imported or executed no
wrapper, engine, bridge, learner, matcher, fixture builder or test. Full native and
measured lifecycle execution remains closed until the successor source/apparatus
and its focused repair controls have been independently checked.
