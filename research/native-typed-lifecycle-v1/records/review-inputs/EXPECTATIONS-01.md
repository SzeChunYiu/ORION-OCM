# Typed lifecycle protocol review

Qualified conceptual acceptance of PROTOCOL.md, SHA256
e8273fd48a5ae1ce5d24e6604dc7429cec92e79be1e6ef892ff39f63eabceb49.
No protocol change is requested. Implementation, controls, native requests and process
apparatus remain unreviewed until their exact source freeze. Native, bridge, learner
and measured lifecycle execution remain closed.

The accepted input fixture packet stays unchanged. This scope is reconstruction
qualification of three typed parameters with class regression; it adds no search
bank, corpus allocation or utility comparison. The first authored task already has
the ordinary syl solution, so its longer supplied proof cannot be a learning baseline.

The protocol correctly separates native proof acceptance, bridge correspondence,
joint support, structural candidate validity and ordinary-alias eligibility. It
retains every generic occurrence and supported-body disposition. A supported UNKNOWN
stops before witness admission after recording all classifications. ALIAS is a
noneligible interface witness, not an acquired method. A no-candidate, refusal or
alias-only outcome remains valid and must not trigger fixture selection.

Six atomic permutations give a finite declared reconstruction domain. They do not
qualify composite recipe inputs or DV. Ordinary alias matching retains its broader
legitimate substitution domain. The fixed old class payload is a reconstruction
regression, not a repeated training episode or new class-acquisition result.

## Independent expectations before implementation read

- Pin reviewed PR155 source and every adaptation, exact fixture envelopes, all
  ordinary contracts, native prefix, bridge source, selected old class payload and
  its producer/admission binding. Historical receipts establish provenance only.
- Fresh A's native claims must match the exact authored proof tokens, statements,
  ordered essential context, type/floating context and permitted library. Preserve
  theorem population and trusted-assertion identities, including all partial failures.
- Generic extraction must be causal on source-bound traces. Candidate identities
  and complete joint support come from actual typed bodies and unchanged registered
  bridge results; no expected syl/jca body or utility selector may be injected.
- Full alias evaluation must retain ALIAS, NO_ALIAS_IN_REGISTERED_DOMAIN and UNKNOWN,
  with exact ordinary catalogue scope. Incomplete matching cannot confer eligibility.
  Admission of an alias as a witness must not add it to learned eligibility.
- Every admitted own candidate trace and recipe must denote the same exact issued
  target and ordered holes. Native checker receipts must bind that request and normal
  proof, not merely a claimed PASS or aggregate theorem count.
- A must exit before the supervisor creates the serving projection. Enforce the
  explicit field allowlist and input/source identities. Preserve candidate-owned
  admitted proof/trace; omit source training wrapper proofs, histories and results.
- Fresh B must read that projection and current library/admission identities only.
  It must not recover omitted training data by paths, imports or global objects.
  Hashing declared library bytes is distinct from interpreting training proof data.
- For every projected witness and each declared permutation, compare constructor
  and emitter output against an independently formed issued request. Matching the
  two outputs alone cannot establish the requested target or ordered-hole identity.
  Native checking must interpret the exact issued proof against the permitted prefix.
- The fixed class regression must use exactly the bound admitted chunk and any
  explicit versioned adaptation. It must not rerun old discovery/training or weaken
  the established class proof/type/DV boundaries.
- Freeze adversarial controls and all argv/output paths before outcomes. Distinguish
  structural refusal, native rejection, interruption, no candidate and alias-only
  qualification. Preserve changed eligibility, tamper and restoration behavior.
- Account for fresh processes, projection, native and bridge calls, all-candidate/
  alias work, source/input custody and failed attempts. Define timer windows and
  uninstrumented internals explicitly; fresh process does not imply OS-cache eviction.
- Final terminals must enumerate witness and learned-eligible IDs separately.
  Source/trace/bridge/native success remains interface evidence; no useful learning,
  search benefit, corpus opportunity, serving, novelty or lifetime advantage follows.

These are checks of the declared scope, not an additional experiment. Root retains
the separate source/fixture/apparatus execution gate after the implementation freeze.
