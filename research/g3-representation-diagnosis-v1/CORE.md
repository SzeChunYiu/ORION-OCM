# G3.4 representation-insufficiency diagnosis v1

Timeout / hard-wall does **not** license `JUMP`. Each diagnosis has a frozen
witness; ground truth is recovered independently of the classifier.

Parents that map every failure to `SEARCH_MORE` or `REPRESENTATION_INSUFFICIENT`
are recorded as overclaims. A timeout-to-JUMP parent is the planted mutant.
