# Typed-wff trace acquisition and reconstruction — contract v1

This is an authored interface qualification, based on immutable main
6032fa6ff06e021ef2adcd142060e68a7642aef6. Historical class sources and receipts remain
unchanged. No native execution is authorized by this document; independent source and
fixture review must precede the separately issued execution gate.

## Fixed boundary

Use exactly three homogeneous parameters of native type wff. Keep the existing
three-class case as regression. Mixed types, set variables, nonempty source/active/
applied-rule DV and extra floating leaves refuse. Recipe reconstruction also refuses
composite images and noninjective renaming; this restriction does not apply to the
ordinary alias test. A versioned parameter list records canonical IDs and native types; an explicit
context binds those IDs to native variables and their exact floating labels.
Every reached floating leaf must be one of the three mandatory declarations. Other
unused declarations in the fixed native prefix are not extra proof leaves.

Preserve the previous limits: at most 256 trace/body nodes, proper chunks with two
through eight semantic DAG nodes, and at most 4096 expanded proof labels. Preserve
ordered essential ports and repeated premise occurrences. Atomic bijections must
preserve type and the complete declared three-parameter domain.

No finite bank, ordinary catalogue or supported Boolean bridge grammar is widened.
There is no search, scheduling, timing-benefit, corpus opportunity or novelty assay.
The fixed prefix, ordinary contracts, native checker and bridge sources receive exact
bindings. Native checks must preserve the exact expected assertions and theorem count;
a saved accepted flag or semantic truth table cannot qualify a proof.

## Authored fixtures and candidate policy

Development proposal only: H1=ph→ps and H2=ps→ch. The proposed full proof routes use
ordinary syl/jca/simpr/jaoi contracts already present in the exposed catalogue. One
root concludes ph→ch; the other concludes (ph∨ps)→ch. Their native validity, exact
node bounds and distinct joint-context signatures have not yet been established.
Before native dispatch, freeze the exact two task envelopes, proof construction
recipes, native contracts, floating contexts and resulting source-file identities.
Do not alter fixtures or bounds within that frozen qualification to rescue an outcome.
Any diagnosed repair requires a preserved, separately reviewed successor generation.

Extraction enumerates every proper reached semantic root using the generic ancestor
rule; it never selects by assertion label, intended fragment, expected body or outcome.
Canonicalization uses typed parameters, ordered holes and the full checked recipe.
Support grouping requires at least two distinct joint premise/query meanings from the
unchanged Boolean bridge interface. Meaning supports are not native proof authority.
All mechanically proposed supported candidates are retained in sorted canonical-ID
order; no expected body is inserted and no utility selector is introduced.

Preserve production eligibility: exact ordinary one-step matches make a candidate
ineligible as a learned method. Record every match. Ordinary matching examines all
legitimate composite and repeated substitutions within the registered typed ground
grammar/catalogue, preserving native types and DV. Unsupported or incomplete matching
is UNKNOWN and cannot establish nonalias eligibility. Such a candidate may be separately
qualified as an interface witness, with witness_ids distinct from eligible_method_ids.
If all admitted witnesses alias, report TYPED_INTERFACE_QUALIFIED_ALIAS_ONLY and empty
eligible_method_ids. No acquisition benefit follows. A missing/unsupported alias check
cannot turn a candidate eligible. No-candidate and refusal remain explicit outcomes.

## Lifecycle after independent review

Producer A receives only the fixed authored training fixtures and permitted library.
It obtains fresh native traces, extracts candidates, checks exact emitted proofs,
persists the typed body/admitted trace/contracts and identities, then exits cleanly.
Discovery support and correctness authority remain separate. A create-only serving
projection omits training wrappers, training source proof bodies and discovery histories.
The admitted candidate’s own proof/trace and constructor contracts remain required.

Fresh B reads only that projection, exact admission/library bindings and its registered
reconstruction requests. It validates schemas and current bytes, reconstructs identity
and alpha-renamed proofs, and compares admitted-constructor and recipe outputs. The
native checker then checks the exact issued target, ordered holes and proof under the
bound prefix. B never reopens training data or treats an old PASS flag as authority.
Record source/process/request/proof identities and costs, including failed attempts;
cost recording serves custody, not a speed claim. Do not rerun any previous producer.

## Required engineering controls before native dispatch

Use explicitly labelled structural fixtures and a native-call prohibition. Cover the
class regression, wff parameter/floating binding, actual graph alpha normalization,
identity/renamed emission, repeated and ordered holes, wrong type/hole, changed trace
or contract, extra floats, unsupported DV and output/node limits. Include independent
expected proofs/outputs, not only round trips through the same implementation.

The first reviewable tranche contains typed context, structural constructor, extraction
and emission source with pure controls. Native training/admission and cold lifecycle
apparatus remain closed until their concrete fixture/source freeze is reviewed.
The syl→jca suggestion may fail or be an alias; neither result licenses body insertion.
