# Parent ownership — what this tranche does not claim

## External mathematics

| parent | what it owns | how this tranche uses it |
|---|---|---|
| Mealy, *A Method for Synthesizing Sequential Circuits* (1955, Bell Syst. Tech. J. 34, doi:10.1002/j.1538-7305.1955.tb03788.x) | the state/feedback transducer itself | every registered system is a Mealy machine; none of that construction is new here |
| Moore, *Gedanken-Experiments on Sequential Machines* (1956) | the length bound for distinguishing two machines, and partition refinement | route A uses the bound only as a cross-check and reachable product exploration as the authority; route B uses partition refinement as its independent mechanism |
| Hopcroft (1971), automaton minimization | minimal-state form of a transducer | route B's composite reducibility test |
| Hartmanis and Stearns, *Algebraic Structure Theory of Sequential Machines* (1966) | decomposition and the relation between state structure and behaviour | the background for why a state cell need not reach the external behaviour, which is what the intermediate layer measures |
| Boyd and Richerson, *Culture and the Evolutionary Process* (1985) | inheritance between instances as a channel distinct from individual change | the shape of the cross-instance invariant, not its finite instance here |
| Lamport, *The Part-Time Parliament* (1998, doi:10.1145/279227.279229) | externally attested state change | the shape of the admission guard |

## Repository parents

`gmi-833-aj4-process-organizations-v1` owns the registered organization set, the 260/148 count
and the lifecycle separation between expressible, realizable, reachable and chosen.
`gmi-833-aj11-bounded-completeness-v1` owns the bounded atlas and its unbounded-semantics
boundary. `gmi-833-developmental-reuse-v1` owns the result that a macro library adds zero
expressive power, which is why the `L7 -> L8` invariant is stated on description and search
geometry instead. `gmi-833-g0-governed-self-change-v1` owns the propose/verify/adopt
transitions. This tranche rebuilds the AJ4 set independently and reports agreement rather than
importing the numbers.

## What is NOT claimed novel

Transducer theory, minimization, the product construction, the length bound, macro libraries,
attested update, inheritance channels, and the ladder itself — the nine levels are quoted
verbatim from the issue and are not this tranche's proposal.

## The residual contribution

1. Eight named invariants, one per transition, each exactly computable and each with a clause
   decomposition so a partial pass is visible.
2. Their exhaustive measurement over one registered universe, by two independent mechanisms
   agreeing on 27 published quantities.
3. Nearest negatives with measured minimum edit distances for all eight transitions.
4. The level-structure verdict: three behavioural transitions, two relative to a declared
   boundary, three of description only; one intermediate layer with 112 members; and a base
   that is not cumulative.
5. A prohibition checker with all three branches exercised and a configuration that trips it.
