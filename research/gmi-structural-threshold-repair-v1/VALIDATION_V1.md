# Validation readout

The analytic optimum is39 per call and312 per eight-input sweep, with the
existing shared-sum witness attaining it under the explicit opcode contract.
This covers all coefficient magnitudes and unit counts in the two registered
flat source shapes. It does not certify an expanded delegating class.

All23 focused tests pass normally and optimized on CPython3.12.13, zero skips.
The18 portable tests cover geometry, dependency, exact attainment, arbitrary
large coefficient controls, incompatible layout, and the three corrected
claims. Five standalone tests cover original-source bindings and corruption,
the actual archived output-table omission, whole receipt and relocation.

CPython3.12.13 and3.12.3 independently emit the identical complete receipt,
normally and optimized. CPython3.11.15 and3.13.12 return the expected typed
UNVERIFIABLE layout result; they are not recorded as passed scientific bounds.
The complete native versions, executable hashes and outputs are retained in
raw/validation/NATIVE_INTERPRETERS_V1.json and bound by the manifest.

Independent root and closure proof reviews accepted STR-1–5 and the helper
implementation. Closure confirmed the open/closed midpoint argument, active
dependency incidence, constant/dead-gate accounting and layout-conditioned
39 lower/upper match. The CALL opcode is counted; callee work is not.

No old whole-grammar search, candidate benchmark, timing or ecology experiment
was executed. Native finite truth checks are verification work only.
