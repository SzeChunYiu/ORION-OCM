# ORION-V2 method contract on the current OCM runtime

This is the separately versioned revalidation requested by the V1 reopening
record. It binds ORION-V2 `b15abb41e1f9219ea793a15c5e641ac6579adb35` to the
complete OCM runtime inventory at `7f8488edd77992ba0313fa9457294ed7bbc623cf`.
It preserves the historical V1 manifest, replay and reopening decision.

The three direct consumer/check modules are unchanged from V1, but 21 runtime
files were added and 23 changed. V1 therefore cannot cover this runtime.
`RUNTIME_DELTA_V2.json` records every changed file with both available identities.
The new manifest binds all 178 current `src/` files plus `pyproject.toml`, and
the three exact theory source files. A later source addition, deletion or byte
change reopens this packet and fails the V2 workflow until separately reviewed.
Research-only changes outside those inventories do not change the runtime claim.

The packet retains the four original rows:

| Row | Runtime obligation | Executed scope |
| --- | --- | --- |
| M1 | Fair finite grammar search | All targets generated by 85 programs of length at most three; primitive/guided slot bound |
| M2 | Samples are not polynomial proofs | 595 rational numeric/coefficient comparisons and actual wrong-program rejection |
| M3 | Persistent generator support | Same two training and two held-out tasks; loading after restart; separate revocation of each training support and holdout-validation support; history retained |
| M4 | Deterministic finite identification | All binary three-query classes of sizes one through four, every possible in-class truth, and every single-observation deletion |

All source bindings resolve real Git objects and compare current working bytes
before and after execution. Imports use a new private copy containing only
verified source/data. The replay additionally records each loaded `ocm` module's
path and source hash, rejecting modules outside the private tree. This is an
import inventory, not function-level coverage. The interpreter and standard
library remain trusted; this is not an adversarial-code sandbox.

Replay with the exact theory checkout available:

```bash
OCM_THEORY_ROOT=../ORION-V2 python -B research/theory-runtime-integration-v2/test_bindings.py
python -I -B research/theory-runtime-integration-v2/check.py --theory ../ORION-V2 --compare research/theory-runtime-integration-v2/REPLAY_V2.json --out NEW_PATH
```

Use CPython 3.12. Output creation is exclusive; existing evidence is never
overwritten. `--compare` checks every deterministic field including full nested
checks, authority, manifests, workflow/checker inventory and loaded source
identities. Only the interpreter description is execution metadata. Duplicate
JSON keys, non-finite values, dropped fields and Boolean/integer substitutions
are rejected. `--revoke-row M3` returns revalidation with exit 2. Every nonzero
V2 exit fails CI; the old warning-only V1 job remains historical.

This is bounded engineering revalidation. It grants no runtime adoption,
independent semantic parity, scientific validation, novelty, M5 statistics,
protected M11/M12 outcome, unrestricted learning or whole-system advantage.
The old arithmetic and identification checks remain finite evidence even though
their parent theorems state quantified results. Those theorems and assumptions
are not proved by matching source hashes or by a green job.
