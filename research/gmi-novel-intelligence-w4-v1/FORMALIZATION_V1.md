# Novel-intelligence W4 formalization V1

Issues: #592 item 39 (novelty ladder W4), #602 P4 residual novelty.

Freeze authority: `FREEZE_V1.md` with
`sha256("GMI-NOVEL-INTELLIGENCE-W4-V1/RQM-FAMILY-RECURRENCE") =
57ddb8d1195e0eba61e0527828882bce1f9e0da24a4474ef3a69b7c6c71789c9`.

Parents: `#796 research/gmi-unseen-form-prediction-v1` (V6 exact RQM;
W2/W3 green; W4 not claimed), `GMI_PREDICTIVE_RESIDUAL_QUOTIENT_THEORY_V1.md`
(PRQ-1), `GMI_PREDICTED_MACHINE_INTELLIGENCE_FORMS_V1.md` (RQM / W1).

## 1. Claim boundary

Claimed only when all W4 boxes tick:

```text
W4_RESIDUAL_QUOTIENT_STRUCTURAL_DOMAIN_AT_REGISTERED_FINITE_FAMILY_SCOPE
NOVEL_INTEL_LADDER_W2_W3_W4_GREEN_AT_EXACT_RQM_FAMILY_SCOPE
```

Not claimed:

```text
NEW_DOMAIN / J4
NEW_FORM_OF_INTELLIGENCE_PROVEN
atlas phase-hole occupancy
P4 surviving-unseen-domain
```

Evidence class: P1 finite mathematics + P2 exhaustive/cost-bounded executable
witness on a registered parametric family. No P4 real-scale transfer claim.

## 2. Objects

Reuse upstream residual-quotient machine `(P_assign, R_assign, decoder)` with
cost `C = |range(P)| + |range(R)|`. Architecture macros remain forbidden in the
search grammar. Search is conditional on registered `q_P`.

A **parametric family** `F(k)` for `k ∈ {2,3,4}` is the map
`k ↦ (q_P^{(k)}, q_O^{(k)})` with `max_m = k` and flat gap `1`.

### Theorem W4-A — family law from PRQ-1

For each registered `k`, PRQ-1 under fixed `q_P^{(k)}` forces `|R| ≥ k`.
The constructive fiber labeling achieves `|R|=k` and `C*=2+k < flat=k+3`.
Hence the C0 family law is exact before search.

### Theorem W4-B — fixed-budget is not the family law

Let parent `P_B` recover with residual alphabet size at most fixed `B=3`.
On `F(2)` it overprovisions relative to tight `max_m`; on `F(4)` it fails
exact recovery. Therefore `P_B` does not track the recurrent residual law
across the registered family.

### Theorem W4-C — W4 at registered finite family scope

If (i) upstream V6 is green with W4 false, (ii) the C0 law holds and recovers
on every `k ∈ {2,3,4}`, (iii) parent reduction leaves a material residual vs
pure predictor, flat table, and `P_B`, (iv) remint preserves the law, and
(v) held-out `k=5` matches the law while `P_B` fails, then novelty-ladder
**W4 domain status** is earned at this registered finite family scope only.

## 3. W4 box discharge

| Box | Discharge |
|---|---|
| W4.1 upstream compose | Import V6 witness; require W2/W3 green, W4 false, phase-hole refused |
| W4.2 family registered | `F(k)` for distinct `k∈{2,3,4}` |
| W4.3 property-first family law | C0 predicts `|R|≥k`, `C*=2+k` before search |
| W4.4 neutral recovery | P-fixed residual grammar recovers on every member |
| W4.5 parent residual | Material vs predictor+flat; fixed-budget not family law |
| W4.6 remint | History/label remint preserves law+recovery |
| W4.7 fresh outside family | `k=5` holds; fixed-budget fails |
| W4.8 phase-hole honesty | Occupant claim remains refused |

## 4. Item 39 ladder upgrade

| Rung | Status |
|---|---|
| W0–W3 | green (inherited + family recurrence) |
| W4 domain status | **green at registered finite family F(k)** |
| J4 NEW_DOMAIN | **not claimed** |

## 5. Falsifiers

- Upstream V6 W4 already true, or any V6 box red.
- Any family member with `C < 2+k` under registered `q_P`.
- Fixed-budget parent tracking `max_m` on all members.
- Remint changing `max_m` or breaking recovery.
- Held-out `k=5` admitting zero-residual recovery or fixed-budget success.
- Any attempt to emit `NEW_DOMAIN` / phase-hole occupant from this receipt.
