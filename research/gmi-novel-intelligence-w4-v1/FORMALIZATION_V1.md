# Novel intelligence W4 formalization V1

Issues: #592 item 39 (W2/W3/W4), #602 P4 residual novelty.
Upstream: #796 `research/gmi-unseen-form-prediction-v1/` (V6 exact RQM scope).

Freeze authority: `FREEZE_V1.md` with
`sha256("GMI-NOVEL-INTELLIGENCE-W4-V1/RQM-FAMILY-RECURRENCE") =
57ddb8d1195e0eba61e0527828882bce1f9e0da24a4474ef3a69b7c6c71789c9`.

Parents: upstream V6 capsule; `GMI_PREDICTIVE_RESIDUAL_QUOTIENT_THEORY_V1.md`
(PRQ-1); `GMI_PREDICTED_MACHINE_INTELLIGENCE_FORMS_V1.md` (F1 RQM);
`GMI_NOVEL_DOMAIN_THEOREMS_V1.md` (W0–W4 ladder definition);
`GMI_OPEN_NICHE_PREDICTION_V1.md` (negative lesson: phase-hole was a known parent).

## 1. Claim boundary

Earned only if all W4 boxes tick:

```text
W4_RESIDUAL_QUOTIENT_STRUCTURAL_DOMAIN_AT_REGISTERED_FINITE_FAMILY_SCOPE
NOVEL_INTEL_LADDER_W2_W3_W4_GREEN_AT_EXACT_RQM_FAMILY_SCOPE
```

Meaning (novelty ladder W4 at registered scope):

> The residual-quotient law is **recurrent** on a registered parametric family
> and **parent-nonreducible** relative to pure predictor, flat table, and
> fixed-budget residual parents.

Not claimed:

```text
NEW_DOMAIN                         # requires J4
NEW_FORM_OF_INTELLIGENCE_PROVEN
atlas phase-hole occupancy
P4 surviving unseen domain
```

P4 surviving unseen **morphology** may be recorded true at this family scope
when W4 ticks; that is morphology residual, not domain residual.

Evidence class: P1 finite mathematics + P2 exhaustive/cost-bounded executable
witness composed on upstream V6. No P3 real-scale transfer claim.

## 2. Composition contract with #796

This package **must not** duplicate the V6 capsule. It:

1. imports `unseen_form_prediction_v1.py` as a sibling module;
2. requires upstream freeze digest unchanged;
3. requires upstream all-six V6 boxes green, W2/W3 true, W4 false;
4. requires upstream phase-hole occupant claim false;
5. only then runs the family recurrence campaign.

If upstream W4 were already true, this capsule would refuse to re-claim it.

## 3. Objects

Same as upstream: histories `H`, predictive quotient `q_P`, target `q_O`,
fiber multiplicity `m(p)`, residual alphabet lower bound `|R| >= max_p m(p)`.

Family generator `ecology_for_k(k)` yields partitions with `max_m = k`,
`|S_P|=2`, `|S_O|=k+2`, predicted `C* = 2+k < flat = k+3`.

## 4. Theorems

### Theorem W4-A — family residual law

For every `k ∈ {2,3,4}`, PRQ-1 under registered `q_P(k)` forces
`|R| >= k`. The constructive fiber labeling achieves `|R| = k` at cost
`C* = 2+k`, strictly below the flat-table parent at `k+3`.

### Theorem W4-B — fixed-budget parent is not the family law

Let `B = 3` be a constant residual alphabet. Then:

- on `k=2 < B`, exact recovery is possible but the tight min residual is 2 ≠ B;
- on `k=3 = B`, local recovery matches B (no local kill);
- on `k=4 > B`, exact recovery is impossible under budget B.

Therefore no single fixed budget realizes the residual-quotient law across F.
The recurring structure is the **k-tracking residual law**, not a constant
augmentation budget.

### Theorem W4-C — fresh extrapolation

For `k'=5 ∉ F`, the same C0 law predicts `C*'=7 < 8` and `|R|>=5`; fixed
budget `B=3` fails. Neutral recovery under `q_P(5)` confirms the prediction.

## 5. W4 box discharge

| Box | Discharge |
|---|---|
| W4.1 upstream compose gate | V6 green; W2/W3 true; W4 false; phase-hole refused |
| W4.2 parametric family | F(k) for k∈{2,3,4} registered with distinct max_m |
| W4.3 property-first family law | C0 `|R|>=k`, `C*=2+k` before search for every k |
| W4.4 neutral recovery | P-fixed residual grammar recovers law on every member |
| W4.5 parent reduction | material residual vs predictor+flat; fixed-budget fails as family law |
| W4.6 remint replication | history/label remint preserves law+recovery on every member |
| W4.7 fresh outside family | k=5 prediction holds; fixed-budget fails |
| W4.8 phase-hole honesty | occupant claim remains false |

## 6. Item 39 ladder (after this capsule)

| Rung | Status |
|---|---|
| W0 existence | green (upstream + family) |
| W1 necessity/compression | green |
| W2 parent separation | green (upstream + family) |
| W3 empirical niche | green (upstream + remints) |
| W4 domain status | **green at registered finite family scope** |

## 7. Falsifiers

- Upstream V6 fails or claims W4 already / phase-hole occupant.
- Any family member with exact `C < 2+k` under registered `q_P(k)`.
- Fixed-budget `B` tracks `max_m` on every family member.
- Remint changes `max_m` or breaks recovery on any member.
- Held-out `k=5` admits zero-residual recovery or min cost ≠ 7.
- Any claim of J4 NEW_DOMAIN or phase-hole occupancy in this receipt.
