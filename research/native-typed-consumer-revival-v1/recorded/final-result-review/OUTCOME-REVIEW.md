# Actual consumer revival: independent outcome review

**TYPED_INTERFACE_QUALIFIED_ALIAS_ONLY.** The separately authorized fresh consumer completed the fixed seven-claim reconstruction and all registered refusal controls. The original lifecycle remains **LIFECYCLE_FAILED**; its completed A supplied the persisted alias witness, and this later attempt establishes consumer recovery from that state. No outcome or native run was repeated by this reviewer.

## Native and reconstruction evidence

B PID **1829521**, parent 1829517, exited 0 and was reaped. All six atomic permutations of the three-wff witness and the fixed class identity were reconstructed. In each case, both constructor and recipe output exactly the pre-issued normal proof, target and ordered holes. Each proof has 11 normal labels and two expanded semantic applications. The six wff claims retain witness `3a3edbba...`; the class claim retains old regression identity `5c8ff077...`.

One fresh native batch accepted all seven issued claims after the unchanged 4,095-proof prefix: **4,102 verified proofs**, under the same 96 trusted assertion identities used by A. Exact native request digests, trace proofs, ordered essential hypotheses, native floating types and empty DV match the issued claims. Database, suffix, log, wrapper-result copies, source and prefix hashes agree. Reading a native log is corroboration; current source-bound native receipts supply the acceptance evidence.

The two separately issued negative replays changed only the registered first premise or target of `typed-reconstruct-0`, preserving the proof. Both made one actual native call, verified the complete identical 4,095-proof prefix, and failed at `native_check` with that exact pending label. The wrong-hole result is an essential-hypothesis mismatch; the wrong-target result is a final proved-assertion mismatch. No earlier prefix failure was counted as a negative success. No selected negative trace was accepted.

All nine structural controls retained their expected refusal classifications. Total actual native calls in the revival are **three**; A previously made two, and the failed original B made none. The witness remains an ordinary `jccir` alias, with **zero eligible learned methods**. This is typed proof/interface and persistence-boundary qualification; it does not establish nonalias acquisition, learned search benefit, language transfer, current OCM serving integration, scaling, speedup or novelty.

## Source, process and historical custody

The launch-intent and final observer maps agree on the exact 28 frozen source/input files, nine historical bindings, both prepared B envelopes, request, gate, executable and review identities. They additionally bind the original admission/discovery records. Current bytes match those maps. The outer observer fulfilled the envelope and exited-A checks required by the source review; its retained script matches the hash in the launch intent.

The projection `e9053afd...` and seven-claim request `e94c26d0...` remain byte-identical to the failed attempt. Original A PID 1777505 exited before this B started. Its process/result/state chain and the projection's historical source-freeze origin remain exact; the successor source freeze is separate. No A, discovery, alias classification, admission, bridge or reprojection was rerun. Original failure/result/cost receipts remain unchanged.

B retained **128 scoped open attempts**, ending with completed main and source-custody status. Every recorded owned path is an allowed serving/source input or exact declared cache probe. The previously failing root-helper probe is followed by source loading. The observed cache probes are read-only and their paths remain absent on readback. No teaching-body or producer-result read appears in this scoped B audit. The observer separately hashes historical A records for custody; that is not a B learning-data read.

The audit starts after B startup/gate and excludes its output subtree and external standard-library paths. It is not a universal read sandbox. Prefix hashing reads bytes; the native checker interprets the library, and B interprets current ordinary contracts and admitted proof payloads. Complete import-byte accounting remains unavailable.

[OUTCOME-DATA.json](OUTCOME-DATA.json) retains 123 passing source/data assertions and exact read scope. These are custody and outcome checks over retained records, not new experimental trials. No project runtime, verifier, bridge, learner, matcher or reconstruction code was executed during review.

## Timing and public claims

The revival's measured outer window is **2.805816586 s**; its nested child interval is **2.759380297 s**, with 2.663508 user seconds, 0.052068 system seconds and 107,808 KiB RSS. Native-wrapper intervals are nested within the child. The original failed attempt's separate outer window is 2.156473901 s, so the sum of those two disjoint measured windows is **4.962290487 s**.

The revival outer timer starts after initial observer imports, directory checks, metadata parsing and pin-map construction. It includes opening custody, launch/child, final custody and result/log identity reads; final PROCESS serialization is excluded. The sum is not total project or lifetime cost. Source preparation, pure controls, review, packaging and waiting are outside these windows. Do not add nested child or wrapper intervals. Fresh processes were used; OS caches were not evicted. These single fixed executions support no timing advantage.

The reviewed public HANDOFF and corrected SUMMARY express these limits accurately. SUMMARY's preservation field was clarified to distinguish candidate-payload reuse from ordinary-contract and prefix interpretation; the former version is retained. Their pending review-status field may now be replaced with this receipt's identity, without altering runtime or outcome bytes. The original failure must stay visible alongside the successful consumer revival.
