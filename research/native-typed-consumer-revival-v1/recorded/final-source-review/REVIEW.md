# Consumer-only revival: independent source review

**SOURCE_AND_APPARATUS_QUALIFIED_FOR_SEPARATE_ROOT_GATE.** No remaining source blocker was established within this bounded review. Root's authorization remains necessary, including the prepared-envelope and historical-origin checks below. This receipt is not an execution authorization or a native result.

## Bound generation and repair

- Source-review request: `59ad1c031f7bfe3ac3cdbccd6aabe1df36b6f59fe6da054d5185540b3cf97337`.
- Raw source freeze: `6df58238b2c500999cb78f61aeff4035cfcd85699b58c410a8ed1a994c7e27db`.
- Run request: `ef6d300c02eb29944d224dbac5e220afadc73295cb457685a1a665295ba552f9`.
- Seventeen source files equal the frozen snapshot. Only `run_b.py` differs from the corresponding original files; the other four root helpers and all copied typed/native authority files are exact.

The patch derives exact cache paths using `cache_from_source` for pinned Python source paths. Its cache branch accepts read-only probes only when the target does not exist. Existing caches, writes and unregistered names or tags refuse. Attempts are recorded before refusal. Root-helper probes receive the same source-bound treatment as engine probes, without a wildcard `.pyc` allowance. Lexically owned paths resolving outside the root now refuse through an explicit path-escape branch; byte paths are decoded before constructing `Path`.

The rest of B's execution body, starting at audit-hook installation, is byte-identical. Refusal helpers, exact claim checks, constructor/emitter comparisons, ordinary-contract authority, empty eligibility checks and exact native-negative acceptance remain unchanged. The pre-repair B file is retained and equals the original failed source.

## Actual pure import controls

The 11 controls passed in PID **1796587**, parent 1796586, with exit 0 and a reaped process. The actual previously failing `life_payload` cache probe is followed by reading its source. The same is recorded for `life_requests` and `life_native`, along with the engine probes. These are real helper imports under the new guard; the control never calls B's main routine or reconstructs a claim.

The other retained controls cover declared absent read, declared source read, existing declared cache, write probe, absent and existing unissued caches, wrong cache tag, teaching path and an existing cache symlink. Source maps before and after equal the final 17-file manifest. Native/bridge/learner counters are zero and the explicit prohibited-call list is empty. The native-wrapper module is imported but its verification function is not invoked; authority files seen in final source-hash reads are not native interpretation.

The observed control process interval was 0.044725754 s, with 0.034054 user seconds, 0.008513 system seconds and 19,616 KiB RSS. These are engineering-control costs, not consumer reconstruction or comparative performance. No controls were rerun by this reviewer.

## Historical input and execution boundaries

Projection `e9053afd68b7ac6eb8ff7475ddd374277e4ac79c20ac9ee038f91f74cc6738c1` and seven-claim request `e94c26d0fec274b0d3940382f2ed9611d4829db1a9ba5a7919288d807195eb83` equal the first attempt byte for byte. The six typed atomic permutations and single old class identity remain issued exactly as before. No claim or proof has been selected after observing new native outcomes.

Projection origin retains A's original `f60811fd...` freeze, exited process, result, state, discovery and admission bindings. Its witness equals A's persisted witness. The new B input envelope binds the successor freeze separately. Eligible methods remain empty, and the supported witness remains an ordinary `jccir` alias. Old native admission is provenance; it cannot grant current B acceptance.

No producer, discovery driver, original supervisor or teaching fixture is copied into this successor. Its entry invokes only fresh B. Imported extraction/alias modules remain inert unless called; B's unchanged path does not run acquisition, alias classification, the registered meaning bridge or reprojection. The previous failed lifecycle and its costs remain immutable.

Before and after the one declared child invocation, root/outer custody must compare **both** prepared B envelopes to `RUN-REQUEST.prepared_input_envelope` and `prepared_gate_envelope`, as well as the 28 frozen source/input files and the exact request/gate. The unchanged common gate does not itself compare those two envelope files to the optional run-request fields. Root has accepted this required outer check. The historical exited-A process/result/state and exact projection chain must also remain bound. `OUTER-PATHS.json` supplies the exact consumer argv and separate create-only receipt/log paths; it is prepared metadata, not an executed observer receipt.

A completed consumer would require the positive native batch plus both exact issued-claim negative replays: three prospective native calls. Until those actual records exist, neither the six typed reconstructions nor the class regression nor native negative checks are qualified by this revival. A later success must be reported as separately registered recovery from persisted A, not as a successful original once-only lifecycle. Original failed-attempt and revival costs must remain distinguishable; nested intervals must not be double counted.

## Custody and wording limits

[The readback](BINDINGS.json) records 96 passing source/data assertions, exact read scope and successor inventory. They establish current/snapshot/original identity, actual control/process bindings, old A provenance and unchanged serving inputs. They are a data reader, not additional experimental trials. No reviewed code, native checker, bridge, learner, matcher or reconstruction function was executed by this reviewer.

The frozen protocol's description of the original probe as absent is read with this correction: the original root guard rejected it before testing existence; absence was established on later filesystem readback. No cache consumption follows from the original attempt. The guard remains a scoped audit, not a general filesystem sandbox; complete import-byte accounting is not claimed.
