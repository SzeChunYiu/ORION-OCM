# Typed consumer revival: prospective protocol

This is a separately reviewed consumer-only successor to the preserved first
lifecycle attempt. No native execution is authorized by this document.

Producer A completed its two native replays, emitted one supported ordinary
jccir alias, persisted an interface witness, and exited. Its eligible method list
is empty. The first B failed before native reconstruction when importlib probed
an absent root-helper bytecode path. Preserve that failure and all costs.

Reuse the original PROJECTION.json and B-REQUEST.json byte for byte. Keep the
projection's origin.source_freeze bound to A's original candidate02 freeze.
Do not reproject, rerun A, invoke the meaning bridge or extractor, change
candidate selection, or turn the alias into an eligible learned method.

The source correction permits only absent, read-only bytecode probes whose exact
paths are derived from the pinned Python source paths. Existing cache files,
unregistered cache paths and write probes refuse. Record all attempted owned
opens. Preserve the source/library/projection read boundary.

A new execution/input envelope binds successor source and unchanged historical
projection/request identities. The old producer source identity remains distinct
from the current consumer source identity. All seven previously issued claims
remain unchanged: six atomic wff permutations and one old class identity
regression, followed by the same two exact native-negative controls.

Expected complete consumer path: one fresh positive batch replay and two native
negative replays. Every negative must fail at its exact issued label after the
complete pinned prefix verifies. No original receipt grants current acceptance.

Run the exact declared consumer argv once only after independent source/protocol
review and a separate root gate. Preserve any refusal or failure; no automatic
retry. Report this as typed interface qualification with empty learned eligibility
if successful, never as acquired-method utility, search benefit or novelty.
