# Gate metadata history

The first authorizer source refused because its expected prepared B-GATE object omitted the already hash-bound descriptive scope field. Root read the actual envelope, preserved the preimage and corrected that metadata comparison before any new gate or native action.

The two source generations here are exact copies. This was a metadata-only authorizer correction, not a native attempt. Root then issued one execution gate and repeated the same authorization once for clarity; the consumer was launched only once. The actual gate and launch intent are retained under recorded/. Root's historical authorization remains distinct from an instruction to run these files again.
