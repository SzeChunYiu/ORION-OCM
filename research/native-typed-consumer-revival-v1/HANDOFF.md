# Typed consumer revival: alias-only interface qualification

The fresh consumer completed **TYPED_INTERFACE_QUALIFIED_ALIAS_ONLY**. It reconstructed all six atomic permutations of one three-wff witness and the fixed class regression. Both reconstruction paths matched the exact issued requests and passed the native checker. The witness is an ordinary jccir alias; **zero methods are eligible for learned serving**.

This is an authored interface and persistence-boundary result. It establishes no search benefit, language transfer, useful acquisition, speedup or scientific novelty.

## What actually executed

The original producer completed native training, unchanged meaning-bridge support grouping, generic extraction, complete alias dispositions, separate alias-witness admission and persistence, then exited. Three occurrences formed two bodies: one supported alias and one body with insufficient support.

The original lifecycle then failed before B reconstruction: the read guard refused a root helper's import-cache probe. It remains failed. Absence of that cache was established on subsequent readback, not by the original rejecting hook.

A separately reviewed consumer repair permits only exact declared, absent, read-only cache probes. Its once-only fresh B reused the original projection and request bytes; acquisition, bridge, discovery and projection were not repeated.

## Results and controls

- Six typed reconstructions: 11 normal proof labels each; one fixed class reconstruction: 11 labels.
- One fresh native batch verified seven issued claims: 4,102 proofs under the unchanged 96 trusted assertions.
- Two native negative invocations rejected the exact wrong-hole and wrong-target claims after the full 4,095-proof prefix, at the issued label.
- Nine structural refusals cover exact-hole/target, reconstruction substitution, type/DV and payload/eligibility boundaries.
- Eleven prior pure import/read-scope controls passed with zero native, registered-bridge or learner calls. These are separate from the native outcome.

## Costs

| Retained execution | Measured outer window |
| --- | ---: |
| Original failed lifecycle, including completed A and failed B | 2.156473901 s |
| Consumer-only revival | 2.805816586 s |
| Sum of these disjoint windows | 4.962290487 s |

B child wall was 2.759380297 s, with 2.663508 s user CPU, 0.052068 s system CPU and 107,808 KiB maximum RSS. There were five actual native calls across A and revived B; the failed B made none.

The revival outer timer begins after observer imports, empty-output checks, gate/request/manifest parsing and pin-map construction. It includes custody preflight, launch/child, final custody and result/log identities; final PROCESS serialization is excluded. Setup before that timer, authoring, source preparation, pure controls, review, packaging and waiting are unmeasured/excluded. Child and native timers are nested attribution. Full import-byte counts are unavailable; explicit helper and native-wrapper I/O counters are separate. Each run used a fresh process; OS caches were not evicted.

## Custody and scope

B PID 1829521 exited 0 after original A PID 1777505 had exited. Before/after checks cover the 28 frozen source/input files, nine historical bindings, both prepared B envelopes, request/gate identities and original A-to-projection chain. Sources, requests and historical records stayed unchanged.

Only the native checker interprets the complete library prefix; custody hashing also reads its bytes. B interprets admitted candidate proof payloads and current ordinary contracts, without training proof bodies or training results.

The original source generation and failed attempt stay authoritative for what they actually executed. The revival has its own source freeze, request, gate and receipts. Independent retained-data review accepted this alias-only scope: 123 checks pass, bound by receipt 5b206702b087d9b58ae63f4a44716a57df2037e1142fbf79379992ae9d6397ac.

[Machine-readable summary](SUMMARY.json) records exact bindings and costs. Full native/source/process archives remain on the laptop; no prefix or full assertion catalogue is included in this public capsule.
