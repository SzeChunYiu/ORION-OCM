# Unary qualification records

Current authority is the **110-control metadata successor**, tied to the eleven
Python sources in [SOURCE_FREEZE](SOURCE_FREEZE.json).

[INDEX](INDEX.json) binds four lossless archives and compressed member maps.
Read the package's [qualification note](../QUALIFICATION.md) for the result scope.

- language.tar.gz: original development failures and the historical 83-control run.
- cache-key.tar.gz: the separate counter repair and historical 84-control run.
- metadata.tar.gz: strict metadata repair and current 110-control run.
- independent-review.tar.gz: the independent source/record review, unchanged.

Member paths are relative to each original root; no extraction prefix is added.
The compressed tree maps retain directory/file modes. No symlinks were present.
[OMISSIONS](OMISSIONS.json) distinguishes absent temporary cases and external host
inputs from the complete set of 179 retained regular evidence files.

Counts from overlapping generations must not be added. These are authored parser,
solver and verifier controls for a conventional symbolic parent, without an OCM
runtime, learner, transfer, general-language, cost-superiority or novelty result.
