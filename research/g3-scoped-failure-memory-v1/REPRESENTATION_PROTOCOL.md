# Scoped representation continuation of #205 — local authored development v1

## Freeze and inherited state
Continue the existing `g3-scoped-failure-memory-v1` rather than introduce another cognitive core. Keep `experiment.py`, original PROTOCOL and evidence unchanged. The donor `methods.py` Git blob remains `50323a33418b8ef8bb6500ddeba4b9d1f795e9e3`. The actual OCMRuntime, field, evidence registry and constitution are reused.

First reproduce the exposed #205 eight-task acquisition/five-arm study unchanged locally. Extend its SAME persisted lineage by an append-only representation admission; do not recreate a blank field for the continued arm. All raw failed attempts and the first 64 certificates are inherited. The predecessor is a local reproduction, not the original remote process or an independently protected lineage. RESET is a separately labelled control. The historical hosted state may also be used as a snapshot-bound input, but its cross-host times cannot establish local whole-lifetime payback.

## Candidate language fixed before new solve outcomes
P0: no memory. P1: original exact (program,input) dictionary. P2: trie-indexed longest certified prefix plus execution of the remaining suffix through the unchanged interpreter. These representations are supplied conventional mechanisms, not invented by OCM. The episode-derived values, selected representation and current authorization are separate objects.

Misleading candidate Q0: collapse programs agreeing at input 0. It fits the registered input-0 training projection; no claim of all-input equality follows. A deterministic first pair of training programs agreeing at 0 but differing at 1 is a refutation when EVAL(1) joins the observation language. Both exact syntax and Q0 fit that initial projection but Q0 predicts an unjustified equivalence later. Q0 never enters the solver without qualification. A valid old narrow certificate becomes inapplicable to the expanded alphabet, not retrospectively false in its old scope.

Allocation: enumerate the unchanged grammar through length 5. Exclude ALL 24 historical #205 mathematical task identities. Select eight minimum-length-4 validation identities with salt `ocm-g3-representation-validation-v1-20260909`, and 64 minimum-length-5 later identities with salt `ocm-g3-representation-test-v1-20260909`, by SHA-256(salt + NUL + fingerprint), then fingerprint. No replacement; fail closed if populations are insufficient. The two strata are disjoint by minimum length. No target solutions are supplied to workers; no outcomes are consulted in allocation. This is authored finite-distribution development, not protected data or independent proof-family sampling.

Selection rule: among safety-qualified P0/P1/P2, minimize validation interpreter primitive steps, then index lookups/edge lookups/terminal lookups, then registered order P0/P1/P2. This is a fixed *interpreter-work-coordinate* objective, not a physical or total-cost scalarization. Publish the entire vector and measured timings. No ML router is introduced and no global exact-policy optimality is inferred from this empirical finite choice.

## Common solver and intervention
All arms call unchanged `methods.solve`, in its original search order with its original exact verifier, budget 2000 slots and maximum length 5. P2 substitutes only E(prefix,x)=v proven by the independent coefficient evaluator, then computes E(suffix,v); total deterministic instruction semantics establishes composition. No test online updates. Every first solution, counterexample list, slot count and status must equal the uncached parent.

Fixed arms: primitive, ordinary-point, ordinary-prefix, ocm-point, ocm-selected, representation-revoked, memory-revoked, restored, reset-ocm. The conventional parents receive identical records and checks. OCM representation admission depends conjunctively on new representation assurance AND inherited memory evidence. Removing only representation assurance falls back to P1; withdrawing the memory reverts to P0; restoration returns P2 if selected. Each consumer is a fresh OS process with source-only startup; each phase retains its actual persisted state. Repeated revocation/restoration is NOT counted as learned generations.

## Measurements and interpretation
Retain acquisition, selection, index building/checking, admission, revision, startup, solve wall/CPU, whole-child wall/user/system CPU, process high-water RSS (not additive), payload/archive bytes, exact interpreter steps, all lookup categories and actual certificate consumption. Nested timers are not added. Retain complete populations, failures and all comparison arms. Single fixed-order physical runs are descriptive and not unbiased speed estimates. Energy, active k/N and unmeasured physical quantities remain UNKNOWN. Full strong-parent lifetime benefit is unestablished unless the complete matching ledger supports it; an interpreter-step improvement alone never proves payback.

Conditional positive: `PREFIX_REPRESENTATION_CAUSALLY_USEFUL_AT_INTERPRETER_STEP_SCOPE`, only with exact result parity, actual proper-prefix consumption, live/restored equality and both distinct withdrawals. Parent tie is `PARENT_SUFFICIENT`. A selected conventional representation is not new domain knowledge. Counterexample diagnosis is confined to the declared observer alphabet; bounded search without a representation witness stays RESOURCE_BOUND/CANNOT_CHECK, never automatic JUMP.

Freeze source hashes after synthetic engineering controls, before the first validation/test run. Retain any failed exposed run separately; do not retune source, salts, counts or selection to rescue its scientific result. This work does not close full G3.2/G3.3/G3.4, strongest library-learning comparisons, G6, language/formal-proof transfer, protected replication or issue #165. Those require their distinct registered evidence, not a relabelled collection of local controls.
