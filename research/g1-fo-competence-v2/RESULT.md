# G1.3.2 F/O competence result

**Terminal:** `COMPETENCE_IN_FO_STATE_AT_MICRO_SCOPE`

- After G2 `("inc","inc")` admit + L1 Adj–Noun construction admit, both live
  as procedure atoms in one ledger. F/O delta: ledger bytes grow; G2 and L1
  each add a positive slice.
- `solve.py` nloc and `planner.py` nloc stay constant (536 / 170).
- Held-out L1 pairs compose from the ledger-loaded construction; reset without
  the skill does not.
- Hostile Π table solves the eight polynomial identities and is absent from
  `solve.py`, `planner.py`, `methods.py`, G2 experiment, and L1 experiment.
- G1.3.2 is `EARNED_AT_MICRO_SCOPE`, not three-domain predominance. Procedural
  competence remains authored. `MINIMUM_SELF_EXTENDING_VESSEL` is not issued.
- v1 `COMPACT_VESSEL_PARTIAL` / `PARTIAL_AT_POLYNOMIAL_SCOPE` is not rewritten.
