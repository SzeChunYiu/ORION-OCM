# N1 Phase G attachment evidence — historical negative archive

This directory preserves the raw Phase G note and two result receipts from the historical branch `n1/phase-g-attachment` at `d2b009f464bf52fc60adc81c79e8d1929b3c1560`.

These bytes are **historical negative evidence**, not current-main engineering authority. The old branch is far behind current `main` and also contains parser/induction source changes, so this archive deliberately does **not** import its `CURRENT_ENGINEERING` pointer, engineering source archive, self-application ledger row, or runtime/parser code.

Observed terminal at that historical source/scope: attachment evidence with refutation substantially reduced ambiguity/derivation count but still produced `INTERPRETED 0`; the registered positive-demonstration-only route reached its scoped negative terminal. Read the preserved note and JSON receipts for exact metrics and environment.

This archive does not claim the current N1 implementation reproduces those numbers, does not unlock N2, and does not establish that all future negative/membership evidence routes fail. Any modern-source rerun must be separately registered and source-qualified.
