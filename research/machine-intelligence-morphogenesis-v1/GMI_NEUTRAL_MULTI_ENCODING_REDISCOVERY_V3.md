# GMI Neutral Multi-Encoding Rediscovery v3

Status: **WIDENED K4-STYLE EXACT CONTROL / R1**

Date: 2026-09-12.

Purpose: widen the exact zero-prior control beyond one evaluator implementation. The search receives only primitive scalar observables and generic arithmetic cost expressions. Historical family names are absent from all three evaluators.

## Three evaluator encodings

The same candidate realization is independently evaluated as:

1. recursive arithmetic AST;
2. postfix stack program;
3. explicit dependency graph evaluated topologically.

These are independent implementations of the primitive cost semantics. They are **not** claimed to be independently authored scientific grammars.

## Registered paired cells

Twelve mechanism pressures are represented only by primitive costs:

```text
P01 reusable compact law vs explicit records under revision
P02 compact sufficient state vs history replay
P03 conditional execution vs static union execution
P04 tied invariant state vs free state under mismatch
P05 factorized residual vs full residual
P06 calibrated distributional state vs point state under decision risk
P07 online search vs compiled serving under reuse
P08 checker-gated admission vs direct acceptance
P09 reusable world model/planning vs per-goal direct policy
P10 routed specialization vs shared realization under heterogeneity
P11 local residual update vs global rewrite
P12 aggregation vs single predictor under risk price
```

Each has a matched negative twin designed to reverse the primitive lifecycle inequality.

## Executed result

Across 24 positive/twin cells:

- all three evaluators return identical numeric costs;
- all three evaluators return identical winners;
- every registered positive/twin pair flips winner;
- evaluator disagreements: `0`.

Terminal:

`ZERO_PRIOR_MULTI_ENCODING_MECHANISM_REDISCOVERY_V3_GREEN`.

## Claim ceiling

This is stronger than a one-implementation toy, but it is not broad K4/K5 closure:

- the cost semantics are still hand-registered;
- the three evaluators implement equivalent primitive arithmetic rather than independently authored expressive grammars;
- there is no real learning-scale search;
- no protected external family generator is used.

It is a control showing that twelve recovered property directions are not artifacts of one evaluator implementation.
