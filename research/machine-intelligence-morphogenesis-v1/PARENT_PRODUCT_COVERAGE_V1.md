# Parent-product coverage of the GMI stack v1

Status: **foundational subtraction map.**

The purpose is to test the strongest null:

```text
PARENT_PRODUCT_SUFFICIENT_FOR_GMI_SIGNATURE
```

rather than comparing ORION one mechanism at a time against deliberately incomplete parents.

## Layer 0 — What can be computed?

Parent foundations:

```text
Turing machines / lambda calculus / register machines
universal programming languages
cellular automata / universal dynamical systems
neural computational universality
probabilistic programming
```

They already establish many mutually translatable universal execution substrates.

Track-B residual: **none at pure computability level**.

Disposition:

```text
UNIVERSAL_COMPUTATION_PARENT_SUFFICIENT
```

## Layer 1 — How should systems be factored/composed?

Parents:

```text
circuits / programming-language modularity
category-theoretic compositional systems
coalgebra / dynamical-system composition
factor graphs
production systems
modular neural networks
software/component architectures
```

Factorization can create large resource differences without changing computability.

Track-B residual: a cross-paradigm predictive account of which factorization is developmentally/resource advantageous in which ecology.

## Layer 2 — How is experience converted into change?

Parents:

```text
gradient optimization / automatic differentiation
Bayesian inference
RL / dynamic programming
program synthesis / CEGIS
production/chunk learning
continual learning
meta-learning
learned optimizers
```

Track-B residual: no generic novelty in "systems update from experience"; only cross-paradigm structural prediction/invariance remains open.

## Layer 3 — How is future search/proposal biased by past experience?

Parents:

```text
Bayesian priors/posteriors
MDL / code-length bias
Levin search / OOPS
PAC-Bayes meta-priors
meta-learning
retrieval/library learning
heuristic search
```

OCM #323 contributes a concrete empirical instance of history-induced proposal-rank change.

Track-B residual: a common pre-target structural predictor across morphology families.

## Layer 4 — How much does reaching a target cost?

Parents:

```text
algorithm/runtime complexity
stochastic shortest path / MDP
rational metareasoning
first-passage / drift analysis
optimization convergence theory
sample complexity / PAC bounds
information theory / coding
```

Track-B residual: none for a generic costed state graph or given potential; structural identification of the relevant geometry may remain.

## Layer 5 — Why does one architecture/morphology win a regime?

Parents:

```text
No Free Lunch / inductive bias
algorithm selection
portfolio methods
bounded rationality
rate-distortion / information bottleneck
learning-curve/performance prediction
AutoML / NAS
hardware-aware architecture search
```

Existing theory already supports regime-dependent winners and, in some models, explicit representation/organizational phase transitions.

Track-B residual: predict concrete developmental-equivalence morphology-frontier transitions from machine structure across paradigms rather than choosing from a fixed supplied portfolio.

## Layer 6 — How are architectures discovered?

Parents:

```text
NAS / AutoML
evolutionary architecture search
NEAT/HyperNEAT
AutoML-Zero
GP / CGP
grammar/program synthesis
FunSearch / AlphaEvolve
QD / novelty search
```

Track-B residual: architecture-neutral search from a shared low-level grammar only matters scientifically if preceded by a theory prediction and followed by parent-equivalence reduction.

## Layer 7 — Can the search/generator itself improve?

Parents:

```text
learned optimizers
MetaBBO
automatic algorithm design
evolved mutation/search operators
evolvability search
PowerPlay/OOPS
DGM / program self-improvement
```

Track-B residual: cross-ecology multi-generation improvement in the efficiency of discovering new frontier morphology classes after complete cost accounting.

## Layer 8 — How are claims verified/governed/revised?

Parents/components:

```text
formal verification/proof kernels
TMS/ATMS/provenance systems
versioning/event sourcing/databases
safe policy-improvement / external governance
experimental-science methodology
```

OCM integrates these explicitly and may exhibit useful lifecycle behavior, but integration alone does not create a fundamental intelligence law.

## Layer 9 — What can never be universally predicted?

Parents:

```text
halting / Rice undecidability
Gödel/provability limits
Blum speedup
No Free Lunch
finite-trajectory limits on open-ended claims
```

They forbid an unrestricted exact universal developmental predictor or one globally best morphology.

## Product null

Put together, the strongest parent product already provides:

```text
universal execution substrates
+ multiple factorizations
+ multiple learning/update laws
+ history-dependent priors/bias
+ cost-to-target theory
+ resource-rational selection
+ architecture search
+ meta-optimization/evolvability
+ verification/governance components
+ impossibility boundaries
```

This covers an enormous fraction of any verbal "general machine intelligence framework".

Therefore **a grand integrative diagram is not enough novelty**.

## Narrow live residual

The strongest surviving candidate contribution is:

\[
\boxed{
\text{cross-paradigm structure/history}
\to
\text{compact predictive developmental geometry}
\to
\text{prospective morphology-frontier transition}
\to
\text{endogenous recovery/discovery}
}
\]

with one invariant family retaining predictive meaning across at least several materially different morphology classes and validation regimes.

If that fails or is fully reproduced by the parent product:

```text
PARENT_PRODUCT_SUFFICIENT_FOR_GMI_SIGNATURE
```

is the correct ultimate conclusion.

## What ORION can still contribute under parent sufficiency

Even if the foundational residual dies, ORION-OCM can remain scientifically useful as:

- a causal developmental metrology;
- an explicit evidence/warrant/governance architecture;
- a common experimental harness for comparing developmental morphologies;
- a real persistent-machine implementation;
- an empirical testbed for lifetime/resource/verification phase regimes.

Do not conflate that useful contribution with discovery of a new universal law.