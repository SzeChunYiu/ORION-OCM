# GMI Theory Gap Ledger v1

Status: **OPEN-ASSUMPTION / LOGIC-GAP LEDGER — NO COMPLETENESS CLAIM**

Status date: 2026-09-12.

Purpose:

> Track every currently known logical gap between the exact finite GMI theory/calibrations and a strong cross-paradigm predictive theory of machine-intelligence morphogenesis. A gap is not closed by persuasive prose. Each item has a closure condition and a claim it blocks.

This ledger should be revised only additively with explicit dispositions. Do not delete failed/closed items from history.

---

# 0. Status vocabulary

```text
CLOSED_FORMAL_AT_SCOPE
    exact theorem/no-go closes the logical issue under named assumptions;
    empirical relevance may remain open.

CLOSED_METHOD_AT_SCOPE
    a prospective experimental/methodological contract blocks the loophole;
    execution is still required.

OPEN_BLOCKING_E1_E3
    must be frozen/resolved before strengthened issue #419 confirmation.

OPEN_BLOCKING_CROSS_PARADIGM
    E1 can proceed, but strong shared-law language is blocked.

OPEN_BLOCKING_NEW_MORPHOLOGY
    predictive law may be supported, but novelty/unknown-form claim is blocked.

OPEN_BLOCKING_K2_K3
    only blocks meta-morphogenesis/RSI-adjacent claims.

OPEN_LONG_HORIZON
    important theory extension beyond current E1-E3 scope.

OUT_OF_SCOPE_CURRENT_PROGRAMME
    explicitly not solved by current Track-B evidence.
```

---

# 1. Foundational semantic object gaps

## G-01 — exact semantic quotient computability

**Question.** Can `S_Omega` be computed or approximated in realistic code/math/science obligations?

**Current state.** `CLOSED_FORMAL_AT_SCOPE` for finite exact definitions; `OPEN_BLOCKING_CROSS_PARADIGM` for real regimes.

The quotient theorem is exact and canonical at its registered finite scope, but computing future-distinguishability over rich/open-ended environments can be intractable or undecidable.

**Closure path.** For each real regime, specify one of:

```text
exact bounded quotient;
parent-model sufficient state with proved assumptions;
operational approximation/distortion contract;
partial quotient/lower-bound only.
```

**Blocked claim.** Universal exact semantic-state recovery.

---

## G-02 — stochastic semantic equivalence

**Question.** How should the quotient be defined when protected consequences are distributions rather than deterministic traces?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

Candidate extension:

\[
x\sim_\Omega y
\iff
\forall w,
\mathcal L(Trace(x,w))
=
\mathcal L(Trace(y,w))
\]

under registered stochastic semantics.

Approximate equality requires a prospectively frozen divergence/risk contract.

**Closure path.** Formal stochastic quotient + exact/approximate realization theorem + finite hostile.

---

## G-03 — approximate equivalence is not automatically a quotient

**Question.** How should approximate semantic adequacy be represented without pretending an arbitrary distance threshold is transitive?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM` for approximate domains.

**Closure path.** Use one of:

```text
explicit distortion decision problem;
bisimulation/pseudometric style relation with proved properties;
rate-distortion/sufficient-statistic parent formulation;
set-valued admissibility rather than quotient language.
```

**Blocked claim.** Approximate canonical semantic quotient in noisy domains.

---

## G-04 — continuous/analog precision lower bounds

**Question.** What replaces finite state-count/bit lower bounds when realizations use continuous variables, analog precision or probabilistic codes?

**Current state.** `OPEN_LONG_HORIZON`.

Finite cardinality results already state their assumptions.

**Closure path.** Register distinguishability/noise/precision semantics; use rate-distortion, metric entropy, communication/information complexity or parent-specific bounds where faithful.

---

## G-05 — semantic interpretation map `kappa` identifiability

**Question.** For opaque learned systems, when can a sound map

\[
\kappa:Z\to S_\Omega
\]

be established rather than merely postulated?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

**Risk.** A flexible post-hoc `kappa` can make the normal form unfalsifiable.

**Closure path.** Freeze behavioral/interventional correspondence tests and establish only the part of `kappa` needed for the registered obligation; allow `CANNOT_IDENTIFY_KAPPA_AT_SCOPE`.

---

# 2. Demand-signature gaps

## G-06 — demand/context/response type leakage

**Current state.** `CLOSED_FORMAL_AT_SCOPE` / `CLOSED_METHOD_AT_SCOPE`.

Closed by:

```text
pi -> exogenous context P;
gamma_F -> external feedback contract;
lambda_R -> external retention/availability contract;
interference/plasticity -> response;
```

Refs: `GMI_REALIZATION_DEMAND_SIGNATURE_V1.md`.

Empirical measurement still open under G-09.

---

## G-07 — query/update geometry conflation

**Current state.** `CLOSED_FORMAL_AT_SCOPE` for insufficiency of one overloaded scalar.

`GMI-MN5` constructs matched `sigma/Delta_U` worlds with opposite factor-granularity preference under different `Delta_Q`.

**Remaining work.** Determine which structured summaries of `Delta_Q/Delta_U` are sufficient in E1/E2.

Status of that residual: `OPEN_BLOCKING_E1_E3`.

---

## G-08 — drift/version/lineage conflation

**Current state.** `CLOSED_FORMAL_AT_SCOPE`.

`GMI-MN3/MN4` show lineage demand is induced by protected historical distinctions; `nu` alone cannot identify A5 need.

`beta_lin` is a structured projection of the obligation/lineage quotient, not a separate metaphysical atom.

---

## G-09 — coordinate measurement/identifiability

**Question.** Can each V1 coordinate be measured before protected outcomes with the same meaning across families?

**Current state.** `OPEN_BLOCKING_E1_E3`.

Required artifact: G0 measurement sheet for every primary coordinate, including identifiability class I0-I3.

**Kill.** `SIGNATURE_NOT_PRE_OUTCOME_IDENTIFIABLE`.

---

## G-10 — signature compression sufficiency

**Question.** Does compact `Xi_obl` retain enough structure to predict the registered frontier/mechanism core?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

`GMI_PREDICTIVE_SUFFICIENCY_NO_GO_V1.md` supplies the conditional-sufficiency target and collision logic.

**Closure evidence.** Collision search + residual withheld-structure tests + protected family-held-out transfer.

**Kill.** `ARCHITECTURE_NEUTRAL_RESIDUAL_REMAINS` or `PREDICTIVE_SIGNATURE_RESPONSE_COLLISION`.

---

## G-11 — low dimension can hide high information

**Current state.** `CLOSED_FORMAL_AT_SCOPE` as a no-go; `CLOSED_METHOD_AT_SCOPE` as a prevention contract.

Dimension alone is rejected as compactness evidence.

Future E2 must bound precision/description/measurement complexity and pass remint invariance.

---

## G-12 — primitive vs derived coordinate double counting

**Question.** Are `nu`, use intensity and derived `eta` being counted as independent evidence when one is largely determined by the others?

**Current state.** `OPEN_BLOCKING_E1_E3` for statistical modeling, although V1 marks the distinction.

**Closure path.** Freeze causal/measurement graph; report collinearity/redundancy; compare primitive-only vs derived-summary predictors.

---

## G-13 — measurement burden itself

**Question.** Does estimating `Xi` require expensive interventions, graph discovery or human labeling whose cost erases the claimed realization advantage?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

**Closure path.** Add demand-measurement burden to the complete lifecycle/search ledger when GMI prediction is itself part of the developmental system.

Do not treat an omniscient `Xi` oracle as free in K2/K3 claims.

---

# 3. Response-law gaps

## G-14 — history/path dependence

**Current state.** `CLOSED_FORMAL_AT_SCOPE` as a memoryless-response no-go; `OPEN_BLOCKING_E1_E3` for scope choice.

Before E1 choose:

```text
H0 reset/fixed-history conditional law;
H1 dynamic law with registered history/state summary.
```

**Kill.** `STATIC_RESPONSE_INSUFFICIENT__HISTORY_REQUIRED`.

---

## G-15 — sufficient realization-state summary

**Question.** If H1 is chosen, what summary of optimizer/plasticity/memory/routing/history is sufficient without becoming full candidate identity?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

This is the response-side analogue of demand-signature sufficiency.

**Closure path.** Bounded-information `Phi(M,h)` + response collisions/residual tests + remint/implementation invariance.

---

## G-16 — response descriptor lookup-table vacuity

**Current state.** `CLOSED_FORMAL_AT_SCOPE` as a no-go; `CLOSED_METHOD_AT_SCOPE` via bounded pre-outcome `Phi` contract.

**Remaining empirical burden.** Demonstrate prediction under the constraint.

---

## G-17 — hardware/compiler co-design

**Question.** When morphology and substrate/compiler are co-designed, can `M` and context `P` still be cleanly separated?

**Current state.** `OPEN_LONG_HORIZON`.

Likely treatment:

```text
substrate fixed -> P;
co-designed mapping/compiler -> part of realization/search state and charged;
physical prices/constraints -> P.
```

Requires explicit compilation boundary in E4/real hardware claims.

---

# 4. Mechanism theory gaps

## G-18 — factorization vs incremental repair

**Current state.** `CLOSED_FORMAL_AT_SCOPE` as a conceptual separation; `CLOSED_METHOD_AT_SCOPE` via A1/A3 witnesses.

Empirical separability remains `OPEN_BLOCKING_E1_E3`.

Hostiles C1/C2 in witness registry are mandatory calibration.

---

## G-19 — speculative isolation vs historical persistence

**Current state.** `CLOSED_FORMAL_AT_SCOPE`.

A4 and A5 independence constructions plus MN1/MN3 establish separate obligation drivers.

Empirical witness validation remains open under G-22.

---

## G-20 — authority/serving separation necessity

**Current state.** `CLOSED_FORMAL_AT_SCOPE` conditionally.

MN2 proves side information is necessary when serving compilation aliases developmental quotient states and reacquisition is unavailable/charged.

Not universally required.

**Remaining work.** Measure the fiber/information burden in non-toy systems.

---

## G-21 — compute/time/communication lower bounds

**Question.** Current strongest mechanism theorems are mostly information/state lower bounds. Can GMI prove nontrivial work/latency/communication lower bounds from obligation geometry?

**Current state.** `OPEN_BLOCKING_NEW_MORPHOLOGY` for a strong theoretical novelty claim.

Candidate parents:

```text
communication complexity;
dynamic data-structure lower bounds;
online/streaming lower bounds;
proof/verification complexity;
incremental computation lower bounds.
```

Without such results, much of frontier prediction remains empirical lifecycle economics.

---

## G-22 — mechanism witness identifiability in opaque systems

**Question.** Can A1-A7 be scored without source-label leakage for neural/probabilistic/programmatic parents?

**Current state.** `OPEN_BLOCKING_E1_E3`.

The witness registry is the method contract; controlled calibration must now show:

```text
known-positive detection;
known-negative detection;
C1-C6 collision separation;
implementation-equivalence robustness.
```

---

## G-23 — mechanism causal non-separability

**Question.** Some mechanisms may be structurally coupled: changing A1 can necessarily change A3/A6 or capability.

**Current state.** `OPEN_BLOCKING_E1_E3` for atomic effect claims.

**Allowed disposition.** Report joint bundle effects if no valid intervention isolates the mechanism.

Do not force causal atomism where the design space does not permit it.

---

## G-24 — mechanism thresholds and core sensitivity

**Question.** Does `K*(c)` depend arbitrarily on witness threshold `tau` or frontier tolerance `epsilon`?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

**Closure path.** Freeze thresholds on measurement calibration; report sensitivity/stability regions; prefer continuous mechanism pressure when discrete core membership is unstable.

A brittle one-threshold core is not a law.

---

## G-25 — emergent interaction beyond pairwise mechanisms

**Question.** Can useful behavior arise only from a high-order conjunction not predictable from marginal A1-A7 effects?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

The mechanism lattice permits interactions, but E1 must test at least central preregistered interactions and retain room for discovered higher-order terms.

A successful conjunction does not prove each component independently causal.

---

## G-26 — A7 heterogeneity equivalence

**Question.** What counts as “materially different local realizations” without relying on family names?

**Current state.** `OPEN_LONG_HORIZON` / secondary for current VLC core.

Keep A7 secondary or `NOT_IDENTIFIABLE` until a behavior/resource equivalence test is frozen.

---

# 5. Frontier / decision gaps

## G-27 — noisy Pareto frontier membership

**Current state.** `CLOSED_METHOD_AT_SCOPE`; execution open.

`GMI_E1_E3_THEORY_VALIDATION_GATE_V2.md` requires uncertainty-aware frontier/core reporting.

Status for E1: `OPEN_BLOCKING_E1_E3` until procedure frozen.

---

## G-28 — candidate-class incompleteness

**Current state.** `CLOSED_FORMAL_AT_SCOPE` as a no-go against universal optimum claims; `OPEN_BLOCKING_NEW_MORPHOLOGY` for open-world novelty.

All frontiers are conditional on registered `M_avail` unless a completeness theorem exists.

Unknown-form language requires headroom + neutral search + parent reductions, not “best of current portfolio.”

---

## G-29 — resource valuation / post-hoc scalarization

**Current state.** `CLOSED_METHOD_AT_SCOPE`.

Pareto primary; scalar valuation prospectively frozen where needed.

**Remaining gap.** Real deployment may have partial/uncertain preferences rather than one price vector.

Status: `OPEN_LONG_HORIZON` for robust decision under preference uncertainty.

---

## G-30 — horizon/stopping-rule sensitivity

**Question.** Many realization rankings reverse with effective horizon. What if horizon is uncertain or endogenous?

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM` for general law.

**Closure path.** Treat `H`/survival/use process as distribution/robust range; report crossover sensitivity rather than one fixed horizon.

---

# 6. Search / morphogenesis gaps

## G-31 — adaptive protected-set overfitting

**Current state.** `CLOSED_METHOD_AT_SCOPE` by the D/V/P search firewall; execution remains `OPEN_BLOCKING_E1_E3`.

**Kill.** `ADAPTIVE_SEARCH_OVERFIT_TO_PROTECTED_WORLDS`.

---

## G-32 — neutral grammar prior leakage

**Current state.** `CLOSED_METHOD_AT_SCOPE` by search-encoding tournament requirement; execution remains `OPEN_BLOCKING_E1_E3`.

A vocabulary without the string “VLC” can still strongly favor VLC through primitive costs/defaults.

---

## G-33 — morphogenetic search completeness

**Question.** Failure to recover a morphology may mean the prediction is wrong or the bounded search cannot reach it.

**Current state.** `CLOSED_FORMAL_AT_SCOPE` as normative/search separation; empirically `OPEN_BLOCKING_NEW_MORPHOLOGY`.

**Closure path.** Multiple search methods, explicit reachability diagnostics, exact exhaustive small worlds, and search-budget scaling.

Do not interpret one search failure as impossibility.

---

## G-34 — search burden/prior information

**Current state.** `CLOSED_METHOD_AT_SCOPE` conceptually; `OPEN_BLOCKING_E1_E3` empirically.

Charge:

```text
grammar description;
search implementation;
proposal/evaluation work;
failed candidates;
manual tuning;
measurement/verification;
```

A human-coded target-specific search prior can otherwise launder morphology information.

---

# 7. Cross-paradigm / empirical gaps

## G-35 — family-held-out invariance

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

This is the central E2 burden.

Required:

```text
same coordinate definitions;
same mechanism witnesses;
held-out materially different family;
surface remint;
intervention-direction transfer;
native parents first refusal.
```

**Kill.** `NO_CROSS_PARADIGM_INVARIANCE`.

---

## G-36 — real-regime transfer

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM`.

Synthetic success must transfer separately to:

```text
code maintenance;
Lean/formal mathematics;
changing factual/scientific knowledge.
```

Definitions may not be silently rewritten per domain.

---

## G-37 — noisy/graded verification

**Question.** Science and factual systems do not have Lean-like exact admission.

**Current state.** `OPEN_BLOCKING_CROSS_PARADIGM` for general verifier theory.

Need explicit false-accept/false-reject, evidence update, statistical confidence and provenance semantics.

A4 “verify-before-adopt” may become posterior/admissibility-threshold semantics rather than theorem checking.

---

## G-38 — closed-loop/endogenous ecology

**Question.** What if machine actions change future task distribution, evidence availability or dependency structure?

**Current state.** `OPEN_LONG_HORIZON`.

Then `Xi_obl` is not purely exogenous; it becomes policy-coupled.

Likely extension:

\[
(\Omega_t,\Xi_t,Z_t)
\leftrightarrow
\text{machine actions}
\to
\Omega_{t+1}.
\]

Requires control/causal dynamic treatment.

---

## G-39 — multi-agent/distributed obligations

**Question.** Partial orders, replica divergence, communication failures and strategic agents can make one scalar developmental history invalid.

**Current state.** `OPEN_LONG_HORIZON`.

Distributed-systems parents already own much of this structure; GMI should import them before claiming general distributed intelligence laws.

---

# 8. “Phase” / scaling gaps

## G-40 — finite crossover vs asymptotic phase transition

**Current state.** `CLOSED_METHOD_AT_SCOPE`.

Current E0/V2 supports exact finite frontier crossovers only.

Strong phase-transition language remains `OPEN_LONG_HORIZON` pending size-indexed scaling evidence.

---

## G-41 — hysteresis / path-dependent regime boundaries

**Question.** If morphology adaptation itself has switching cost/history, the boundary for entering a mechanism may differ from the boundary for leaving it.

**Current state.** `OPEN_LONG_HORIZON`.

This is especially relevant once `Gamma` actively changes morphology online.

A static equilibrium phase diagram may then be insufficient.

---

# 9. Novelty / parent-reduction gaps

## G-42 — bounded parent reduction criterion

**Current state.** `OPEN_BLOCKING_NEW_MORPHOLOGY`.

Need explicit equivalence/reduction metrics:

```text
semantic preservation;
resource overhead;
development/update overhead;
search/compiler description cost;
mechanism preservation;
```

If a candidate compiles to a parent product with bounded overhead and no lost developmental distinction, novelty claim collapses.

---

## G-43 — source-code novelty vs morphology novelty

**Current state.** `CLOSED_METHOD_AT_SCOPE` as a prohibition.

New diagrams/classes are irrelevant without behavioral/resource separation.

Empirical novelty remains open under G-42.

---

## G-44 — open-world headroom

**Question.** Even if known parents are dominated, is the predicted frontier hole feasible rather than forbidden by a deeper lower bound?

**Current state.** `OPEN_BLOCKING_NEW_MORPHOLOGY`.

Need:

```text
lower-bound analysis;
known-parent upper bounds;
nonempty feasible gap;
neutral search/recovery.
```

---

# 10. K2/K3 / meta-morphogenesis gaps

## G-45 — meta-state and meta-obligation

**Question.** What is the semantic quotient when the protected object is the process that chooses representations/morphologies itself?

**Current state.** `OPEN_BLOCKING_K2_K3`.

The current K3 metric is useful but not a full meta-semantic theory.

---

## G-46 — measurement/search cost of learning `Xi` and `R_M`

**Question.** A meta-system that “learns the theory” may spend enormous work estimating demand/response laws.

**Current state.** `OPEN_BLOCKING_K2_K3`.

K3 complete burden must include theory/model acquisition, not only later morphology search savings.

---

## G-47 — evolving verifier/constitution

**Question.** Current normal form keeps `V/C` external to prevent self-certification. What if a meta-system proposes changes to its own verifier/constitution?

**Current state.** `OUT_OF_SCOPE_CURRENT_PROGRAMME` / `OPEN_BLOCKING_K2_K3` for RSI-adjacent claims.

A higher-order external meta-constitution/admission boundary is required; otherwise “self-improvement” can be faked by weakening the judge.

---

## G-48 — recursive self-improvement claim

**Current state.** `OUT_OF_SCOPE_CURRENT_PROGRAMME`.

Nothing in #418/#419 establishes RSI.

Required at minimum:

```text
verified improvement of morphogenetic process on fresh families;
complete self-modification cost;
fixed external evaluation/constitution;
nontrivial improvement beyond AutoML/meta-search parents;
replicated multi-generation effect.
```

---

# 11. Priority closure plan

## P0 — before strengthened protected E1/E2/E3

Close/freeze:

```text
G-07 residual structured Delta_Q/Delta_U measurement
G-09 coordinate measurement/identifiability
G-12 primitive/derived redundancy plan
G-14 H0/H1 history scope
G-22 witness calibration
G-23 atomic vs bundle intervention labels
G-27 frontier uncertainty procedure
G-31 adaptive search firewall implementation
G-32 alternate search encodings
G-34 complete search burden ledger
```

## P1 — before cross-paradigm law claim

Need evidence on:

```text
G-01 real semantic-state operationalization
G-02/G-03 stochastic/approximate semantics where used
G-05 kappa correspondence
G-10 signature sufficiency
G-13 measurement burden
G-15 response-summary sufficiency
G-24 threshold/core stability
G-25 interactions
G-30 horizon uncertainty
G-35 family invariance
G-36 real transfer
G-37 graded verification
```

## P2 — before new morphology language

Need:

```text
G-21 stronger resource lower bounds where possible
G-28 candidate-class honesty
G-33 multi-search reachability evidence
G-42 bounded parent reductions
G-44 feasible frontier headroom
```

## P3 — before K2/K3/RSI-adjacent language

Need:

```text
G-45 meta-semantic state
G-46 theory-learning cost
G-47 fixed higher-order constitution
G-48 replicated multi-generation gain
```

---

# 12. Claim dependency graph

Use this as a hard ordering, not a narrative suggestion.

```text
SEMANTIC OBLIGATION SPECIFIED
        |
        v
SEMANTIC ADEQUACY / QUOTIENT CORRESPONDENCE
        |
        v
TYPED PRE-OUTCOME DEMAND + CONTEXT MEASUREMENT
        |
        v
MECHANISM WITNESSES VALIDATED
        |
        v
CONTROLLED MECHANISM REVERSALS (E1)
        |
        v
SHARED FAMILY-HELD-OUT PREDICTIVE RESIDUAL (E2)
        |
        v
CROSS-FAMILY MECHANISM / INTERVENTION INVARIANCE
        |
        v
NEUTRAL RECOVERY ON UNTOUCHED PROTECTED WORLDS (E3)
        |
        v
SEARCH-ENCODING / IMPLEMENTATION REPLICATION
        |
        v
REAL-REGIME TRANSFER (E4)
        |
        v
BOUNDED PARENT REDUCTION + FEASIBLE HEADROOM (E5)
        |
        v
DISTINCT THEORY-PREDICTED MORPHOLOGY CANDIDATE AT SCOPE
        |
        v
META-MORPHOGENESIS (E6) [separate additional burden]
```

Skipping an arrow does not make the later claim false by logic alone; it means the current evidence does not establish it.

---

# 13. Current closed logic improvements from the #418 hardening cycle

The following gaps are materially stronger than at the start of #418:

```text
factorization != versioning;
query geometry != update geometry;
drift != historical persistence;
speculative isolation != persistence;
physical modules != incremental repair;
serving state != developmental sufficient state;
demand != response != resource context;
current exogenous signature != full dynamic response when history matters;
finite crossover != critical phase transition;
low dimension != low information;
mechanism semantics != literal implementation encoding;
normative frontier != bounded morphogenetic recovery.
```

These are theory advances even before E1-E3 results exist.

---

# 14. Current terminal

```text
GMI_THEORY_GAP_LEDGER_V1_FROZEN
NO_THEORY_COMPLETENESS_CLAIM
P0_GAPS_BLOCK_STRENGTHENED_E1_E3_CONFIRMATION_UNTIL_FROZEN
```

The correct scientific posture is not “all logic gaps are filled.”

It is:

> Every currently identified gap has an explicit type, status and closure path; new gaps discovered by hostile testing are added rather than explained away.
