# GMI E3 status v1 — math + code bridge

Status date: 2026-09-11. Formal GMI-v1 synthesis is already merged on main. This tranche begins empirical real-domain validation.

## Current result

The math/code bridge now has one shared contract for:

```text
developmental situation/state
morphology realization
external verifier/constitution/development protocol
raw burden/resources
capability-resource profile
K0/K1/K2/K3
RESET / CONTINUED / strongest-parent comparison
failure/censoring semantics
```

Formal mathematics and execution-verified coding have been mapped into that contract without introducing domain-specific definitions of intelligence or developmental capital.

Current theory-level terminal:

```text
GMI_E3_MATH_CODE_COMMON_PROTOCOL_FROZEN
```

A deterministic static schema validator accepts the frozen math/code contract and rejects the registered hostile mutations:

```text
math-only resource semantics                 -> rejected
removing K2 from common capital ladder       -> rejected
removing verifier from external contract     -> rejected
removing one of the two domain mappings      -> rejected
```

This is a **design/schema result**, not real capability evidence.

## Domain execution gates

### Mathematics

#46 remains locked. This tranche does not unlock it.

Allowed now:

```text
reuse/audit proof-replay-v1
build common adapters/receipts/meters
static schema tests
parent and future-protocol freeze
```

Protected theorem execution and GMI math K1/K2 remain gated on the domain owner.

### Coding

#208 is open for its H0 infrastructure/metering work and future prospectively frozen H1 protected comparison.

The E3 schema should be added to that execution rather than creating a parallel coding benchmark.

## Strong parents frozen

Mathematics:

```text
Lean kernel / pinned environment          checker
LeanDojo-v2 / Pantograph                  infrastructure
ReProver-style retrieval + proof search   strong parent
LeanAgent-style lifelong proving          mandatory developmental parent
strong current agentic prover             capability reference
```

Coding:

```text
same frontier foundation-model harness
persistent retrieval/trajectory memory
reflection/failure feedback
explicit skill libraries
Repo-To-Skill / ContinualSkillBench-style parents
Terminal-Bench-style capability calibration
protected/private audited tasks for confirmation
```

Public SWE benchmark results are calibration only where contamination/evaluator validity is unresolved.

## What E3 has NOT earned

```text
NO empirical math developmental result
NO empirical code developmental result from this tranche
NO cross-domain K1
NO K2
NO heterogeneous lifelong-machine claim
NO evidence that OCM beats strongest parent products
```

## Immediate next authorized work

```text
E3-1  run static validator/tests in repository CI/local execution
E3-2a wire GMI episode/resource receipts into #208 H0 coding infrastructure
E3-2b map existing proof-replay-v1 receipts into GMI episode schema without new protected theorem outcomes
E3-3  domain-owner public/calibration runs under frozen parents
E3-4  separate protected within-domain K1 freezes
```

## Theory falsifier

If either domain requires changing the meaning of semantic developmental state, morphology, raw burden, K1/K2 or generality rather than merely specializing verifier/capability coordinates, return:

```text
GMI_THEORY_REVISION_REQUIRED_<field>
```

Do not repair the mismatch by silently creating domain-specific intelligence definitions.

## Claim ceiling

```text
GMI_E3_COMMON_MATH_CODE_THEORY_INTERFACE_SPECIFIED
EMPIRICAL_E3_UNESTABLISHED
```
