# RV-377-142 — FREEZE: the class-rate law revived clause-only (M′ / P′ / O′), two further fresh ecologies

Successor of RV-377-140 (`CLASS_RATE_MORPHOLOGY_LAW_RED_AT_REGISTERED_SCOPE`, clauses P and O). Under
the #373 revival law only the tripped clauses change, each by the minimal change its root cause names,
and the corrected law is tested prospectively on two fresh ecologies chosen by the same deterministic walk.
Frozen before any run; commit precedes the first search.

## 1. Root causes carried over (RV-377-140 adjudication)

* P predicted "≤ 1/3 everywhere" from a base rate; the selection receipt's own rule-36 replay of the
  exact-search row was admissible on every fresh ecology and the class appeared on 1/3, 2/3, 1/3, 1/3.
* M predicted from the Stage D'/E' `S5h` standard-protocol closed form; the IR memory row replays below θ
  under rule 36 on all four fresh ecologies (0.7708 / 0.7917 / 0.8125 / 0.8229) and the `E_cr4` elites fail
  the same bar; conversely `E_cr2`/`E_cr3` recovered memory 2/3 with the row inadmissible — a row cannot
  predict *absence* of searched elites.

## 2. The corrected clauses

> **P′.** Program class present (≥ 1/3 seeds) iff the registered exact-search row (`program_search`)
> is rule-36 admissible on the ecology (min over the six V1 interventions ≥ θ) with ≥ 1 fx margin over the
> best constant; otherwise ≤ 1/3.
>
> **M′.** Memory class present (≥ 2/3 seeds) iff the best registered IR memory row (`hamming_knn_k3`,
> `exemplar_table`, `soft_retrieval`) is rule-36 admissible with ≥ 1 fx margin; otherwise the law
> **abstains** on the memory class (a hand-built row licenses presence, never absence).
>
> **C.** Unchanged: coefficient class ≤ 1/3 on witness-free ecologies; no bound where witness-bearing.
>
> **O′.** rate(program) ≥ rate(coefficient) wherever P′ predicts program present.

Retrodiction on the RV-377-140 record (not evidence, a consistency check): P′ present on all four fresh
ecologies (rows 1.0 / 0.9583 / 1.0 / 0.9583, margins ≥ 3.5 fx) — observed 1/3, 2/3, 1/3, 1/3 ✓; M′ abstains
on all four (rows < θ) — no claim, no miss; C ✓ (0/3 on `E_cr1`, `E_cr4`); O′ ✓ (program ≥ coefficient on all
four). On the earlier record (RV-377-141 table): `E_sym5` program row admissible → present, observed 1/12 ✓
(≥ 1/3 is over 3-seed batches; 1/12 fails the batch form on 3 of 4 batches — recorded as a **known tension**
of P′ at the 3-seed granularity, not hidden).

## 3. Fresh ecologies (deterministic walk, `STAGE_CLASSRATE_SELECTION_V44_CLASSRATE_SELECT6.json`)

| name | canonical idx | coeffs | bc | `hamming_knn_k3` min6 / std | `exemplar` | `program_search` min6 | witness-bearing |
|---|---|---|---|---|---|---|---|
| `E_cr5` | 8 | (−½, −½, −½, ½) | 0.8333 | 0.8229 / 0.8333 | 0.5833 | 1.0 (+4.0 fx) | no |
| `E_cr6` | 9 | (−½, −½, −⅜, −½) | 0.6875 | 0.7917 / 0.7917 | 0.375 | 0.9583 (+6.5 fx) | no |

Three seeds each, 20 000 charged evaluations, tag `V45_CLASSRATE_billy`, laptop billy, ≤ 2 processes.

## 4. Frozen predictions

| ecology | P′ | M′ | C | O′ |
|---|---|---|---|---|
| `E_cr5` | program **≥ 1/3** | abstain | coefficient ≤ 1/3 | program ≥ coefficient |
| `E_cr6` | program **≥ 1/3** | abstain | coefficient ≤ 1/3 | program ≥ coefficient |

## 5. Falsifiers (stated once)

| id | law RED if |
|---|---|
| F-P′1 | program class 0/3 on either fresh ecology (P′ predicts present on both) |
| F-C1 | coefficient class ≥ 2/3 on either (both witness-free) |
| F-O′1 | rate(coefficient) > rate(program) on either |

None tripped → `CLASS_RATE_MORPHOLOGY_LAW_V2_SUPPORTED_AT_REGISTERED_SCOPE__M_ABSTAINS` (a positive on the
program and coefficient clauses, with the memory clause honestly outside the law's domain until a
searched-elite existence certificate replaces the row predictor — named as the next lever, not adopted).
Any tripped → `CLASS_RATE_MORPHOLOGY_LAW_V2_RED_AT_REGISTERED_SCOPE`, filed; this is the one revival iteration
RV-377-140 permitted, so no RV-377-143 on the same clauses.

Receipts: `STAGE_B1_V45_CLASSRATE_billy_E_cr{5,6}_S{0,1,2}.json`, scoring
`STAGE_CLASSRATE_FRESH_V45_CLASSRATE_billy.json` (V1 family; V2 augmentation beside it).

---

# RV-377-142 — ADJUDICATION (receipts `STAGE_B1_V45_CLASSRATE_billy_E_cr{5,6}_S{0,1,2}.json`, scoring `STAGE_CLASSRATE_FRESH_V45_CLASSRATE_billy.json` + `_V2AUG`, billy-laptop)

Six searches, 20 000 charged evaluations each, scored by §3 (rules 36/40/32/23) under the V1 family with
the V2 family augmented beside it.

| ecology | bc | memory | program | coefficient | classes by seed | V2 |
|---|---|---|---|---|---|---|
| `E_cr5` | 0.8333 | 0/3 | **1/3** | 0/3 | S0 ∅, S1 {PROGRAM}, S2 ∅ | identical |
| `E_cr6` | 0.6875 | 0/3 | **1/3** | 0/3 | S0 ∅, S1 ∅, S2 {PROGRAM} | identical |

The two recoveries are exact program machines with large separations, and both survive charged atrophy:

* `E_cr5` S1: min over six **1.0**, margin **4.00 fx**, 13 → 7 nodes, atrophied carrier PROGRAM.
* `E_cr6` S2: min over six **0.9583**, margin **6.50 fx**, 26 → 8 nodes, atrophied carrier PROGRAM.

| clause | prediction | outcome |
|---|---|---|
| **P′** | program ≥ 1/3 on both (exact-search row admissible: 1.0 / 0.9583, margins ≥ 4 fx) | **HELD** — 1/3 on both |
| **M′** | abstains (memory rows below θ under rule 36: 0.8229 / 0.7917) | no claim made; observed 0/3, consistent |
| **C** | coefficient ≤ 1/3 on both (witness-free) | **HELD** — 0/3 on both |
| **O′** | program ≥ coefficient wherever P′ predicts present | **HELD** — 1 ≥ 0 on both |

| falsifier | tripped? |
|---|---|
| F-P′1 (program 0/3 on either) | no |
| F-C1 (coefficient ≥ 2/3 on either) | no |
| F-O′1 (coefficient > program) | no |

**Terminal: `CLASS_RATE_MORPHOLOGY_LAW_V2_SUPPORTED_AT_REGISTERED_SCOPE__M_ABSTAINS`.**

## Stated plainly, including what this does not buy

* P′ passed at its **weakest possible margin**: it predicts "present (≥ 1/3)" and the observed rate is
  exactly 1/3 on both ecologies. The prediction is directional and the bar is low. The tension the freeze
  disclosed in advance — `E_sym5` recovers program on 1 of 12 seeds, which fails the ≥ 1/3 batch form on 3
  of 4 three-seed batches — is **not** resolved by this test and remains recorded.
* M′ made no claim on either ecology, which is the honest state: a hand-built row licenses presence, never
  absence, and on these two ecologies no memory row is admissible. The memory clause is therefore outside
  the law's domain here, not supported by it.
* **C is the clause that keeps accumulating.** The coefficient class is now recovered **0 of 43** times
  across the corpus at 20 000 charged evaluations under the atrophied reading (16 diagnosis + 9 lane B +
  12 RV-140 fresh + 6 here), on every registered and fresh ecology, witness-bearing and witness-free alike.
  `COEFFICIENT_CLASS_NOT_NEUTRALLY_RECOVERED_AT_20K__0_OF_43`.

## Instrument disclosure

`score_fresh` keys its receipt list off the selection receipt, and `STAGE_CLASSRATE_SELECTION_V44_CLASSRATE_SELECT6.json`
lists all six chosen names (`E_cr1`…`E_cr6`). Only `E_cr5`/`E_cr6` were run under the `V45` tag, so the
scoring receipt's `status` field reads `INCOMPLETE__RECEIPTS_MISSING` and its `missing` list names the
twelve `V45`-tagged `E_cr1..4` files that were never intended to exist (those ecologies were scored under
`V43` in RV-377-140). The `E_cr5`/`E_cr6` rates are complete at 3 seeds each and are what this record
adjudicates. No threshold, prediction or clause was changed after the outcomes.
