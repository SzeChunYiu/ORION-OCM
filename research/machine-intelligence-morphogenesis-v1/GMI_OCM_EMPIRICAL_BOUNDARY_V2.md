# GMI ↔ OCM empirical boundary v2

Status: **evidence synchronization after post-v4 #323 results**. This file does not reinterpret #323; it imports its registered terminals into GMI-v1.

## 1. Current evidence state

The strongest OCM developmental evidence remains:

```text
K1 / C2 history-induced cognition/search capital:
  SUPPORTED at authored program-search grammar scope

K2 / C3 history makes acquisition of new K1 cheaper:
  NOT_ESTABLISHED at the current grammar
```

The C3 retained-capital recombination chain stopped under its registered rule after only `2 / 9` fresh seeds met the registered benefit bar. GMI-v1 therefore treats K2 as **unearned** in this lane.

## 2. Independent-authorship M2-P2 terminal

The scored Stage-3 replication terminated:

```text
CANNOT_CHECK_NO_ADMITTING_WORLD
```

across 8/8 scored worlds because the frozen admission rule refused all worlds. In addition, the inherited admission law was violated on two scored worlds while `LIBRARY_ONLY = RESET` and shuffled history was worse than RESET on all eight.

Consequences for GMI-v1:

- this result does **not** falsify the broad K1 phenomenon;
- it also does **not** supply independent-authorship positive replication;
- no cross-author generality upgrade is permitted from M2-P2;
- `CANNOT_CHECK` remains a first-class theory/evidence terminal.

## 3. Later controller revisions

Post-v6.3 controller work produced additional falsified revisions and rollbacks under registered stop rules. The current lane kept the better-supported incumbent rather than asserting monotonic version improvement.

This is evidence for the methodological requirement encoded in GMI/HST:

```text
newer implementation != more intelligent by definition
```

A developmental change earns credit only through a matched verified burden/capability trajectory under the declared protocol.

## 4. Current live K1 confirmation

A new full-arm K1 confirmation batch on fresh seeds was registered prospectively after the later controller boundary was recorded. Until those outcomes are complete, GMI-v1 records:

```text
K1_CONFIRMATION_NEW_BATCH_PENDING
```

and does not upgrade the authored-grammar evidence.

## 5. Theory consequence

The OCM evidence currently supports one concrete instantiation of GMI-v1's distinction between:

```text
semantic/capability state
vs
realization/update mechanism
vs
resource burden
```

because controller revisions can change resource trajectories without changing the target contract/verifier.

It does **not** establish the GMI synthesis theory by itself, and it does not establish K2/K3.

## Terminal

```text
OCM_K1_C2_SUPPORTED_AT_AUTHORED_SCOPE
OCM_K2_C3_NOT_ESTABLISHED_AT_CURRENT_GRAMMAR
M2P2_INDEPENDENT_AUTHORSHIP_CANNOT_CHECK_NO_ADMITTING_WORLD
NEW_K1_CONFIRMATION_PENDING
NO_EMPIRICAL_UPGRADE_BEYOND_REGISTERED_SCOPE
```
