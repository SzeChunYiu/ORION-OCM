# GMI Zero-Prior Closure Status v2

Status: **AUTHORITATIVE CURRENT SYNTHESIS / STRONG TERMINALS NOT CLAIMED**

Status date: 2026-09-12.

Purpose:

> Consolidate the recursively hardened state of GMI after theorem expansion, exact microscopes, hostile grammar attacks, domain reductions, estimator bounds and synthetic response-law calibration. This file supersedes v1 as the current claim boundary; earlier files/receipts remain part of the preserved evidence record.

---

# 1. Hard prerequisite for unknown-species discovery

GMI may not treat an unknown-form prediction as strong evidence of discovery until the same architecture-neutral derivation machinery can recover the registered known machine-intelligence family set from zero prior.

Required chain:

\[
\text{obligation/context}
\rightarrow
\text{semantic sufficient state}
\rightarrow
\text{lower bound}
\rightarrow
\text{constructive realization}
\rightarrow
\text{development/lifecycle law}
\rightarrow
\text{negative twin}
\rightarrow
\text{neutral recovery}
\rightarrow
\text{held-family prediction}.
\]

Derivation levels remain:

```text
K0 post-hoc home
K1 structural direction
K2 formal property derivation
K3 quantitative response law in registered base regime
K4 neutral rediscovery
K5 held-family prediction
K6 real-regime replication
```

Strong target: all registered families structurally derived/reduced, broad K4 recovery, and major frontier choices K5+.

---

# 2. Structural mechanism basis

Most historical families now reduce to combinations of a smaller architecture-neutral basis:

```text
shared coefficient / basis state
explicit record / exemplar memory
associative/content-addressable memory
probabilistic belief state
symbolic / program / constraint state
predictive / recurrent / dynamical state
search / frontier state
routing / conditional composition
verification / authority
low-rank / residual / quotient compression
distributed / population state
morphogenesis / parameter expansion
physical/nonclassical carrier state
```

Major reusable structural coordinates now include:

```text
target-family description complexity
metric covering radius
spectral/effective rank
feature/source alignment
symmetry deviation and residual complexity
dependency core/residual geometry
mode heterogeneity and gradient alignment
predictive mutual information / Hankel rank
communication cut capacity
retention-safe dimension and curvature
proposal failure-mass reduction
reuse/amortization horizon
niche probability and archive cost
precision/noise/reliability burden
transfer modulus
```

The registered family list and historical-family reductions are in:

```text
GMI_KNOWN_MACHINE_INTELLIGENCE_FAMILY_REGISTRY_V1.md
GMI_KNOWN_FAMILY_REDUCTION_MAP_V1.md
```

---

# 3. Theorem-heavy zero-prior tranche

Issue #432 is complete. Formal property/base-response derivations now cover, at minimum:

```text
linear sufficient statistics
shared-law versus arbitrary memory information
finite Bayesian/posterior sufficiency and necessity
metric nearest-neighbor error/coverage
associative-memory coding geometry
kernel representer theorem / GP posterior structure
recursive partition/tree property
boosting weak-edge response
ensemble diversity/correlation law
future-response quotient / Hankel state
predictive-information state lower bound
finite-group symmetry sharing
graph-message locality and cut communication
dynamic routing / fixed-core dynamic-residual routing
low-rank residual/adaptation
conditional specialization / mode rank
search/verifier/compile amortization
typed tool composition and reliability
continual-update safe subspaces / nonlinear local drift
model/value/policy control
control bisimulation quotient
AR/latent/flow/score/energy/adversarial generative structure
denoising-score and ELBO identities
exploration/meta-state sharing
quality-diversity archive value
distributed sufficient-statistic aggregation
physical precision/reliability accounting
quantum claim boundaries
```

This is structural/theorem coverage, not broad K4/K5 closure.

---

# 4. Developmental potential is now composition-aware

Standalone domain breadth is not a rejection criterion.

Current theory includes:

\[
B^*_{dev}(S;\Theta)
=
\inf\{\pi^Tb:S\text{ reaches the registered intelligence constitution}\},
\]

and domain/module complementarity

\[
\Delta_D(\Theta)
=
B^*_{-D}-B^*_{+D}.
\]

Proved base laws include:

```text
narrow module can change B*_dev from infinity to finite
exact narrow-module adoption inequality
KKT marginal developmental value allocation
irreducible-floor condition for infinite developmental burden
breadth-versus-cost selection law
morphology-switch/reuse threshold
```

Therefore a narrow verifier, planner, database, theorem prover or physical module may be essential to a strong composite species even if it has little standalone breadth.

---

# 5. Neural development/generalization hardening

The neural gap is smaller but still blocking.

Closed/calibrated base cases:

```text
quadratic gradient development:
    exact spectral/condition-number law

nonlinear accessibility:
    exact dead-ReLU counterexample
    leaky-activation escape counterfactual

smooth nonlinear development:
    PL-condition geometric convergence law

overparameterized linear development:
    zero-initialized gradient descent -> minimum-norm interpolant
    initialization nullspace is preserved

finite-family generalization:
    log(M)-dependent sample bound

algorithmic stability:
    expected train-test gap bound
    regularized convex ERM stability

PAC-Bayes:
    posterior/prior information complexity parent bound

feature alignment:
    exact teacher-student law
    R_r = B + (B+sigma^2) r/(n-r-1)
    reduced features beat full OLS iff
    B < sigma^2(d-r)/(n-d-1)

layered nonlinear composition:
    depth-separation parent theorems establish real representation savings
```

Still OPEN-BLOCKING:

```text
high-dimensional nonconvex basin/accessibility prediction
feature-learning trajectory dynamics
SGD/optimizer-state/normalization/depth/width response law
modern overparameterized nonlinear protected-risk prediction
prospective mechanism selection among stability/margin/PAC-Bayes/compression/alignment
```

No claim of a complete theory of deep learning is allowed.

---

# 6. Shared versus specialized learning moved beyond static structure

Current laws now include:

```text
mode-span rank = minimal linear shared basis dimension
local gradient-interference law g_i^T g_j
curvature-aware finite-step no-harm condition
exact block-isolation theorem
shared-core + specialized residual decomposition
```

A finite-data scalar-context law is exact:

\[
R_{sep}=\frac{\sigma^2}{N},
\qquad
R_{share}=\tau^2+\frac{\sigma^2}{mN}.
\]

Sharing wins iff

\[
\tau^2<\frac{\sigma^2}{N}\left(1-\frac1m\right)
\]

before extra specialization/routing cost.

Executed synthetic receipt:

```text
270 parameter cells
4 theoretical ties
266 non-tie cells
266/266 winner predictions correct
```

This is a genuine finite-data K3/K5-style calibration in a simple observable-context model, not neural MoE closure.

---

# 7. Predictive/recurrent state now has multiple independent lower bounds

Current stack:

```text
future-response quotient:
    minimal arbitrary exact semantic state

TV packing:
    approximate cardinality lower bound

Hankel rank:
    minimal exact linear predictive dimension

predictive mutual information:
    I(history;future) <= H(state)
```

Recurrent state has also been corrected for legal history access:

```text
streaming parity with current-input interface:
    one-bit recurrence, O(T) work

full-history replay queried every prefix:
    O(T^2) work

single final query with full history supplied:
    stateless O(T) replay is competitive
```

Therefore recurrence is predicted from semantic history dependence **plus history-access/reuse economics**, not history dependence alone.

---

# 8. Symmetry and routing are hybrid phases, not binary choices

Exact symmetry theory now predicts:

```text
fully shared/equivariant core
shared core + sparse/low-rank/local symmetry-breaking residual
fully free realization
```

according to residual burden.

Routing theory now predicts:

```text
stable fixed dependency core
input-specific dynamic residual routing
```

because all union-minus-average routing opportunity lies in the residual dependency family.

These corrections prevent one exception from forcing a false all-or-nothing architecture flip.

---

# 9. Generative theory is structurally broad but selection remains open

Formal/parent identities now cover:

```text
AR chain factorization
joint entropy chain rule / total correlation
latent information decomposition
flow absolute-continuity support constraint
score uniqueness
Gaussian denoising -> score identity
ELBO/KL latent inference identity
energy representation + normalization coupling
ideal adversarial discriminator/JS objective
latent component information lower bound
```

The remaining core problem is to predict, before development, the family burden curves

\[
C_R^*(\epsilon)
=
\inf_{q\in R:\,KL(p\|q)\le\epsilon}B_R(q)
\]

for competing realization families.

OPEN-BLOCKING:

```text
conditional-law complexity
transport complexity
score/noising/sampler complexity
latent quotient discoverability
energy sampling/normalization burden
adversarial witness/optimization difficulty
protected cross-family selection
```

---

# 10. Control/world-model theory is now semantic-quotient based

Current closed base laws include:

```text
Bellman contraction / value fixed point
value/Q -> direct policy compilation
policy-gradient score-function identity
value baseline unbiasedness
exploration collision/no-go
model/planning versus policy amortization
exact MDP bisimulation quotient
approximate reward/transition quotient error bound
```

A reusable world model need not reconstruct raw environment state; it must preserve distinctions that change registered rewards or transition mass into decision-relevant classes.

OPEN-BLOCKING:

```text
learned quotient discovery
partial-observability control quotient
model error/generalization
exploration in large environments
protected model-based/model-free/hybrid prediction
```

---

# 11. Pre-outcome descriptor estimation is now assumption-indexed

Identifiability no-go results remain in force: incomplete finite data cannot universally identify unseen structure.

Where simple assumptions make estimation possible, exact/confidence laws now exist for:

```text
dependency-edge frequency and union recovery under p_min
residual-event probability and zero-observation upper bounds
scalar context heterogeneity with noise correction
active residual-category coverage under p_min
matrix/effective rank under operator-norm uncertainty and spectral gap
```

Every architecture prediction must report:

```text
latent descriptor
observable information
identifiability assumption
point estimate / uncertainty
phase-boundary distance
abstention rule
negative twin violating the assumption
```

---

# 12. Transfer is now quantitative

For fixed developed machine loss:

```text
bounded loss:
    |R_Q-R_P| <= L_max TV(P,Q)

bounded density ratio dQ/dP <= W:
    R_Q <= W R_P

K-Lipschitz loss:
    |R_Q-R_P| <= K W_1(P,Q)

unseen target support:
    source-only performance is distribution-free unidentifiable
```

Transfer should be measured on the semantic quotient when surface remints are irrelevant.

Developmental transfer regret remains OPEN-BLOCKING because the target ecology can change the optimal morphology/development path.

---

# 13. Exact zero-prior rediscovery evidence and preserved failure

## Green local evidence

Small exact neutral-property search has recovered, with negative twins:

```text
shared coefficients versus indexed cells
recurrent versus memoryless state
input-conditioned versus fixed routing
symmetry tying versus free matrix
low-rank versus full residual state
```

Remint successor covered 2,080 coordinate/state/edge/basis remints with zero mismatches.

## Preserved RED: grammar-bias hostile

The first coefficient/memory negative twin

```text
[0,0,0,0,1]
```

was hard only in the polynomial-coefficient grammar.

An independently registered add/multiply/equality DSL gives the three-node realization

```text
eq(4,x)
```

while the affine positive target costs five AST nodes.

Therefore the predecessor twin is RED as a **cross-grammar incompressibility control**.

An exhaustive semantic hostile enumerated all

\[
5^5=3125
\]

functions in that DSL and preserved the failure.

Successor theory replaces handpicked surface hardness with a multi-grammar counting theorem:

\[
P[K_G(F)<L]
<
\frac{2^L}{q^N}
\]

for a uniform random exact labeling, and across `m` frozen grammars

\[
P[\min_jK_{G_j}(F)<L]
<
m\frac{2^L}{q^N}.
\]

A protected random-label generator, with grammar/compiler/search costs charged, is now the correct incompressibility twin.

Desired successor terminal:

`ZERO_PRIOR_COMPRESSION_MULTI_GRAMMAR_GREEN`.

---

# 14. Domain-discovery status after hostile reductions

Current strong count:

```text
proved new H3/H4 ordinary-classical domains: 0
proved new H0-H2 resource domains:          0
```

Matched-parent reductions currently demote/classify:

```text
relational local-global state          mechanism/constraint kingdom
constructive closure                   developmental-state mechanism
local cellular/field state             distributed/dynamical kingdom
HDC/vector-symbolic algebra            vector-program kingdom
population-hereditary state            population/search kingdom
event partial order                    symbolic concurrency kingdom
quotient/invariant state               core semantic-quotient mechanism
energy relaxation                      search/dynamics unless physical resource law separates
reaction networks                       physical/dynamical unless lifecycle separation survives
```

Quantum computation remains the nonclassical calibration case. Practical intelligence advantage is still task/lifecycle specific and must include preparation, measurement, shots/noise and error-correction burden.

Physical carriers now also have mandatory precision and stochastic-reliability accounting.

---

# 15. Remaining blocking theory gaps

The unresolved theory is now concentrated:

## T1 nonlinear neural development

Pre-outcome productive-region/PL-like geometry, feature-learning transitions, stochastic optimizer dynamics.

## T2 modern nonlinear generalization

Prospective mechanism selection and held-family protected-risk prediction.

## T3 high-dimensional semantic complexity estimation

Real compressibility, metric/intrinsic dimension, dependency/residual/mode structure under uncertainty.

## T4 nonlinear/partial-observation predictive state

Finite-data predictive quotient/state construction and lifecycle-optimal realization.

## T5 nonlinear specialization dynamics

Alignment drift, router learning, communication/load balance, partial sharing.

## T6 generative burden-curve prediction

Predict `C_R^*(epsilon)` across competing factorizations.

## T7 control/world-model development

Learned control quotient, exploration, model bias, POMDP transfer.

## T8 nonlinear continual development

Large nonlocal update geometry and real replay/adapter/expansion phases.

## T9 broad neutral-search/grammar independence

Independent low-level encodings and held-out family generators at learning scale.

## T10 physical/nonclassical end-to-end frontier

Preparation, precision, noise, reliability, readout, energy and strongest-parent comparison.

These are typed gaps, not unexplained architecture names.

---

# 16. Remaining empirical gates

Mathematical hardening cannot substitute for:

```text
broad neutral leave-one-family-out search
multiple disjoint search encodings
held-family quantitative response prediction
real neural/generative/control training sweeps
real code/math/science/control transfer
physical/nonclassical hardware evidence where claimed
fresh protected replication after every theory correction
```

A protected failure must remain in the record and generate a fresh successor set.

---

# 17. Highest-information next experiment

The highest-information next experiment remains a **broad leave-one-family-out neutral rediscovery tournament**, now with an added grammar-hostility requirement.

For each held-out family/property:

1. freeze GMI structural/quantitative prediction;
2. generate semantically reminted development/validation/protected ecologies;
3. run at least three materially different low-level grammars/search encodings;
4. charge grammar/compiler/search burden;
5. include strong parent-equivalent alternatives;
6. score property/mechanism recovery, not source-code resemblance;
7. distinguish theory RED from grammar/search RED;
8. preserve every failure and freeze a fresh successor before adaptation.

The first protected compression/memory twin should use a preregistered random-independent-label generator rather than the falsified handpicked vector.

---

# 18. Current claim ceiling

Allowed:

> GMI now provides broad structural derivations/reductions for the registered historical machine-intelligence family set, many exact/base quantitative response laws, explicit developmental-potential/composition theory, hostile domain reductions, assumption-indexed descriptor estimators, and small exact neutral-rediscovery evidence. Remaining gaps are typed and concentrated in nonlinear/real estimators, broad neutral search and protected empirical prediction.

Not allowed:

```text
known-family K4/K5 closure
complete theory of deep learning
all domains/species known
any new theory-generated domain established
real-scale unknown-species discovery validated
```

---

# 19. Terminals

Completed narrow terminal:

`THEOREM_HEAVY_ZERO_PRIOR_TRANCHE_CLOSED`

Still unclaimed:

`ZERO_PRIOR_COMPRESSION_MULTI_GRAMMAR_GREEN`

`KNOWN_FAMILY_REGISTRY_ZERO_PRIOR_GREEN_V1`

`KNOWN_FORM_ZERO_PRIOR_DERIVATION_GREEN_AT_REGISTERED_SCOPE`

`NO_KNOWN_UNTYPED_OR_UNTESTED_BLOCKING_GAP_AT_REGISTERED_SCOPE`
