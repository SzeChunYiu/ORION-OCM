# Native proof/runtime qualification

The first source-frozen integration run completed
`PROOF_RUNTIME_LIFECYCLE_COMMISSIONING_PASS`: **24 of 24 registered stages**.
This is exposed F0 apparatus, not useful learned cognition or FLT reconstruction.

## Measured behavior

| Obligation | Observation |
|---|---|
| Actual OCM solving | Two `OCMRuntime.solve` ANSWER results, each followed by authenticated admission |
| Mechanical construction/checking | Two separate closed worker calls and fresh fixed-target Lean kernel checks |
| Persistent support | Fresh processes restore LIVE, OPEN after withdrawal, and final LIVE states |
| Executable custody | Cold status readers have no bound session or executable operator callbacks |
| Discovery A withdrawal | Formal proof stays LIVE; applicability becomes false |
| Run B withdrawal with C present | Independent checked route C keeps formal support LIVE |
| Both B and C withdrawn | OPEN; reinstatement restores existing support |
| Shared checker/environment S withdrawn | Both routes unavailable; no false independence claim |
| Status and revision | No additional proposal/check/authentication attempts or checked-run evidence |

Two checked runs may construct the same proof term. They supply distinct checked
execution provenance and revocable supports, not two independently invented
mathematical arguments. Both share the registered checker/environment assumption.

The fixed Lean target and original constant meanings are unchanged. The generic
checker permits `propext`, `Classical.choice` and `Quot.sound`; this particular
lifecycle additionally requires the expected empty observed axiom list for F0.
It does not change the checker's general axiom contract.

## Costs and scope

- Driver wall: **42.449141277 s**, through final freeze verification.
- Outer GNU time wall: **42.54 s**, including process startup and final serialization.
- Reported process accounting: **31.22 s user + 6.98 s system**.
- Reported maximum RSS: **90,676 KiB**. This is GNU time's process-tree accounting,
  not a measured simultaneous aggregate memory peak of every concurrent process.
- Files before final result serialization: **14,230,299 bytes**, including raw
  records, source snapshots and generated artifacts.

Cold runtime acquisition/preparation predates this run and remains separately
recorded in the [proposer commissioning](../mechanical-proof-v1/RESULT.md).
Do not sum nested session/checker times with outer time. OCM ResourceVector values
retain their existing logical operation model; they are not exhaustive hardware
work measurements. Full-field navigation, hashing and replay are charged here,
and no locality or performance advantage follows from this measurement.

## Source and engineering evidence

- Runtime manifest: `93aa17a738a8511bbb8996eff91e81da0ec5868db50d0f81ab26809e38661894`.
- Lifecycle freeze: `f71f9053b8dd2270c9a41bcb4e4415f26cc5ee03d1c49b04ae035f5e0f2d5b8f`.
- Native result: `c1d93fa010515a97f8746bd53252253c8b7d46a95c1c8e78222a023d0ae2f5da`.
- **96 focused controls** passed in 14.61 s, including 20 atomic-admission controls.
  Mocked native boundaries in these controls are distinguished from the native run.
- **1,303 main tests** passed with no skips in 293.39 s in the declared symbolic-only
  engineering environment. The earlier missing-cvc5 collection problem is resolved
  for this new qualification; historical failed receipts remain unchanged.

Raw records, exact source snapshots, process envelopes and review are indexed in
[records/CORE.md](records/CORE.md). Binary Lean outputs may be retained by hash and
size in the archive; their original bodies remain at the registered Linux run path.

The OCM host, filesystem and issuer journal remain trusted. Parent/replay Python
uses the registered executable with `-I -S -B`, explicit source roots and bound
import origins. That is not a whole-host sandbox or a proof about arbitrary plugins.

## Research consequence

The canonical route now connects mechanical construction to checked, persistent
OCM support. It remains conventional symbolic construction plus kernel checking
and provenance-aware storage. There is no new matched parent comparison or novelty
claim. The next scientific gate requires real proof-region reconstruction followed
by explicit method acquisition, causal consumption on new tasks and lifetime-cost
comparison with equally adaptive symbolic parents.
