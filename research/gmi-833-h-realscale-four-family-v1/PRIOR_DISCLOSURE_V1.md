# PRIOR DISCLOSURE V1 — real-scale four-family tranche (GMI #833 Section H)

Frozen with FREEZE_V1.md, BEFORE any implementation or outcome. Every input
channel of this package is declared here with a blindness argument, following
the v2 standard (research/gmi-833-blind-recovery-v2-v1/PRIOR_DISCLOSURE_V1.md).
The audited history this answers: PR #960's ten-gate ledger left exactly one
gate open per family (`real_scale_test`: training from samples, statistical
generalization, real numerical conditioning, real kernel realization); the
v2 audit had found task authorship and basis authorship open in v1. This
package closes the real-scale gate under the closed-channel standard.

## CH1 — task source

CLOSED BY: the battery classes of FREEZE F2, generated by
`battery_realscale_v1.py` from declared parameter-complete lattices and the
D-10 seed rule only. Completeness is in the declared parameters: ALL
coordinate supports (2^p - 1), ALL interaction pair-supports (2^6), ALL lags
(0..D_MAX), the COMPLETE dyadic noise lattice, the COMPLETE kappa lattice
(D-6), the COMPLETE monotone step-link tier on the observed grid (D-5).
No per-family selection exists anywhere in the generator: the same registry
rows feed every family's adjudication; family member classes, boundary
classes, and null arms are partitions of ONE registry defined by structural
coordinates (which stratum generated the target), assigned names only in the
posthoc adjudicator.

BLINDNESS ARGUMENT: enumeration removes choice, exactly as v2's B_BOOL2/2^16
argument; the residual declared choices (which lattices, which p, which n)
are derived in D-2..D-6 from structural identities (Hadamard existence,
per-sample resolution, double-mantissa span), none referencing any family
morphology. The real interface (continuous x) is the point of the tranche:
real scale is a declared scope extension, and its encoding (tercile
order-statistic quantizer, D-1) is derived from the parent carrier, not from
any family's feature practice.

## CH2 — primitive basis

CLOSED BY: the tier rules of FREEZE F1 — atoms + constants {-1,0,+1};
binary {ADD, MUL} (the two real field laws; the exact analogue of the
parent's {ADD_MOD, MUL_MOD}); U_ORD(R) = complete one-sided order tests at
every observed order statistic + NEG; storage = the parent's indexed read on
the encoded lattice; temporal = parent delay cells. Every family's
characteristic gate is present but UNPRIVILEGED: threshold cuts appear as
one member of the complete cut tier; cross-coordinate products as one binary
law; links as the complete monotone closure; state as one cell tier. The
strata are the grammar's own depth/structure closures, not a family menu.

BLINDNESS ARGUMENT: same symmetry/completeness form as v2 CH2. Residual
bound, stated honestly: the monotone tier is complete within monotone STEP
maps on the observed grid; smooth links (logit/probit) are closure limits —
claimed as a declared boundary, not membership (D-5, forbidden promotion
SMOOTH_LINK_TIER_MEMBERSHIP).

## CH3 — fingerprint clauses and family names

POSTHOC-ONLY: family vocabulary appears only in `posthoc_adjudicate_v1.py`,
the ledgers, and the reconciliation files, all committed strictly after the
outcome files. The lexical screen (extended denylist) and the #855 A2
semantic screen (imported from
research/gmi-833-no-smuggling-audit-v1/audit_core_v1.py) assert that no
search-visible file (battery, search, oracle) carries family vocabulary or
satisfies any target fingerprint; positive control = a planted macro
primitive with a family-shaped 11-coordinate signature MUST be flagged;
negative control = the frozen basis MUST stay CLEAN.

## CH4 — cost model

FROZEN in FREEZE F4: integer description/serve prices, EXEC-B charged, no
scalarization (lexicographic selection; the scalarization-reversal terminal
is structurally impossible, and this is stated as a design choice justified
by the A2 cost audit, not as an outcome).

## CH5 — adjudicator thresholds

FROZEN in FREEZE F5/F6 BEFORE outcomes: every threshold is either a derived
constant (D-1..D-14), an exact counting identity (0/200 nulls, fiber
cardinalities, residual-quotient bounds), or a frozen prediction cell (F7).
The adjudicator carries a bijective clause->check mapping table with a
bijection assertion, executes real interventions (cell ablation, coefficient
zeroing, link second-differencing) on the recovered machines, and passes a
permuted-relabel hostility selftest plus a self-scan for expected-solution
literals.

## CH6 — screen

FROZEN: `test_realscale_v1.py` runs (a) the lexical denylist over every
search-visible source (family tokens: automaton, regression, classifier,
glm, kernel, basis, nearest, exemplar, perceptron, neural, decision-tree,
recurrent-network, and morphological variants) and (b) the A2 semantic layer
via the imported #855 standard on the real-scale primitives (each carries
the full 11-coordinate signature), with the positive/negative controls of
CH3.

## CH7 — channel specific to this tranche: the numeric substrate

The exact dyadic integer-scaled substrate (no floats in the search path) is
declared BEFORE outcomes with its motivation: byte-reproducibility across
hosts and the separation of SELECTION boundaries (a scientific claim) from
FLOAT artifacts (an engineering property, measured by the D-12 control).
Blindness argument: the substrate is chosen by a reproducibility/exactness
criterion stated in the freeze; it privileges no family (all strata solve in
the same exact arithmetic); and the float64 control runs the SAME tasks so
any float-specific advantage is measured, not assumed.

## Machine models (frozen)

M_STATIC: one stratum machine per task (F1 table) over the real interface;
semantics = the exact held-out prediction tuple. M_TEMPORAL: delay
augmentation c ∈ [0, D_MAX] of any static stratum; semantics = the exact
held-out stream prediction tuple. Recovery morphology is measured on the
champion and the drift fiber (F5); never enforced by the model.

## Terminals and claim ceilings

Per family: RECOVERED_AT_REGISTERED_REAL_SCALE / NOT_RECOVERED_AT_SCOPE
with one-stage attribution and a revival chain (negative results get
revival at original strength before any terminal). Boundaries are delivered
as concrete counterexample cells (earned-by-counterexample) or measured
degradation loci (earned-empirically), each labelled. Claim ceiling and
forbidden promotions: FREEZE F7 block. The four #833 rows are eligible for
tick ONLY if all ten gates hold (nine inherited from PR #960's ledger +
real_scale_test from this package), with scope stated on the row text.
