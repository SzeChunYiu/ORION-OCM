# What this revival recorded

The frozen [ordinary-cut opportunity audit](../ordinary-cut-opportunity-result-v1/CORE.md) is unmodified. Its 76 `CUT_PROPOSAL` screens were all `UNKNOWN` at `UNSUPPORTED_GROUND_SYNTAX` (`wff operator` 28, `wff type` 48). Matcher counters were absent.

This successor keeps those tiny class/Boolean parsers as an unnormalized predecessor control and admits the same token vectors by matching P1 syntax axioms already present in the constructor DAG: class terms `cin`/`cun`/`cdif`/`csymdif` and wffs `wss`/`wcel`/`wceq`/`wi`/`wa`/`wo`/`wb`/`wn`/`wxo`. It does not invent a language, resample roots, repeat native export, or reopen `TRACE_UNUSABLE` / `UNKNOWN_INTERFACE` rows.

| Stage | Recorded count |
|---|---:|
| Frozen training roots retained | 128 |
| Frozen trace unusable (not re-enumerated) | 57 |
| Frozen interface unknown (not re-enumerated) | 54 |
| Frozen roots enumerated | 17 |
| Replay proposal occurrences | 76 |
| Distinct canonical bodies | 74 |
| Grounds admitted as wff | 76 |
| Still `UNSUPPORTED_GROUND_SYNTAX` | 0 |
| `ALIAS_FOUND_PROOF_READY` | 44 |
| `SCREENED_NEGATIVE_IN_DOMAIN` | 32 |
| Aliases that used cut premises | 0 |
| Native calls | 0 |

The complete matching run is [records/replay-02](records/replay-02/RESULT.json). All 76 screens have `coverage_complete=true`. The 44 aliases are closed P1 theorems for the query alone (`ssinss1`, `inss`, `elsymdif`, `rcompleq`, and kin); none consumed the cut's extra premises. The 32 negatives (30 distinct canonical bodies) are therefore two-node sequents whose query is not a one-step P1 theorem.

A 180-second outer-containment control [records/replay-01](records/replay-01/RESULT.json) truncated the last 13 screens. Completing the family required 366.067 s wall / 347.704 s user CPU, above the original 60 s consumer deadline and 180 s outer containment. Token-states stayed at 1,773,758 of the registered 2,000,000 bound.

Predecessor refusals remain: `ssrin`-style `( A C_ B -> ( A i^i C ) C_ ( B i^i C ) )`, `A e. B`, and `A = B` still fail the original class-packet checker and now parse as wffs under P1 syntax contracts. [Admission tests](test_syntax_admission.py) record that split.

## What this does not claim

G2.3 (primitive-alias) is checkable in this one-step screening domain and is mixed: 44 aliases, 32 negatives. That is not an all-alias collapse and not a useful-acquired-method result.

G2.4 (causal use) cannot be checked here. There is no restart, fresh task, or removal ablation.

`CAUSAL_METHOD_REUSE_SUPPORTED` is false.

The 54 DV/hypothesis interface refusals and 57 unusable traces remain later extensions. Repairing screening grammar does not qualify those excluded roots.
