# Independent review — completed, mixed scheduler result

The frozen conventional indexed scheduler passes source and retained-outcome review.
This supports exact finite proof-cost parity for these authored cells. Timing is mixed.
There is no general speedup, new algorithm or OCM novelty conclusion.

| Exposed cell | Tree cost, both | Layered scheduler s | Indexed scheduler s |
|---|---:|---:|---:|
| Positive, ordinary | 3 | 0.294217 | 0.303692 |
| Positive, I enabled | 2 | 0.262869 | 0.292199 |
| False, ordinary | No proof within 8 | 0.446021 | 0.339729 |
| False, I enabled | No proof within 8 | 0.423212 | 0.346806 |

All eight cold processes rebuilt the same 69,219 ordinary actions from the same
4,191 contracts and 255-formula/15-class bank. The same six I instances were added
in enabled cells. I occurs in both successful enabled derivations. No false proof
was returned; this does not exclude intermediate macro activity.

All four successful arms emitted the same 21-label proof: three semantic and three
syntax applications, eleven floating labels and four hypothesis occurrences.
Four separate native checks accepted the exact target after 4,095 old proofs, under
the same 96 trusted assertions. Every checker followed its own exited B process;
all twelve child PIDs are distinct and the original A had already exited.

The scheduler minimizes positive unit rule cost plus ordered premise-tree costs.
Repeated premises count repeatedly, empty rules start at one, and positive cycles
cannot create unsupported facts. Parallel action identities survive indexing.
Every optimal premise is strictly cheaper than its head, supporting finalization
with the declared stable tie. This is not a minimum native-proof or shared-DAG cost.
The core source and independent tiny-graph expectations were reviewed before dispatch.

Complete sorting and index construction are inside each scheduler timing. Indexed
arms materialize 137,790 or 137,802 incidences before local queue traversal.
Cold ordinary compilation costs 3.784639–3.924733 seconds and dominates these calls.
Operation counters measure different work and are not interchangeable speed ratios.
No separate setup/settlement timing supports finer attribution of the mixed result.
These are single fixed-order observations, not repeated performance estimates.

The twelve focused controls include 3,159 cases from the explicitly listed rule
pool, checked against synchronous min-plus and decreasing-budget tree enumeration.
Initial missing-API RED and subsequent GREEN generations are retained. The corrected
contract removes only a three-byte heredoc artifact before dispatch.

645 retained-data checks and the additional public/prefix comparisons pass. The
first reviewer reader produced four false alarms from one eligibility assertion:
baseline eligibility was incorrectly expected to include I. Frozen source and actual
records establish empty baseline eligibility. Both reader/result generations and
READER-CORRECTION.json remain; no study or native outcome was rerun.

Read REVIEW.json for scope and exact bindings, SOURCE-REVIEW.json for source clearance,
OUTCOME-REVIEW-v2.json for actual data checks, PUBLIC-AND-PREFIX-REVIEW.json for public
summary comparisons, and FILES.json for this review's inventory. Original raw records
remain in the adjacent native-indexed-deduction-control-v1 directory.

Keep both schedulers available. A cached compiler or persistent index would be a
separate experiment with maintenance and invalidation costs. This study does not
establish broader curriculum feasibility, language transfer, lifetime economics,
active-subspace scaling, autonomous acquisition or a paper-level breakthrough.
