# Parent ownership — `gmi-833-kl-revival-v1`

Assimilation first. Every mechanism this package leans on is owned by a parent,
named before the delta. **Nothing in the machinery below is claimed novel
here.** The residual contribution is two levers applied under rules fixed in
`FREEZE_V1.md`, each measured on material that did not exist when the parent
lane filed its dispositions.

## Row K

| parent | what it owns |
|---|---|
| `gmi-833-capability-predictor-v1` (#1012) | `F = C_hat(M,E,R,H,D,U)`, `survivor_mask`, `predict`, the single `emit` funnel, the four dispositions, the typed uncertainty constructors `U-1a FeasibleSet` / `U-1b ConfidenceSet` (#851), KP-1A image exactness, the degeneracy standard. **`F` is not modified, re-derived or re-fitted**; it is driven through the parent evaluation package's `install_universe`, which proves in-process that its code is unchanged. |
| `gmi-833-capability-predictor-evaluation-v1` | the real-system training protocol (`REAL_SOURCE`, word length, windows, optimizer, epochs, batch, threads, seed rule), the 99/100 band (`FREEZE_V3_ADDENDUM` §3, imported from `gmi-833-real-transition-receipts-v1` #903), the external evaluator's `extcap` definition, `make_spec` / `install_universe` / the disjointness certificate, and the three receipts `REAL_MEASURED_V1/V2/V3.json` from which the bridge is **derived**. The protocol is reused byte-for-byte. |
| `gmi-833-body-residual-akl-v1` | `BR-1` (a truthful-by-construction bridge has no positive singleton on the three real populations), `BR-2` (the 0-of-155,520 census and the resolution curve), `BR-3`, the `CB-PROTO` / `CB-MAX` definitions, and the black-box world-installation pattern for route B. Its three revealing populations are **not re-scored** here. |

External parents of the ideas: set-valued / interval prediction with abstention
is the conformal-prediction and imprecise-probability tradition (Vovk,
Gammerman & Shafer, *Algorithmic Learning in a Random World*, Springer 2005,
doi:10.1007/b106715; Walley, *Statistical Reasoning with Imprecise
Probabilities*, Chapman & Hall 1991); a reject option in classification is
Chow 1970 (*IEEE Trans. Inf. Theory* 16(1), 41–46, doi:10.1109/TIT.1970.1054406).
These are cited as the parents of the *form* (predict a set, abstain when the
set is not a singleton), not re-verified here, and no coverage-probability
claim is borrowed from them: the emissions here carry a `FeasibleSet` premise,
never a probability.

**Delta.** The parent lane asked what a bridge truthful *by construction* can
deliver and answered 0. This package registers the middle of the same family —
commit only where three prior receipts never disagreed, abstain elsewhere — as a
**derived** object (`CR-1` re-derives it from the receipts; the table is not
input), freezes the set-valued emissions on a fourth population before it is
trained, and reports the census, the truthfulness and the soundness on that
population. `SB-L*` is not a fourth point law and the parent's §6 terminal is
taken as a premise, not contested.

## Row L

| parent | what it owns |
|---|---|
| `gmi-833-real-developmental-validation-v1` | the continual-learning instrument `train_continual_v3.py` (net, constants, seeds, criterion, splits), the matched-control design (POS/NEG offsets, `U`), `CL3-P8`, the pilot-budget practice, and the 7/7 result on seven real sources. The instrument is reused byte-for-byte except for the source table, the explicit pilot rule `PR-1` and the output directory. |
| #909 / #908 (`Define evolvability quantitatively`, `Formalize developmental potential separately from current capability`) | `Ev_Q(U) = Q(U)`, the quantity predicted by `EP-1`; and `C_pot` (the best evaluation score reached by any proposal, `C_pot_QH_B2` in every parent receipt), the developmental-potential quantity that `EP-4` (amendment 1) orders. Neither definition is changed here. |
| `gmi-833-body-residual-akl-v1` | `FFA-1` and its two-directional validation; `FC-2`'s enumeration that every repository blob is endogenous. `FFA-1P` is `FFA-1` read by provenance, and every `FFA-1` rejection remains an `FFA-1P` rejection. |
| `gmi-833-developmental-reuse-v1` | the developmental-reuse results and the recorded reason the row was open. |

External parent of the custody idea: pre-registration / prospective
prediction with time-stamped commitment (Nosek et al. 2018, *PNAS* 115(11),
2600–2606, doi:10.1073/pnas.1708274114). Wikimedia's revision record supplies
the outside timestamp and content hash; it is used as an attestation service,
not endorsed as content.

External parent of the diagnosis in amendment 1: loss of plasticity / dead
units in continual learning (Dohare et al. 2024, *Nature* 632, 768–774,
doi:10.1038/s41586-024-07711-7) is the hypothesis `L4_DIAGNOSIS_V1.json` was
built to test and **rejected** for the window-1 miss (every history body keeps
5–6 of 6 units live); the miss is attributed instead to the hit threshold inside
`Ev_Q`, with the parent's own graded `C_pot` ordering intact.

**Delta.** The parent lane proved futurity cannot be constructed in-session and
named the lever. This package executes it: a task-family generator and an
evolvability prediction frozen with a committed timestamp, posterior-dated
external material admitted only by a mechanical rule, custody decided clause by
clause with the outside party's own content hash, and the prediction scored —
on two registered windows, the second opened by a numbered amendment after a
one-stage attribution of the first window's single miss, with the first window
reported verbatim beside the second. Window 2's sixth admitted source `P06`
measured `AT_FLOOR_OR_CEILING` under the registered pilot rule `PR-1`, so
`FREEZE_V1.md` §4.6 clause 2 excluded it by name and replaced it with the next
admitted candidate in the same ordered list (`P07`, revision 1375698478,
created 12:49:31Z) — a mechanical, posterior and attested replacement (D4),
recorded in `POSTERIOR_SOURCES_V2_EXTENDED.json` and replayed by both routes.

## What is NOT claimed novel

`F`, the registration protocol, the band, the external evaluator, the
continual-learning instrument, `Ev_Q`, `CL3-P8`, `FFA-1`, the world-installation
route, and every external reference above. Also not claimed: any assessment of
whether those parent results are correct, or any claim about Wikipedia content.

## The residual contribution

Two rows re-tested after one lever each, under rules fixed before any material
existed, with two materially independent routes, hostiles that move their own
quantities, nulls the results must beat, and the exact numbers that decide each
row.
