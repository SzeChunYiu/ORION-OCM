# Z3 parent ownership disclosure

Assimilation-first: absorb the strongest parents, name them, then state the
residual. Nothing below is claimed novel except the residual.

## Parents that already own the substance

| parent | what it owns | citation |
|---|---|---|
| finite semantic presentation relabeling | bijective state-label transports; identity/inverse/composition; canonical mechanism fingerprints invariant under valid relabelings; the explicit refusal to infer search invariance from semantic invariance | `research/gmi-833-remint-equivariance-v1/` (on `main`), claim ceiling `GMI_FINITE_SEMANTIC_REMINT_EQUIVARIANCE_AT_REGISTERED_SCOPE` |
| finite grammar bias | the three-way separation of semantic coverage, description bias and reachability bias; the isometric-relabeling invariance theorem; the same-semantics counterexample reversing a grammar-relative choice | `research/gmi-833-g0-grammar-bias-v1/` (on `main`), claim ceiling `GMI_FINITE_GRAMMAR_BIAS_AND_REMINT_BOUNDARY_AT_REGISTERED_G0_SCOPE` |
| verified architecture transforms | the transform category with fail-closed composition and the price-conditional directed pseudometric | `research/gmi-833-transform-geometry-v1/` (on `main`) |
| the `65552`-candidate universe and candidate-ID relabeling invariance | the enumeration and the relabeling check itself | `research/gmi-833-heldout-20-transitions-v1/` (on `main`) |
| state minimisation / redundant states | that declared states can be behaviourally redundant | A. Nerode, *Linear automaton transformations*, Proc. Amer. Math. Soc. **9** (1958) 541. DOI 10.1090/S0002-9939-1958-0135681-9 |
| Moore/Mealy transducers | the machine model | G. H. Mealy, Bell Syst. Tech. J. **34** (1955) 1045, DOI 10.1002/j.1538-7305.1955.tb03788.x; E. F. Moore, Automata Studies (1956) 129. |
| invariance as a criterion for scientific quantities | the general demand that a physical/scientific claim not depend on the chart | E. Noether, *Invariante Variationsprobleme*, Nachr. Ges. Wiss. Göttingen (1918) 235. |

## What is explicitly NOT claimed novel

- Relabeling equivariance as a concept, or any of its algebra.
- The separation of description bias from reachability bias.
- The fact that finite automata can carry redundant states.
- The candidate universe, the scoring interface, or the `60` registered worlds.
- Any statement about real compilers, hardware substrates, or search dynamics.

## Residual contribution of this tranche

1. A **registry** covering all six transformation categories the row names, each
   entry carrying the exact quantity it must or must not move — and a machine
   check that every `DECLARED_RELEVANT` entry actually moves it.
2. The transport of relabeling invariance onto the **morphology-selection
   verdict**: `0` alarms over `200` renamings, whole-universe conjugation, and a
   direct full-scan control.
3. The **equivariance statement in the right coordinates**: the verdict is a
   function of `(p, lambda/eta)` alone, so "transformed resource accounting"
   means transforming price and error weight together.
4. The **exact non-invariance census**: `12544` declared-stateful candidates are
   behaviourally stateless, `8448` of them outside the obvious syntactic family,
   and the mixed-convention verdict flips on exactly `20` registered worlds.
   The syntactic family the freeze registered as a hypothesis (`4096`)
   undercounts by exactly `49/16`, and that is recorded as an instrument failure.
5. The **invariant replacement** `bits_eff`, shown to agree with the declared
   convention on all `60` worlds and to survive dead padding that moves the
   declared one.
6. The **quantified grammar bias** in this universe: `21904` behavioural classes,
   multiplicity ratio `785`, and exact reachability shares by radius under a
   frozen mutation graph.
7. A **null whose first detector failed** at `197/200`, whose three survivors are
   characterized and verified rather than explained away, and whose revival uses
   only an invariant the frozen registry already contained.
