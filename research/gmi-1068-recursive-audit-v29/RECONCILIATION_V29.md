# R1 whole-round reconciliation

Only R1 may be re-earned by this successor. Every original atomic record and
previous local repair is preserved byte-for-byte in canonical JSON comparison.
The original 222-record inventory therefore remains35 fulfilled/187 unresolved.
Two immutable V16 qualifications remain separate, yielding185 active unresolved.
R2-003 and R2-007 remain UNKNOWN. No dependency edge or downstream status changes.

The predecessor is the actual merged V28 snapshot, whose SHA256 is
`72d1dd18bc4ce99d6bcd288722c58459e38b7ac8c2cae4292414cbe29322c4cb`.
Both observed_main and audited_base are fixed to merged V28 main
`2405c7bd06f9236b545ce501e5b9f9b5f24fd5e5`.
All remaining top-level metadata is preserved with strict JSON type equality.

## Exact scope and historical preservation

[The frozen original crosswalk](../gmi-1068-r1-integrated-core-v29/ORIGINAL_CROSSWALK_V29.json)
retains the prior R1 scope verbatim, all eight original required-result texts,
and exact references to ten original atomic records. The new R1 scope is its
original_scope.proposed_scope; historical_reconciliation_status remains historical.
Only new R1/R14/R15 evidence and local repairs are appended. Parent evidence
digests are recomputed through the unchanged DAG without promoting descendants.

## Evidence boundaries

Actual generic path evaluation yields a faithful map from its evaluation quotient;
an inverse requires every target arrow to be generated. Cyclic free categories
are not enumerated by the finite DAG adapter. Identified words have no unique
raw inverse. The proper ambient injection preserves absent labels and named units.
The structure of all descendants is validated before semantic failure short-circuits.
Wide inclusion is compared only on available source queries, including failed joins.

Across models, response recovery needs a fixed query map, fixed injective output
map and actual commuting equation. Per-model bijections are insufficient unless
their information is retained. Unrepresented target queries remain outside that
claim. Reported codebook and source sizes are costs, not minimum-storage proofs.

The six survivor, six optional and seven parent rows retain their source-specific
loss witnesses and premises. Relative observer information does not erase physical
adequacy, enriched information, outcome labels, probabilities or resource balances.
No whole-GMI, novel-mathematics or empirical-intelligence result is asserted.

## Audit defenses

The actual receipt and snapshot must pass before adversarial mutations are used.
Checks reject every original-record status mutation, rewritten R1 scope/history,
other-round promotion, extra fields, stale parents, changed preregistration bytes,
coupled result/round rewrites, cost-report edits and changed formal source bytes.
Independent expected cost records cover both named models and all180 family reports.
Raw timing bytes are separately bound by the snapshot artifacts; their variability
does not enter deterministic science inputs or supply scientific authority.
