# Publication constitution v2

Harvest of #165 §15 freeze fields from capsules already in this repository.
Does **not** close [#144](https://github.com/SzeChunYiu/ORION-OCM/issues/144).
Does **not** invent E4, a second machine, or independent authors.

v1 (`research/publication-constitution-v1/PROTOCOL.json`) stays on disk
unchanged. Terminal remains `PUBLICATION_CONSTITUTION_FROZEN_WITH_GAPS`.

## Freeze-complete vs still MISSING

Executable copy: [`PROTOCOL.json`](PROTOCOL.json) `section15.catalog`.

**Freeze-complete** (all 21 §15 *before protected confirmatory execution* boxes,
filled from existing capsules — question, parents, ablations, salts, budgets,
meters, and the rest of the freeze list):

| box | harvested from |
|---|---|
| decisive scientific question | each capsule `CORE.md` / experiment docstring |
| contribution level | claim ceilings (`L0`–`L2`; none promoted to general cognition) |
| strongest parent/product | ordinary library, TMS/nogood, antichain, grammar-induction, … |
| causal ablation | revoke / method-removal / SDS-support / source-method |
| prior-information manifest | primitives, blobs, checkers, SK catalog, taught lexemes |
| task-family generator | exact identities + SHA-256 salt ranking, or finite Cayley / frozen witnesses |
| protected splits | train/val/test salts and counts in `experiment.py` |
| comparator versions | ordinary persist vs OCM admit/restart/revoke vs named parents |
| prompts/configs | no LLM; `not applicable` where no prompt exists |
| budgets | search slots, max length, MP budget, N, generation size |
| resource meters | attempts, unique checks, enumerations, wall, bytes, `(κ,Ω,χ)` |
| analysis | frozen `experiment.py` / `study.py` / `diagnose.py` |
| stopping/exclusion | first execution freezes salts; no post-hoc retune |
| non-inferiority margins | none; mechanism terminals do not require lifetime payback |
| negative terminals | per-capsule first-class negatives |
| independent unit | polynomial fingerprint, tape pair, incident, lemma identity, … |
| power/precision | `CANNOT_CHECK_NOT_A_SAMPLING_STUDY` where the ecology is exhaustive |
| multiplicity | one registered ecology per capsule |
| leakage/shortcut audit | disjoint salts; prior-exposed identities excluded |
| parent-favouring controls | ordinary parent receives the identical object/search |
| independent validation subset | validation tournament / held-out / shadow generation |

**After-execution now filled from receipts** (not E4 / not a fresh host):
failures, machine-readable receipts, raw cost vectors, immutable figure
tables, exclusions with reasons, checksum manifests, in-ecology independent
scorers/checkers (G3.4 / G6 / math-n4 kernel).

**Still MISSING** (cannot be filled without inventing a result):

| box | status |
|---|---|
| disjoint replication (E4) | `MISSING` |
| G2.4 / G3.1 raw traces on this head | `MISSING_ON_THIS_HEAD_CI_ARTIFACT` |
| red-team reviewer simulation | `MISSING` |
| independent claim/result correspondence review | `MISSING` |

**Explicit `CANNOT_CHECK`** (answered, not faked):

| box | conversion |
|---|---|
| §16 independently authored families for E3+ | need a **human independent author** to donate world/task families; internal generators remain audit-only |
| §15 after-execution fresh-host rerun | need a **second machine** / fresh host; this checkout is not that host |
| crashes/timeouts as a retained confirmatory log | `CANNOT_CHECK_NO_CRASH_TIMEOUT_LEDGER` — G3.4 records timeout-as-diagnosis, not a crash archive |

§20: if principle / mechanism / prediction / evidence / parent / closure are
`None`, the gate is `DO_NOT_BUILD`. The same gate fires if independent-authorship
or fresh-host answers are `None`.
