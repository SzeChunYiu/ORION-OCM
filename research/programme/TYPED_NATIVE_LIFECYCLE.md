# Typed proof lifecycle: alias-only interface result

The fixed authored control has a checked producer and a fresh consumer, connected
by an unchanged persisted projection. The original lifecycle failed during consumer
import. A separately qualified consumer-only repair then reconstructed every issued
claim. It did not rerun acquisition or erase the original failure.

| Stage | Actual outcome | Scope |
|---|---|---|
| Producer A | Two native calls; two training claims, one admitted witness | Two authored proof routes; bounded Boolean meaning checks |
| Extraction | Three proper occurrences, two canonical bodies | One body has two distinct complete joint-support signatures |
| Alias normalization | Supported body matches ordinary theorem `jccir` | No eligible new method; the other body has insufficient support |
| Original B | Import-audit refusal before reconstruction | No B native call; original lifecycle remains failed |
| Repaired fresh B | Six typed permutations and one class reconstruction | Same persisted projection and requests; no new learning |
| Negative controls | Two exact native rejections and nine structural refusals | Refusal stage and pending theorem are bound in the receipts |

The narrow repair permits absent, read-only Python cache probes for exactly pinned
source paths. Existing or unregistered cache reads and writes still refuse. Source,
historical producer records and both prepared consumer envelopes remain bound across
the separately authorized invocation. Eligibility remains empty after reconstruction.

Read the [source qualification](../native-typed-lifecycle-v1/CORE.md),
[first attempt](../native-typed-lifecycle-attempt-v1/CORE.md), and
[consumer recovery](../native-typed-consumer-revival-v1/CORE.md) as separate records.
They retain exact inputs, code, audit records, reviews and measured cost boundaries.
These publication capsules omit explicitly listed large inputs; they are not
standalone reproductions or authority to rerun the closed controls.

This is an authored interface qualification. It establishes no useful new method,
typed OCM serving, held-out transfer, elapsed-time advantage, complete lifetime
economics, novelty or general language competence. Five native invocations across
the original attempt and recovery are validation work, not five learning episodes.

The next decision is whether real exposed training proofs supply useful reusable
cuts beyond an ordinary theorem library. Use the
[ordinary derived-theorem parent](../native-ordinary-lemma-parent-v1/CORE.md) first,
retain all unsuccessful opportunities, and test actual later search consumption.
Keep proof-family holdouts and the bounded language bridge's obligations separate.
