# Attempt01: completed A, failed consumer import

**LIFECYCLE_FAILED_WITH_COMPLETED_ALIAS_ONLY_A.** The once-only attempt did not complete the registered typed lifecycle. A's native qualification, generic extraction, alias classification and persistence completed. Fresh B failed during a root-helper import, before payload validation, reconstruction or native checking. This is an implementation failure in the serving read guard, not a native proof rejection.

## What the retained evidence establishes

A PID **1777505** exited zero and was reaped. Its two current native calls verified the 4,095-proof prefix plus the two authored training claims (4,097 proofs), then the same prefix plus one extracted admission claim (4,096 proofs). Both use the same 96 trusted assertion identities. Exact issued claims, normal proofs, ordered hypotheses, typed floating contexts and empty DV match the returned native traces. Native logs, source/database/prefix/suffix hashes and result copies agree.

The two registered meaning checks each completed 255 worlds and passed their stated meaning gates. Their retained complete-joint signature digests differ. Generic extraction returned three occurrences at roots 15, 18 and 22, grouped into two structural bodies. The two-node semantic body has two distinct joint supports; the four-node body has one and is retained as insufficiently supported. These are actual outcomes consistent with the prospective manual audit, which was not used as a candidate selector.

The supported body `3a3edbba45f4bd28ccf7a357a28e1087df380bd2fe151d8d87dc27284fd093a2` was classified **ALIAS** to existing ordinary `$p` theorem `jccir`. The named parent's exact sequent matches the admitted claim. The catalogue scan visited all 4,191 assertions. The witness was admitted and persisted for the interface experiment; **eligible_method_ids is empty** in A's result, state and projection. This does not establish nonalias acquisition or useful learned search.

State is 7,393 B, SHA `aa17e26e71626c9195a5e1a62f68271e0e49903d0872ec38317a259fbcf03a78`. Projection is 7,821 B, SHA `e9053afd68b7ac6eb8ff7475ddd374277e4ac79c20ac9ee038f91f74cc6738c1`. It retains the admitted witness's own trace and contracts, with the exact exited-A provenance bindings. The issued B request is 6,774 B, SHA `e94c26d0fec274b0d3940382f2ed9611d4829db1a9ba5a7919288d807195eb83`: all six typed atomic permutations and one fixed class regression were issued, but were not executed by B.

## Exact failure and custody

B PID **1777530** started after A exited, then exited 1 and was reaped. The retained traceback reaches `run_b.py:39`, then `life_common.load`, then importlib `get_code/get_data`. The final recorded open attempt is `__pycache__/life_payload.cpython-311.pyc`, read mode, flags 524288. The frozen guard permits only absent `engine/` bytecode probes and rejects this root-helper probe as `B_FORBIDDEN_READ`.

The cache path is absent at review. The original hook did not inspect that root path's existence before refusal, so the record proves a rejected cache probe rather than successful cache consumption. The loader can probe an existing cache even under `-B`; that flag prevents writing bytecode. The narrow mismatch is between allowed engine probes and root modules loaded through the same importlib mechanism.

B's initialized open audit and empty work dictionary survived. Its entire output directory contains only `FAILURE.json` and `OPEN-AUDIT-FAILURE.json`. No result, reconstruction or native archive exists. **Zero B native calls** follows from the bound traceback and source ordering as well as this output population; absence of a numeric field alone is not the evidence. No negative native control or class reconstruction was reached.

Outer observer, lifecycle, A and B identities and stdout/stderr bindings agree. The exact root gate, run request and source freeze are unchanged; all 48 frozen source/input hashes still match the reviewed candidate02. [ATTEMPT01-DATA.json](ATTEMPT01-DATA.json) retains 111 passing data/custody assertions and their exact read scope. These are review checks over existing records, not new native or learning experiments.

## Costs and scope

The outer execution window is **2.156473901 s**; its nested lifecycle child window is 2.126048313 s. A's child window is **1.977414555 s** (1.866633 user, 0.032083 system, 106,768 KiB RSS); failed B's is **0.055272333 s** (0.046556 user, 0.004232 system, 39,252 KiB RSS). The lifecycle's own interval is 2.101602856 s, with a nested 0.009018339 s projection interval. These overlapping windows must not be added. Outer cost includes opening/final custody and the child, excluding final outer receipt writing; prior authoring and controls are outside this attempt.

The original failed attempt, costs and current-native A results must remain immutable. Structural interface progress is established in A; the complete fresh-consumer/native lifecycle remains unqualified. No performance, broad curriculum, search-consumption, lifetime or novelty claim follows. No native, bridge, learner, alias matcher, reconstruction or fixture-builder run was performed during this review.
