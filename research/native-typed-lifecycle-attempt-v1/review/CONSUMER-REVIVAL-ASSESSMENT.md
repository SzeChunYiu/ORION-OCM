# Consumer-only revival: bounded feasibility assessment

**FEASIBLE_TO_REGISTER; NOT AUTHORIZED OR QUALIFIED FOR EXECUTION.** The observed failure is confined to the import read guard. A exited cleanly with current-native admission and a hash-bound serving projection. Repeating acquisition would add no evidence needed to repair this failure.

A successor should be a separately named consumer-only attempt with a preserved source diff, independent control receipt and new root execution gate. The original `LIFECYCLE_FAILED` terminal and all its costs remain final historical facts. A successful revival would qualify recovery from this persisted A; it must not be presented as a successful original once-only lifecycle.

Keep the following bytes unchanged: A state and admission, serving projection, exact seven-claim B request, class payload, ordinary catalogue, prefix and native/typed authority sources. In particular retain projection SHA `e9053afd68b7ac6eb8ff7475ddd374277e4ac79c20ac9ee038f91f74cc6738c1` and request SHA `e94c26d0fec274b0d3940382f2ed9611d4829db1a9ba5a7919288d807195eb83`. Do not rerun A, the bridge, extraction, alias classification, admission or the original supervisor.

Keep the projection's original A source-freeze provenance. Bind the successor consumer source and current input envelope separately. The old `project()` deliberately requires its A state to match its current freeze; rerunning it under a changed source freeze would confuse acquisition provenance with successor execution. Consume the already issued projection and request instead. Root or the outer observer should verify the old exited-A/result/state/projection chain before dispatch; B continues to receive only its serving input allowlist.

The minimal guard change should derive exact cache paths from registered pinned Python source paths, including root helper modules. Admit only read-only probes to absent corresponding cache files. Continue refusing existing caches, unregistered cache paths, writes and forbidden teaching reads. Preserve attempted-open recording before refusal. A generic wildcard permission for `.pyc` files would weaken the source-execution contract.

Qualify that pure import/guard change before native dispatch: the actual root-helper import reaches source with no cache; the clean absent engine case remains valid; existing cache and unregistered/teaching paths refuse; write probes refuse. The import control can use the real harmless helper modules while explicitly prohibiting native, bridge and learner calls. These controls address the observed stage without rerunning acquisition or target evaluation.

The successor retains all seven issued claims, both constructor/recipe comparisons and the exact negative-check acceptance rule. On a completed consumer path, three native calls are expected: reconstruction batch, wrong-hole rejection, wrong-target rejection. This is a prospective count, not an observed result. Record new child/outer costs and source/input/read custody, then review the retained actual results. Report original failed-attempt cost and revival cost separately before any additive end-to-end total.

No rerun is authorized by this memo. Review the actual narrow patch, source/input bindings and new apparatus before root decides whether to execute once.
