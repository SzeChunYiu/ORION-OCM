# First typed native lifecycle attempt

**Overall result: LIFECYCLE_FAILED.** A completed training through persistence;
the fresh consumer stopped on an import-audit defect before reconstruction or
native checking.

The fixed, engineer-authored training proofs passed native verification. Generic
extraction returned three occurrences of two distinct bodies. One body had two
distinct complete joint signatures and was admitted and persisted as an interface
witness. The complete registered ordinary-theorem check identified it as an alias
of `jccir`; its learned-method eligibility correctly remained empty. The other
body had insufficient distinct support.

A exited before projection and a fresh B process started. B refused an importlib
cache probe at `__pycache__/life_payload.cpython-311.pyc`; the path was absent on
subsequent filesystem readback. Its audit allowed absent engine cache probes but
omitted root helper probes.
The failure occurred before payload validation, reconstruction and B native calls.
The failure audit, process receipts and completed A results are retained.

## What this establishes

- Native training, registered meaning checks, generic extraction, full alias
  classification, native admission and persistence completed on the two supplied
  routes. The experiment made two native replay calls, both in A.
- The supported body duplicates an ordinary theorem. It supplies no new method
  eligibility, learned search benefit or OCM novelty.
- Fresh-consumer reconstruction, native B checks and the complete lifecycle remain
  unqualified by this attempt. A source-review pass did not imply runtime success.

## Repair and next experiment

Preserve this failed attempt and its exact source. Qualify a narrow read-audit
repair for absent, read-only cache probes corresponding to pinned Python sources;
existing caches and unissued paths must still refuse.

A consumer-only revival requires a separate prospective protocol and source review.
It must use the exact existing projection and issued request, without repeating A,
discovery or the meaning bridge. A later success cannot relabel this attempt.

The observed outer interval was about 2.16 seconds on laptop billy. It is a single
failed control run, with nested process intervals and explicit accounting limits;
it is not a speed, lifetime-cost or comparative-performance result.
