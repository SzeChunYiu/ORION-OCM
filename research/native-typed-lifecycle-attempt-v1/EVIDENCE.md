# Evidence and reproduction boundaries

`records/` preserves the exact outer, supervisor, producer and failed-consumer
outputs, including native request/result/log/suffix records, progressive discovery
checkpoints, the serving projection and the issued seven-claim request.
`FILES.json` binds every published file; `OMITTED-DATABASES.json` binds the two
full native database files retained on laptop billy rather than duplicated here.
This capsule cannot independently replay the omitted prefix by itself.

The [independent data readback](review/ATTEMPT01-DATA.json) records 111 successful
custody/data assertions over the actual attempt. It is a read-only review, not a
second acquisition or native experiment. The source snapshot and full native
inputs remain bound by the original request, source freeze and root gate.

[The result-note review](review/ATTEMPT01-CORE-FINAL.json) binds the exact text
published as `RESULT.md`. The prior wording and its correction record are retained.
The correction distinguishes the observed rejected cache probe from the subsequent
filesystem observation that the cache path was absent.

Process windows are nested. The outer interval includes opening/final custody and
its child, while excluding the final outer receipt write. Source authoring, earlier
structural controls, review and publication packaging are outside that interval.
Import bytes are not fully instrumented. Do not add overlapping intervals or use
this single failed control as a speed or complete lifetime-cost comparison.

`records/EXECUTION-GATE.json` authorized the original attempt exactly once. That
authorization has been consumed. It does not authorize replay, repaired-source
execution or the separately proposed consumer revival.
