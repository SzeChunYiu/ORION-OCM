# Source and authority boundaries

All four published inputs are pinned to commit `448a54b851d0a188f76543a876bb963c315d0c6e`, tree `68d3ace8619c0f331ca7ae3a686f8dddc5ee8cfc`; each blob is unchanged from `55495939dc4682ac027c44ff1cb62cb3cb94e204`. [INPUTS.json](INPUTS.json) records exact Git blobs, SHA-256 values and the released-result archive member.

The retained [extract.py reference](inputs/extract.py.reference) defines the selection and serialization. Its historical replay-01 contains 24 negative occurrences identifying 22 unique candidates. Materialization uses its first matching released-root/cut traversal, then binds each row to the actual historical admission label and index. Screen and extraction orders happen to agree here; this was checked, not assumed. The 22 selections come from 11 released training roots. The newer 32-candidate result is not substituted.

Every row preserves the complete selected cut object, including its body, canonical query/premises, context, proposal and historical status. Canonical query/premises translated through the retained context agree with the proposed target and ordered hypothesis statements. This is token/data reconciliation; it neither interprets proof meaning nor requalifies canonical-ID generation. Per-record digests use the explicit JSON encoding in [RECONCILIATION.json](RECONCILIATION.json).

[ADMIT.json](inputs/ADMIT.json) historically reports 22 native-verification calls and 22 accepted rows. This task performed none. Root's retained [prefix resolution](inputs/PREFIX-RESOLUTION.json) binds the 1,806,915-byte `b12e2bac…` prefix to the prior 4,223-proof/100-axiom authority. Its reported current-byte match is root's earlier custody result, not a new verification here. This does not establish a joined base-plus-cohort library or current member eligibility. The 100-axiom inventory includes syntax/definitions; it is not a claim of four inferred independent assumptions.

[PROPOSED-SUFFIX.mm](PROPOSED-SUFFIX.mm) deterministically concatenates the 22 original proposed proofs under their issued labels, preserving hypothesis order, spaces and block boundaries from the historical serializer. No original per-row native database/suffix receipt was compared in this materialization; these are reconstructed proposed bytes. [Namespace observations](SUFFIX-NAMESPACE-NOTE.json) preserve repeated hypothesis-label strings without treating them as either a native defect or native acceptance.

[materialize.py](materialize.py) is a 121-line stdlib data tool, not a producer import, native checker or new experiment harness. [MATERIALIZATION.json](MATERIALIZATION.json) records its actual Linux PID/path and readback; it is not a runtime qualification receipt. All original files and repository state remain unchanged.
