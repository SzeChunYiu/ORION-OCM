# K2 retry: registered, not executed

Status: **REGISTERED_NOT_EXECUTED; K2 REMAINS NOT_ESTABLISHED**
Date: 2026-09-13

`DC-3` records the registered negative: the attempt to show retained capital
makes acquisition of **new K1** cheaper terminated `NOT_ESTABLISHED`, with the
recombination bar holding on **2 of 9** fresh seeds. `DC-4` named the lever.
This file registers the retry before it is run, so the retry cannot be
reinterpreted after its outcome is known.

**Nothing here improves the K2 verdict.** K2 is `NOT_ESTABLISHED` before this
registration and remains so after it.

## 1. The single-stage attribution being acted on

The failure is attributed to **retained-capital recombination at the #323
grammar** — not to retention, not to measurement, not to the K2 definition.
The retry changes exactly that stage and nothing else.

## 2. Held fixed (changing any of these starts a new registration)

- the K2 criterion `E[B_H^acq(K1_new)] < E[B_RESET^acq(K1_new)]`;
- the registered recombination bar, unchanged in form and threshold;
- the fresh-seed protocol and seed count;
- the assay, its gates and its leakage alarm;
- `m* ∉ history` enforcement;
- the matched-control construction.

## 3. The one change

A different recombination operator over retained capital. The operator must be
registered by name and source digest **before** execution, and the prior 2/9
attempt is retained as evidence rather than replaced.

## 4. Parent subtraction is required, not optional

Facilitated variation and meta-learning parents already target higher-order
improvement of future adaptability. A K2 positive therefore requires, in
addition to clearing the bar:

- an explicit comparison against the strongest applicable parent mechanism;
- a statement of what the OCM route supplies that the parent does not;
- `PARENT_SUFFICIENT` recorded as the honest terminal if the parent matches it.

Clearing the bar without this subtraction establishes nothing about GMI.

## 5. Outcome rules, fixed in advance

| outcome | terminal |
|---|---|
| bar holds on a preregistered majority of fresh seeds, parent subtracted | `K2_SUPPORTED_AT_REGISTERED_SCOPE` |
| bar holds but a parent matches it | `PARENT_SUFFICIENT` |
| bar does not hold | `NOT_ESTABLISHED_SECOND_GRAMMAR` |
| assay or gate failure | `CANNOT_CHECK` |

A second `NOT_ESTABLISHED` at a different grammar makes a structural
obstruction the more economical reading. That possibility is registered now, in
advance, so it cannot be argued away later as a third grammar's problem.

## 6. What this registration does not do

It does not execute the retry, name the operator, claim an operator exists,
claim 2/9 is near a boundary, or predict the outcome. It fixes the rules.
