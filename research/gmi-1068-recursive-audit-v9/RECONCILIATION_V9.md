# R0–R17 reconciliation after semantic foundation recovery

This successor preserves the original 222 IDs. It adjudicates the eight
original R0 governance requirements and advances local mathematics for
R1/R2/R14. These are different kinds of evidence.

## Original governance closure

All eight GMI2-R0-001 through GMI2-R0-008 requirements are CLOSED as governance
delivery/validation, and R0 is EARNED. The independent audit reads the actual
historical freeze, 205-row registry, parent/theorem records, dependency DAG,
merge rules and result. It checks historical ancestry and unchanged bytes
separately from semantic constraints exercised by real-data mutations.

The old checker accepted four invalid metadata changes. The successor rejects
them, distinguishes checked-invalid from unavailable evidence, and adds
independent hostile checks of the registered records. This earns governance
validation, not the scientific claims named in those records.

Every other original requirement retains its V8 status: 214 remain unresolved.
No descendant becomes earned solely because its governance parent is earned.
The original NOT_STARTED ledger fields are historical and remain unchanged.

## Mathematical recovery

V9 proves that target information is recoverable from a retained input exactly
when it is constant on that input's fibers. The decoder is unique on attained
inputs; extension to unused values requires the stated existence conditions.
A collision refutes every decoder, and commuting invertible presentations
preserve the result.

The process/context countermodels use literally the same ambient typed-history
domain. Different evaluators reverse action rankings for one available process;
different admission rules change capability under one fixed evaluator.
This removes the earlier domain-typing ambiguity. It does not force every
possible context formalism to be independent or select a unique ontology.

An identity symbol is recoverable from composition with retained unit
existence/laws. Removing the symbol and removing those laws are different
operations. Thus symbol-count minimality is not an invariant semantic goal.

## Original scientific requirements

R1-001/006/007 and R2-002/003/009 gain specific semantic results. R14 gains
general kernel proofs and actual history-based countermodels. Whole original
requirements remain open pending the broader declared translation class and
integrated foundation statements. No all-family derivation or empirical
novelty is claimed.

The ranking-reversal example also verifies why an unlabelled attainable value
set does not recover the action-to-value association. Earlier R3 already
retained the surrounding process/context structure; this is a constructive
boundary witness supporting that qualification, not its retraction.

## Custody and next work

V9 freeze: 86a8ecb556cbd970d39e013a639fd7e1e92ddc99, before successor
implementation. H merged separately in #1102; the successor consumes its
unchanged V8 snapshot. See the [package](../gmi-1068-r0-foundation-repair-v9/CORE.md)
and [current snapshot](SCOPE_SNAPSHOT_V9.json).

The next predictive construction concerns exact distinguishing-resource
thresholds and their representation consequences. It requires its own freeze,
independent algorithm, zero-cycle falsifiers and comparison to weighted
bisimulation/energy-game parents. Broader R1–R17 work remains active.
Overall closure is OPEN; internal review is not external replication.
