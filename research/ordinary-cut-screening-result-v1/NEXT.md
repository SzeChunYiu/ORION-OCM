# Next: exact syntax-result memoization

The recorded parsing stage supplies the next bounded engineering question: can conventional exact memoization avoid repeated syntax work while preserving the same answers, refusal semantics, full parent and occurrence order?

The proposed implementation is not qualified by this result. It needs a separate source/contract review, focused controls and execution gate. Preserve this completed attempt unchanged; do not replace its timing or unfinished dispositions with a later run.

Bind any cache entry to the exact grammar/parent, parameter context, requested type and token sequence. Resource/refusal outcomes must remain distinct from complete nonmembership. Cache hit handling must preserve checkpoint accounting and proof authority. Measure cache construction, lookup, storage and copied/materialized data as well as parsing; no cost saving is assumed in advance.

A separately registered comparison should retain the full 4,323-parent and all 76 occurrences in the same order, unchanged study limits and complete counters. Report both completed screening coverage and total cost, including regions that remain unresolved. This is a standard parent engineering improvement, not a new algorithm or acquired method.

Other root/context/boundary limitations remain separate. No cache prototype or future screen authorizes native admission, learning or a novelty claim.
