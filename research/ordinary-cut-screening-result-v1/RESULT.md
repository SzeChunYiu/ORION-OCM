# What the run established

All 76 original proposal occurrences remain in saved order, including duplicate canonical bodies. The exact parent still contains 4,323 contracts: 4,223 theorems and 100 axioms. Its authority is inherited from the prior native export; it was not requalified by screening.

| Zero-based occurrence | Count | Recorded outcome | Parent rows visited |
|---|---:|---|---:|
| 0–3 | 4 | Complete `ALIAS_FOUND_PROOF_READY`, two witnesses each | 4,323 each |
| 4 | 1 | `UNKNOWN` during rule matching at the shared soft deadline | 314 |
| 5–75 | 71 | `UNKNOWN` before context acquisition after that deadline | 0 each |

The eight returned witnesses use existing parent labels `ssinss1` and `ssinss1OLD`; their emitted proofs contain 32 labels in total. These are four completed occurrence screens, not eight learned methods. Syntax witnesses were replayed structurally in the screening path; no native theorem admission occurred.

Exactly one three-class-parameter grammar context was instantiated, with complete recorded grammar coverage there. False coverage on the 71 rows stopped before context does not establish unsupported grammar. The fifth row has complete grammar coverage but incomplete screen coverage.

There are **zero complete negative screens**. Empty alias lists on unresolved rows do not establish absence of an ordinary alias or a learning opportunity. The completed four rows cannot be extrapolated to the remaining 72.

The terminal `ALL_RETAINED_SCREENING_OCCURRENCES_RECORDED` means every disposition was saved. It does not mean every screen finished. The observer and screening child exited zero and were reaped; outer containment did not fire. [Independent counts](review/outcome/COUNTS.json) · [full row records](records/result/RESULT.json)

This result leaves the acquisition question open. It establishes no new method, useful learning, held-out utility, lifetime benefit or novelty.
