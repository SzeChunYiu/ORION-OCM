# Integrated donor/runtime development protocol v1

Base: 4853a5d0f0d3ca5170aaa2010e4919ae5b88551d.
Other lanes inspected and frozen for intake: #206 eacb33de890481f739eec69853bcf7e165481655; #205 789bea2a85fcdb3d7fd78c571c83f43b33e545fd. Their frozen evidence is not overwritten. This is development and integration qualification, not protected replication or full #165 closure.

## Actual donor, not a namesake

Execute the pinned stitch-core 0.1.29 Rust/Python donor on independently checked training programs. Settings: iterations=4, max_arity=1, threads=1, silent=True. Preserve complete returned abstractions and rewrites, including refusals. Admit only closed unary contexts that expand exactly to the registered inc/dec/double/square instruction language and terminate in their single argument. Unsupported donor syntax remains CANNOT_CHECK; compression is not usefulness.

For an engineering acquisition comparison, enumerate exact primitive polynomial identities through length seven. Fixed hash ranking uses salt integrated-donor-runtime-20260909-v1 with disjoint tags train/validation/test. Train uses 32 minimum-length-five identities; validation uses 32 minimum-length-six identities; test uses 64 minimum-length-seven identities. Report any insufficient population rather than changing counts. These are exposed-development populations, not an independent protected sample. Compare all admitted Stitch candidates, the complete Stitch library, a conventional repeated-fragment parent (first eight fragments of length 2..4 ranked by distinct-task support, decreasing length, then lexical order), and no method. Select strictly lower aggregate validation expanded-state work, with deterministic candidate order for ties; otherwise select no method. Keep all test outcomes, harmful transfer, and all construction/selection costs.

## Consumer and exact parent

The successor consumer is breadth-first search over exact polynomial semantic states with expanded primitive length in the state key. Operations still use the unchanged production polynomial normal-form checker. Both OCM and ordinary parents receive identical libraries, budgets and consumer. An independently structured complete token enumeration checks small finite cases. This is a versioned serving change: expanded-state work is NOT historical first-hit rank. A closed finite failure is scoped to the supplied grammar/length; exhaustion is UNKNOWN, never unprovability.

## One actual runtime

An additive runnable prototype must use the existing OCMRuntime, field, evidence registry, operator registry, solve/check/commitment and revoke/reinstate paths. Persist methods in the actual field, not a parallel LineageStore. The host dispatcher and external constitution remain fixed across polynomial donor acquisition, existing version-space language acquisition, and checked proof-method acquisition. Each producer/consumer/revision stage runs in a separate OS process. Retain the ledger and exact prefix identities. No reset or donor-specific cognitive core may be hidden in the principal arm.

Language controls use the existing finite construction learner with declared lexicon, hypothesis grammar and aligned demonstrations. They establish grammar acquisition/consumption at that scope, not unrestricted natural-language comprehension or factual truth of an utterance. Proof controls use #206's miniature Hilbert/SK search as a declared donor, with learned lemma bodies checked by expansion and the unchanged native Metamath verifier under an explicit miniature K/S/MP inventory. They do not qualify the historical 22-method cohort or overwrite frozen #203. Authored examples, alpha-renamed instances and compound substitutions are labelled as such, never presented as new theorem discovery.

## Required controls

Execute ordinary, OCM live, revoked and restored routes; do not manufacture parent results by assigning equal counters. Method use must be present in the generated trace/proof. Bound issued request, method/source identities and actual commitment support. Reject wrong goal, forged result, stale method, wrong domain, malformed proof/negative premise index, missing donor, unauthorized method and changed source. Invalid proof bodies cannot become trusted axioms. Revoke/restore one domain while checking other retained domain methods remain usable.

## Physical costs and closure

Retain monotonic wall/CPU intervals, actual process IDs/exits, per-process peak RSS, field file sizes and actual measured logical events separately. Do not sum peak RSS or relabel file length as bytes physically written. Single-host timings are observations, not architecture-superiority evidence. Charge acquisition, all failed/selected candidates, validation, lookup, proof checking, restart, persistence and revision where measured. Missing costs remain missing. No learned-router permission, three-generation self-evolution, strongest integrated #73 win, independent E3/E4, or full programme completion follows from this development control.
