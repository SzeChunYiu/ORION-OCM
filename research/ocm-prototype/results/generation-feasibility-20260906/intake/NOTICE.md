# Donor intake
Stitch core is ADOPT mechanism; the CLIA translator is ADAPT infrastructure.
sexpdata is ADOPT parser infrastructure; existing Z3/host CLIA validators are
unchanged checkers; cvc5 is the existing generation donor/reference.
The core MIT notice is retained. Bindings/PyPI declare no separate license in
the inspected metadata; no extra license grant or native reproducible-build
equivalence is inferred. The exact published wheel is installed externally.
Its Python entrypoint matches the pinned source hash; the Rust binary is
identified by its actual loaded-file hash, not claimed source reproduction.
The full Python source remains external at the URL/hash in source-records.json.
Cargo pins identify core 0ef5ec7 and lambdas 2c9bfd0; no source build was performed.
Dependencies are pinned in requirements-generation.txt; the CPython 3.13 Linux
wheel restriction is explicit. No Transformer, LLM, model download or learned
output was used in this qualification.
