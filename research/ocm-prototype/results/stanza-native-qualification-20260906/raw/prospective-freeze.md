<!-- ocm-director-20260905:stanza-qualified-native-freeze-20260906 -->
## SUPPORTING — recurrent donor qualified; native prediction pilot frozen before inference

Owner #43/#52, donor selection #71, integrated prototype #73; anchors #50/#62/#69; roadmap #42; acceptance #38/#49. This implements the existing [donor qualification checklist](https://github.com/SzeChunYiu/ORION-OCM/issues/43#issuecomment-5556569925). No new architecture or research lane.

### Executed qualification

Stanza 1.14.0 English `combined_nocharlm` POS → lemma → dependency parsing with shared `conll17` vectors loaded successfully on laptop billy. The exact four model files total **241,742,165 bytes**; their closure digest is `dae9ad20c7bdb98838a19d47d42996af181c89a6aca0a646fd63d6b46e9568cb`. Manifest SHA256: `0e4da5ff3e8612bec4cf4a54954bb41fc0412ca21de616237d2af4460a6cf6de`. Artifact selection preceded acquisition; all SHA256/MD5/size checks passed.

Inspection of the actual loaded pipeline found recurrent Tagger/Seq2SeqModel/GraphParser components, shared frozen vectors and learned dictionaries. No Transformer, PEFT, charLM, contextual lemma classifier, neural tokenizer or MWT model was loaded. **58,559,868 unique parameter elements** include **25,000,000 frozen vector elements**; learned dictionaries add nonparameter state. This qualification supplied zero documents and made zero model forward, training or network calls inside the namespace. Accuracy remains unmeasured at this freeze.

The separate CPU environment pins Python 3.13.12, Stanza 1.14.0 and Torch 2.14.0+cpu: approximately 1.012 GB environment, 236.924 MB retained installer cache and 103.475 MB reused CPython base recorded separately. Load/inspection took 2.766 s wall, 2.731 s CPU and 714,020 KiB peak RSS. These are preparation observations, not inference efficiency.

Imported preparation is not our EWT-TRAIN-only acquisition experiment. The pinned source builder mixes EWT/GUM/GUMReddit TRAIN with PUD/Pronouns TEST and uses EWT DEV for development. This is source-recipe evidence; the historical checkpoint's exact corpus revisions, recipe and training/search costs remain partly unknown. No named split is declared protected through this donor. The qualification packet retains the source, resource, runtime, acquisition, loaded-module and lineage records.

### Minimum next experiment and progression rule

Hypothesis: this existing recurrent donor can improve selected-tree syntax quality on the unchanged development panel while preserving the public-word interface, making a small adapter worth integrating into the existing vessel. Any improvement belongs first to the donor; the identical donor must be available to the native and hosted parents. No OCM-specific learning or capability claim follows from this pilot.

There is no established downstream utility requirement that justifies an absolute LAS acceptance threshold here. The [official Stanza performance page](https://stanfordnlp.github.io/stanza/performance.html) reports configurations with charLM where possible; those scores cannot be transplanted to this exact nocharlm candidate. The rule below is a narrow engineering progression decision, not a claim of useful general language, protected superiority, non-inferiority or LLM comparability.

- [ ] Freeze one exact predictor source closure and external grading wrapper before inference, with the four qualified model artifacts, runtime lock and public stream SHA256 `7ece4dd05bdc5c07caa88213fb5952b75f8a22e26bdc4a379bb83644cfaa48d0`. Retain the original UDPipe model and all G1 receipts unchanged.
- [ ] Run exactly the existing 100 syntax items in their fixed order, in one cold CPU process with one thread and sequential sentence calls. Every assigned row remains in the denominator: 100 IDs, 1,584 supplied words and 99 unique exact word sequences. No replacement sentences or candidate sweep. This is donor-only qualification, not a recreated five-chunk mixed-domain G1 comparison.
- [ ] Supply only case-preserved word lists, as a fresh nested `[forms]` list for each sentence. Never join words into raw text or pass an annotated/CoNLLU-loaded `Document`. Predict POS, features, lemmas and dependency labels; never supply gold annotations or raw evaluation records. Require one output word for every supplied form and unchanged sentence/word boundaries. The actor receives neither gold nor the repository/evaluator; use the already qualified read-only networkless runtime boundary.
- [ ] Validate output schema, form identity and tree structure using the existing gold-free contract. Preserve all refusals, invalid outputs, errors and missing executions explicitly. No retokenization, label repair or fallback predictions after observing output quality.
- [ ] Grade after actor sealing with unchanged `grade_g1_matched.syntax_score` and the official CoNLL evaluator. A thin external wrapper binds predictions/public IDs and the exact DEV file SHA256 `dd514122385fd3374dd10051ddaf477c957d3da0bba48931d6f969820ece233f`; it must not manufacture a two-arm G1 receipt. Cross-check the existing UDPipe rows with the same scoring functions.
- [ ] Report base LAS, full LAS, UAS, UPOS, exact tree and exact typed tree, validity/completion and per-item deltas. Retain 100-row primary development accounting and disclose the duplicate-pair and known TRAIN-surface overlap; unique-input and no-detected-overlap-with-custody-EWT-TRAIN summaries are diagnostics, never a replacement denominator or evidence of no Stanza training exposure. No token-i.i.d. confidence intervals or significance claims.
- [ ] Progress to a thin same-vessel adapter only if all 100 outputs are valid, base/full LAS and exact/typed tree counts do not decrease from the frozen UDPipe panel, and base LAS or exact tree count strictly improves. This dominance rule limits engineering churn; it does not establish a minimum useful language capability. Mixed tradeoffs require an explicit new decision before integration, not a post-hoc gate revision.
- [ ] Record setup, source/model hashing, cold loading, per-item inference, total process-tree CPU/wall, peak RSS, output/persistence bytes and external grading cost separately. One process has an operational 600 s deadline, inherited from the existing G1 chunk allowance. No timing/efficiency comparison to five cold-start mixed-domain chunks; unknown imported training costs stay unknown.

ADOPT: Stanza recurrent models/vectors/dictionaries as donor mechanism **O** and native comparator; Python/PyTorch/Stanza runtime as infrastructure; official evaluator as external checker. ADAPT: only forms-to-typed-tree and a receipt-bound external grading wrapper. **Π and C remain unchanged.** Check binding/segmentation refusal and a clean path; do not build another parser or controller.

Resources: existing CPU laptop and qualified environment only, no training, model download sweep, cluster provisioning or hosted inference. Stop after the fixed panel. A failure receives one diagnosed interface/custody repair if appropriate, with the first attempt retained; no parameter or output tuning.

Terminals: `DONOR_QUALITY_PROGRESSION_ONLY`, `NO_DOMINANT_DEVELOPMENT_GAIN`, `INPUT_CONTRACT_MISMATCH`, `CANNOT_CHECK_EXECUTION_OR_BINDING`, `EXECUTION_DEADLINE_EXCEEDED`, and imported training/cost uncertainty. Storage is descriptive here; no resource/utility dominance rule has been established. `PARENT_SUFFICIENT` is reserved for an actual later same-vessel/native comparison, not this donor-only run. Broad G1 admission and OCM advantage remain unestablished.

Paper mapping: Figure 2's donor-quality development precursor and the dependency/parameter/resource table. This pilot cannot populate a protected capability or lifetime-efficiency figure. Compression, transferable operator learning and sparse/local execution retain their existing owners and unmet gates.
