# Witness contract and evidence custody

The five exact probes and expected outcomes were fixed in
[raw/probes.json](raw/probes.json) before the single execution.
The runner checks two stored winner bodies against declared primitive witnesses.
No candidate, template, grammar production or normalization rule is searched.

Existing generation_clia.equivalent creates the unrestricted negated-equality
obligations. Existing CLIA validation precedes the unchanged Z3 5.1.0.0 worker.
For the Bool-valued predicate, both sides use ite(condition,1,0), an injective
encoding into the existing Int-function equivalence interface.
No expected values or illustrative counterexamples constrain the solver.

The orientation control returned SAT at h0=0,h1=1.
The threshold control returned SAT at h0=1.
The Real-parameter record is deliberately outside the accepted Int signature:
its refusal establishes a host boundary, not a Real-equivalence result.
Unknown or unavailable checking could not pass the recorded aggregate.
Acceptance required all five expected outcomes, call deltas 1/1/1/1/0,
four returned native entries and zero compression calls.

Only the 101-line fixed-probe caller was new in this scratch execution.
Verifier, grammar, process worker, observation wrapper and supervisor were unchanged.
Their exact executed copies and the prospective source hashes are under raw/.
No production runtime source changes are part of this publication.
Execution belongs to the frozen source packet, not the later main runtime.

## Prior evidence and chronology

The first winner comes from the original public induction packet:
[prior CORE](../stitch-public-induction-20260906/CORE.md).
The second comes from its normalized-input successor:
[prior sequence](../stitch-primitive-rewrite-20260906/CORE.md).

PRIOR-REFERENCES.json binds six exact existing repository files: each predecessor's
manifest, execution seal and adapter body. Each matched its original scratch bytes
and tracked git blobs at the recorded publication base and merged main 63c3fe5.
Large prior trees are not copied.
The raw probes retain the original absolute provenance paths; the mapping supplies
their portable repository locations and hashes.

raw/PREPARED.json, raw/PROPOSAL.md and raw/root-launch-v1/launch.json retain prospective
NOT_RUN/PREPARED statuses. Actual completion is in
raw/root-launch-v1/completed.json and raw/capture-v1/calls/caller-receipt.json.
Earlier induction primitive_alias_assessment=NOT_RUN fields remain untouched.

## Portable inspection

COPY_MAP.json maps all 55 copied scratch files to this packet; every byte is preserved.
SHA256SUMS covers every other packet file, including COPY_MAP and this readout.
The original execution seal covers 27 files relative to raw/capture-v1.
It is preserved verbatim rather than regenerated for publication.
Validate the portable files with sha256sum -c SHA256SUMS from this directory;
no runtime import or solver is needed to inspect hashes or the retained obligations.

The exact launch retains original host paths. Runtime libraries are pinned in
raw/runtime-bindings.json rather than vendored. Re-execution would require explicit
path/runtime preparation and a new execution record; this packet claims no replay.
The runtime inventory does not cover the full stdlib, OS or transitive system libraries.

Publication performed copy/hash and existing git-blob checks only.
No tests, source imports, Z3 calls, donor calls or actor reruns were performed.
