# Public induction contract and limits

## One call, two imported programs

The fixed TRAIN order is max3, then mpg_guard2. Both previously checked
native programs are explicit imported priors in [experiences.json](raw/experiences.json).
Exactly two task bodies are staged. The unchanged five-entry fixture manifest
contains registry metadata, not additional task bodies or target answers.

Pinned Stitch 0.1.29 is called once through generation_stitch.induce with
iterations=1, max_arity=2, threads=1. No shuffle, example replacement, retry,
later target specification, later synthesis or donor rewrite API call is added.
The raw donor response, including null fields and settings, is preserved before
typed admission checks. Proposed syntax is not authority for runtime promotion.

Seven source modules and both task fixtures are copied from exact b03b749;
they also match ed2ce8cc. The frozen source and runtime binding files remain
unchanged. Current-main publication adds evidence only.

## What actually passed

Four distinct native Z3 invocations returned unsat:
two rechecked the imported TRAIN programs against their original public specs;
two checked each original program against its decoded, expanded donor rewrite.
These are whole-program obligations, not four independent learning successes.

The inferred macro is typed (Int, Int) -> Int:
(+ #1 (* (- 1) #0)). Direct algebra gives h1 - h0, an existing primitive with
permuted arguments. That is the source-based PRIMITIVE_ALIAS interpretation.
No dedicated alias equivalence obligation ran, so the original receipt's
primitive_alias_assessment=NOT_RUN is preserved. No useful new semantic
operator or later-task benefit is established.

## Preserved preflight and observation repair

[preflight-history-v1](raw/preflight-history-v1/ARCHIVE.json) preserves original
source, manifest, launch and qualification bytes. Five initial harmless controls
pass. A narrow observation-accounting control then records one pass/two errors
on the predecessor and three passes on the repaired launcher. Invalid or
incomplete response files now remain UNKNOWN while raw output and the final
receipt/seal are preserved. These controls ran no native checker or discovery.

The single actual call follows that fixed successor. Publication did not rerun
controls, discovery, verification or grading.

## Cost and custody

The donor's 6872 -> 5357 weighted syntactic cost is its compression objective,
not runtime, memory, cognition or whole-lifetime economy. Source acquisition,
corpus selection and installation are imported/external priors.
Caller self CPU/RSS, native child receipts and supervisor wall are separately
recorded. Total process-tree CPU/RSS, energy and complete lifetime cost are UNKNOWN.

This is allowlisted input/export with trusted fixed-source execution, not an
OS isolation claim. [COPY_MAP.json](COPY_MAP.json) maps every original byte copy;
[RAW-INDEX.json](RAW-INDEX.json) names the capture seal; SHA256SUMS binds publication.
Historical absolute paths and preflight status labels are unchanged. Actual
root-launch records establish later execution.

All scratch source, two TRAIN fixtures and transparent
[root launch wrapper](raw/root-run-frozen-launch.py) are included. Runtime
binaries/caches are excluded; exact dependency hashes and availability
requirements remain in the original manifest. Existing donor licensing and
task-source limitations are retained in their pinned source records; no
additional license grant is inferred.
