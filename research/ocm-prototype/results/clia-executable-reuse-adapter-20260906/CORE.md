# Executable CLIA reuse adapter — engineering controls

Owner: #62 comment 5556647228; cross-cutting #50/#69/#73. Classification: INFRASTRUCTURE.

This adds data-only verified max3/guard2 programs, explicit host rebinding, and
pointwise checked application through the existing OCM executive. The ordinary
native library receives the same program, checker, cache and two-bound support semantics.

- **55 tests passed in 8.10s on laptop billy.** [Raw final result](raw/combined-final.xml)
  and [command/source bindings](raw/combined-final-command.json).
- Actual fresh-process controls cover both native and OCM application. Wrong value,
  mismatched tuple, descriptor corruption, missing rebind, and withdrawn authority refuse.
- Real archived donor programs pass grammar/universal verification and compilation;
  this check does not apply prospective inputs or establish a reuse-study result.
- Both lower and upper support bounds exclude history-only provenance. Per-function
  query registration and the shared CLIA authority remain authority dependencies for the registered program claim.
- The optional G1 catalogue-builder preserves its default two-donor path. Expanded
  syntax/synthesis receipts offer four slots; production SV may filter apply slots
  before backend visits when their descriptor inputs are outside the reacting graph.

Read [the contract and remaining study requirements](CONTRACT.md). Raw files are
bound by [SHA256SUMS](SHA256SUMS). The source is bound by the final command receipt.

No prospective reuse panel, hosted comparison, protected study, useful acquisition
claim, sparse-work claim, or efficiency inference is included. All source, setup,
replay, checking, persistence and global executive work still require study accounting.
