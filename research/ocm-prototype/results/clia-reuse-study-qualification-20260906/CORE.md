# Reusable-program study runner qualification

This packet qualifies the runner and external grader for the public development protocol in [protocol/CORE.md](protocol/CORE.md).
**No prospective acquisition, application panel or syntax inference is reported by this qualification packet.**

The actors reuse the existing checked CLIA adapter and fixed UDPipe1 donor.
The native comparator keeps the same persistent functions, compiled cache, support, history, revision and audit powers.
OCM uses the existing SV executive. No core, adapter or frozen task/protocol implementation is changed.

- [REPLAY.md](REPLAY.md): operating commands and historical-path portability.
- [QUALIFICATION.json](QUALIFICATION.json): actual commands, test totals, source hashes and scope.
- [SHA256SUMS.json](SHA256SUMS.json): packet inventory.
- `raw/`: retained failures, corrected controls and fresh-worker fixture records.
- The six files under `protocol/` are the exact published prospective bytes; new F0/F1 metadata is separate.

The actual worker controls use previously exposed unit programs and tuples.
They include five fresh post-acquisition processes per arm, covering read-only reload audits, explicit callable rebinding,
history-only withdrawal, query-registration withdrawal, unrelated-function retention and restoration.
They do not substitute fixture setup for the prospective study's two independently metered acquisitions or six-stage denominator.

The actual cvc5 call meter was qualified with a separate unary constant task.
An initial zero-argument constant fixture failed; the diagnostic repetition returned native worker exit -11.
The unary fixture revival is retained. The scientific donor and task settings were unchanged.

At execution, F0 requires committed source, exact published protocol/request bytes, task/model/training identities,
installed dependency/native binary records and fixed resources. F1 requires independently acquired canonical
program/task/checker equality before resolving aliases or applying any prospective argument tuple.
Every actual start/completion at the native synthesis/check boundary is retained; library counters do not prove absence.

The external grader checks selected values with independent exact Python arithmetic, actual refusal evidence,
both support bounds, prior immutable outputs, raw invocation records, source custody and all assigned denominators.
Historical search withdrawal is distinct from withdrawal of reuse authorization; neither is mathematical falsification or neural unlearning.

Function and cost verdicts remain separate. Actor self CPU, reaped-child CPU and direct wait4 are reported with their scopes.
Complete process-tree CPU is not established, so the cost terminal remains `CANNOT_CHECK_COST`.
A function-only parent-sufficient result would not establish an efficiency or ME-specific residual claim.
