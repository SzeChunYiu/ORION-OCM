# Current-core G1 context for the exact sparse donor trial

Current f4f3f55a42e1458e24d36036ce17a2abf4289763 restored the original
restore.max3.01 context exactly. The restore inspected 450 original outer rows /
384 events, retaining 78 atoms, 146 hyperedges, 43 evidence records, the actual
revoked history ID and original registry/evidence epochs. State hash is
d83d7ac9a171e4ea123b5dfc213d8cc82f34cde3469b852a57d3bd78bf3d5705.
Config remains UNIFORM, alpha 1/3, exact_extraction_max_atoms 0; all fine outputs
remain required. PROPAGATED belongs to the separate existing controls.

RESTORE_RECEIPT.json is a read-only restoration result, not a full-consumer result.
The guarded inspection made zero SV/fixed-point/model/operator/tool calls.
The real model/solver modules were not imported. PID 288570 exited 0 and was reaped.
Restore wall 0.610107 s is descriptive preparation work, not a donor timing.

The ledger prefix is exactly the first 450 outer rows before QUERY_OPENED 385.
No event/hash was rewritten. Original complete inputs and source are unchanged.
CONTEXT_MANIFEST.json binds all copied inputs and 188 current source files.
HOST_BINDING_MANIFEST.json separately binds seven existing public fixture files
required by descriptor validation; this dependency was found through
D.validate -> validate_task -> load_task. It is not a new task or serializer.

Use the ordinary Python API, in a fresh process with the existing G1 environment:
    from bound_context import load
    arguments, metering = load(context_root, new_worker_directory)
    # Future authorized call only:
    result = exact_sparse_donor_consumer.evaluate(**arguments, arm=registered_arm)

bound_context.py has not been executed here. It makes a NEW worker-private copy,
calls the qualified restore helper, then the existing V.bind for both live
descriptors and existing V.catalogue. It verifies unchanged KSO, registry and
evidence identities and exactly four catalogue entries. Shared input is never
a writable runtime. No generic module-path loader or serializer was introduced.

arguments contains ks, task, operators, revoked, config and commit_authority.
metering contains runtime, binding_receipts, catalogue, authority_identity and
the real mutable checks/counters. Take per-call slices/deltas for first/repeat;
do not report cumulative observations as independent per-call work.

Replay restores two operator manifests and zero host callables. This is an
explicit trusted-host binding prerequisite, not authority inferred from a log.
Both saved programs are LIVE; all seven checker-prior source hashes and installed
Z3 distribution 5.1.0.0 match. V.bind itself verifies the live descriptor and exact
manifest contract. Actual binding/application remains unexecuted in this packet.

For this clia_apply request, syntax and cvc5 catalogue entries return
NOT_APPLICABLE before prediction/synthesis. No model weights or cvc5 acquisition
are needed. The selected application and fixed pointwise check still use real Z3
parse/substitute/simplify, which must execute and be charged. Do not replace them
with the historical answer. A full current SV consumer result is still pending.

The consumer boundary is current SV.solve, including commitment and its fixed
check. It does not include the outer V.apply post-solve evidence admission or a
new lifetime study. The original request, manifests, warrants, support, checker
and all four offered operators are retained; no weak placeholder operator is used.

Only source-derived duration fields may be excluded from semantic equality:
- evaluate result: consumer.answer.application_wall_s;
- metering checks: checks[*].check_wall_s;
- preparation only: binding_receipts[*].bind_wall_s.
Retain their raw values. No value, identity, status, support, certificate, trace or
authority field is an allowed semantic exclusion. Candidate residual receipts
are assessed by their own required contract, not erased to match the reference.

Cold total includes imports, source/input verification, copying/restore,
V.bind compilation/persistence and catalogue construction plus both consumer
calls. Warm is the immediate second call on that same bound context; no output
or factorization cache. Shared extraction/custody work remains separately charged.
Original F0 confirms 4,294,967,296 address bytes and CPU 0. The new 120 s/process,
seven-pair first/repeat gate belongs to the root trial freeze, not this receipt.

The historical restore stage assigned four math applications and one syntax row.
Any workload-amortization projection must identify that heterogeneous five-row
workload; repeating max3 twice does not measure it. No scaling/performance claim
or actual-G1 consumer pilot has been made by this input-preparation subtask.
