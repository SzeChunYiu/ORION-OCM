# Exact sparse donor: launch package

Prospective: no real G1 binding, consumer call, model call or trial timing has run.

Read MANIFEST.json first: SHA256 abb6abaa5db39a89b0f36e18f8b17320d2b51e22fff8cec229109a5056cf5bfa.
Protocol: https://github.com/SzeChunYiu/ORION-OCM/issues/72#issuecomment-5558797826.
Parent registers the final manifest before the single capture launch.

## Scope

This substitutes numerical navigation inside direct current SV.solve, with its
actual commitment decision, all four-channel outputs, surprise, full catalogue,
Z3 application and fixed pointwise check. Preparation copies/restores the saved
field and binds both actual programs through the existing trusted host.

It does not append a durable query/result or run post-solve OCM admission.
Field authority must remain unchanged. Binder persistence is charged.

Seven AB/BA pairs give 14 fresh processes and 28 calls. Each process makes a first
and immediate repeated call. CPU 0, 4 GiB address space and 120 seconds apply equally.
All calls must complete exactly. The wall-time gate requires median paired warm
B/A <= 0.8 and median complete two-call process B/A < 1. No output/factor cache;
candidate residual checks are charged. Reference never imports SymPy.
No asymptotic or lifetime claim follows from this finite input.

## Sources and controls

DONOR_SOURCE.json binds four helper/donor files at repository commit bac7f937;
numerical source is unchanged from 6678900. The manifest binds the immutable
211-file context, executed core f4f3f55, exact task/config/field/revocation/program
identities, module origins and runtime files. Original files remain untouched.

SYNTHETIC_QUALIFICATION.json -> synthetic-controls-v1.log / .xml -> retained
synthetic-controls-v1/ records: 20 synthetic non-actor controls passed. A sealed
no-alarm capture and relocation, exact exclusions, corrupted values/vectors/events,
missing arms, errors/timeouts and process-group cleanup are covered.
These establish trial plumbing only, not observations of G1.

Raw metadata is retained. The manifest lists finite comparison rules. Invocation
timestamps are telemetry; event identity, request digest, result, operator/checker
identities and counts remain bound. Unexpected event keys fail. Self/direct-wait4/
reaped-child CPU scopes remain distinct; whole-tree CPU is unverified.

## Operation

LAUNCH.json contains the exact commands. Use g1-env/bin/python and the fixed files;
do not regenerate the manifest. Capture and grade outputs are create-only.
All assigned failures are preserved; no retries or fastest-run selection.
