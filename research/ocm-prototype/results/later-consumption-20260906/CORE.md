# Later learned-definition consumption: preparation

**Prepared protocol, not a native result.** Owner issue #62. One deliberately exposed,
researcher-designed integer absolute-difference task tests the consumption boundary
of the previously acquired `fn_0(h0) = not(h0 >= 1)` definition.

- [Protocol and falsifiers](PROTOCOL.md): exact task, three parents, four-check ceiling.
- [Unexecuted first preparation](preparation-history/eba6a712/HISTORY.md).
- [Engineering controls](CONTROLS.md): source preservation and mocked refusal checks.
- [Exact launch manifest](preparation/manifest.json): source, environment, task,
  library and request hashes; all prospective native launch commands.
- Raw candidate output will be created at `run-v1/candidates`; semantic assessment
  will be created separately at `run-v1/assessment`. Neither exists in this packet.

The next action is independent source review followed by a root-owned prospective
freeze of the exact manifest SHA256 and launch commands. Do not launch from this
preparation record alone. No retries or follow-up induction are registered.

The unchanged cvc5 worker returns builtin solution terms. It does not enable the
`sygus-sol-gterm` output option. A returned `fn_0` body dependency can qualify
observed consumption; a correct primitive expansion remains `NO_OBSERVED_USE`.
GEN grammar availability, helper declarations and equivalent primitive text cannot
supply missing use evidence. This observation constraint is retained explicitly.

No OCM-specific arm, general useful-learning, causal search benefit, whole-lifetime
performance or manuscript claim is made by this packet.
