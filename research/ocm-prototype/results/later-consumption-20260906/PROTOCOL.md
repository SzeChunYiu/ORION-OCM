# Fixed public assay

Source authority: [issue #62 prospective preparation](https://github.com/SzeChunYiu/ORION-OCM/issues/62#issuecomment-5560899587).
The repository starting state is `02e32dfa6755ec737ef472c054c9b3117af79e5b`.
The launch manifest binds every used source byte independently of that lineage.

## Question and inherited state

Does the exact previously sealed predicate appear as a dependency in a checked
solution of the distinct function `absdiff2`? The saved library came from maximum
and guarded-function programs; this exposed two-argument specification was added
later. Its design is a supplied research choice, not protected evaluation.

Adapter SHA256: `a82caf571767fc87113aa51455e1708b59ad120749e8eaf676184a64fee0a887`.
Predecessor seal: `275012f6e1429c08bd481631ff821f20dff8e3ee02393294241cbf2f8181e5e8`.
The library loader verifies both and checks the exact typed singleton definition.
The earlier raw acquisition, induction, normalization and alias-witness records are
history; this assay neither reruns nor replaces them.

`public_absdiff2_v1` constrains its answer to be at least both `x-y` and `y-x`, and
to equal one of them. Exactly their maximum satisfies all three constraints. For
integers this is absolute difference, including equality, negatives and both orders.
The complete SMT-LIB source is `../../clia_fixtures/public_absdiff2_v1.sl`.
Only the specification is supplied to the candidate worker; test-only example
solutions are never inserted into candidate requests. No OS namespace isolation is
claimed: native donor inputs are controlled, not the host filesystem's visibility.

## Assignments and unchanged donor powers

| Order | Route | Supplied search language |
| --- | --- | --- |
| 1 | C | Original implicit CLIA; strongest primary parent |
| 2 | E0 | Existing complete explicit primitive grammar |
| 3 | B | Exactly E0 plus sealed helper and singleton `GEN_fn_0` |

B is the ordinary adaptive native library parent, before an OCM arm. Every E0
primitive alternative and every original constraint remains available in B.
The worker keeps its exact existing options: `sygus=true`, `incremental=false`,
`tlimit-per=5000`, `check-synth-sol=true`. No induction, restricted grammar, option,
new rewriting identity, repeated call or result-driven follow-up is added.

Each native worker uses laptop CPU0, a 4 GiB address-space ceiling, native 5000 ms,
and a GNU outer timeout: 20 seconds for candidates; 10 for checking. The already
qualified supervisor retains stdout/stderr, uses process-group cleanup and a
24/14-second watchdog respectively, including the existing two-second kill grace.
The caller adopts the existing supervisor and unchanged worker directly; it does
not silently use `clia_process.invoke`'s default 15-second candidate deadline.

## Raw records and checker authority

`later_consumption_capture.py` dispatches C, E0, B once and creates a complete raw
inventory seal before parsing or grading candidate output. Failed or timed-out
rows stay in the three-row denominator. Source drift stops subsequent dispatch,
retains its failure and marks the remaining assignments not run.

`later_consumption_assess.py` requires that seal, then uses the existing primitive
CLIA grammar and `clia_checker.check` specification construction. Its native
boundary is captured by the same direct-worker supervisor, in a separate process.
It restores the temporary native-call interception after each existing checker call.

At most four native Z3 obligations are reached, in this order:

1. C's primitive grammar/specification obligation, if checkable.
2. E0's primitive grammar/specification obligation, if checkable.
3. B's fixed acquired helper versus host-expanded returned function equality.
4. B's expanded primitive grammar/specification obligation.

B's expansion failure is kept separately even when the specification check passes.
The equality uses the exact sealed helper in the left definition, independently
expanded primitives on the right, and negated equality over arbitrary integers.
Malformed, out-of-grammar or unbound output reaches no semantic obligation.
No obligation is synthesized from a target solution. Dynamic checker input hashes
are retained at dispatch; the source-bound construction and argv are frozen first.

## Observable consumption and terminals

Only actual `fn_0` applications in the validated, let-expanded returned `absdiff2`
body count. An unused let binding does not count. A helper declaration must match
exactly but does not count. Primitive similarity and input echoes do not count.
The current worker exposes no GEN derivation record. Unsupported annotations are
refused; adding cvc5's separate output option would require a different experiment.
See the [primary output-tag documentation](https://cvc5.github.io/docs-ci/docs-main/output-tags.html#sygus-sol-gterm).

- Checked B with observed calls: `LEARNED_DEFINITION_CONSUMPTION_QUALIFIED`.
- Checked B without observed calls: `NO_OBSERVED_USE`.
- B timeout, invalidity, failed equality or uncheckable output:
  `CANNOT_CHECK_CONSUMPTION`; no claim that the learned definition is useless.
- If C matches capability with less declared total work, report `PARENT_SUFFICIENT`
  for that public utility scope, whether or not B emits the helper. The assessor
  leaves utility unestablished pending the separate cost comparison.

Observed syntax cannot establish causal search guidance, useful new semantics or
future task savings. This acquired definition remains a primitive specialization.

## Costs and launch discipline

The packet retains completed native-worker CPU/RSS and capture wall; timeout worker
CPU/RSS and full process-tree/history costs remain unknown. Candidate and checker
costs are separate. Preparation/binding, acquisition, normalization, induction and
repair are not assigned zero costs. Tiny public timings support no adoption claim.

Use the exact `launch_commands.capture` then `launch_commands.assess` arrays from
the manifest only after independent root review and prospective freeze. Both stages
are create-only; existing output refuses another attempt. All raw records are kept.
Changing the manifest, source, task, helper or settings requires a separately bound
successor, never a replay under this identity.
