# Public hosted comparator plumbing pilot

Two fresh Codex CLI 0.129.0-alpha.15 requests used explicit gpt-5.5/high on laptop billy with persistent public lesson/procedure memory and a finite exact checker. Language role/tense outputs and polynomial identity/execution were correct for these disclosed examples; this is not a matched OCM/LLM comparison, protected evaluation, non-inferiority claim, or general-capability estimate.

Read semantic-verification.json for exact outcomes and costs. Raw JSONL retains an incorrect checker flag and the successful repair. The JSON schema intentionally allowed free-form status strings; their case/wording difference is not a format failure. Independent SymPy checking was performed after model execution outside its task workspace.

No OCM source or private evaluation oracle was given to the agent. Workspace-write sandboxing, on-request approvals, disabled external apps/multi-agent/model memories, and per-run ignored user config were used without changing login or global config. Filesystem read isolation remains partly instructional; all observed tool reads/writes stayed within the public task workspace. Backend weight snapshot is not exposed by CLI events.

Raw provider-catalog error bodies are retained privately, excluded here. Sanitized names and error counts are in catalog-diagnostic.json. SHA256SUMS.json binds every other file in this subset. Paths/thread identifiers in the raw events describe this disclosed local run; no credentials were found in the reviewed subset.
