# Supplied primitive rewrite qualification — prepared, not executed

Owner #62 [intake5560216859](https://github.com/SzeChunYiu/ORION-OCM/issues/62#issuecomment-5560216859).
SUPPORTING public engineering qualification; no learning, synthesis or persistence.
No native rewrite or Z3 call has run in this packet. Root must register the exact
manifest before the one fixed launch. Earlier induction remains unchanged.

## Fixed question
Can pinned Stitch rewrite the supplied identity
Abstraction("-", "(+ #0 (* (- 1) #1))", 2) into the already available binary minus
without changing bindings, unary minus, existing binary minus or original CLIA semantics?
The supplied identity is imported existing knowledge, not learned abstraction.

## Assignment and native calls
Two serial groups, each in one fresh process and each with one rewrite dispatch:
1. Manual: four fixed public functions: orientation; bound variables/non-shadowing
   lets; existing unary+binary minus; signed constants+nested matching expressions.
   At most four native equivalence checks against untouched manual originals.
2. TRAIN: exact max3 and mpg_guard2 saved programs and public tasks from the original
   induction input. At most two equivalence checks and two fixed normalized-program
   specification checks. Historical original checks are imported custody, not rerun.

Within each group preserve every assigned row. Decoding/grammar refusal makes that
row CANNOT_CHECK_NORMALIZATION; do not invent a new symbol, grammar or normalizer.
A killed/failed rewrite may leave later checks NOT_RUN; planned maxima are not
actual native invocation counts. Groups are fixed cases, not retries.
Raw native JSON is written before decoding or checking. Checks use the exact original
as the left side and the raw rewritten program, decoded without a macro library,
as the right side. Manual expected primitive trees additionally reject no-op,
reversed-order and incomplete normalization. Only their comparison uses the
existing encode/decode rendering convention (e.g. signed -3 renders as (- 3)).

## Decisions
MANUAL_BOUNDARY_QUALIFIED requires all four exact expected trees and four PASS
equivalence results. TRAIN_NORMALIZATION_QUALIFIED requires both original-grammar,
equivalence and fixed-spec results PASS, plus at least one effective binary-minus
conversion. Both unchanged TRAIN programs produce
TRAIN_EQUIVALENT_NO_EFFECTIVE_NORMALIZATION, never a successful effective rewrite.
The final packet distinguishes raw capture completeness from normalization quality.
No claim of a new useful operator, later use, runtime gain or OCM residual follows.

## Scope and costs
CPU0; 4GiB virtual address-space per group; 45s outer timeout; 2s kill grace;
49s supervisor watchdog; fixed checker 5000ms/10s. Two groups only.
Record raw process start-through-cleanup wall, caller wall/self CPU/self peak RSS,
all donor/check arguments/results/errors and before/after source/runtime hashes.
Total process-tree CPU/RSS and imported historical acquisition costs remain UNKNOWN.
Supervisor wall includes repeated custody hashing; this is not a timing study.
Known identity/source/input bytes are explicit priors. No costs are hidden in learning.

## Preparation evidence
controls/combined-v1.json binds eight harmless fake-native controls, all passing.
Retained stub baseline: five failures. First candidate: four passes, one comparison
failure caused by signed-literal spelling; existing parser rendering fixes it.
Final controls include clean exact-rule forwarding, no-op rejection/separation,
shadowing refusal, inventory loss and truncated/nonobject native-response custody.
No actual Stitch/Z3 call occurs in these controls.
manifest.json binds the launch/source/input/runtime/qualification closure;
launch.json supplies the exact future command. prior-binding.json binds the
unchanged prior source and the 20-file sealed original capture.
