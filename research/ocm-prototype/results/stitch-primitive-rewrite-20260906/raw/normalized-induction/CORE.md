# Normalized public TRAIN induction — prospective packet

Status: ready for root freeze; no actual induction or native solver call has run in this packet.

This is one fixed adopted-Stitch plumbing trial on the same two exposed TRAIN functions.
It changes their syntactic basis using the already qualified supplied-minus rewrite.
It is not a transfer, persistence, learned-usefulness, or OCM advantage experiment.

## Exact inputs and inherited evidence

- Two original tasks: jmbl_fg_max3 and jmbl_fg_mpg_guard2; task objects are unchanged.
- Candidates are copied verbatim from the sealed BFS TRAIN qualification rows.
- Normalization seal: 63f7f7a107befd2b8332ff33ff8404e04118f51ca7ca9481c94630a7e3551a77.
- Both normalized candidates previously passed their original fixed specification and
  original-versus-normalized equivalence checks. These earlier checks are historical.
- experiences.json contains the normalized candidate and its actual historical fixed-spec
  receipt. input-lineage.json maps each exact candidate back to the sealed row.
- predecessor/ retains the original input and both relevant seals/normalization records.
- The original induction result proposed a subtraction alias; it remains unchanged.
- The supplied rule (- a b) = (+ a (* (- 1) b)) is an imported primitive identity.
  It is not supplied as a learned library, an invented operator, or a new compression setting.
- Donor rewrite metadata num_abstractions=1 counted that supplied identity, not discovery.

## Unchanged execution contract

The original caller.py, launch_once.py, supervision.py, seven source modules and fixture
files are byte-identical to the qualified induction packet (adapter source b03b749).

1. Existing generation_stitch.induce rechecks the two normalized programs against the
   same original public specifications with the fixed checker.
2. If preparation passes, dispatch exactly one compress using iterations=1, max_arity=2,
   threads=1, silent=True, with the same two task IDs. No extra keyword is introduced.
3. Existing typed admission, primitive expansion and whole-program equivalence run on
   returned rewrites. Up to two further fixed Z3 verifications may be reached.

Maximum schedule: one compress and four native Z3 children; actual boundary, entry,
return and exception counts come from raw request/result records, including fast paths.
Each checker call retains timeout_ms=5000 and deadline_s=10.
No donor rewrite API, synthesis, persistence, retry, second corpus, or fallback is allowed.
The BFS hole-choice option belongs only to the completed normalization predecessor;
the compress call retains its original settings and default traversal.

The process uses CPU 0, 4 GiB address space, 60 seconds plus 2 seconds kill grace.
The existing supervisor has a 64-second watchdog. Python is nonoptimized.
launch.json is the exact command; output capture-v1 is create-only.

## Admission and negative outcomes

NO_NEW_ABSTRACTION, typed refusal, CANNOT_CHECK_REWRITE, exceptions and timeouts remain
valid outcomes. Raw donor return is preserved before admission or equivalence assessment.
PROPOSED_ABSTRACTIONS establishes only a proposed typed library and checked TRAIN rewrite.
Primitive-alias assessment remains NOT_RUN: no new alias filter or verification was added.
Even a nonempty library is not evidence of a new useful operator or runtime benefit.
No later target program or specification is supplied to the caller.

## Costs and scope

Charge current imports, input processing, checks, compression, assessment, source binding,
process launch, output and cleanup. Retain donor cost as its own weighted syntax measure.
Caller self CPU/RSS and native checker observations have distinct scopes.
Complete process-tree CPU/RSS, internal memory, energy and whole-lifetime cost remain UNKNOWN.
Original acquisition, initial induction, failed normalization and successful normalization
are inherited public history, with their costs retained separately; they are not free learning
and are not charged by this single invocation. No whole-lifetime economics claim follows.
Runtime inventory omits the full stdlib, OS and transitive system libraries; no OS isolation claim.

## Harmless qualification and custody

Five original wrapper controls and three repaired timeout-observation controls passed.
The extra pure input check verified exact row/task/check joins and both encode/decode
roundtrips, with native entry replaced by refusal and no Stitch/Z3/cvc5 module loaded.
controls/ and controls-v2/ retain actual stdout, stderr, exit status and synthetic captures.
Manifest bindings are checked before and after actual execution; malformed partial responses
remain UNKNOWN while their bytes, receipt and seal are preserved.
