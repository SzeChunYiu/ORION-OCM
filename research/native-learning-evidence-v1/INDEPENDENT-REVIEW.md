# Independent lifecycle review — scoped acceptance

The actual retained authored control passes this source/lifecycle review, with no
remaining finding in its fixed scope. No learner, solver or native outcome was rerun.

| Arm | Tree actions | Action attempts | I actually used |
|---|---:|---:|---|
| Baseline | 3 | 141,792 | No |
| Enabled | 2 | 72,579 | Yes |
| Canonical ID removed | 3 | 141,792 | No |
| Exactly restored | 2 | 72,579 | Yes |
| Replacement U | 3 | 141,804 | No |

All seven arms retain the same 69,219 ordinary instances from all 4,191 contracts.
Removal preserves the exact baseline derivation and work; restoration recovers the
exact enabled derivation and work. Each positive proof has the identical 21 labels:
3 semantic, 3 syntax, 11 floating and 4 logical-hypothesis occurrences.

Five separate native checks verify the exact task premises/query and the full 4,095
old proofs plus the new theorem, retaining the same 96 trusted assertions. Wrong-hole
mutation is rejected for essential-hypothesis mismatch after the old proofs pass.
False tasks yield bounded no-proof only; semantic nonentailment comes from the
separate authored semantics review.

Seven distinct fresh B PIDs begin after A's recorded exit/reaping. Native checkers
begin after each B exits. Recorded requests, result PIDs, emitted proofs, exact target,
source and stream bindings agree. The allowed-field serving projection excludes
supervised discovery histories; B reads only admitted chunks and permitted inputs.

The final 24 focused controls include actual graph renaming, repeated-hole expansion
accounting, exact-hole refusal, original-pin tamper refusal and canonical-ID removal.
The earlier accounting fixture failure is retained. Alpha is a graph-data control,
not a newly native-verified training example.

This is supervised near transfer of conventional mechanical chunking. It is not an
OCM integration, autonomous new mathematics, language transfer, scalability result,
novelty claim or publication-ready result. The baseline shares post-A state, so it
is a use ablation rather than an acquisition-history comparison. One fixed run per
arm does not establish wall-time speedup; cold ordinary compilation dominates.

Read REVIEW.json for compact bindings; OUTCOME-REVIEW.json retains 551 data and
source-integrity checks. These checks are not independent experiments. Earlier
source generations and expectations are preserved beside the final review.
