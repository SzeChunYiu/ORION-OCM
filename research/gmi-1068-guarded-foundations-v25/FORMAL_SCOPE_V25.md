# V25 formal scope — anchored histories and relative information

This file describes the kernel evidence for Y1–Y4 in the committed V25 freeze.
The original scope is relative to the declared typed observation interface.
No minimum number of symbols, independent table cells, shortest description,
absolute ontology, or derivation of all machine intelligence is proved.

## Read-first result

For arbitrary lawful V19 partial algebras, anchored trees retain every Empty
as its true identity arrow. Strong partial associativity makes their recursive
Option evaluation agree with the actual V19 nonempty-word fold.
For actual V11 categories this includes malformed joins and actual Path evaluation.

For arbitrary presented carriers in a fixed ambient label type, all raw tree
responses carry exactly the information in the lawful labelled padded table.
The table itself determines membership via local units.
With independent external object names, the least retained information is the
pair (padded table, object-to-identity map), in the attained-code decoder order.
The object map cannot be silently supplied to a decoder that receives only a table.

## Y1: trees, words, category interpretation

`BracketV25` defines actual Arrow/Empty/Seq syntax, recursive evaluation,
two-sorted leaf maps, validity guards and nonempty words.
`flatten` maps an Empty anchor to a singleton identity word.
Concatenation preserves both operands, including nested empty anchors.
There is no unanchored empty-word default.

`OptionFoldV25.run_append` concerns the actual V19 recursive run.
`run_product` and `fold_append` derive concatenation from strong Option
associativity. Equality includes None, not just jointly defined products.
`flatten_eval` and `guarded_flatten` are arbitrary-tree induction theorems.
The latter rejects any invalid leaf, including absent arrows and nonunit anchors.

`CategoryTreesV25.observer` uses actual BundledCategoryV19.mul.
Arrow/Empty/Seq equations and aligned/rejected pairs bind actual endpoint tests
and C.comp. Category laws are supplied by the lawful target record.
`guarded_flatten` derives the whole-tree word equation.
`pathTree`, its nil/cons equations and `path_interpretation` bind the actual
V11 Path and eval, retaining both original endpoint tags.
Nothing identifies arbitrary raw trees with already-well-typed paths.

## Y2: presented carrier and recovery

`PresentedCoreV25.Presented` contains a predicate S on an explicit ambient L
and an actual V19 Algebra on the subtype of present labels.
It allows an empty carrier; ambient labels need not all be present.
`lookup` and `padded_lookup` bind both membership checks, subtype multiplication,
and mapping the actual result back into L.

`padded_some`, `result_membership` and `padded_present` bind the precise
successful equation, its input/output membership, and agreement on present inputs.
`carrier_from_row` uses an actual right local unit.
`table_determines_carrier` follows; no census conclusion is assumed.
`padded_assoc` derives strong associativity on ambient L.
The padded operation need not have local units at absent labels and is not
asserted to be a lawful Algebra on all L.

`PresentedUnitsV25.padded_unit` proves exact subtype/ambient true-unit
correspondence. `full` constructs an actual presentation of any Algebra
with carrier True; `full_padded` and `full_mul` bind its operation.

`RawObserversV25` binds recursive Arrow/Empty/Seq behavior, pair extraction,
guarded flattening, observer equality iff table equality, and core equality.
`ConstructorBindingsV25.raw_arrow_census` explicitly extracts membership.
The raw observer may query an absent ambient singleton and returns None.
A category with no objects has no typed leaf queries; these are different interfaces.

`observer_recovery`, `core_recovery` and `raw_threeway_recovery` reuse
freshly replayed V9 attained-image recovery. Their quantifiers cover every
model family and every encoding, including empty families and collided codes.
The theorem derives decoder existence from fiber constancy.
It does not require or manufacture an off-image default value.
Information minimality permits arbitrary equivalent encodings/compression.

## Y3: actual loss controls and corrected survivor meanings

The contract registers both the response contrasts and inherited preserved laws.

- TYPE: `failure_tag_response_loss` lifts actual discrete/join V19 algebras
  into raw presentations. The inherited default collision erases None while
  retaining a different legality response. Derived endpoints are not extra fields.
- PROCESS: `absent_present_singleton` compares lawful empty/singleton support
  in common Bool labels; `equal_size_labels` compares different singleton supports.
  Their full labelled padded tables differ, consistently with the census theorem.
- IDENTITY: `constant_unit_failure` uses the actual V9 constant product;
  it is associative but has no true unit, and designated Empty insertion changes
  the observation. This is a weakened candidate interface, not a lawful raw Empty.
- COMPOSITION: `composition_response_loss` uses the actual C4/V4 operations
  through full Presented records; the inherited theorem retains admission and units.
- ASSOCIATIVITY: `nonassoc_brackets` changes triple output despite local
  units/coherence; `weak_definedness_brackets` loses definedness agreement while
  retaining weak associativity/local units/coherence. Both premises are registered.
- IDENTITY_LAWS: `designated_insertion_changes` evaluates actual left/right
  projections with supplied candidate-empty maps. The opposite neutral direction
  and associativity are registered; genuine IsUnit is never assumed and contradicted.

`coherence_matching_failure` and the inherited isolated-coherence theorem
show a missing matching-endpoint join, not a bracket disagreement.
The inherited minimum-three theorem is only for total unital nonassociative
operations; no minimum-size claim is made for every partial countermodel.
`naive_empty_loss` proves that erasing anchors merges distinct-empty and
same-empty queries whose lawful raw responses differ.
All six original rows are accounted for as information or laws, not six
independently stored primitive fields.

## Y4: named objects and actual encoding transport

`NamedObserversV25.NamedPresented` requires only true-unit landing of identityMap.
`Complete` additionally requires injectivity and coverage of all true units.
The general recovery theorem deliberately works even before completeness.
Python's complete category presentations impose the stronger bijective condition.

ArrowPair recovers padded multiplication; Empty recovers the actual identityMap.
`observer_eq_iff` and `observer_recovery` derive the paired-information result.
`raw_translation` binds the actual two-sorted query translation.
`CategoryTreesV25.named` instantiates this structure from an actual category:
`named_table`, `named_identity`, `named_complete`, `named_observer` and
`category_named_carrier` register its exact operation, encoder and total carrier.

`TreeTransportV25` instantiates its induction with the actual V19 category
roundtrip TableIso, objectForward/objectBackward and categoryForward/categoryBackward.
Both response equations and both query inverse equations are registered.
These imply reflection as well as preservation, including None and Empty anchors.
`AlgebraTreesV25` uses actual pack/unpack for the reverse reconstruction.
Its `unpack_raw` connects reconstructed-category observations to raw unit-anchored
observations in the actual full presentation, not merely equal carrier cardinalities.
`same_guarded_word` proves invariance for all trees with the same flattened word.

`CarrierControlsV25.swapped_names` holds the table fixed while changing Empty
responses. `table_only_nonrecovery` is a decoder impossibility theorem;
`paired_revival` restores recovery by retaining the map. Both encoders are complete.

## Replay and evidence boundary

The static contract registers 177 exact types and constructor/operation equations
against 26 fresh source compilations: 13 immutable inputs and 13 new modules.
The isolated SOURCE stage compiles with pinned Lean4.19 and warnings as errors.
AUDIT assigns the actual declarations to explicit reviewed types and prints their
kernel dependencies. No runtime type discovery or generated weakened statements
enter replay. Classical.choice, propext and Quot.sound are permitted standard
foundations; placeholders, unchecked assumptions and unsafe declarations are rejected.

Run the package driver for integrated evidence, or from this package run:
`GMI_LEAN_BIN=/home/billy/.elan/bin/lean /home/billy/.local/bin/python3.12 -c 'import check_lean_v25; print(check_lean_v25.evaluate())'`

The Python exhaustive classifiers, concrete corpus ordering/counts, decoder search,
validation failures and symbolic-kernel/Python correspondence are separately
reviewed finite evidence. Lean does not prove the Python implementations correct.
The four source-valid mutation controls are root-owned integration evidence.
No R1 status changes or broader programme completion follow from this file alone.
