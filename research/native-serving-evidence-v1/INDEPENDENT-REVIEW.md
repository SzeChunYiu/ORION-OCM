# Native OCM serving — independent final result

**ACTUAL_FIXED_SERVING_ADOPTION_SUPPORTED** in the exposed authored finite regime.
The full eight-cell comparison has four checked positives and four bounded refusals.
All eight processes exited cleanly with exact source/input/request/result bindings.

The two enabled positive calls actually used I in the selected derivation. This occurred
through the real OCM registry → SV → native checker → commitment path for the OCM cell,
with exact core-event and durable intent/use records. The ordinary and enabled routes
returned identical expanded proofs: 21 labels, including 3 semantic applications.
Minimum abstract rule-tree cost fell from 3 to 2; action attempts fell from 141792 to
72579. The expanded native proof did not shrink.

Twenty fresh native replays ran in the complete comparison: 16 current imported-method
qualifications and 4 output checks. Each verified 4096 proofs under the same 96 assertions.
The four false cells returned NO_PROOF_WITHIN_BOUND proposals and CANNOT_CHECK, with no
answer. Enabled false cells still made 48 macro action attempts; empty selected-proof IDs
refer only to a returned proof, not to internal method activity.

| Task / policy | Direct observer seconds | OCM observer seconds |
|---|---:|---:|
| positive / ordinary | 7.145353 | 8.901232 |
| positive / I | 7.596574 | 8.700848 |
| false / ordinary | 6.844105 | 8.048329 |
| false / I | 6.794619 | 8.148931 |

OCM adds overhead in every matched pair. Direct I-enabled is slower than ordinary in
this single ordered run. No cold speedup is established. Every arm still compiles 69219
ordinary instances from 4191 contracts; source/input checks repeatedly scan bound data.
Some arithmetic and warrant internals remain uninstrumented. No sparse-execution,
scalability or complete-instrumentation claim follows.

The complete lifecycle window is 62.897335 seconds. The retained failures add 2.022482
and 16.824716 seconds, totaling 81.744533 execution-window seconds. Observer windows begin
after opening observer checks; lifecycle windows include per-arm startup/custody and
supervisor handling but start after their opening manifest check. Review, controls,
packaging and that opening check are outside these totals. Cold means fresh process,
engine and compiler, not OS-cache eviction. Direct also imports OCM and shares hashing.

The first failed attempt rejected metadata before search. The second completed durable
ordinary OCM/native use but failed final diagnostic export on legitimate infinite scope
bounds. Both attempts and their costs remain intact. Narrow apparatus repairs project
only metadata and encode diagnostic float bits; no task, scope or algorithm was tuned.
The missed envelope assumption in the earlier review is explicitly corrected.

This establishes serving adoption of external methods in a three-class, empty-DV finite
bank. It does not establish endogenous acquisition, general language competence, novelty,
lifetime benefit, a merged product or a best standalone conventional parent cost.
The shared conventional engine supplies the computation; OCM supplies the exercised
warrant, dispatch and durable-use integration.

REVIEW-FINAL.json is the compact receipt. OUTCOME-REVIEW-03.json binds 757 retained-data
checks; PUBLIC-REVIEW-draft.json binds 266 public-summary checks. Neither is an additional
experiment. Historical source, apparatus and failed-run receipts remain independently
available. The only pending public edit is replacing its review-pending sentence with
the final receipt identity; no outcome rerun is required.
