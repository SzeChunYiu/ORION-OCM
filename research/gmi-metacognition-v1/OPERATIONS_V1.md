# Derived metacognition: read first

[META-1–4 theorem](METACOGNITION_THEOREM_V1.md): confidence = surviving-candidate
success margin; error cost = its complement under declared unit loss;
EVC = one-step myopic value with exact 13/35 greedy-optimality condition;
strategy selection = measured finite-register partition (bold-32 + ties).

Run from this unit directory:

```sh
python3 -I -B test_metacognition_v1.py -v
python3 -I -O -B test_metacognition_v1.py -v
```

The test entrypoint compiles its sibling model from source; exact rational
arithmetic only.
