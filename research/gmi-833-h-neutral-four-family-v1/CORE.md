# One lower grammar across four bounded structural analogues

This package is a preparatory Section-H result, not four completed family
derivations. It freezes one finite `Z3` process grammar before evaluation:
external arguments and constants; generic modular addition and multiplication;
zero test; generic indexed parameter reads; and an optional delay cell. The
generator receives no family label or family-specific candidate menu.

Blinded external obligations and property predictions were committed at
`32566daa` before the checker existed. The same grammar is then generated once
per interface arity, semantically quotiented, and filtered for exact protected
behavior. Only after selection does an external structural classifier attach
the descriptions persistent-state, affine-score, nonlinear-link, or
cross-coordinate lifted interaction.

The no-smuggling audit is semantic, not merely lexical. It checks a stable
grammar digest before and after every search, provenance closure over the
registered lower operations, total denotations on complete carrier domains,
and complete target-independent enumeration of all 27 unary and 19,683 binary
generic indexed maps. Indexed payload construction and lookup costs are fully
charged. A source-separated formula/residual oracle imports no primary checker
and reproduces every frozen prediction.

Positive/negative pairs recover or reject each bounded structural property.
Exact raw-price reversals compare compact composed programs with generic indexed
maps; the sequential witness separately proves the state/history-replay phase
boundary. Analytic counting and residual-quotient arguments supply finite lower
bounds.

The target-encoded historical microscope remains valid only as an exact
calibration: its named coefficient-list/state-table/etc. alternatives cannot
establish common lower-grammar recovery. Its dependent P0 closure terminal is
superseded for Section-H neutral-recovery authority.

All four named family rows remain unchecked. Each lacks the required real-scale
test, and the finite analogues do not establish training, statistical
generalization, real numerical conditioning, real kernels, or independent-team
replication. The shared-grammar row was already closed by PRs #931–#937 and
#951 before this package completed; PR #960 is a non-mutating independent
corroboration and does not claim or rewrite that checkbox.

Terminal:

`ONE_LOWER_NEUTRAL_GRAMMAR_RECOVERS_FOUR_POSTHOC_STRUCTURAL_CLASSES_AT_REGISTERED_FINITE_SCOPE__FAMILY_GATES_REMAIN_OPEN`
