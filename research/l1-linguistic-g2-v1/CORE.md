# L1 linguistic G2 causal reuse (microworld)

Learned Adj–Noun intersective construction, persisted and restarted, actually
invoked on held-out fillers, removed by evidence revocation. Grammar-induction
parent stores train bigrams and does not contain held-out pair identities.

Held-out combo `two green pyramid` is evaluated as `tokens[-2:]`, so the
numeral is dropped. Frozen `recursive_composition` /
`held_out_construction_combinations` are `EARNED_ADJ_NOUN_SUFFIX_NUMERAL_DROPPED`,
not numeral composition. Issue #165 recursive-composition remains OPEN.

Not corpus-scale N1. Open-weight LM reference is `CANNOT_CHECK_NO_MODEL_WEIGHTS`.
L2/L3 remain locked.
