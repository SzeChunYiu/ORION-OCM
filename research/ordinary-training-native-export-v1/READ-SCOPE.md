# Read scope and qualification

## Inspected sources

The initial [source read record](SOURCE-READS.json) retains its original bindings.
[Supplemental source read record](SOURCE-READS-02.json) adds later bounded reads.

Inspected the existing opportunity protocol, population proposal, root source
review, release/consumer entry code, and pinned donor source index/observer.
Read the old six-anchor trace runner to identify the entry limitation.
Read relevant mmverify token/read/verify methods and class structure without
running it. Read typed_constructor.py to establish its exact span and hypothesis
requirements. These are source-interface observations, not corpus results.

No set.mm file, released prefix, actual release file, new proof body, census
outcome or protected corpus outcome was opened. The registry descriptor came
from the lead's metadata message; this review did not audit its record contents.
The actual release and governance bindings remain missing.

## Executed authored controls

[PURE-QUALIFICATION-01.json](PURE-QUALIFICATION-01.json) records 14 passed controls,
PID 2009352, pinned Python, actual cwd/imported file paths and unchanged hashes.
They cover missing bindings, fixed integer ordinals, runtime/donor drift, exact
new-axiom authority, canonical identities, span binding, unsupported trace
dispositions, empty exclusions and retained full P1.

These fixtures are invented metadata/structures marked AUTHORED. They import
neither donors, native verifier, constructor/bridge nor learner.
The retained [initial red log](CONTROLS-RED.txt) is the original request-validator
stub result; it is not a qualified native or complete controls run.

## Actual isolated entry refusal

[ENTRY-QUALIFICATION-01.json](ENTRY-QUALIFICATION-01.json) records the actual
`-I -S -B trace_export.py` command, child PID 2010470, child cwd, entry file,
helper import paths and exact pre/post source/runtime hashes.
[Child RESULT](synthetic-entry-refusal-01/RESULT.json) shows the synthetic
missing-pin request refused with exit 1 before gate, prefix or native-module read.
Native/learner calls were zero. The sibling helpers loaded successfully despite
isolated startup. This is distinct from the helper runner's explicit sys.path.

The refusal proves that missing inputs fail at the intended real entry.
It does not execute or qualify the successful native branch, source parser over
a released corpus, resource adequacy, trace export, cut opportunity or learning.

## Current boundary

This capsule is a reviewable source successor only. Its nine production sources
are bound in the request template; copied donors retain their original hashes.
The full native path still requires actual root bindings, a new authority
contract, an observer contract and a separate root gate.
A resulting native/trace candidate will need its own source-bound qualification.
No old native receipt, authored fixture or startup refusal covers that future run.
