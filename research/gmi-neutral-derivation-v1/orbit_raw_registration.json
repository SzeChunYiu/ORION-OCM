{
  "version": 1,
  "status": "FROZEN_BEFORE_EXECUTION",
  "input": "Complete finite Boolean-cube table with rational vector outputs; compiler receives only table and dimensions",
  "scope": "Recover and check affine restriction, exhaust paired coordinate permutations, predict incidence orbit count before generic equation compilation",
  "families_held_out": false,
  "cases": [
    {"id": "raw_local4", "inputs": 4, "outputs": 4, "table_rule": "y[r]=1+2*x[r]+x[(r+1)%4]", "group_size": 4, "dimension": 3, "active_entries": 12},
    {"id": "raw_sum4", "inputs": 4, "outputs": 1, "table_rule": "y[0]=1+sum(x)", "group_size": 24, "dimension": 2, "active_entries": 5},
    {"id": "raw_dense_rectangle", "inputs": 3, "outputs": 2, "table_rule": "y[0]=1+x[0]+2*x[1]+3*x[2];y[1]=2+4*x[0]+5*x[1]+6*x[2]", "group_size": 1, "dimension": 8, "active_entries": 8},
    {"id": "raw_copy4", "inputs": 4, "outputs": 4, "table_rule": "y=x", "group_size": 24, "dimension": 1, "active_entries": 4}
  ],
  "negative": "Two-bit parity is not affine on its complete Boolean cube and must be rejected by this compiler, not mislabeled as a failed intelligence obligation",
  "cost": "All table checks and paired permutations charged. Factorial search is admitted only at finite sizes; no scalable ecological inference claim. Deriving real-domain affinity from unobserved inputs is forbidden.",
  "interpretation": "Tasks were authored with knowledge of existing mechanisms. Architecture names/templates are absent from inference and compilation; this is prospective finite property recovery, not literal prior-free discovery."
}
