# Parent ledger for gmi-833-h-family-tranche-d-v1

## The narrow prior (harmless, disclosed)

1. The nine rows of this package, in the exact wording of issue #833 Section H:
   Evolutionary/population search., Cellular/local-field computation.,
   Distributed/collective intelligence., Tool-using/solver-routing
   intelligence., Neuro-symbolic/statistical-symbolic hybrids.,
   Continual-learning systems., Meta-learning systems.,
   Self-modifying/morphogenetic systems., Multi-agent emergent communication
   systems.
2. The eleven-coordinate rule of section H as stated in
   `gmi-833-h-family-requirement-ledger-v1/LEDGER_V1.json` (`HRL-1`),
   including the `CROSS_SCOPE_GATE_COMPOSITION` ban.
3. The real-scale definition of `gmi-833-h-real-scale-classical-v1/FREEZE_V1.md`
   section 7 (sha256-bound real source, n_fit >= 100000, n_held >= 20000,
   exact held-out arithmetic, charged resource accounting).
4. The known-machine-intelligence register of the obstruction census
   (`gmi-833-h-obstruction-census-v1/FROZEN_FAMILY_REGISTRY_V1.json`) as the
   post-hoc structural register. The census result itself is not evidence here;
   the registered channel labels (`STOCHASTIC_SOURCE_CHANNEL`,
   `UPDATE_FEEDBACK_CHANNEL`, `EXTERNAL_PEER_TOOL_CHANNEL`,
   `TRIPLE_PARITY`) are attached only after recovery.

## What is not claimed

- No universal recovery; no named-family row is closed anywhere in this
  package.
- No finite witness is evidence for real-scale behavior (`FGS-3`).
- No certificate from a parent scope composes with a certificate here
  (`FGS-2`, `CROSS_SCOPE_GATE_COMPOSITION`).
- No row owned by another lane is re-earned: H02/H03 (the four-family real-scale
  battery), H04 (closed), H05–H14 (tranche A), the deep/neural rows (tranche B),
  the ten statistical/generative rows (tranche C), the symbolic/SSM/retrieval
  rows (FDT revivals), the M rows, and the L row.

## Strongest parents

- `gmi-833-h-obstruction-census-v1` for the shared neutral grammar shape, the
  registered channel labels and the post-hoc-registry discipline.
- `gmi-833-h-family-tranche-a-v1` for the eleven-coordinate ledger shape, the
  exact serving-allocation crossover model, the disjoint held-out discipline
  and the R01-on-distinct-key correction.
- `gmi-833-h-real-scale-classical-v1` / `gmi-833-h-real-scale-revival-v1` for
  the real-scale threshold and the `OPEN_REAL_SCALE_PENDING` attribution form.
- `gmi-833-h-family-gate-soundness-v1` for `FGS-2`/`FGS-3` (scope gluing and
  finite-evidence non-promotion).

## Residual contribution

A single shared neutral grammar over the programme's `{0,1}^8` Boolean
primitive set (digest identical to the census and tranche A), nine per-row
single-sigma gate ledgers that stop at 10 of 11 in finite scope and leave `R11`
registered `OPEN_REAL_SCALE_PENDING`, a post-hoc structural classifier that
attaches the registered channel labels only after recovery, deterministic null
batteries over support-disjoint classes, and a checker that refuses closed-row
claims without a real-scale receipt.
