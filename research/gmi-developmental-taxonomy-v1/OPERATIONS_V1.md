# Developmental taxonomy: read first

Theorem + exact finite witnesses for the five-type taxonomy (item 3 / A3).

Run from this unit directory:

```sh
python3 -I -B test_taxonomy_v1.py -v
python3 -I -O -B test_taxonomy_v1.py -v
```

The test entrypoint compiles its sibling model from source; exact integer
arithmetic only, CPython 3.8 safe.
