# GMI #913 capability-identification abstention freeze v1

Parent: #833 Section K. Source main: `d121aa20e5a31f57ce1194e8b635e7ce4d74762e`.

## Frozen closure target

This tranche targets exactly one proof-only row:

- require abstention where capability is not identifiable.

It does not build or validate the full capability predictor, attach empirical
calibration, predict real trained systems, or close any other Section K row.

## Frozen parent subtraction

The tranche must pin and import the merged #837 foundation, #848 external
capability contract, #851 typed uncertainty/query-identification contract,
#854 compact axiom core, and #906/#907 finite capability-bound laws. Exact
identified sets, images of finite sets, and reject-option/partial-identification
logic are parent mathematics rather than novelty claims.

Frozen parent blobs:

- foundation result: `c0c574c4ec6e237d5fdafa694eac131399625a70`;
- morphology/capability result: `bdc5c3cd42e312d8c7af52f7ba84220631a25f8a`;
- global uncertainty result: `9ab16cf59087214e093ace3b18c6d08fc79ab871`;
- axiom-core result: `3366a3bc7236d286f8d123bf53e4e3b2d22ad7b9`;
- capability-bounds result: `7ff80bab4a0b9e967e02cc6943bbe8828e3acea0`.

## Frozen formal scope

- a finite registered nonempty candidate realization/model domain;
- a surviving feasible or confidence subset, kept type-distinct;
- a total external capability-score query on the registered domain;
- exact rational score values and exact rational confidence failure budgets;
- a point prediction is licensed only by a singleton query image.

## Frozen theorem obligations

1. For nonempty survivor set `C` and capability query `q`, emit
   `IDENTIFIED(y)` iff `q[C]={y}`. If the image has multiple values, emit
   `CANNOT_IDENTIFY` with the complete identified set and no point value.
2. An empty survivor set emits `INCONSISTENT_REGISTERED_ASSUMPTIONS`; absent or
   non-total query semantics emit `CANNOT_CHECK`. Neither is an abstention over
   a valid non-singleton identified set.
3. A forced member of any non-singleton exact image is not uniformly sound:
   another surviving candidate has a different value. A coarser constant query
   remains identifiable despite unresolved model identity.
4. A confidence set may transport its exact failure budget to the identified
   set/point. A bare feasible set carries no fabricated coverage or calibration.

## Frozen falsifiers

The result is red if a multi-valued image emits a point, the identified set is
incomplete, inconsistency is called uncertainty, missing query semantics are
called non-identification, confidence is converted to certainty, a feasible set
fabricates calibration, a coarser identified query is forced to abstain, parent
drift is accepted, or the one-row result is promoted to a completed predictor.

Allowed terminal only after analytic proof and exact replay:

`GMI_833_CAPABILITY_IDENTIFICATION_ABSTENTION_AT_REGISTERED_FINITE_SCOPE`
