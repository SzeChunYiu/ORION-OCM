# Next boundary: separately gated screening

This source tranche has not re-screened the 76 retained proposal occurrences. The [original audit](REFERENCES.json) remains accepted as an unresolved result. The next work is a separately reviewed screening-only successor using the same full 4,323-contract parent and retained proposal order.

The future screen must explicitly preserve `SyntaxUnknown` at every agreed exception seam. Unsupported grammar, resource refusal and failed replay remain `UNKNOWN`; a local parse witness cannot certify complete applicability coverage. No existing gate or receipt authorizes this successor run.

Keep cold grammar construction, copied contract storage and parser/matcher work visible. Lark's internal chart work is not instrumented and must not be reported as zero. One shared grammar per fixed screening invocation is a prospective engineering choice, not an observed cost saving.

The prototype's 512-token and 4,096-emitted-label boundaries are explicit syntax-domain limits. Grammar-based parsing replaces the old handwritten Boolean depth contract. These facts must remain separate from the registered 2,000,000 matcher-state and 60-second soft study bounds. The parser call is not cooperatively interrupted internally; outer containment remains necessary.

Other root/context/boundary refusals remain separate. This capsule neither fixes them nor establishes useful learning opportunities, native admission, serving benefit or novelty. The next result must distinguish complete ordinary coverage, an alias witness and unresolved screening; none may be inferred from absent counters or empty result lists.
