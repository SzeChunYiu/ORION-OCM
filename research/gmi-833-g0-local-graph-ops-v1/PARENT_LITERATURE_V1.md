# Parent literature and ownership — E3

The mathematics of local update, graph neighborhoods and permutation symmetry is parent-owned. This tranche claims only a bounded exact G0 integration/certificate.

## Local/distributed computation

- Cellular-automaton and local-interaction traditions: synchronous state updates from bounded neighborhoods are established parent concepts.
- LOCAL-style distributed graph computation: one-round information is restricted to a node's local neighborhood; E3 uses only the elementary one-step locality principle, not a new distributed-complexity theorem.

## Permutation symmetry / set and graph maps

- Zaheer et al., **Deep Sets** (NeurIPS 2017): permutation-invariant aggregation and permutation-equivariant set maps are direct parents for the symmetry pattern used by `GLOBAL_BROADCAST`.
- Modern geometric/graph deep-learning literature treats shared local neighborhood aggregation as permutation-equivariant under vertex relabeling; E3 imports the symmetry fact while deliberately avoiding a neural/GNN architecture claim.

## Repository parents

- #868 / PR #873 owns the architecture-uncommitted G0 operational-core programme and raw-resource discipline.
- #864 owns semantic presentation/state relabeling equivariance and the boundary that semantic invariance alone does not imply search invariance.
- #875 / PR #881 owns finite grammar representation/reachability bias; E3 does not reopen those rows.

## Residual owned here

Typed POINTWISE/GLOBAL_BROADCAST/NEIGHBOR_UPDATE contracts, exact finite separation hostiles, exhaustive three-site relabeling certificate, exact raw resource accounting, deterministic receipt/oracle, and two-row #833 reconciliation.
