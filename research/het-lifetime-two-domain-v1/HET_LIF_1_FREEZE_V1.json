{
 "schema": "OCM_HET_LIFETIME_TWO_DOMAIN_FREEZE",
 "version": "V1",
 "lane": "LANE_HET_LIF_1_TWO_DOMAIN_CORE",
 "owner_issue": 151,
 "secondary_issues": [165, 50],
 "execution_spine": 165,
 "written_utc": "2026-09-10",
 "status": "PROSPECTIVELY_FROZEN_BEFORE_ANY_PROTECTED_RUN",
 "non_final": "EXPLICITLY_NON_FINAL",
 "trigger": "FLAGSHIP_EXPERIMENT_V1.json entry gate 'At least two materially different domains use the same core' = PARTIAL: 'language/mathematics/procedural types registered per #165 section 8; heterogeneous lifetime study not executed (LIF-02 synthetic calibration only)'. ME_LIFETIME_BENCHMARK_V0 remains blocked on N3/N5 protected-instance binding. This freeze registers the MINIMAL executable heterogeneous lifetime study that the registered estate can run TODAY.",

 "question": {
  "central": "does the REGISTERED developmental core — the executive lifecycle, the fragment-mining semantics, the no-slowdown admission rule, persistent state with OS-process restarts, the HDI-14 + P3 cost ledger, and the external CHECKER_C discipline — serve TWO materially different domain families from #165 section 8 within ONE persistent lifetime, with cross-domain N/k measured and every cross-domain index charged?",
  "not_the_question": "this is NOT the amortization test. No DC-10 amortization claim, no lifetime-economics claim, no capability-dominance claim is authorized by this study; those belong to FE-DEV-AMORT-1 which stays FROZEN_DESIGN_NOT_AUTHORIZED. This study closes at most the two-domain core-registration gate and nothing else",
  "scope_of_answer": "the two frozen domain substrates below, the registered core units at their merge-commit identities, the frozen task streams, at the minimal registered n"
 },

 "machine": {
  "core_units_unchanged": {
    "executive_lifecycle": "the registered M1 8-step lifecycle (solve -> mine -> validate on fresh acquisition -> admit with full ledger -> checkpoint -> OS-PROCESS restart with pid evidence -> fresh protected acquisition -> fresh obligations), reused verbatim from the M1/M1B scaffold by import",
    "mining_semantics": "registered fragment mining semantics: support >= 2, top 16, over CHECKED solutions only (the registered learn_generator rule). For domain M the rule is re-instantiated by a research-lane ADAPTER over implication traces with the SAME constants and the SAME support/top semantics — the constants and rule are frozen here, the re-instantiation is an adapter, never a src/ocm edit",
    "admission_rule": "the registered no-slowdown admission rule unchanged: a candidate admits only if it is not slower on EVERY held-out task (validate_generator semantics; the M2-P1 gate analysis stands — the rule is never relaxed)",
    "persistence": "checkpoint to disk + OS-process restart with pid change evidence at every stage boundary, both domains",
    "cost_ledger": "HDI-14 families + E3-P3 physical meters (wall, RSS, I/O, storage) on every serve in BOTH domains; store bytes charged, never converted to slots",
    "checker_discipline": "CHECKER_C as an independent subprocess unit per domain (D1: the frozen polynomial identity checker; D2: the implication-systems exact enumerator), internal replay never counts"
  },
  "domain_serving_paths": {
    "declared": "D1 is served natively by the registered learner src/ocm/learning/methods.py M.solve imported AS-IS; D2 is served by the substrate's own exact forward-chaining search (the B0-D2 conventional system per COMPARATOR_LADDER_FREEZE_V1) driven through a research-lane adapter that feeds the SAME mining/admission/persistence/ledger machinery",
    "honest_registration": "the two domains use different native solvers because no registered solver is generic across both; the claim under test is that the DEVELOPMENTAL CORE (mining/admission/persistence/ledger/checker/lifecycle) is the same unit serving both — a solver-level sameness claim would be false and is not made. If the study finds the core itself must diverge per domain, the terminal CORE_DIVERGENCE_REQUIRED records exactly where"
  },
  "machine_identity": "src/ocm imported at fixed merge-commit shas, recorded in the run manifest; any drift = RECEIPT_BINDING_DEFECT"
 },

 "domains": {
  "D1_formal_language_acquisition": {
   "section8_type": "language family (formal-language acquisition)",
   "substrate": "the sealed M1/M1B polynomial normal-form estate: frozen worlds M1_PARTITIONS_V1.json sha256 a5b0cbc1814042a7b4ddcfbc8658e63c4b352fade22cfdcd91c1968bea13921e, protected set 62f199960eecfb0822bc724be9877cef9aa0fa87695917ead50ab4b1d98b236a",
   "authorship": "registered estate (M1 freeze); E3 clause P1 is satisfied for this substrate by its own registered provenance — independently authored families are required for E3 CONFIRMATORY promotion, which this study is NOT (it is a core-registration study at E2 scope)",
   "streams": "the frozen train/dev streams; protected rows never used as developmental stream (M1 protected_set rule)"
  },
  "D2_formal_reasoning_mathematics": {
   "section8_type": "mathematics family (formal reasoning: closure / reachability / minimum-cost seed set)",
   "substrate": "the P1-E3 independently authored implication-systems families: research/independent-authorship-gate-v1/p1e3_authored/ (10 families / 60 instances; taxonomy-disjointness certified; author unit = FRESH_MODEL_SESSION__MODEL_PROXY)",
   "authorship_label": "HUMAN_GATE_BYPASSED__MODEL_PROXY — carried verbatim from P1E3_AUTHORSHIP_FREEZE_V1; never marked externally obtained",
   "streams": "per-family splits frozen BEFORE any run: every family carries exactly 6 instances in instances.jsonl order (verified 10 families x 6 = 60); per family, instances 1-2 = dev stream (developmental), instance 3 = validation (admission evidence), instances 4-6 = protected (never touched during development); the split is by frozen instance_id ordering inside instances.jsonl, recorded with per-member core digests at build time"
  },
  "material_difference": "different #165 section 8 type registries (language vs mathematics), different task interfaces (normal-form program synthesis vs forward-chaining closure/reachability/min-seed), different independently authored instance sets, disjoint surface vocabularies (taxonomy-disjointness certified for D2)"
 },

 "design": {
  "lifetimes": {
   "count": 4,
   "structure": "2 domain orders (D1->D2, D2->D1) x 2 frozen seeds {20260910, 20260911}; each lifetime is one persistent machine through BOTH domains with stage boundaries and OS restarts inside each domain (>= 2 stages per domain, so >= 4 transitions per lifetime)",
   "minimal_n_declaration": "n=4 lifetimes is the MINIMAL registered heterogeneous demonstration (both orders covered, seed pair fixed); descriptive statistics only; no confirmatory hypothesis test is powered at this n and none is run — any amortization contrast reported is DESCRIPTIVE and labelled as such"
  },
  "arms_per_lifetime": {
   "CONTINUED": "no reset across domains or stages; pays every dev charge; the principal persistent machine",
   "RESET_AT_BOUNDARY": "the same machine, state reset at each stage boundary (the integrated reset ablation)",
   "STRONG_ADAPTIVE_PARENT": "the strongest parent control riding the same streams with the same history/persistence/adaptation rights (M1B parity clause): per domain, its own decision-list applicability machinery outside OCM bookkeeping; comparators evolve too, never a frozen-parent strawman"
  },
  "budgets": {
   "serving_budget_slots": 200000,
   "dev_budget_slots": 200000,
   "dev_charge_rule": "each domain's dev enumeration cost is a MEASURED OUTPUT sealed into the run manifest at first execution (a single fixed ledger entry per domain, charged identically once per run to every arm using that domain's dev history, M1C rule); D1's sealed 14427131-slot dev charge is reused if the dev phase re-executes byte-identically, else re-measured and re-sealed",
   "justification": "200k serving / 200k dev are the registered constants frozen by COMPARATOR_LADDER_FREEZE_V1 (this study inherits them unchanged)"
  },
  "capability_gate": {
   "form": "Performance_CORE >= Performance_parent - delta per domain",
   "delta": 0.2,
   "metric": "CHECKER_C-verified competence per domain at the frozen serving budget",
   "rule": "frozen before any protected outcome; no cross-domain transfer claim survives a materially worse capability reading in the receiving domain"
  },
  "cross_domain_readouts": {
   "same_core_evidence": "machine-identity manifest: the imported core units and their shas identical across both domains within a lifetime; every place the study needed a domain-specific branch is recorded with its location and justification",
   "N_k_bytes": "per #165 section 8: total persistent N by domain and cross-domain, active k per serve, k/N trajectory, persistent/active bytes, cross-domain index entries and their charged maintenance cost",
   "within_domain_next_task_acquisition": "descriptive paired contrast CONTINUED vs RESET_AT_BOUNDARY on next-task acquisition cost per domain (descriptive only, no test)",
   "harmful_transfer_refusal": "count and disposition of cross-domain fragment/gate refusals on HARMFUL_LOOKALIKE and UNRELATED probes (a minimum of 4 lookalike probes per lifetime, 2 per domain, drawn from frozen family members; refusal must be recorded, indiscriminate transfer counted against)",
   "maintenance_charging": "every index, storage and restart cost charged in the ledger in BOTH domains; no uninstrumented state"
  },
  "null_discipline": {
   "no_confirmatory_test": "no hypothesis test is run at n=4 beyond the frozen descriptive contrasts; any future confirmatory version is a NEW registration (no E2->E3 relabeling, evidence-class rule)",
   "selftest": "the runner machinery passes a hostile selftest on toy worlds BEFORE the real run: planted-defect alarm (a tampered frozen identity must halt the run) + no-alarm control (clean toy run passes), both asserted",
   "leakage": "protected splits disjoint by construction; no protected row may appear in any developmental or validation stream (asserted at build time with member digests)"
  }
 },

 "terminals": {
  "set": [
   "SAME_CORE_TWO_DOMAINS_REGISTERED (the registered developmental core served both domains in one lifetime; cross-domain N/k/bytes/index charging recorded; machine identity identical across domains)",
   "CORE_DIVERGENCE_REQUIRED__<where> (a domain-specific change to a core unit was unavoidable; the location is recorded — negative for the gate)",
   "ADAPTER_DOMINATES (the adapters, not the core, carried the cross-domain function; core-sameness is vacuous — negative for the gate)",
   "CAPABILITY_GATE_FAILED (core materially worse than parent in a domain; no transfer claim)",
   "RECEIPT_BINDING_DEFECT (frozen-identity or sealed-input mismatch; halting)",
   "ASSAY_DEFECT (control failure; halting)",
   "CANNOT_CHECK_<reason>"
  ],
  "precedence": "RECEIPT_BINDING_DEFECT > ASSAY_DEFECT > CAPABILITY_GATE_FAILED > ADAPTER_DOMINATES > CORE_DIVERGENCE_REQUIRED__<where> > SAME_CORE_TWO_DOMAINS_REGISTERED > CANNOT_CHECK_<reason>; halting terminals stop the run before any readout is reported",
  "gate_effect": "SAME_CORE_TWO_DOMAINS_REGISTERED is the registered evidence candidate for the flagship entry gate 'At least two materially different domains use the same core'; the gate flips only on an updated #165 evidence sync citing this study's receipts — never on this freeze alone"
 },

 "hosts": {
  "run": "laptop billy (ssh billy-laptop; E3 host per E3_PROTOCOL_FREEZE_V1 P8/P9)",
  "replication_if_run": "billy-old (ssh billy-old) optional E4 rerun; LUNARC batch optional for any scaled arm",
  "never": "Mac mini — git/gh and single-file edits only"
 },

 "forbidden": [
  "any amortization, economics, or DC-10 claim from this study",
  "relaxing the no-slowdown admission rule or moving any threshold/constant frozen here",
  "editing src/ocm or any registered comparator/learner module (adapters live in this research lane only; drift = RECEIPT_BINDING_DEFECT)",
  "relabeling this E2-scope study E3/E4/E5 or promoting it to confirmatory (a confirmatory heterogeneous study is its own prospective registration with independently authored families for BOTH domains under E3 clause P1)",
  "touching protected D2 instances (4-6 per family) during development or validation",
  "running before this freeze is merged and the hostile selftest has passed on the run host"
 ],

 "entry_gates": [
  "this freeze merged to main before any protected run (pre-registration discipline)",
  "COMPARATOR_LADDER_FREEZE_V1.json merged (its cross-rung constants are inherited)",
  "the HET-LIF-1 runner + adapters + hostile selftest merged and green on the run host before the real run",
  "the P1-E3 instance split (dev/validation/protected with member digests) emitted and sha-bound before any serve"
 ],

 "explicitly_not_authorized": [
  "any run before every entry gate holds",
  "any FE-DEV-AMORT-1 protected execution (flagship authorization unchanged by this study)",
  "claiming the two-domain gate SATISFIED from this freeze; only receipts + an #165 evidence sync can do that"
 ]
}
