# PASS-L46 — computational claims with no independent implementation

**Screen (package-level, mechanical):** census computational claim objects (proof mode in
{FINITE_EXECUTABLE_CERTIFICATE, COMPUTER_ASSISTED_EXHAUSTIVE, STATISTICAL_EXPERIMENT,
MECHANIZED_PROOF, EMPIRICAL_EXPERIMENT} x claim-bearing class) joined to package-tree
two-route evidence: independent-oracle files (g0-* pattern) or >=2 executors with an
agreement/cross-check artifact (#863 BFS-vs-Floyd-Warshall, #875 oracle precedents).

**Disposition over 231 packages (91 with computational claims):**

| status | packages |
|---|---|
| TWO_ROUTE | 16 |
| SINGLE_ROUTE (flagged: claims lack independent implementation) | 75 |
| NO_COMPUTATION | 140 |

**Tier-S verification (10 packages, 13.3% of SINGLE_ROUTE + 31% of TWO_ROUTE), rate 100%
agreement except one recorded correction:**
- TWO_ROUTE verified real 4/5 read: adaptive-row-confidence (`visit_oracle_v1.py`),
  weighted-transient-transfer (`transfer_oracle_v1.py`), causal-rung-repair
  (`census_v1.py` raises on "independent intervention/counterfactual disagreement" —
  two internal routes, fail-closed), native-adjoint-repair (`adjoint_reference_v1.py`:
  "Independent arithmetic and exact-polynomial finite differences").
- **Correction (false-positive class recorded):** `gmi-pr551-delta-70c4f676-v1` is a
  custody source-packet mirroring morphogenesis files; its "oracle/agreement" tokens come
  from embedded docs of OTHER packages. Mechanical TWO_ROUTE corrected to
  MIRROR_PACKET (no executors of its own).
- SINGLE_ROUTE verified 5/5 read (capability-predictor, learning-law-selection,
  developmental-uncertainty-transport, current-program-assay, capability-ceilings-f2-tranche3):
  one main executor each, no second route present.

**Registered second_impl axis (rescore v2, arrivals only):** 11 True / 13 False / 173
ABSENT — the 173 legacy rows never registered the axis; the tree screen covers them.

**Identification output:** the L46 population = computational claim objects in the 75
SINGLE_ROUTE packages (+ the corrected mirror packet), enumerated per package in
P3_SCREENS_V1.json. Rescreened-not-adjudicated beyond the Tier-S rate; the package-level
disposition itself is mechanical and fully populated.

**Claim:** every computational package in the corpus now carries a two-route disposition at
branch-base scope; the no-independent-implementation population is identified as
75 SINGLE_ROUTE packages.

## Revival update

REV-L46-TWO-ROUTE-PROGRAMME filed: per-package independent oracles for the 75
SINGLE_ROUTE packages (highest-claim first), + L34 lane retrofits the second_impl axis
for the 173 legacy rows. This sweep supplies the fully-populated population.
