# Exact 116,570,467-candidate census at registered finite scope

## Result

The prospectively frozen direct-execution route completed. Every one of the
`116,570,467` candidates in cumulative `G0-fin-v1` budget `B=(5,1)` was
decoded from its exact stratum rank, structurally validated by construction,
reranked, and evaluated on the protected inputs `(), (0,), (1,)` at step cap
6. The five executed stratum totals were exactly `5`, `121`, `6,859`,
`707,281`, and `115,856,201`.

This is a complete census, so individual and distinct-pair inclusion
probabilities are both exactly one. It is not a 100-million-sized virtual draw,
an estimator, or an extrapolation from #1007.

## CENSUS-100M-1 — direct scale certificate

For label count `n` and one register, the frozen instruction alphabet has
`q_n=1+3n+n^2` elements. Mixed-radix decoding is a bijection from
`[0,q_n^n)` to all ordered `n`-instruction tables. The worker visits every
integer in each interval once and reranks its decoded digits. Chained chunk
receipts report the closed-form count, sum, and square-sum of every consecutive
rank interval; their five-stratum total is `116,570,467 > 10^8`.

The accepted run comprised 120 atomic chunks, completed in 3.246649 seconds,
used 60,227,584-byte peak RSS, and retained 821,744 bytes of run artifacts.
The terminal receipt chain is
`e4ca71a4ae00ab72e14c8bb0b003dda940c0d22b5a3f54fced0d9095deb7fb2b`.
A second run from an empty directory completed in 3.368089 seconds and matched
all seven registered scientific custody fields exactly, including the terminal
chain, worker transcript, class count, multiplicity square-sum, and complete
histogram hash.

## SEM-CENSUS-1 — complete protected-semantic coverage

The cumulative census has exactly 973 protected semantic classes at the frozen
interface. Thus 116,569,494 presentations collapse and the exact collapse rate
is `116569494/116570467`. The exact sum of squared class multiplicities is
`3,429,058,444,270,403`, giving inverse-Simpson effective semantic count

```text
13,588,673,776,598,089 / 3,429,058,444,270,403
  = 3.9628002839389738...
```

There are zero singleton and zero doubleton classes in the complete census.
The committed 973-row histogram is bound by SHA-256
`c7713a47167c7139005c025ab0d1674d2eeedb6e0c8832d457b3600b09ebd2dd`.
“Complete” here is interface-, grammar-, and budget-relative; it is not
completeness of an unbounded semantic universe.

## Independent route and hostile controls

A separately written Python codec and interpreter, importing neither the C
worker nor its Python orchestrator, exhaustively reproduced all semantic
histograms for `n=1..4` (714,266 candidates total). It agrees on 4, 18, 92,
and 391 classes and their complete histogram hashes. At full scope it also
independently verifies every closed-form stratum count, rank sum, and rank
square sum, plus the exact effective-count arithmetic. This is deliberately
described as a complete small-scope semantic oracle and full-scope structural
check, not as a second 116-million semantic run.

Hostiles cover skipped/duplicated ranks, invalid intervals, instruction-order
changes, duplicate histogram keys, receipt-chain/count/histogram tampering,
wrong effective denominators, protocol drift, and forbidden claim promotion.

## Claim boundary

The earned claim is only
`GMI_833_116570467_CANDIDATE_EXACT_CENSUS_AT_REGISTERED_FINITE_SCOPE`.
It does not establish unbounded/all-machine/all-semantic coverage, 10^8
execution at `B=(8,4)`, the alternative effective-coverage route, an unbiased
or unique grammar, absolute architecture freedom, grammar neutrality, physical
randomness, QD/open-ended search, family recovery, capability, real-scale
validation, or complete GMI. #220/#221 remain authoritative and untouched.
