# GMI #926 cognitive-function re-audit v2 freeze

Parent: #833 Section M. Frozen main:
`e71ddcf8ad5e924df3d381483cdac3b3ba5307f4`.

## Exact closure target

This tranche targets exactly three unchecked rows:

- re-audit hierarchical skills/chunking;
- re-audit planning and stopping;
- re-audit causal cognition/intervention/counterfactuals.

It does not target metacognition, social cognition, communication, imitation,
teaching, culture, empirical validation, or any comment/addendum row.

## Frozen parent custody

- foundation result:
  `c0c574c4ec6e237d5fdafa694eac131399625a70`;
- axiom-core result:
  `3366a3bc7236d286f8d123bf53e4e3b2d22ad7b9`;
- charged hierarchy core:
  `057ba5b705751d6fc2c6fc5f7571d78664a389a6`;
- hierarchy executable:
  `34266004877277de3e7c1faa377a462b383a4f56`;
- planning/stopping core:
  `42d5531cf2be37e2c08417c6c60a2b7621486308`;
- planning/stopping executable:
  `7219b55a64d1baf5d9fe03a9b40f5c0bceec00b7`;
- causal-rung repair core:
  `8fca71ecdca2dec265460b8f27560f1957d779a5`;
- causal-rung executable:
  `f0f80cd3c5377046d58b32019b69a9f92606d64b`.

## Frozen audit conclusions to test

1. **Hierarchy is a charged reuse policy, not an anatomical or universal-depth
   primitive.** Exact finite string chunking is shortest path over legal
   dictionary calls. Exact finite acyclic library acquisition is min-plus
   dynamic programming over retained sets. A chunk/higher skill is supported
   only when its complete acquisition, validation, storage, lookup, transport,
   and execution charges beat the flat alternative on the registered demand.
   Greedy parsing and universal shallow/deep claims require hostile failures.
2. **A goal and model are inputs to planning; stopping is a Bellman comparison.**
   On a finite acyclic computation graph, stopping is optimal exactly when the
   best registered terminal action value is at least every cost-charged
   continuation value computed with future optimal computation included.
   A merely myopic one-step EVC rule is not generally sufficient and must be
   retained only at its proved scope. Ties remain sets; a missing goal/model or
   cyclic unbounded computation graph cannot be silently completed.
3. **Causal answers are fiber-relative.** For admitted models, evidence
   identifies a target exactly iff it is constant on the compatible-model
   fiber. A finite exact model list gives a terminating identified / partially
   identified / incompatible procedure and attaining endpoint witnesses.
   Observational equivalence can preserve different intervention or
   counterfactual targets, requiring abstention or bounds. The audit does not
   infer a causal graph/model class from data or promote population identities
   to finite-sample estimation.

## Required hostile controls

- a longest-match greedy chunking failure and a workload where hierarchy loses
  after complete charges;
- a planning instance where myopic one-step EVC stops but a two-step
  information plan has positive net value, plus a tie-preserving control;
- observation-equivalent causal models with different intervention and/or
  counterfactual targets, an incompatible evidence case, and an identified
  constant-on-fiber control.

## Frozen claim boundary

The result is red if it erases first-use or descendant charges, treats a named
option/macro/planner/causal algorithm as a primitive, derives goals from value-
free dynamics, silently breaks optimal ties, equates one-step EVC with general
optimal stopping, reports a point outside a singleton causal target fiber,
accepts parent drift, or uses floating-point decisions.

Allowed terminal only after analytic proof, hostile twins, exact normal and
optimized replay, and narrow reconciliation:

`GMI_833_HIERARCHY_PLANNING_CAUSAL_REAUDIT_AT_REGISTERED_EXACT_SCOPE`

Forbidden promotions include `UNIVERSAL_HIERARCHY_DEPTH`,
`GOALS_DERIVED_FROM_DYNAMICS`, `MYOPIC_EVC_IS_UNIVERSALLY_OPTIMAL`,
`OBSERVATION_IDENTIFIES_CAUSATION`, `GENERAL_CAUSAL_DISCOVERY_SOLVED`,
`EMPIRICAL_COGNITIVE_VALIDATION`, and `COMPLETE_GMI`.
