# Qualification readout

The fixed exposed native control selected PROGRAM body(1,0) from the existing
32-body grammar after feedback(1,16),(2,0). Its acquisition ledger is
desc35/upd419, with every charge retained. These are native-model coordinates,
not host time, energy or a new method-construction cost.

The acquired, sham and restored pre-query joint states are identical. All16
served answers and complete post-query states/native costs agree. Initial-body
replacement changes the eight odd-input answers. The zero-label experience
control selects(0,0). Acquired serving costs exec16 under the declared B0 meter.

All six explicitV2 calls return complete records. Their distinct final-vector
count is3; this is an apparatus observation, not a learning/architecture verdict.
The receipt retains116471 native charge writes for these calls, plus4 for the
interrupted feedback call. The interruption retains desc25/upd1 and returns no
answer. Its family aggregate remains unresolved.

The original historical main function labels substituted incomplete evidence
decided. The successor refuses it and accepts the complete-record control.
No historical runner/campaign or existing source file was modified.

17 focused tests pass normally and optimized. The original fault-at-write3
coverage failure and its complete source-bound record remain retained. Its
correction to write4 changes only where the explicit fault is injected.

See VALIDATION_V1.json for exact runtime/source/payload and executed commands.
No protected transfer, population capability gain or full-lifetime claim follows.
