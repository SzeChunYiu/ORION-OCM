# Exposed current PROGRAM assay — fixed protocol

Status: apparatus qualification, frozen before these native control calls.
Source boundary: df777cd79ba5ec6925fd82552e9341f8d73f3577; see SOURCE_BINDINGS_V1.json.
No historical campaign, genotype search or candidate tuning is scheduled.

1. Compile the seven archived native dependency modules from verified source bytes.
   Use the unchanged corrected VM and B0 native cost ledger.
2. Construct existing zoo.program_search(grammar=1, budget=32), seed0.
   This selects among32 predeclared XOR-linear bodies; no new grammar/method is built.
3. Initialize once. Retain complete typed object graph. Give exactly
   (input1, target16), then (input2, target0), calling end_event after each.
   Retain the full ledger and joint object graph after each event.
4. At the quiescent acquired state, clone the entire VM/Machine/Basis/Ledger graph.
   Four arms start with identical acquired state and cost history:
   acquired (untouched), initial-body (assign grammar.initial()),
   sham (assign acquired body), restored (assign initial then acquired body).
   Only PROGRAM state changes; stores, genotype, RNG and ledgers remain cloned.
   The disabled body is valid, not a dangling edge, exception or missing node.
5. Serve inputs0..15 once per arm, no further feedback/revocation/search.
   Retain every output, abstention flag and native ledger increment.
   Compare complete outputs/ledgers for acquired, sham and restored.
   Initial-body is a method-state intervention, not a claim of biological forgetting.
6. Separately repeat the two acquisition input events with labels0,0.
   This exposed experience control has the same event count; differing incurred
   computation remains charged. Query once on0..15. Do not reacquire per arm.
7. Qualify actual parent ecology.run_genotype on existing E_smooth3 with the same
   legal program_search, explicitly all six INTERVENTION_FAMILY_V2 members.
   Retain all native charge writes, all exposed VM call arguments/results/errors,
   full returned traces/resources and final machine/program states for every call.
   This separate assay contains the parent protocol's registered revocation.
8. Run one predeclared fault injection after the third native ledger write.
   Retain the incurred write before raising, complete prior/failed call intervals,
   error and partial machine state. No failed call counts as an answer.
9. Execute exact historical packet main/verdict source functions with substituted
   complete/incomplete measurement records and an isolated temporary output path.
   No native candidates or campaign are called by this legacy aggregation control.

All inputs, target choices and tests are exposed to the authors. There is no
protected holdout, prospective random ecology population or new-obligation transfer.
Input1/2 already occur in acquisition; remaining query inputs are same-task
untrained inputs, not an independently protected test. The stock TRAIN/UNSEEN
parity split does not distinguish XOR masks m and m xor15 on its even TRAIN set.
The V2 scored-input check is a runtime no-leak check for its stated unseen criterion,
not a general theorem that verification/testing cannot inform a developer.

External cloning, serialization, observation and body assignment use host Python;
their physical/work cost is unmeasured and no native zero-cost setter is claimed.
Native ledgers measure only the supplied Machine contract. Acquisition is paid
once in the source run; copied historical charges are not new executions. Report
branch-serving deltas separately. No lifetime benefit, capability gain,
architecture selection or general learning verdict is licensed.
