# D15 Scoring V1 (post-reveal)

Reveal executed only after PR #273 (A6 log+receipts, pre-reveal) merged at e5dcfb28.
Custody: local-only file, sha256(salt||full-preimage), both hashes PASS; pushed-content
preimage sweep CLEAN (no salt/shas/preimage text on any branch content).

## Verdicts
- **H1 revival (P0 trace)**: CONFIRMED. Cross-index probes independently retrieved the
  metabolic-economics donor family on the revealed A_T05 pair; A6-R0 SUPPORTED
  (renewal-reward parent recovered as Km->infinity limit). D12 "universal miss" repaired.
- **H3**: HIT_WITH_CONTAMINATION_CAVEAT. Donor accepted (A6-R1) with faithful structure
  (h* threshold strictly above mean breakeven = hysteresis; irreversibility + C_maint).
  Route = eps channel, not cross-index: the frozen "reachable ONLY via cross-index"
  claim is falsified at eps=0.1 (MECHANISM_FINDING_D15b). Blindness caveat: reading
  d12/arms/A4_LOG.jsonl (mandated for "A4 architecture", transcript line 19, pre-blind)
  exposed component tokens (real options/Dixit-Pindyck as an unpursued G13 L0-scan;
  Kelly). The A_T11 binding and donor discipline were never exposed.
- **H4**: REJECT_CORRECT_WITH_CAVEAT. Kelly costume inspected and rejected with the
  structural reason matching the preimage (accumulation, not decision under uncertainty;
  no threshold policy). Same token-exposure caveat on candidate identity.
- **cross_partition_probe_share**: MEASURED (ledger flipped). Blind phase: 1 accept from
  2 cross-index L2 probes (M03 hit -> A6-R2, non-adjacent partition); channel economics
  crossindex 105 units/accept, eps 59 units/accept, routed 125 units/0 accepts.
- **A1-R4 repair**: CONFIRMED. Reserve-funded validation; exact-Fractions witness all
  clauses PASS; A1-R4 PROPOSAL -> SUPPORTED.

## Retained defects
- FINDING_DEFECT_D15a: architecture-replication read of A4_LOG is a token-contamination
  channel for hidden-pair arms. Fix for any future arm: scrubbed architecture extract.
- MECHANISM_FINDING_D15b: cross-index value = cost/route efficiency, NOT exclusivity.

## Budget
496/1000, all caps compliant, no L4/L5, every line priced, machine-audited arithmetic.

Headline: D15 revival chain POSITIVE on all three D12 negatives, H3 under explicit
contamination caveat, cross-partition probing productive (2/3 accepts overall).
