# R5 — G15: the ratchet read along orbits

Row: G15 (archive/ratchet), rung R5 (ladder question: ratchet along orbits).

## Verdict (first sentence)

LIFT_SURVIVES — T07's statement read at R5 holds verbatim along every orbit: for the
immutable archive driven by any trajectory Σ_0, Σ_1, … (deterministic OR kernel-random),
the running max of the frozen order's best-so-far admissible object is monotone
non-decreasing pathwise, because it is the running maximum of a functional evaluated
along a sequence — pathwise monotonicity needs no independence, no kernel structure, and
no metric.

## Scope note (what survives and what does not)

Survives: the ORDER-ratchet (T07's own claim) along all orbits — the strongest free
statement in this capsule. Does NOT survive to hypervolume: the HV functional is not the
frozen total order, and under capacity it fails (hostile H4, R4 file). Under unlimited
capacity HV is inclusion-monotone (parent-owned), so along orbits with monotone archive
growth HV also ratchets — but that is the R4 parent statement applied to a path, not an
R5 finding; recorded to prevent double-claiming.

## Assumption passes (A3)

- Remove ix-elitism: pathwise monotonicity of the running max needs elitism (retention);
  without it even the order-ratchet fails — a fortiori FAILS, same pass as T07's R0
  conditional form, not re-derived.
