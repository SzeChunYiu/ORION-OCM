# Extra ordinary-cut negatives (replay-02 tail)

**Terminal:** `EXTRA_NEGATIVES_NATIVE_VERIFIED_LIBRARY_ENLARGED`

Completing P1 matching (#180 replay-02) added eight unique
`SCREENED_NEGATIVE_IN_DOMAIN` cuts that the 180 s replay-01 control left at
`UNKNOWN_RESOURCE`. Native admission #179 only compiled the 22 unique bodies
from that truncated run.

Those eight compile as ordinary `$p` from the frozen RAW `CUT_PROPOSAL`
bodies and `proposal.proof`, the same way #179 did. **8/8 native-verify** on
the pinned custodian prefix through `pssdif`. All eight are two-semantic-step
P1 compositions. None is an exact named-P1 statement alias. One statement
duplicates already-admitted `cut-lemma-21`. One new zero-premise lemma
enlarges the bank for a later 2-decision search.

This is not `CAUSAL_METHOD_REUSE_SUPPORTED`. Do not check #165 G2.4 from
this capsule.

[Result](RESULT.md) · [summary](SUMMARY.json) · [admit](records/admit-01/ADMIT.json)
