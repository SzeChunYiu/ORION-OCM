# Grand GMI Compositional Language Claims V1

Status date: 2026-09-12. Uses `CLM-*` identifiers independently of the concurrently advancing master `GG*` ledger.

| ID | Claim | Status | Scope |
|---|---|---|---|
| CLM-1 | For a product semantic space and a declared semantic-role partition `pi`, the minimum role/block lexical associations of an exact block-separable code are `A(pi)=sum_B prod_{i in B} n_i`, with one transmitted block token per block. | THEOREM | finite exact block-separable code class |
| CLM-2 | With lexical price `lambda` and transmission price `c`, the exact resource-optimal partition minimizes `lambda A(pi)+c|pi|`. | THEOREM | declared scalar resource model |
| CLM-3 | As `c/lambda` increases, the block count of every optimal partition can only decrease; optimal code morphology moves monotonically toward larger chunks/holism. | THEOREM + 2,688 EXACT SWEEPS | finite product semantics |
| CLM-4 | Once every required block value has a learned token, block-separable codes productively generate their Cartesian recombinations without a full-tuple entry. | THEOREM | lookup-per-block construction |
| CLM-5 | For Cartesian-product leave-one-out training with all factor sizes >=2, every nonholistic partition recovers every held-out block value from remaining examples; a holistic lookup lacks the held-out full-tuple entry. | THEOREM + 94,851 EXACT CHECKS | k=2..4, sizes 2..4 exhaustive microscope |
| CLM-6 | Partial compositionality can be a strict resource-optimal phase between full composition and holism. | EXACT WITNESS | (3,3,3) meanings, unit lexical price |

## Aggregate terminal

`GRAND_GMI_COMPOSITIONAL_LANGUAGE_TRANCHE_ALL_GREEN`.

## Claim ceiling

The theorem derives resource-conditioned coding morphology **inside the declared block-separable code family**. It does not claim all natural or emergent languages are product codes, that compositionality universally improves generalization, or that the scalar resource prices are universal physical constants.