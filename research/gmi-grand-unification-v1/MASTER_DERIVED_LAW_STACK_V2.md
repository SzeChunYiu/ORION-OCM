# Grand GMI Master Derived-Law Stack V2

Status: **ADDITIVE MASTER INTEGRATION; NO NEW PRIMITIVES**  
Date: 2026-09-12  
Predecessor authority: `GRAND_GMI_MASTER_THEORY_V1.md` + `MASTER_CLOSURE_LEDGER_V1.md`.

## 0. Primitive tuple unchanged

Grand GMI continues to require only

\[
\boxed{
\mathfrak G=(\mathbf P,\mathcal B,\mathcal E,\Omega,\Theta,\rho,\mathcal D,\varepsilon).
}
\]

The five quantitative tranches integrated here add **derived laws**, not ontology:

- epistemic acquisition `eta_Omega`;
- compositional-language partition morphology;
- planning semantic resolution;
- robust decision precision;
- generalization identifiability radius `R_gen`.

Each is constructed entirely from the master tuple and earlier semantic/cut/transformation objects.

---

## 1. Extended derivation chain

The V2 quantitative chain is

\[
\boxed{
\begin{aligned}
(\mathbf P,\mathcal B,\mathcal E,\Omega,\Theta,\rho,\mathcal D,\varepsilon)
&\longrightarrow (S^*,d_Q)\\
&\longrightarrow R_{gen}\\
&\longrightarrow \eta_\Omega\\
&\longrightarrow (\kappa,\tau)\\
&\longrightarrow \text{decision precision gates}\\
&\longrightarrow Reach_{\mathcal D}\\
&\longrightarrow (\mathcal F,\mathcal F^{reach},\text{realization fibers}).
\end{aligned}
}
\]

Specialized semantic/process structure then yields closed-form morphology laws such as compositional code partitions and planning-search regimes.

This ordering is conceptual rather than a mandatory execution order: some tasks have no learning/acquisition phase, and some `kappa/tau` bounds can be derived before an explicit `R_gen` calculation. The dependency claims below are the authoritative content.

---

## 2. Generalization radius is derived, not primitive

Given a protected evidence map `D(e)` induced by boundary/probes/development and a held-out obligation target `T(e)`, define

\[
R_{gen}
=
\sup_{z\in D(\mathcal E)}
\inf_y\sup_{e:D(e)=z} d_Y(y,T(e)).
\]

All symbols are supplied by the master tuple plus the declared target metric/tolerance already permitted by `epsilon`/response geometry.

Thus:

\[
\boxed{R_{gen}=\mathsf{Gen}(\mathcal E,\Omega,\Theta,\mathcal B,\mathcal D,\varepsilon).}
\]

No new ontological category is introduced.

**Dependency theorem DG-1.** Exact/prospective generalization is a property of the ecology/evidence/obligation fiber geometry, not an additional machine primitive.

---

## 3. Epistemic acquisition is transformation/development on ecology uncertainty

For finite deterministic experiments, `eta_Omega` is the minimum worst-case intervention resource required to refine the compatible ecology fiber until the obligation target is identified.

It uses only:

- ecology `E`;
- intervention family `Theta`;
- obligation-induced world quotient;
- resource meter `rho`;
- development/control policy over experiments.

Therefore

\[
\boxed{\eta_\Omega\subseteq (\tau,\mathcal D,\Theta,\rho)}
\]

as a named specialization: it is transformation complexity whose purpose is to acquire semantic distinctions.

**Dependency theorem DG-2.** Active learning, exploration and task-sufficient causal discovery require no new primitive beyond recursive development plus intervention channels.

---

## 4. Decision precision is an admissibility gate on resources

Given nonempty finite actions and the corrected finite robust-score assumptions,
a finite certified loss-field error `delta` yields robust regret at most
`2 delta` **when the machine selects an estimated minimizer**. A certified
`alpha`-suboptimal selector instead gives `2 delta + alpha`; its selection cost
remains charged. All-infinite robust scores cannot be subtracted to define
regret; the qualified finite-optimum extension is in the decision-precision theorem.

Thus tolerance `epsilon` or exact-action margin `Delta` induces an admissibility constraint

\[
2\delta\le\varepsilon
\quad\text{or}\quad
2\delta<\Delta.
\]

The precision certificate is supplied by the physical/estimation realization and charged in `rho`.

Hence

\[
\boxed{
\text{decision precision}
=\text{constraint on }(\rho,\varepsilon,\Omega,\mathcal E),
}
\]

not a new kind of intelligence state.

**Dependency theorem DG-3.** Numerical precision, sensor accuracy, quantization fidelity and estimator confidence enter Grand GMI as resource-conditioned adequacy constraints on existing transformations.

---

## 5. Language morphology is a semantic-cut specialization

When exact semantic state factorizes as a product of obligation-relevant roles, a block-separable communication protocol corresponds to a partition `pi` of those roles.

The exact resource profile

\[
A(\pi)=\sum_{B\in\pi}\prod_{i\in B}n_i,
\qquad L(\pi)=|\pi|
\]

is determined from:

- semantic factorization in `S*`;
- an internal/distributed semantic cut `kappa`;
- lexical/storage/transmission coordinates in `rho`.

Thus

\[
\boxed{
\text{language partition morphology}
=\mathsf{Morphology}(S^*,\kappa,\rho).
}
\]

**Dependency theorem DG-4.** Holistic, partially compositional and fully compositional codes are resource phases of the same cut problem under the declared block-separable code class, not primitive cognitive modules.

---

## 6. Planning resolution is obligation quotient + transformation complexity

In the leaf-verifier regime, the required plan prefix induces a quotient of successful trajectories; exact query cost follows from epistemic acquisition of that quotient.

With transition-congruent semantic state, planning instead reduces to dynamic programming on `S*`.

Therefore

\[
\boxed{
\text{planning regime}
=\mathsf{Plan}(S^*,\Omega,\Theta,\eta_\Omega,\tau,\rho).
}
\]

**Dependency theorem DG-5.** Search, planning and deliberation require no planning-specific ontology. Their complexity changes when the obligation resolution or process information interface changes.

---

## 7. Five-way closure diagram

The new laws compose as follows:

```text
                         admitted ecology E
                               |
                         evidence / probes D
                               |
                    +----------+-----------+
                    |                      |
              R_gen(E,D,T)          semantic state S*
                    |                      |
       unresolved target ambiguity       | factorization / transitions
                    |                      |
          active experiments eta          +------ semantic cuts kappa
                    |                      |             |
                    +----------+-----------+             |
                               |                         |
                        transformations tau              |
                               |                         |
                 decision precision / tolerance          |
                               |                         |
                  +------------+-------------+-----------+
                  |                          |
          planning/search regime       code/language morphology
                  |                          |
                  +------------+-------------+
                               |
                       physical resources rho
                               |
                 reachable physical Pareto frontier
```

The diagram is not an architectural pipeline. It is a dependency graph of derived mathematical objects.

---

## 8. Quantitative law closure achieved since Master V1

Master V1 listed richer language, planning and causal discovery among the next application programmes. Those entries now have nontrivial exact sectors:

### Language

`COMPOSITIONAL_LANGUAGE_MORPHOLOGY_THEOREM_V1.md`

- exact partition-lattice lexicon/message profiles;
- monotone chunking phase law;
- productive recombination theorem;
- 43,008 phase cells and 94,851 recombination cases exact.

### Planning

`PLANNING_SEMANTIC_RESOLUTION_THEOREM_V1.md`

- exact semantic-class verifier query law;
- plan-prefix search law `b^d-b^(d-q)`;
- exponential information/computation separation witness;
- quotient dynamic-programming theorem.

### Causal discovery / active learning

`EPISTEMIC_ACQUISITION_THEOREM_V1.md`

- exact obligation-relative adaptive experiment recurrence;
- semantic identification can be strictly cheaper than full accessible-world identification;
- 65,536 obligation-labelled finite cases exhausted.

### Approximate decisions

`ROBUST_DECISION_PRECISION_THEOREM_V1.md`

- sharp `2 delta` robust regret law;
- exact decision stability for `Delta>2 delta`;
- tolerance-to-precision morphology gate;
- 2,985,984 exact perturbation cases.

### Generalization

`GENERALIZATION_IDENTIFIABILITY_RADIUS_THEOREM_V1.md`

- exact minimax observation-fiber target radius;
- local no-free-extrapolation theorem;
- prospective prediction gate;
- 37,422/37,422 refinement checks.

---

## 9. What remains irreducible after V2 integration

The new laws do not erase the master boundary. Remaining work falls into four types.

### A. Parent-domain instantiation

Derive sharper `kappa/tau/rho/R_gen/eta` expressions for particular stochastic, continuous, quantum, biological, theorem-proving and modern learning ecologies.

These are applications of the ontology unless they expose a phenomenon that cannot be represented by the current process objects.

### B. Physical measurement

Measure real substrate resource maps `rho` under the actual obligations rather than substituting external constants when morphology claims depend on them.

### C. Prospective discovery

Use the new `R_gen` gate plus the existing preregistration protocol to freeze an operationally identifiable but currently unoccupied region before targeted realization search.

### D. Independent falsification

Replication by independent authors/implementations and modern-scale systems remains scientific evidence, not something formal closure can manufacture.

Unrestricted total exact computation remains impossible by the halting boundary.

---

## 10. V2 master conclusion

The recursive programme since Master V1 has strengthened the law layer without enlarging the primitive ontology.

The unified statement is now:

\[
\boxed{
\text{Intelligence morphology is the physically reachable resource frontier of processes that}
\atop
\text{acquire, preserve and transform exactly the distinctions required by an obligation,}
\atop
\text{to the declared tolerance, under the admitted ecology and process laws.}
}
\]

Generalization determines which target distinctions are inferable. Epistemic acquisition determines what must be measured to resolve the remaining ambiguity. Semantic cuts determine what must cross boundaries. Transformation complexity determines what must be computed locally. Decision precision determines how accurately. Development determines what can be reached. Physical resource laws determine which realizations survive the frontier.

No architecture-specific primitive appears in that chain.
