# Structural threshold bound: current authority

The current proof is [STR-1–5](STRUCTURAL_THRESHOLD_ANALYTIC_CORRECTION_V1.md).
For exact parity on all eight inputs, two faithful flat-linear threshold
source shapes have minimum 39 candidate-frame opcodes per call, or 312 per
sweep, under the declared opcode-layout contract. The shared-sum construction
attains it; the registered XOR construction uses 11 per call. This is one
code grammar and one resource coordinate, not a universal neural comparison.

The all-coefficient proof uses at least three active hidden predicates and
at least six active input incidences. It does not infer arbitrary-coefficient
coverage from a finite grid. The previous output grid omitted a legal
four-input threshold function, and fixed variable order was not always the
cheapest rendering. The direct geometric/dependency proof repairs the bound.

The previous universal exclusion under expanded delegation is withdrawn.
Two opaque wrappers can have identical candidate-frame overhead while their
callees differ. No full callee-work, timing, memory or physical-cost ordering
follows. Skip connections also change the admitted architecture.

The complete [original records and repair unit](../gmi-structural-threshold-repair-v1/CORE.md)
preserve the earlier theorem, checker, test, ledger and receipt byte for byte.
The V1 receipt remains historical; V2 replays the full corrected payload.
Native opcode-layout validation is distinct from authenticating a historical
interpreter or claiming every future Python release has that layout.
