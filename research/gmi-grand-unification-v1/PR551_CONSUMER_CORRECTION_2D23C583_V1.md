# PR #551: current consumer and inner-search interpretation

Reviewed head: `2d23c5834d1eff8555761273a5952c9f35191688`.
This current assessment preserves the [earlier field and campaign review](PR551_SCIENTIFIC_REVIEW_F359A957_V1.md)
and the [source-bound 7c516293 consumer review](../gmi-b6-consumer-census-v1/PR551_CONSUMER_REVIEW_7C516293_V1.md).
The later head adds 32 freeze-prose lines; the scanner, native sources and ten
arm records used in that frozen unit are unchanged. No recorded result or
registration is rewritten, and this review supplies no new empirical run.

## Accepted evidence and exact scope

The portable [consumer census](../gmi-b6-consumer-census-v1/CORE.md) repairs the
native primitive/port classifier. Its original 17-row cohort contains six
LINEAR/AFFINE parameter-port adjacencies; four AFFINE rows were missed by the
DOT/LINEAR whitelist. Three of the six numeric nodes are on syntactic OUTPUT
ancestry. These statements concern graph structure, not operative coefficient
learning or causal contribution to the output.

The 70 exported arm rows contain 63 distinct genotype strings and no GRAD;
they do not expose all 800 reported final archive cells or the full search.
Five retained source archives contain eight GRAD-bearing graphs among 350
cells, with recorded seed-fingerprint matches. This contradicts broader
campaign-wide absence, without proving a successful gradient learner. The
absent tenth selected-source archive remains unverifiable. Exact serialization
deduplication and fingerprint matching do not authenticate independent runs.

The S1 raw/pruned graph strings and six capabilities equal the previously
retained witness. Its [existing consumer proof](../gmi-witness-mechanism-v1/CONSUMER_SEMANTICS_V1.md)
already covers the declared finite query/feedback/revoke event sequences.
The constructive DENSE removal preserves that behavioral projection; code,
state and resource costs differ. This scoped positive and the incomplete,
leak-disclosed campaign are both retained.

## The new ledger retraction is warranted

The [new appendix](https://github.com/SzeChunYiu/ORION-OCM/blob/2d23c5834d1eff8555761273a5952c9f35191688/research/machine-intelligence-morphogenesis-v1/GMI_B6_DEVELOPMENTAL_MORPHOGENESIS_RV_377_180_FREEZE.md#L746)
withdraws the conjecture that the packet ledger misses SEARCH's inner candidate
work. The native [SEARCH loop](https://github.com/SzeChunYiu/ORION-OCM/blob/2d23c5834d1eff8555761273a5952c9f35191688/research/machine-intelligence-morphogenesis-v1/gmi_microscope/vm.py#L310)
executes each scored candidate through `gr.execute(M, ...)` and calls `M.op`
for its error arithmetic. [Machine.op](https://github.com/SzeChunYiu/ORION-OCM/blob/2d23c5834d1eff8555761273a5952c9f35191688/research/machine-intelligence-morphogenesis-v1/gmi_microscope/core.py#L164)
charges native activations in the current phase; emulated scoring arithmetic
executes macros whose native operations are charged. The blanket inner-search omission claim
is therefore unsupported. This is an instrument-level accounting statement,
not proof that the abstract ledger measures all host work, energy or elapsed time.

The retained distinction is useful: counting genotype evaluations does not
specify the work performed by each evaluation. However, the parameter ratio
`2401/16` alone establishes neither roughly 150 times measured `upd` work nor
a universal bound on evaluation-count/compute error. SEARCH is guarded, is
skipped without feedback, considers a finite grammar prefix and iterates over
stored evidence. Candidate operation counts and all other evaluation work also
matter. An empty evidence store, for example, executes no candidate-scoring
operations even if the outer prefix length changes.

The prose addition supplies no bound event/work trace identifying the cause
of the reported unfinished E_twin2 source. Retained SEARCH survivors and node
eligibility cannot distinguish proposal, typing, evaluation, placement and
selection. Preserve the reported stall as an observation; its causal diagnosis
and workload ratio require the corresponding records. The ledger retraction
does not settle those separate claims.

## Replay authority

The [full census payload](../gmi-b6-consumer-census-v1/CONSUMER_CENSUS_RECEIPT_V1.json)
is reproduced with 437 independent graph-role/ancestry comparisons. The
capsule binds the complete immutable unit, including all 30 archived source
and raw members, and retains every original output field. Twenty-two focused
controls pass normally and with optimized Python, including the real AFFINE
false negative, missing-source abstention, and a self-consistent manifest
rewrite during a mocked worker. Static evidence validation cannot establish
historical host identity, complete ancestry, current-bar adequacy, or a new
universal theory result.
