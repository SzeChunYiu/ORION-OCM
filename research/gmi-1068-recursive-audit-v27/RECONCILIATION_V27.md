# V27 reconciliation

## Sole original requirement

R1-009 asks for translation to parent process formalisms. The bounded parent set
is the seven exact rows of AJ1 PARENT_COMPARISON.json. Its original names, parents,
native/additional roles and statuses remain unchanged; the new ledger adds explicit
maps, observations, assumptions and information boundaries. Ledger structure alone
cannot certify the truth of those added statements.

This successor closes only that original requirement using the actual registered science.
Every other original record, repair history and whole-round status is preserved.
The predecessor is V26 snapshot SHA256
9fe6b0e3c4ab2d0de34a3af453191d6e0fa99df61adb1332deb8c52b8fae74d6.
Observed main and audited base both bind the verified V26 merge
f2516e9740b9e0fe1bb369d69aa4b551b1f9ad3d.

## Evidence and limits

Actual V11 free paths connect relation-LTS generators to preserved and lifted
literal target paths. Forward edge preservation and successor lifting are checked
separately. Finite trace equality alone does not recover branching.
Whole coalgebra systems are different objects from their execution states. Faithful
underlying-set forgetting may collapse structured objects and revive failed joins;
retaining structured object labels repairs that declared response observer.

Arbitrary Rel permits an input without an output; actual TotalRel forbids that
case while admitting empty input sets. Finite normalized matrices remain a declared
specialization. Subnormalized events retain exact coefficients; labelled tests
retain zero events and nested outcome pairs. Reassociation of outcome pairs is
explicit, with no global strict test-category or general ancillary-equivalence claim.
The ordered-weight kernel retains V14's original premises and its declared extension;
exact Fraction execution does not silently supply a generic Real/Rat Lean instance.

Task translation preserves external interface labels. Recomputing exact ranges
can break strong partial associativity; the repair keeps declared interfaces and
composes through explicit inclusions. Physical possibility and feasibility of those
inclusions remain supplied premises. Possible composites need not have possible factors.
Standard enriched-process interpretation remains parent-owned paper evidence.

Independent finite tests use state walks, complete lift sets, trajectory products
and raw relation pairs. All known corpus counts are actual loop increments; named
controls and hostiles are separate. Complete input validation and coherent semantic
mutations have actual positive baselines. Current source custody expands inherited
receipts and freezes both preregistration files before any new implementation.

## Accounting and integrity

Adjudication is34 original requirements fulfilled,188 original unresolved.
Two unchanged qualified replacements give186 active unresolved, without closing
original R2-003 or R2-007. Eight fulfilled governance plus26 scientific originals
do not imply a whole-round result: R1 stays OPEN and R3 stays STALE; only R0 is earned.

The V22 provenance repair remains enforced: no extra/missing top-level snapshot
fields, Boolean/float aliases or changed issue/ref metadata can pass a rehashed
snapshot. Receipt and snapshot scopes cannot authorize each other through coupled
edits. Final pins, actual receipt and prewrite validation are mandatory; unavailable
proof prerequisites cannot be converted into positive authority.

Lean witness locators use the names written at their source declarations; the
independent typed proof registry retains their fully qualified names. Prewrite
validation caught and corrected that locator distinction before snapshot creation.
