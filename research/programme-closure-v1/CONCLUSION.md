# ORION-OCM programme conclusion — #165

**Terminal:** `PARTIAL_SIGNATURE_ONLY_G2_AND_G3_AND_G4`
**Closed:** yes, by #165 §18's own condition. **Mixed, not positive.**

> "The programme closes scientifically when every required gate has an explicit bounded
> disposition." — #165 §18
> "A complete negative or conditional result is still **100% completion of the programme**."

Machine-readable: [`PROGRAMME_CLOSURE_V1.json`](PROGRAMME_CLOSURE_V1.json). Generated by
`closure.py`, guarded by 31 tests.

## How this ledger is built so it cannot flatter the programme

- Terminal vocabularies are **parsed from a vendored copy of the #165 body**
  (`ISSUE_165_BODY.md`, sha256 `0aeb5bbe…`), not transcribed. A disposition the roadmap
  never registered cannot be assigned, and a hand-edited body changes the digest.
- Every disposition cites a **receipt that exists on disk**. A missing file is an error,
  not a footnote — and it fired once during construction, on a G7 receipt that lived only
  on an unmerged branch.
- Every `CANNOT_CHECK_` carries a reason **and** a stated conversion. A bare
  `CANNOT_CHECK_` is refused by the membership rule.
- The **programme terminal is derived** by a rule written before the dispositions were
  filled in, not asserted beside them.

## Gate dispositions

| gate | disposition | receipt |
|---|---|---|
| **G1** | `COMPACT_VESSEL_PARTIAL` | `g1-vessel-freeze-v1/TERMINAL.json`, PR #187 |
| **G2** | `CAUSAL_METHOD_REUSE_SUPPORTED` + `PARENT_SUFFICIENT` | #192, `g2-acquisition-economics-v1/` |
| **G3** | `METHOD_COMPOSITION_SUPPORTED` + `CANNOT_CHECK_FAILURE_MEMORY_AND_REPRESENTATION_GROWTH_NOT_RUN` | #193 |
| **G4** | `EXACT_META_POLICY_SUFFICIENT`, `LEARNED_ROUTER_NOT_NEEDED`, `EXACT_EARLY_EXIT_VALUE_SUPPORTED`, `PRICE_REGIME_ONLY`, `PARENT_SUFFICIENT`, `CANNOT_CHECK_PRODUCTION_OCM_LIFETIME` | `g4-horizon-exact-v1/SUMMARY.json`, #152 |
| **G5** | `DATABASE_PARENT_SUFFICIENT` + `CANNOT_CHECK_PACKED_FIELD_BEYOND_KIND_HANDLES` | `g5-physical-denominator-v1/SUMMARY.json` |
| **G6** | `CANNOT_CHECK_MISSING_RAW_TRACES_AND_NO_THIRD_EARNED_SELF_CHANGE`, `SELF_DIAGNOSIS_NOT_IDENTIFIABLE` | `g6-evolvability-v1/RESULT.json` |
| **G7** | `CANNOT_CHECK_ONE_OF_SIX_TRANSITIONS_COMPLETE_AND_THAT_ONE_PARENT_SUFFICIENT` | `developmental-spine/DEVELOPMENTAL_SPINE_V1.json` |
| **PROTOTYPE** | `CANNOT_CHECK_NO_INTEGRATED_PROTOTYPE_EXISTS` | — no lane exists |

## The conclusion, stated plainly

Three gates reached measured positives — **G2** causal method reuse, **G3** method
composition, **G4** exact metareasoning sufficiency. Four gates and the prototype are
unmeasured, each with a stated conversion.

Ordinary parents reproduce the mechanisms at G2, G4, G5 and G7. **Per §12 that is not
programme failure** — the roadmap says so in four words, and §1 disclaims any requirement
that OCM rediscover existing algorithms or that learned components be novel. Mechanism-level
parent sufficiency is the **designed absorption mode** and says nothing against the thesis.

So the thesis is decided one level up, where §12 puts the novelty and where "mere component
integration remains engineering":

| §12 upward target | disposition |
|---|---|
| **phase boundary** | `CANDIDATE_SUPPORTED_E2_SYNTHETIC` — three located edges: soundness precondition, budget window [1408, 1536] bits, storage ceiling below 32 bits per compiled verdict |
| **principled impossibility** | `CANDIDATE_SUPPORTED_TWO_INDEPENDENT_RESULTS` — `rho_R = 0.0` with the legal-feature ladder at ≤0.08%; and no zero-search selector reproducing a utility tournament, compression's argmax at rank 13/16 despite ρ = +0.518 |
| **developmental law** | `CANDIDATE_SURVIVED_ONE_OUT_OF_SAMPLE_TEST` — a three-coordinate conjecture registered before its test |
| **lifetime regime** | `CANNOT_CHECK_NO_REAL_LIFETIME_MEASURED` — break-evens computed (2,136 vs 41 tasks; 32 bits/cell) but only over synthetic or single populations |
| **cross-domain invariance** | `CANNOT_CHECK_NO_SECOND_DOMAIN_RESULT` — one mechanism transferred as a contract with its sign *not* transferring, retained as a negative |
| **integrated interaction effect** | `CANNOT_CHECK_NO_INTEGRATED_PROTOTYPE_EXISTS` |

**Three upper-level candidates are supported at E2; three are `CANNOT_CHECK`.** That, not the
mechanism-level parent sufficiency, is where the programme's residual question actually sits.

Against §21's four outcomes: `PARENT_PRODUCT_SUFFICIENT` is unavailable **in either
direction** while `PROTOTYPE` is `CANNOT_CHECK`, and the ledger refuses to emit it. Outcome A
is not supported by anything currently in the repository. The honest reading is that the
programme has assembled the engineering and has **three synthetic candidates** for the
science, none of them yet measured on a real lifetime.

## What closure does not mean

- It does not mean every checkbox is ticked. The 662-box evidence map at `main@b35093a`
  stands at 29 CHECK / 180 PARTIAL / 211 OPEN / 198 BLOCKED, and closure at gate level is
  a different and weaker statement than closure at box level.
- It does not mean the unmeasured gates are unmeasurable. Each names its conversion.
- **Closure by `CANNOT_CHECK` is weaker than closure by measurement**, and four of eight
  gates close that way. A reader who wants the strong result should read the four
  conversions as the remaining programme, not as a formality.
- It carries no authority to close any GitHub issue. This ledger reads receipts.
