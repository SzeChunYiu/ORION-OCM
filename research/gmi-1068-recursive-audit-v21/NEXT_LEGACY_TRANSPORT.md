# Next legacy transport: exact meanings before retirement

Original R3-009 remains unresolved. This is a queued scope audit, not a result.

The AF formal profile set retains finite history, provenance, resource vector,
terminal machine and capability:
[definition](../gmi-833-af-barrier-context-v1/FORMALIZATION_V1.md).
Its finite implementation instead selects the first BFS path per terminal policy:
[reachable and gamma_profiles](../gmi-833-af-barrier-context-v1/af_core_v1.py).
The resource fields for observations and oracle queries are presence indicators
in that implementation. They must not be relabelled cumulative event counts.

Next task: preregister and prove separate faithful transports for the formal
profile set and the executable selected roster. Compare alternative paths and
cycles explicitly; first adjudicate the historical claim scope. The source
difference alone is not a completed finding that historical evidence is invalid.

Use the [candidate-ranking source](../gmi-833-morphology-selection-schema-v1/morphology_selection_schema_v1.py)
and its [registered theorems](../gmi-833-morphology-selection-schema-v1/MORPHOLOGY_SELECTION_THEOREMS_V1.md).
Preserve the exact candidate identities, resource order, target,
price and tie assumptions of each old candidate-ranking statement.
Equal resource vectors need not erase distinct candidate IDs.
A complete argmin correspondence must not become one implicit tie breaker.

Plan-feasibility Gamma in the earlier FORMAL_CORE_V1.md (the definition
over rooted plans and its joint compatibility condition) is a different
object with intersection semantics, not the AF profile set.
SEL also appears as an [opcode](../machine-intelligence-morphogenesis-v1/gmi_microscope/core.py)
and as a role label: symbolic retirement must
not delete unrelated executable behavior.

Only retire foundation abbreviations after their registered quantifiers and
actual source/evaluator domains have been transported. No automatic inheritance
of all historical Gamma or candidate-ranking results is authorized.
