# Reachability refinement: read first

Theorem + exact finite checks for E. Finite developmental graph with two
laws sharing all static facts (DU-1), admissible scope only.

Run from this unit directory:

```sh
python3 -I -B test_reachability_refinement_v1.py -v
python3 -I -O -B test_reachability_refinement_v1.py -v
```

Sibling source compiled explicitly; exact integer arithmetic, CPython 3.8 safe.
