# Independent PR #150 falsification pilot — pre-outcome freeze

E2 EXPLORATORY ONLY. Not native OCM evidence, E3 confirmation, or independent replication. No favorable result upgrades this ceiling.

Base: main@3430919fd7614589e4fcfb9756dfaffb6ef1b3b4. PR source head: bb70e79050dea04bd0bff8f228fda1dc72dcd09f. Claude source read: 6282bc53e8f2cdd80ca7d5f74564631294984ded. Earlier inconsistent connector metadata is not source authority. No existing branch or negative receipt is modified.

Full ten-heading hypothesis protocol is preserved in the downloadable session bundle as PROTOCOL.md, SHA256 12d52beadbc2156255541f79f64ead9ce2f4ca88048b595492529266a8005d61; local isolated protocol commit db85c3fa34dbad55423e03241e67a806adbcb61d (not a native-repository commit).

## Fixed design

Seeds: 150143144, 150143145, 150143146, 150143147. Unit: whole seed/lifetime, never individual queries. No post-outcome tuning, exclusions, new margins, or protected-outcome feedback. Retain failures, timeouts and abstentions. No statistical superiority claim from four worlds. Source hashes are frozen before execution.

H0: authored development RED tests PR150 local_search cached-score soundness when single-context dependency discovery omits an edge. Compare independently recomputed final fitness and complete-edge ablation. Do not claim published random-continuous NK outputs are invalid without evidence.

H1: bounded relational Boolean formula discovery, 4 binary inputs, frozen depth-bounded generic grammar, 12 generations x 8 heterogeneous episodes per seed, 4 examples + at most 4 active probes. Remint variable positions; expose no task ID/program name. Persist acquired method frequencies. Compare equally persistent exact CEGIS/library parent and reset ablation. Score all fresh input points independently; ambiguous versions cannot authorize universal correctness. Charge candidate scans, library state and maintenance. This is not invention of a cognitive algorithm.

H2: all 16 interventions on finite OR, AND, alternative-conjunction, majority and parity worlds. Infer signed prime implicants and check exhaustively; compare single-context LOO. No supplied support graph. Report discovered alternatives and the full discovery bill. Exact symbolic recovery is a parent, not an OCM novelty claim.

H3: enumerate identical-transcript hidden-cause worlds requiring different repairs; add one paid discriminating probe as ablation. Diagnosis cannot be certain without identification. Learned content/prior routing in H1 gets no supplied routing key. Cached diagnosis and active selection powers are also given to the parent.

H4: scalar-only objective access, 8 binary inputs, 12 generations, maximum 256 calls per generation including final check. Learn a Walsh/pseudo-Boolean surrogate by first-generation enumeration; later refit from a rank-sufficient design, audit and rediscover on mismatch. Three regimes: 8 degree<=2 terms stable; 64 unrestricted-degree terms stable; 8 terms with four replaced supports each generation. Nonzero integer coefficients refresh each generation. Compare global enumeration, faithful adaptive exact symbolic/surrogate parent, stale-model ablation and auxiliary evolutionary search. No private masks/components enter learners. Count discovery/maintenance/query/audit/check calls, model arithmetic and storage separately; final quality uses independent exact evaluation. Surrogate sparsity is NOT native cognitive coupling.

H5: real M11 admission requires native diagnosis -> minimum proposal -> shadow/assurance -> external adoption -> restart -> disjoint fresh reuse, repeated with at least two distinct changes. Missing native custody means CANNOT_CHECK. Mocked lifecycle/model updates do not qualify. Native coupling, strongest Transformer/AutoML/general-meta comparisons remain CANNOT_CHECK unless actually executed.

Capability and total lifetime resources are a vector, not a favorable scalar. Record CPU/wall and logical/array storage where measurable. Unmeasured allocator/energy/native migration/assurance costs are not zero. Scorer-only truth enumeration is separate and never accessible to learners.

Preserve all seven negatives: supplied-key lookup PARENT_SUFFICIENT; failure memory TMS/nogood parent-owned; escalation generator artifact; LOO incomplete; learned relevance maintenance dominated; unary acquisition failed; clause donor no development benefit. The last two remain user-provided constraints until exact artifact resolution.

Review lenses: program synthesis, causal identification, adaptive parents, runtime/resource assurance. These are author-applied lenses, NOT independent reviewer sign-off.

Terminal rules: PARENT_SUFFICIENT closes only the tested mechanism; REFUTED is scoped; CANNOT_CHECK remains for unexecuted obligations. Do not turn an amortization identity into a necessity theorem. No OCM superiority, neural inferiority, autonomous self-improvement or publication-readiness claim.
