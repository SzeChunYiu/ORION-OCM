# Negative-result recovery v1

Base: `4853a5d0f0d3ca5170aaa2010e4919ae5b88551d`.

This additive engineering successor follows #165. It does not edit the frozen
#203 study, #192/#193 outcomes, production authority, or programme checkboxes.
PR #205 already publishes scoped failure-point utility and ordinary-parent parity;
that contribution is credited rather than reimplemented or claimed as this work.

## Grounding intervention

Replace eager source-order nested hypothesis expansion with cached typed
relations and indexed compatible-substitution joins. Keep the original native
matcher, distinct-variable check, action identity, finite bank, library, tasks,
proof search, proof emitter and native checker. Restore original hypothesis slots
before emitting each action. Cache entries live only within one immutable
compile invocation. Cache only exact type success or explicit finite
non-membership; never cache UNKNOWN as false.

Qualification compares complete action multisets against the original grounder,
including repeated premises, shared variables, reversed join orders, cycles and
resource refusal. Physical timing is measured separately from logical work.
Completed-output equivalence does not imply identical timeout behavior.

The #203 task population is already exposed. A one-task local pilot completed
preparation in 3.97 seconds before this note, without producing a target proof.
The complete repeated-population run is engineering recovery, not prospective
scientific evaluation or independent replication. Preserve the original task
SHA-256 `11aa5ae4f887dda9606b4f2def3aa3432811977027fd4a52bc709e7ed8d5a92d`,
8-second per-task solver bound, 3 decisions, 100000 instance limit, 2000000 token
states, 150-second hard per-arm bound, and unchanged native trust inventory.
No unsupported scope or UNKNOWN outcome counts as causal capability loss.

After preparation succeeds, diagnose the registered finite bank separately:
compute exact reachable facts and minimum proof-tree costs. A closed unreachable
set can establish insufficiency of that bank and supported action set, not
mathematical impossibility or failure of the full native library.

## Acquisition and failure-memory economics

Measure and remove repeated semantic computation where it preserves declared
outputs. Compare against an equally equipped ordinary parent. Charge build,
indexing, validation and serving work separately; do not rewrite historical
search-slot charges or relabel them as CPU time. Include all selected task rows,
failed variants and acquisition cost. A faster auditor is not itself a cheaper
learner. A new representation or synthesis parent must be evaluated as a changed
algorithm with its own measured accounting, not an unchanged replay.

## Scientific boundaries

Use four review perspectives: formal semantics, runtime/resource engineering,
program synthesis and acquisition, and causal/statistical evaluation. These are
single-author cross-checks, not four independent people or protected replication.

Engineering defects may be repaired. A strong conventional parent's tie, a false
negative-control goal, an exact-policy zero residual, and an unmeasured coordinate
must not be forced into an OCM-superiority claim. Document whether each cause is
confirmed by intervention, supported by source/evidence, or still a hypothesis.
Publish executable source, tests, all measured outcomes and remaining obligations
on this research branch. No merge, issue closure or full-OCM claim is implied.
