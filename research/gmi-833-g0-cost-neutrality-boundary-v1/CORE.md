# GMI #833 E7 — cost neutrality boundary

This capsule adjudicates the requested no-privilege claim rather than assuming it.

The registered diagnostic cost is exactly label-blind and invariant under certified isometric grammar remints. However, the E2 same-semantics/non-isometric pair reverses raw `(description length, reachability distance)` and therefore reverses selection under every frozen positive weight.

So the stronger statement “G0 does not privilege a known family” is **not established and is false as a universal grammar-neutrality statement** at this scope. The only positive result is family-label blindness plus isometric-remint invariance.

Row-level disposition:

`NO_UNIVERSAL_GRAMMAR_NEUTRALITY__STRUCTURAL_BIAS_COUNTEREXAMPLE`

Claim ceiling:

`GMI_G0_COST_LABEL_BLINDNESS_AND_STRUCTURAL_PRIVILEGE_BOUNDARY_AT_REGISTERED_SCOPE`
