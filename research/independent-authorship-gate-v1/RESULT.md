# Result

Fail-closed policy is in force. ME-X1 is the one world family whose oracle is exact and cheap enough to recover a minimum sufficient cause without reading the planter.

On the frozen development split (`ME-X1-DEV-20260902`, 5 instances × 10 families = 50):

| | n |
|---|---:|
| Independently recovered | 50 |
| `CANNOT_CHECK` | 0 |
| Planted family agrees with recovered class | 18 |
| Planted family disagrees with recovered class | 32 |
| Of the 18 agreements, rejection-sampled POSITIVE artifacts | 17 |
| Hostile “label cause from generator intent” | refused |
| E3 on these generator-authored worlds | `CANNOT_CHECK` |
| Known-answer fixture actions (14) | 14/14 match the independent oracle |

Disagreement is the ladder finding, counted:

- 18 NEGATIVE / empty-cause rows: planter still names a family, recovered min-sufficient cause is `NO_INVALID_ATOM` (action `UPDATE` or `PRESERVE`)
- 9 AMBIGUITY / censored rows: recovered cause is the unknown-atom set (`CENSORED`), not the planted family
- 3 `PROPAGATE_DEFEAT` rows: recovered object is the reopened support set, not the planted C/E family name

The 17 POSITIVE agreements are **not** independent authorship. The generator rejection-samples until the oracle invariant matches the planted family and shares the mechanism taxonomy. Policy retains those rows as artifact-explained negative findings if anyone scores diagnosis against `Instance.family`.

Cause truth on this split is the recovered atom / repair record in `RECOVERY.json`. `Instance.family` is audit-only.

Language construction families, proof-method identity, residual-kind labels, native-learning method I, and M11 `true_layer` remain `CANNOT_CHECK` for independent cause recovery. Independently authored E3+ families are still open. External taxonomy review was not performed.

Receipt: [RECOVERY.json](RECOVERY.json). Audit: [AUDIT.json](AUDIT.json). Controls: `python3 test_gate.py` (ok). Wall time for the 50-instance check is 0.05 s. No novelty claim.
