# Acquisition economics: scoped exact controls

## Exact semantic sharing

Replay the retained #192 candidate tournament, not its protected/test acquisition process. Frozen result SHA256: `896b39429a4fd520cfc7517e73cdd0efb27518a83d7e31f9e6cf99560401db49`; donor `research/g2-macro-operator-v1/experiment.py`, Git blob `4c8cb45c182f12a5e09db873fb7625dc37dbf599`.

The conventional private-evaluation parent and shared pure-evaluation cache receive identical 48 training solutions, 16 ordered candidates and 32 validation targets. Preserve exact token enumeration, first solutions, all validation rows, strict/harmful outcomes and selected method. Do not alter the frozen donor or production checker. Independent final checks bypass the cache. Any changed mathematical/search result kills the comparison.

Run cold processes in fixed private/shared/shared/private order on one host, sequentially. Record full wall/CPU, Linux process peak RSS, actual semantic evaluations, cache calls/hits/entries, source identities and every attempt. This ABBA engineering comparison is not a confidence interval or independent replication. A faster acquisition subroutine does not establish whole-lifetime payback or change the historical search-slot ledger. Sharing is a conventional memoization parent, not an OCM-specific cognitive invention.

## Stronger library-learning parent attempt

A separately labeled E1/E2 parent attempt may use the existing project's pinned `stitch-core==0.1.29`, fixed settings `iterations=3, max_arity=2, threads=1, silent=True`, on the same 48 *training-only* programs encoded as nested unary applications to input `x`. Keep every returned abstraction/rewrite and all refusals. The CPython 3.13 manylinux x86_64 wheel SHA256 is `a37f1ff040637a52862bdadb86ebcf5a620962027465d7bfdaebced590faa1ec`.

Typed admission permits only an exactly decoded, finite unary chain ending in one argument (or previously admitted acyclic unary macros), with expansion length 2 through 8, at least two distinct proper training-target supports, and exact primitive semantics. Reject unsupported arity, free input capture/answer-like zero-argument abstractions, cycles, foreign tokens, primitive aliases, non-equivalent rewrites and duplicate semantics. These refusals are scope/transport results, not a verdict that Stitch is incapable.

If an admissible library exists, evaluate its individual candidate operators against the unchanged primitive and recorded macro parents on the same exposed validation targets, freezing selection before the exposed test can be considered. No retuning parameters, deleting unfavorable tasks or calling compressivity search utility. Charge induction, all candidate evaluation, verification, encoding/storage and later serving separately. Report no method, parent sufficiency and no payback as first-class outcomes. No DreamCoder neural-guidance comparison is claimed unless actually run.

Primary parent literature: Bowers et al., Top-Down Synthesis for Library Learning, arXiv:2211.16605 / DOI 10.1145/3571234; Ellis et al., DreamCoder, arXiv:2006.08381; Minton, Quantitative results concerning the utility of explanation-based learning, DOI 10.1016/0004-3702(90)90059-9. Their results motivate the comparisons; they do not supply missing OCM evidence.
