# Validation boundary

The final focused suite passes23 tests in normal and23 in optimized Python:
16 mathematical/execution tests plus7 custody controls. Exact commands and
actual outputs, interpreter identity and Python source hashes are in
VALIDATION_V1.json. No campaign or native VM is run.

The complete policy census compares384 registered grammar/price/allowed-class
cases with3456 independently enumerated full executions, including every
reachable final retained set. Primitive delivery is positive in this census.
The parsing census compares1008 word/dictionary cases with3136 complete parses.
The principal large witness additionally uses an independent materialized
executor and a proof covering every legal admission time; it is not a claim
that3456 small executions exhaust its larger policy tree.

Both original source scripts reproduce their entire frozen JSON, and full
stdout is retained in the new receipt. The checker adds actual source-function
countercontrols without computing any new empirical observation.

Final isolated normal/O portable replay outputs remain outside this unit and
are bound in transport metadata. Each must equal the entire RECEIPT_V1.json.
Two implementation construction mistakes (one closing parenthesis and one
rule-price/primitive-price dimension check) were diagnosed and corrected before
the final successful suite. No failed scientific campaign was discarded.
