# Pinned source dispositions

The five additions at fc9f6169747cf45dc0562f216d74eae2d0682fdd are
archived with exact blob/SHA/commit paths in SOURCE_BINDINGS_V1.json.
They were merged in PR588 at77968d35. Earlier frozen evidence is unchanged.

| Source implication | Verified scope and repair |
|---|---|
|PVR reuse threshold | Correct for its two complete constant-cost policies, r≥1, stable validity and no eviction. |
|Quotient supplies uniquely what to chunk | Not implied: future-state equivalence and executable grammar factorization are different objects; HCR-4. |
|Two-level witness executes child rules | Neither original script stores productions or calls children. Both match primitive strings against one flat dictionary. |
|Same candidate enumeration in witness/sweep | Witness allows≤3 short for baseline, but≤2 short+≤2 long for its higher class. Its baseline is not included. Sweep permits≤4 from the combined pool and does include its baseline. |
|Exact best cost | Exhaustive over those subsets, but with the fixed greedy parser, not all parses or grammars. HCR-1 repairs parsing. |
|PVR full lifecycle prices | Source pays S per dictionary entry plus ALL invocations. It omits first derivation; it can instead describe already-supplied words with a declared construction fee, not the cited first-use PVR comparison. |
|Substring occurrence count is demand | Occurrences can overlap or disappear behind another chosen chunk; aaa/aa is decisive. |
|Lower C proves each next level saves less | Only a within-candidate partial comparison at fixed S,U. HCR-3 supplies a complete contrary lifecycle. |
|Eviction reinforces shallow depth universally | Additional eviction schedules, dependency states and costs are required; SMR is the stronger matched parent. |

The original72→33→31 witness and all20 sweep rows remain retained calculations.
They concern an authored five-task dictionary register. No measured hierarchy,
family optimum or data-derived natural depth law follows. The ratio table
includes0%, so even its stated universal10–20% summary has an explicit exception.

Reproduction runs only the two original pure finite scripts in temporary
working directories. Their complete JSON and stdout are retained. No native
VM, ecology, protected campaign, deferred prediction or experiment is called.
Controls directly reuse the pinned greedy function to demonstrate abcde cost3
versus the independently enumerated optimum2. A one-request p=abc comparison
shows original S=1,U=1 gives2 while PVR C=3 retention costs4 versus fresh3.
This attributes the mismatch to the first-derivation accounting stage.

Candidate-class enumeration and a supplied cost are conditional models.
The repair does not assert that these original computed outputs are fabricated
or that the exact quotient/PVR parents are false.
