# Parent ledger for gmi-833-h-family-tranche-b-v1

## The narrow prior (harmless, disclosed)

1. The nine rows of this package, in the exact wording of issue #833:
   Feed-forward neural networks., Backprop/reverse-mode credit assignment.,
   CNN/equivariant local-weight-sharing systems., RNNs., LSTM/GRU-like
   gating., Attention mechanisms., Transformer-like dynamic
   routing/composition., Graph neural/message-passing systems.,
   Mixture-of-experts/routing systems.
2. The eleven-coordinate rule of section H as stated in
   `gmi-833-h-family-requirement-ledger-v1/LEDGER_V1.json` (`HRL-1`),
   including the `CROSS_SCOPE_GATE_COMPOSITION` ban.
3. The real-scale definition of `gmi-833-h-real-scale-classical-v1/FREEZE_V1.md`
   section 7 (sha256-bound real source, n_fit >= 100000, n_held >= 20000,
   exact held-out arithmetic, charged resource accounting).
4. The known-machine-intelligence register of the obstruction census
   (`gmi-833-h-obstruction-census-v1/FROZEN_FAMILY_REGISTRY_V1.json`) as the
   post-hoc structural register. The census result itself is not evidence here;
   each of the nine rows carries an obstruction class in the census that is
   recorded as the row's prior disposition only after the class is attached.

## What is not claimed

- No universal recovery; no named-family row is closed anywhere in this
  package.
- No finite witness is evidence for real-scale behavior (`FGS-3`).
- No certificate from a parent scope composes with a certificate here
  (`FGS-2`, `CROSS_SCOPE_GATE_COMPOSITION`).
- No claim about State-space models. — owned by the local cluster package.

## Strongest parents

- `gmi-833-h-neutral-four-family-v1` for the one-grammar shape and the
  post-hoc classifier priority.
- `gmi-833-h-real-scale-classical-v1` / `gmi-833-h-real-scale-revival-v1` for
  the eleven-coordinate ledger shape and the real-scale threshold.
- `gmi-833-h-obstruction-census-v1` for the named-row registry and the
  `FROZEN_FAMILY_REGISTRY_V1.json` channel labels.
- `gmi-833-ai1-ai8-blind-neural-microscope-v1` (PR #946) for the toy-scope
  blind microscope; cited as covered scope, not as evidence.

## Residual contribution

A single registered neutral grammar over a 7-leaf, 6-operator primitive set,
the same grammar digest across all nine row scopes, per-row single-sigma gate
ledgers that stop at 10 of 11 in finite scope and leave `R11` open, and a
checker that refuses closed-row claims without a real-scale receipt. The
`backprop/reverse-mode` row's gate ledger demonstrates exactly one failed
coordinate: `R11`, with stage `missing_channel` and the prescribed lever
`register the update-feedback channel`.
