# M12 Strongest-Parent Parity Audit V1

**Status:** source-derived causal-attribution blocker / M12 engineering and descriptive evidence retained / no general OCM net-benefit claim.

## Question

Can the current M12 heterogeneous lifetime be counted as causal evidence that the **OCM architecture itself** provides a whole-system net benefit over an equally capable conventional parent?

Current answer:

```text
NO — M12_ARCHITECTURE_CAUSAL_CLAIM_BLOCKED_PARENT_PARITY
```

This is not a claim that M12 is invalid engineering. Its persistence, restart, revision, transfer and self-repair regressions remain useful descriptive/mechanism evidence. The narrower problem is causal attribution to architecture.

## Source-custodied finding

`m12_parent_parity_audit.py` binds the current `src/ocm/lifetime/phases.py` Git blob:

```text
1327ca822fe8a2c958526c05e59853dd3ff3a4fd
```

The phase harness assigns several scientific mechanisms using Python arm identity rather than a prospectively equalized capability/treatment contract.

### Phase D — science / investigation

The phase first computes:

```text
is_ocm = isinstance(arm, PersistentOCM)
```

and then changes substantive mechanisms by that flag, including:

```text
causal estimator: backdoor vs naive
experiment selector: select_ocm vs select_entropy
communication commitment gate vs unconditional commitment
science persistence representation
```

Those may all be good OCM mechanisms. But if the conventional parent is denied the strongest faithful versions merely because it is not a `PersistentOCM` instance, the resulting difference cannot identify a whole-architecture residual.

### Phase E — transfer

This phase is partly repaired already. The semantic `WholeSystemParent` receives a shared transfer contract for the matched cells. The remaining OCM identity branch is therefore recorded as `PARTIALLY_EQUALIZED`, not treated as a whole-phase failure.

### Phase F — revision

The source again branches on `PersistentOCM` identity for source-aware knowledge revocation and science-ledger retraction. The parent receives a different representation/mechanism. These are valid mechanism demonstrations but not yet a strongest-parent architecture comparison.

### Phase G — self-repair

The source branches explicitly on:

```text
isinstance(arm, WholeSystemParent)
```

The parent receives `_parent_repair`, while the OCM path uses the self-model diagnosis / registered alternative / proposal machinery. Until both receive matched problem information, candidate powers and budgets, the residual is treatment-plus-capability difference rather than architecture-only evidence.

## Required repair

The prospective comparator is the already registered P6 composite whole-system parent family:

```text
P1 indexed retrieval/RAG
+ P2 structured persistent memory
+ P3 executable skill library
+ P4 continual adaptation
+ P5 truth/reason maintenance
= P6 strongest compatible composite parent
```

The next M12-quality confirmatory harness should dispatch mechanisms by a **registered capability/treatment interface**, not by Python class identity. At minimum freeze before outcome access:

- task/source/lesson identities;
- memory and lifetime permissions;
- retrieval/index capabilities;
- executable method/skill reuse;
- continual-adaptation permission;
- truth/reason-maintenance and revocation powers;
- tools/checkers;
- search/planning budgets;
- preprocessing/index build;
- construction, storage, maintenance, revision, restart and replay costs.

Then compare at minimum:

```text
CONTINUED_OCM
RESET_OCM
P6_COMPOSITE_PARENT
```

with causal mechanism knockouts where the OCM-specific treatment needs isolation.

## Why this helps the general proof

The architecture thesis is strongest if OCM wins **after** the obvious parent mechanisms are absorbed. This audit prevents a false positive in which a lifetime benchmark looks broad but the evaluator itself assigns better algorithms to the OCM-labelled class.

Conversely, if a capability-equalized P6 parent matches OCM, report `PARENT_SUFFICIENT`; the scientific result then becomes a parent-sufficiency/architecture-integration result rather than a net-benefit claim.

Current boundaries:

```text
M12 engineering regression: VALID
M12 descriptive lifetime mechanisms: VALID
M12 whole-system architecture residual: NOT ESTABLISHED
general OCM net benefit: NOT ESTABLISHED
learned router #71: NOT AUTHORIZED
```
