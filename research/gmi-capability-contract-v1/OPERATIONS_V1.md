# Capability contract: read first

27-row contract for item 4 / A4. Each row cites the phenomenology atlas
plus a mature external parent (first refusal).

Run from this unit directory:

```sh
python3 -I -B test_capability_contract_v1.py -v
python3 -I -O -B test_capability_contract_v1.py -v
```

The test entrypoint compiles its sibling model from source; no network,
CPython 3.8 safe.
