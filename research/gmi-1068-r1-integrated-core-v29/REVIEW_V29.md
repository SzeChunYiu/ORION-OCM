# Independent scientific/API review

## Scope and authorship
This reviewer authored the paper notes and independently inspected the actual
production APIs and independent oracle/test sources. This is an independent
review of code and proof interfaces, not a second independent paper authorship.
No inherited or preregistration file was edited.

Reviewed core_v29.py, presentation_v29.py, named_v29.py and information_v29.py
against the frozen five-file contract, exact original crosswalk and parent APIs.
Primary exhaustive outcomes belong to the canonical RESULT and coverage guard;
the supplementary observations below are separately scoped.

## Actual integration checked
GeneratorMap checks endpoint-respecting maps into an actual lawful Typed category.
evaluate_path validates the whole path and uses actual identities/composition.
dag_presentation uses actual V11 enumeration and semantic quotient construction;
its lower map evaluates representatives, and quote exists only after generation.
The seven-path fixture does not stand in for ownKernel(C.Hom).

NamedAdapter constructs a real Presented table over the image of a proper
injection, explicitly reindexes its multiplication and retains object names.
typed_query validates every descendant before absence can yield None.
A missing ambient arrow has no fabricated local inverse.
The named observer and encoded bundled observer keep both endpoints.

Family/report code keeps common dimensions, explicit None tags and complete
query/output maps. Its restricted target is q-indexed through i; repeated query
indices are legal. Output injectivity is reported separately from commutation,
so false-premise controls remain expressible.
Actual V9 codebooks and attained decoders distinguish source, restricted-target
and full-target recoverability; no off-image decoder is invented.

## Diagnosed certificate defect and verified repair
Initial same_record converted dataclasses with asdict, erasing nested class
identity. A genuine one-object/no-generator presentation certificate passed;
replacing its category with a foreign dataclass of identical fields also passed.
That violated the frozen actual-parent-type certificate contract.

Root repaired the common comparator: typed_key now retains each dataclass's
actual type and recursively checks its fields before structural comparison.
Independent replay accepted actual presentation and proper-injection adapters
and rejected foreign nested Category, Functor, deeply nested Table and
NamedPresented instances. The repair addresses the common cause rather than
one particular foreign class. The primary hostile suite now includes eight foreign nested-type rejections.
A separate bounded replay of that actual test method passed:3 valid baselines,
8 foreign-type,53 semantic-certificate and121 malformed-input rejections.

## Supplementary observed evidence
Script: /tmp/gmi-v29-independent-controls.py.
Normal and optimized Python3.12 runs produced identical15-check records:
actual and foreign-class controls above; absent singleton/typed inverse;
retained anchor; malformed suffix after an absent leaf; both encoded endpoints;
repeated-query family; noninjective-output loss; omitted-target-query boundary;
empty attained domain.

Records: /tmp/gmi-v29-independent-normal.json and
/tmp/gmi-v29-independent-optimized.json.
SHA256: c3464ee016af8d704cdaaee7ec5b91caae5a50824fee7a553338b600ad09725d.
These are targeted named review controls, not an enlarged primary population.

## Remaining evidence boundary
The actual primary corpus, exact coverage, custody and source-valid kernel
mutations must pass the canonical driver. FORMAL_REVIEW records the independently
observed77-source/331-entry fresh kernel pass, separate from this API inspection.
Variable timing and source-volume data belong to RUN_TIMINGS_V29.json,
excluded from deterministic scientific inputs and bound by successor artifacts.
No runtime or empirical compression improvement is claimed.

Only R1 whole-round status/scope is eligible under actual successor authority.
All original atom records, R2/R3 statuses and immutable V16 qualified scope remain
unchanged. The reviewed code implements the registered observations; it does not
establish adequacy for arbitrary physical processes or a lossless parent union.
