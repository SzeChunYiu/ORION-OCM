# Registered falsification and evidence levels

The [API](API_V29.md) fixes exact ordering and the [freeze](FREEZE_V29.md)
fixes this study before execution. Counts here are schedule arithmetic.
Actual outcomes belong to RESULT_V29.json after independent checks.

## Joint fixture
Seven paths and49 ordered path pairs; six quotient arrows and36 products;
12 lower/quote inverse comparisons. Class enumeration order is not target order.
The full and restricted models each receive the SAME82 ambient queries.
Both actual named interpreters yield328 evaluations.
There are108 present-leaf queries, hence216 typed interpreter evaluations;
56 absent-leaf cases have no typed inverse.
Only40 restricted-present queries enter the inclusion comparison.
Independent endpoint recursion includes failed joins and both encoded endpoints.

## Semantic negatives and corresponding repairs
- Wrong c/ab split or cross-Hom merge: actual evaluation-kernel classes repair.
- Lower forced to numeric identity: explicit representative evaluation repairs.
- Missing generator: lower remains faithful; inverse requires generation.
- Erased Empty anchor: retained identity guard repairs.
- Swapped named identities with unchanged queries: transported names repair.
- Proper injection treated as permutation: actual image/subtype codec repairs.
- Removed b/c confused with globally absent2: preserve separate membership.
- Identity removed or a,b admitted without c: restore lawful closure.
- Object collapse mistaken for embedding: retain object distinctions.
- Hom collapse: raw commutation remains possible; output recovery needs faithfulness.
- Resource balance forgotten: keep successful-path theorem and actual lift;
  do not relabel it full failure reflection.
- Varying output encoders: retain their codebooks or impose fixed interfaces.
- Noninjective outputs and omitted target queries: enforce the exact I2 premises.
- Malformed later subtree: validate the whole syntax before semantic evaluation.

Valid unchanged instances must be accepted before semantic certificate mutation.
Strict tuple/int/Fraction conventions and failure tags are part of the API;
bool aliases, invalid dimensions and unused malformed entries are not admitted.

## Independent evidence and custody
The90-family/180-report schedule separately tests fixed-interface recovery.
Named controls remain separate; no extra random population is inferred.
All six survivor, six optional and seven parent rows retain original identities.
Crosswalk guards must reject dropped/reordered rows, altered sources/premises and
coupled rewrites, while accepting actual valid ledgers.
A shape guard alone does not establish that explanatory prose is mathematically true.

The63 inherited Lean sources and192 selected types are frozen before new proofs.
The source-derived V9 recovery_transport type receives its first registered audit
here; previously compiled source alone was not that exact type registration.
Four source-valid weakening controls must first compile and then fail AUDIT.
Missing tools/inputs are CANNOT_CHECK, not mathematical success or refutation.
Normal and optimized independent runs must agree on canonical evidence.
Variable wall times are reported separately from deterministic receipt bytes.

No original atom changes; whole R1 eligibility requires the new integrated
mathematics plus actual science and successor gates, not merely row counts.
