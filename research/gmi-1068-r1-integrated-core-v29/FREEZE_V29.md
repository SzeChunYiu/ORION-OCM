# V29 freeze — R1 integrated anchored-observer closure
Preregistered contract; implementation, experiments and proof replay follow its freeze-only commit.
V28 merged as #1124 at 2405c7bd06f9236b545ce501e5b9f9b5f24fd5e5.
No V29 implementation or proof replay exists at freeze.
Only original R1 whole-round re-earning is eligible; no original atom changes.
Do not revise R2/R3, the original DAG, V16 authority or any other round status.
The claim is relative to the registered named/anchored history observer.
It is not absolute signature, storage, runtime or ontology minimality.

## AC1 — actual presentation and observer equations
Use immutable actual V11 free paths, semantic congruence and presented category.
Construct a generic object-fixing evaluation-kernel lowerMap from the quotient
of paths on G to C. It is faithful; require explicit generation of every C arrow
for an inverse quoteMap. Specialize to V11 ownKernel using singleton paths.
The three-generator DAG fixture uses the generic theorem, not ownKernel.
Use actual V26 ProcessMap, object identities and derived composition laws.
Prove actual inverse and faithful properties; bind constructor equations.
Use V25 named_observer and V26 raw transport to derive both named response
equations including Empty anchors, invalid joins and successful outputs.
Prove tree inverse equations on quotient-arrow trees; no inverse recovers raw
paths c and ab after they are identified. Do not replace free histories by quotient classes
before their explicitly declared interpretation.
Reread existing reconstruction, tree-transport and NamedPresented bridges;
reuse them exactly and add only missing interface equations.

## AC2 — precise transport and information conditions
For actual F:C→D, all named raw responses commute iff its object map is injective.
Successful response preservation alone needs no object-injectivity premise.
Hom faithfulness reflects successful outputs within a fixed source Hom.
Global bundled-response reflection also requires object injectivity.
Apply this to actual wide restriction inclusion and quotient-presentation isomorphism.
Resource projection drops balances and does not preserve arbitrary raw failures;
same-path affordable lifting remains a distinct earned statement.

Across a common model family use a FIXED query encoder i:Q→R and fixed injective
output encoder j:A→B. Given the actual commuting equation, source responses and
the target responses restricted to i(Q) have equivalent attained-code recoverability.
Do not infer target responses on unrepresented queries.
Retain the query/output interfaces and codebooks as part of information accounting.
Per-model encoders alone are insufficient, even if every encoder is bijective.
Exact Bool countermodel: source(m,*)=some(m),target(m,*)=some(false),
j_m(b)=xor(b,m),constantcode. Each square commutes; target is recoverable but source is not.
None is preserved. Retaining m or j_m repairs recovery because j_m(false)=m.
This is a standard observer-factorization boundary, not a novel universal intelligence law.

## AC3 — one joint independent finite fixture
V11 DAG objects0,1,2 with generators a:0→1,b:1→2,c:0→2 in that order.
Seven actual paths:3 identities,3 singletons,ab. Quotient by equal endpoints;
c and ab share one class. The target category has6 arrows in lex endpoint order:
id0,a,c=ab,id1,b,id2. Quotient classes use parent enumeration order, with an
explicit lower map into these target indices; do not assume those orders equal.
Construct actual V19 Typed/Table, V25 Presented/NamedPresented and V26 restriction.
Use ambient labels0..6, local injection ell=(0,1,3,4,5,6), absent label2.
External object-name decoding q=(2,0,1), identity encoder(6,0,4).
Restriction A=(0,1,3,5) retains all identities and a; use ell composed with inclusion.
Both models retain the same external anchors and7-label ambient query interface.
Do not fake the proper6→7 injection by padding a permutation or supported arrows.
Read V25 permutation-only map_tree carefully; use a distinct validated injection adapter.

For each model:7 Arrow leaves+3 Empty leaves+49 Arrow pairs=59 basis queries.
Add9 Empty pairs,12 anchored unit insertions for the6 original arrows,
and2 bracketings of a;b;Empty(nameOf2). Exactly82 queries/model,164 comparisons.
These are prospective schedule counts, not observed successes.
Independent expected values come from endpoint chaining, availability and recursion,
not production tables, quotient results, flattened words or observer helpers.
Compare named_eval and named_word on every query; when leaves are present,
also compare independently decoded typed_eval/typed_word and both endpoint labels.
An absent ambient leaf has no inverse typed query; record the guard failure.
Compare inclusion-mapped Option responses only when every Arrow leaf belongs
to the restriction. Removed b/c have None in restriction and Some in the full
model; record these as availability controls, distinct from label2 absent in both.
There is no total reverse functor from every ambient arrow into the restriction.

## Controls and revival
Valid unchanged sources, adapters, mappings and complete ledgers must pass first.
Swap named identities without transporting Empty queries; retained/transported encoder repairs.
Collapse distinct objects through a faithful map; retained structured objects repair failed joins.
Object-injective but nonfaithful maps preserve raw commutation while losing output distinctions.
Use resource balance forgetting as a false embedding; actual typed affordable path is the repair.
Remove an identity from A; add b without its composite c; wrong restricted local identities.
Use a nonunit raw Empty, absent Arrow2, cross-Hom quotient or wrong split of c/ab.
Malformed suffixes remain errors even when an earlier subtree already fails.
Retain structural-error versus valid-query-None separation and strict typed labels.
The fixed/varying encoder control above is separate from raw object-collapse controls.
Add noninjective output-encoder and omitted-target-query counterexamples.
Per-category presentation transport remains pointwise. Across-model recovery
is a separate theorem on a common labelled family with fixed i,j. Define the
restricted target response as q ↦ target(m,i(q)); i need not be injective.
No model-dependent presentation encoder is silently treated as fixed.

## Source-specific rollup
Map all8 original R1 required results and all10 original atoms to exact present evidence.
Retain six V25 survivor rows and their preserved premises under the same observer.
Do not claim independent primitive storage when padded multiplication derives support/identities.
Retain six V26 optional rows: tensor obstruction and no-braiding-for-this-tensor differ.
Retain seven V27 parent rows, exact maps and inverse/slice/information boundaries.
LTS states are not whole coalgebra objects; forgetting can collapse system object anchors.
Support loses probabilities; test aggregation loses outcome labels.
Normalized channels/events, declared tasks/regularity and general enriched interpretations
retain their established scope; there is no common lossless seven-parent equivalence.
The joint chain fixture does not replace inherited omission countermodels.

## Authority and evidence gates
Freeze exact source closure, original result crosswalk, fixtures and contracts before execution.
Freshly replay required static typed registrations and source-valid weakening controls.
Record imported source count/bytes, proof replay time, adapter/oracle time and all codebooks.
No empirical compression/runtime improvement follows from moving data into decoders.
Independent normal/-O receipts and required valid/hostile baselines remain mandatory.
New successor may promote ONLY R1 and must use a substantive R1 path recognized by V3.
Original atom accounting stays35 fulfilled/187 unresolved/2 qualified/185active.
R0 remains EARNED,R1 newly EARNED,R2 OPEN,R3 STALE; preserve all atom scopes.
Refresh downstream parent bindings without promoting their statuses.
Current original R2-003/007 stay UNKNOWN, qualified replacements remain distinct.
Reject status-only edits, stale R0, scope weakening, missing rows/modules, source changes,
coupled receipt/ledger rewrites and false all-GMI promotion.
A corrected R2/R3 round graph needs separately registered additive authority later.
The five-file freeze contains this contract, SOURCE_PINS, ORIGINAL_CROSSWALK,
API_V29 and INHERITED_PROOFS_V29; all are committed before implementation.
Crosswalk preserves all8 literal result texts,10 atom mappings and prior R1 scope.
Only current R1 scope/status may change; all atom records and historical repairs
remain exact. Append registered evidence and refresh dependent parent digests.
The new current R1 scope is exactly ORIGINAL_CROSSWALK.original_scope.proposed_scope.
API_V29 fixes the actual parent APIs, full82-query schedule and90-family schedule.
INHERITED_PROOFS fixes63 immutable Lean sources and192 selected exact types;
one V9 type is source-derived and awaits its first registered replay.
Python inputs include18 actual parent modules; finite-function V11 interpretation
is not misused as a generic target-category interpreter.
The primary schedule has164 queries/328 named evaluations,108 present-leaf
queries/216 typed evaluations,56 absent-leaf cases and40 inclusion comparisons.
Seven path evaluations,49 path pairs,36 quotient products and12 inverse equations
are independently checked. The fixed-interface calibration has90 constructed
families/180 reports/360 source-versus-restricted recovery decisions, with180
full-target decisions recorded separately. These are scheduled counts only.
Use13 test methods in9 modules as specified by API_V29, including4 source-valid
proof weakenings. SOURCE_PINS binds the four companion files and all117 inherited inputs.
No observed outcome, new original atom closure or complete-GMI claim is registered.
