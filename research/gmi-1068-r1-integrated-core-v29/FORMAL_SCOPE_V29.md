# V29 formal scope — integrated R1 observer equations

Read with FREEZE_V29.md, API_V29.md and ORIGINAL_CROSSWALK_V29.json.
This package concerns the declared typed sequential observer.
It does not establish a unique ontology, an independently stored primitive list,
or a universally adequate physical or intelligence model.

## Inventory and replay

The static contract contains 331 registrations over 77 source modules.
The first 192 entries reproduce INHERITED_PROOFS_V29.json in order.
Of those, 191 had an earlier static registration and the V9 recovery_transport
signature receives its first registration here.
The 63 inherited modules are compiled from their pinned source bytes.
The remaining 14 modules and 139 declarations are new V29 work.
No old finite experiment corpus is rerun by the Lean checker.

The V11 declaration-tail format is retained literally, including its RHS.
Other entries are explicit types assigned to the actual named declaration.
Constructor equations bind operations and data, in addition to return types.
Generated audit definitions are not a source of new expected statements:
the committed Python contract contains the fixed expectations.
Every registration is followed by a kernel axiom inspection.

The checker stages all sources in a new isolated temporary directory,
uses Lean 4.19.0 with warningAsError, then compiles the typed audit.
It rejects unproved declarations and distinguishes SOURCE, AUDIT and
unavailable compiler/input outcomes. It does not reuse repository oleans.
Classical.choice, propext and Quot.sound are standard logical dependencies.
There is no extra mathematical assumption introduced as an unchecked axiom.

## Generic evaluation-kernel presentation

EvalKernelV29 uses the actual V11 Path and kernel congruence.
Its quotient has the same objects as C and classes of paths on G as arrows.
lower is Quotient.lift of the actual recursive V11 interpretation.
lower_class, lower_id and lower_comp establish its operations.
lower_injective proves faithfulness directly from the semantic kernel.

Generates requires a path interpreting to every target arrow.
It is used only when constructing quote and its inverse properties.
Classical choice selects a generating path; no effective search is claimed.
lower_quote and quote_lower prove both inverse equations.
quote_id and quote_comp derive the inverse map's laws.
Both maps are actual V26 ProcessMap records with identity object maps.
generates_iff_onto identifies exactly the missing surjectivity requirement.

own_generates uses singleton paths when G is C.Hom.
own_quotient and own_lower identify the inherited V11 constructions;
own_quote proves agreement despite the generic choice of representative.
The three-generator finite DAG is an instance of the generic theorem.
It is not an enumeration of Path(C.Hom), whose identity loops remain paths.

PresentationBridgeV29 proves both bundled-arrow and Tree.map inverses.
These trees have quotient arrows as leaves.
No inverse distinguishes raw paths already equated by their interpretation,
including the direct c edge and the two-edge ab history.

## Actual named raw transport

NamedTransportV29 rewrites the actual V25 named observer into the actual
category observer, then applies the V26 raw transport theorems.
sharp states all named-tree response commutation iff object injection.
transport includes Empty anchors, unsuccessful joins and successful results.
success needs no object-injection premise.
none_iff needs object injection.
output_reflection additionally needs Hom faithfulness.
These are different conclusions; faithfulness alone does not preserve
malformed joins between objects collapsed by a functor.

PresentationBridgeV29 instantiates both named equations on lowerMap/quoteMap.
The inherited object-collapse, nonfaithful bit-group and resource-forgetting
controls retain their separate scopes.
Resource lifting remains the inherited exact same-path affordability result,
not a claim that forgetting resource objects preserves every raw failure.

## Proper ambient injection and external names

ImageAlgebraV29 constructs a real V25 Presented object for an injective
j:A→L, using its image subtype as carrier.
The partial operation is the original operation transported through
put/get; its strong associativity, local units and coherence are proved.
The get operation chooses a preimage only on the attained image.
put_get and get_put establish both directions there.
There is no inverse for an absent ambient label.

padded_put is the actual totalized multiplication equation.
carrier_binding and padded_absent establish presence and missing-label behavior.
This construction applies to proper injections, not only permutations.

AmbientNamedV29.named applies this construction to the actual bundled
category algebra and uses identityMap(n)=j(identity C (q n)).
q is a declared external-name decoder.
The named constructor requires only that its values are actual units;
complete derives complete unique naming when q is injective and surjective.
The theorem does not silently infer those properties of arbitrary q.

observer_transport derives the arbitrary-tree equation from the actual
carrier tests, identity encoder and padded operation.
It preserves complete anchored syntax and all Option failures.
lift_tree constructs a typed preimage of any tree whose Arrow leaves lie
in the image; Empty leaves retain their supplied external names.

AmbientFunctorV29 proves the commuting square through actual ProcessMap
and actual source/target named constructors.
Its local label equation and object injection are explicit.
RestrictedAmbientV29 specializes this square to the actual lawful V26
wide restriction and inclusion, deriving restricted label injection.
carrier_exact identifies the selected Hom predicates and ambient labels.
identity_same proves the common external names retain identical unit codes.

guarded_transport compares the same ambient query in both models only when
every Arrow leaf belongs to the restricted source image.
removed_arrow proves the complementary None-versus-Some contrast.
A removed arrow is not an inclusion-commuting query.
Malformed external encodings are an executable validation concern;
the Lean inductive Tree already has a well-typed syntax datatype.

PresentationControlsV29 kernel-checks the exact Fin6→Fin7 label injection,
missing label2, its full image, the three-name permutation and identity codes.
Those finite codebooks agree with the registered joint fixture.
The entire Python DAG/category/quotient adapter implementation and its
enumeration counts remain independent finite calibration, not a kernel
proof of the Python program or a generated extraction theorem.

## Fixed-family information and its failure controls

FixedEncodersV29.recovery_iff uses the actual V9 attained-image decoder notion.
M, code, source/target signatures, query encoder i and output encoder j are fixed.
The family square and injectivity of j imply equivalent recoverability
of source and q↦target(m,i(q)).
The proof derives pointwise Option-map injection and uses V9 recovery_transport.
No injection of i or coverage of the complete target query space is required.
The target conclusion is explicitly restricted to the encoded queries.

EncoderControlsV29 constructs the Bool/xor family from the freeze.
Every varying encoder is a bijection and every pointwise square commutes,
but the constant-code target is recoverable while the source is not.
Retaining the model or the encoder function repairs recovery.
Separate controls show failure with noninjective fixed output encoding
and with omitted target queries. None remains distinct from every Some value.

PresentationControlsV29 also constructs an empty generating graph for the
nontrivial bit group: its lower map is faithful but cannot generate true.
This prevents deriving an inverse from kernel faithfulness alone.

## Evidence boundary and independent hostile controls

ProofTargetsV29 ends with three independent typed leaves:
presentation_contract, fixed_encoder_contract, guarded_restriction_contract.
The source-valid controls replace each with True, plus an all-empty control.
Only a passed unchanged typed audit can accept the intended theorem scope.

Inherited optional structures and seven parent translations remain selected,
source-specific consequences. Their paper/finite boundaries are unchanged.
Historical crosswalk locators need not all be freshly selected aliases;
INHERITED_PROOFS is the exact fresh inherited registration inventory.
The joint finite fixture tests compatibility of those mechanisms.
It does not replace survivor countermodels, primary attribution or receipts.
Only the separately checked successor can re-earn current R1 authority.
No original atom count, R2/R3 status or V16 qualified replacement is changed
by a Lean theorem or by this scope document.
