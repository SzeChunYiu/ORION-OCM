# Preserved UNKNOWN / CANNOT_CHECK / empirical nonclaims

This file prevents the bridge from laundering theory closure into empirical ORION closure.

## Inherited from G6

The current `research/g6-evolvability-v1` lane explicitly lacks enough evidence for positive developmental-evolvability closure.  In particular this bridge does **not** reconstruct:

- missing source-bound raw failure/probe transcripts;
- disjoint unlabeled intervention→effect observations;
- reusable intervention-effect learning and fresh prediction;
- a third nontrivial earned self-change;
- fresh post-restart effects/full cost/matched adaptive parents;
- real self-change `κ`;
- real-probe `Ω`;
- calibrated repair-posterior `χ`.

Accordingly `κ_D`, `Ω_D`, and `χ_D` are formalized here but remain `CANNOT_CHECK` empirically for ORION at this snapshot.

## Inherited from G7

The current G7 microscope earns its early persistent lineage transitions but explicitly leaves later developmental stages unrun, including governed self-evolution.  This bridge does not relabel those stages as complete.

Therefore empirical ORION R4 is `CANNOT_CHECK` until an earned `D` intervention is inherited by a later registered descendant.

## R5

`GENERAL_OPEN_ENDED_RSI` remains an explicit nonclaim.  The following are insufficient on their own:

- more benchmark repetitions;
- a fixed finite number of domains;
- a long but finite lineage;
- self-reported generality;
- an editable meta-agent architecture;
- positive descendant metaproductivity on one ecology.

## Acceleration / intelligence explosion

No bridge theorem states that recursive evolvability yields unbounded acceleration, superlinear growth, or intelligence explosion.  Those require extra dynamical/resource assumptions and separate falsification.

## Reopen rule

A terminal negative/nonclaim is reopened only by evidence matching its stated prerequisite.  Documentation changes, additional terminology, or rerunning the same confounded data do not count.
