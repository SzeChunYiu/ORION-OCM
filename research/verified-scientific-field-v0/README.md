# General Epistemic Field hardening packet

> Historical directory name only: `verified-scientific-field-v0` was the first framing. The canonical object is now **General Epistemic Field** per issue #93. Do not steer research from the superseded science-only framing.

## Read in this order

1. `GENERAL_EPISTEMIC_FIELD_CONTRACT_V1.md` — canonical field hypothesis, domain views, authority boundaries, falsifiers.
2. `PARENT_KILL_GATE_V0.md` — strongest-parent attack; unified-space novelty is explicitly withdrawn unless a narrower residual survives.
3. `LANGUAGE_FIELD_INTERFACE_V0.md` — language input/output over the same field; reverse-read; fluency boundary.
4. `QUANTUM_DONOR_TRANSFER_MATRIX_V0.md` — ORION-Q/QG mechanisms reduced to domain-general donor candidates with classical parents first.
5. `EXPERIMENT_PROTOCOL_V0.md` — prospective hardening experiments and adoption gates.

## Canonical architecture

```text
M_t = (F_t, O_t, Π_t, C)
```

with:

```text
F_t = one typed persistent General Epistemic Field
O_t = learned/composed/donor operators
Π_t = small domain-general executive
C   = external non-self-certifying constitutional boundary
```

No fifth subsystem is introduced.

## Domain rule

Language, science, mathematics, coding and planning are initially treated as:

```text
typed views + representations + operators + checker/authority interfaces
```

over one field.

A domain-specific core fork is allowed only after a registered falsifier demonstrates that the shared substrate cannot faithfully represent the required behavior.

## Strong-parent rule

Before any field novelty claim, reconstruct the strongest relevant parent/federation, especially:

```text
OpenCog AtomSpace / Hyperon
Soar
NARS
blackboard / production architectures
KG + Datalog + provenance + TMS/ATMS
incremental databases/dataflow
Construction Grammar / semantic parsing
active diagnosis / experiment design
algorithm selection / metareasoning
LLM + memory + tools
```

`PARENT_PRODUCT_SUFFICIENT` is a successful scientific terminal.

## Language rule

OCM should aim for strong LLM-like communication, but language remains a bidirectional field interface:

```text
utterance -> candidate field proposals -> cognition -> intended field obligations -> utterance
```

Surface fluency does not grant semantic correctness or authority. A large parametric realizer is a valid reference and possible negative boundary.

## Quantum rule

Quantum research contributes only mechanisms that survive domain-neutral mapping and strongest classical-parent subtraction. Physical QPU use is strictly downstream and requires full end-to-end access/resource accounting.

## First execution sequence

```text
GEF-E0 substrate extensibility
GEF-E1 current-KSO first refusal
GEF-E2 ambiguity vs underdetermination
GEF-E3 bidirectional language interface
GEF-E5 cross-view revision
GEF-E6 resource responsibility
GEF-E7 same executive across domains
GEF-E8 lifetime k << N
GEF-E9 strongest parent federation
GEF-E10 language fluency frontier
```

Do not expand architecture before these tests identify a missing mechanism.
