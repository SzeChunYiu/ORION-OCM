# Source-design tightening response

Addressed the coordinator's review without adding outcomes or implementation.

1. One lemma and two held-out families are a conditional witness target. The
   training-only check may find no viable allocation; counts are not readiness quotas.
2. Three-node revival is a proposed fixed branch. The actual pinned execution
   protocol must decide it; no final registration or execution is claimed.
3. All-alias concerns new-theorem acquisition at the screened scope, not all
   learning. Empty eligibility remains. Search telemetry can motivate a separately
   registered EBL/control-selection diagnosis.
4. ALIAS-SCREEN.md requires bounded coverage, UNKNOWN versus screened negative,
   every ordered premise occurrence and native-valid repeated/unused hypothesis
   use without assuming a bijection.

Original documents are preserved separately in
`../typed-next-parent-design-v1-original-01/`.
FILES.json binds the successor and records the original manifest hash.
Every document remains at most 150 lines. No corpus outcome, learner,
verifier, experiment or test was executed.
