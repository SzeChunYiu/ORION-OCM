# Source and retained authored-control acceptance

Verdict: SOURCE_AND_AUTHORED_CONTROLS_ACCEPTED; actual joined authority and caller pending.
Concrete blockers: none found within the requested scope.

The reviewed request is e6861977b7d9bdc476aa0c815138b8131732a2270555389f293e53d5236da3b5,
the current source freeze is 25e05de22bcfbbf0faaa769f3e2d65c8845f6d5e53f1e78bba4573618afce452,
and the 125-member FILES.json is c391070c299cfa2c1d408a707fbf59bd680c60ecf56f67605c2258a0d0f04092.
All 125 file identities and all 31 current source-freeze entries reconciled.
All 18 origin identities reconciled, including 14 byte-identical copies.

## Source assessment

- goal_library.py:13–42 binds independently supplied manifest/receipt identities,
  preserves every ordered base proof and exact axiom declaration, and permits
  precisely the declared cohort append. It rejects private emitted-label collisions.
  Receipt bytes establish custody only; their contents are not interpreted as a
  native admission certificate. The complete detached inventory remains resident.
- goal_solve.py:10–21 accepts only goal, ordered premises, typed context and
  finite positive limits. No official target proof, trace, target label, route or
  expected emission is accepted. The resident cohort's conclusions are declared
  shared hints; its proof bodies are not used as a target-proof oracle.
- goal_solve.py:44–80 normalizes hint statements by indexed mandatory floating
  order, leaving original contracts/proofs and grounding substitutions intact.
  Enabled, resident-disabled and restored use the same bank, resident grounding
  and syntax materialization before cohort action ablation. Their eligible
  action sets intentionally differ only at that ablation. Nominal cohort
  eligibility is distinct from actually grounded or consumed actions.
- Existing finite_search count/replace/order/search function ASTs are unchanged.
  Its bounded emitter retains ordered floating and essential occurrences; the
  new argument classifies the three private floating labels. native_match only
  injects the syntax callback at its old type-check seam: ValueError means
  complete syntax nonmembership; SyntaxUnknown propagates to UNKNOWN.
  The four bank functions and WFF_OPS are AST-identical to the named donor.
  The accepted syntax/cache/work/context modules are byte-identical.
- goal_solve.py:83–101 restricts a supported negative to the registered finite
  bank and tree-decision bound. Unsupported parent actions turn failed search
  into UNKNOWN_PARENT_SCOPE. Resource/syntax/materialization failures are UNKNOWN.
  A generated normal proof is explicitly pending native verification.
- goal_native.py:8–55 recomputes cohort consumption from the generated proof and
  later the current native USED set. Tampering with the summary cannot erase
  use; the disabled mode refuses actual cohort proof labels. Exact result,
  task, library, issued query/premises, proof labels and fresh claim bind before
  the call. Current prefix bytes, archived result/request/suffix/log/database,
  full replay and current USED contracts bind through the unchanged wrapper and
  AST-identical result-binding helpers. A saved native flag is insufficient.
  Issued-claim rejection requires the complete expected prefix first.
- cohort_suffix.py:8–50 changes only scoped essential labels and their proof
  occurrences. It records original and transported identities and checks exact
  statement/type/floating order, proof, native frame and unchanged axiom inventory
  by source indexing. Repeated local essential names across members receive
  distinct cohort-e-INDEX-SLOT names; collisions refuse. Input objects remain
  unchanged. Index/frame checks are structural, not native proof checking.

## Retained authored controls

The qualification is four preserved generations, not twelve tests rerun against
the final generation. Source snapshots, process/control/stdout/stderr identities,
pre/post pins and actual file-backed module paths reconcile in the retained records.

| Generation | Cases | Child PID | Stable pre/post pins | Module paths | Child wall s |
|---|---:|---:|---:|---:|---:|
| 01 | 8 | 2643702 | 2073 | 121 | 0.144272769 |
| 02 | 1 | 2648252 | 2075 | 121 | 0.097884402 |
| 03 | 1 | 2652593 | 2078 | 50 | 0.023756702 |
| 04 | 2 | 2667710 | 2081 | 120 | 0.105339142 |

All four recorded children used isolated Python 3.11, cwd /tmp, exited 0 and were
reaped. These child windows are not acquisition or lifetime costs. The suite
history remains exact; it is not a future concrete runtime/source freeze.

The first eight controls exercise exact three-arm emission, composite/repeated
substitution, oracle-field refusal, ordered library custody, bounded/UNKNOWN
outcomes, claim binding, native-boundary refusals and detached input/scope handling.
Successor 02 covers nonfinite wall refusal, proof-derived use despite a tampered
summary, disabled/USED mismatch and view costs. Successor 03 checks reserved labels.
Successor 04 independently expects ABC emissions from an ABV resident frame and
same-bank/grounding/syntax across arms, plus two scoped members sharing an old
essential label, distinct renamed proof occurrences and a collision refusal.
These are meaningful authored controls. Native-shaped records and patched
native-call guards are test doubles; no successful native call is established.

## Remaining integration boundaries

1. Bind exactly one declared cohort and transport map to its proof/contract/origin
   receipts. No larger cohort union is required. Parent-supplied aggregates
   (22 three-class/empty-DV members, 20 ABC + 2 ABV, all three variables used,
   repeated local essential labels) were not independently read as retained rows
   and do not certify current native frames.
2. Freshly verify every ordered P1+C proof under the unchanged current 100-axiom
   authority. The old 96-axiom control and selected-claim receipts do not provide
   this joined authority. Library receipt custody alone cannot replace it.
3. Register goal-only tasks and dependency/exposure exclusions; use identical
   syntax banks and grounding across the causal arms. Retain all unsupported,
   exhausted, native-rejected and uncheckable outcomes with their costs.
4. Bind the actual isolated caller, mode, exact input/source/runtime identities,
   finite resources and create-only output. No actual invocation or gate is
   supplied by this tranche. The soft wall uses checkpoints, not preemption of
   every inner operation; future outer process bounds remain necessary.
5. The current interface has three homogeneous class/wff parameters and empty DV;
   suffix transport accepts base-only proof dependencies. Syntax/proof limits
   and the native 256-node/current-frame boundary can still refuse a generated
   proof. Library snapshot decoding is measured explicitly, not optimized away.

This review executed no target code and read no new retained task, cohort,
candidate, admission or held-out rows. It establishes source readiness within
these boundaries, with no actual learning, native admission, causal benefit or
novel-mechanism claim.
