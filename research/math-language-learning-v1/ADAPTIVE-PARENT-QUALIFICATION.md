# Authored adaptive-parent qualification

The current combined unary/learning run passed **277 tests, zero failures, errors or skips**.
It contains the existing 213 controls plus 64 new authored controls.
No registered study rows, generator, corpus operations or scientific results were executed.

## Current raw authority

The create-only successor root on billy-laptop is:
`/home/billy/orion-director-work/20260907/unary-adaptive-parent-accounting-qualification-v1`.

Final run: `04-final-formatting/{COMMAND.json,PROCESS.json,SOURCE-BEFORE.json,SOURCE-AFTER.json,tests.xml,stdout.bin,stderr.bin,source/,cases/}`.
The before/after/current/snapshot 246-file source/policy/test bindings match.
SOURCE-BEFORE.json SHA256:
`2f87ebefb85a0708d4c3a3698349e6bf69a175843d574771782342a2d4d18962`.
FINAL-HANDOFF.json SHA256:
`023d952c2d7a93320724cdd62fee919c0954d8b08d051b9b3632a064fb7c2000`.

The encompassing observed pytest subprocess took 18.567765476 s wall,
11.669980 s waited-child user CPU and 1.434573 s system CPU; exit 0.
These are engineering-run observations, not comparative parent/OCM speed results.
Recorder setup/source copying and post-run packaging are outside that subprocess span.

The accounting successor preserves 9 RED controls, then 37 focused GREEN and 03-full with 277 passes.
Staged whitespace preflight found one extra final newline; 04-final-formatting removes only that byte
and passed the same 277 controls against the exact final source. The 03-full generation remains intact.
The RED run reproduced discarded binding traversal counts and missing cold-use answer-check counts.
Internal use/selection validation now requires explicit work; the counted answer wrapper records
actual calls before invoking the checker. Public pure wrappers retain their compatible API.
Eight production files and one new test differ from the 268 generation; mathematics is unchanged.

## Preserved predecessor

The original root remains unchanged:
`/home/billy/orion-director-work/20260907/unary-adaptive-parent-qualification-v1`.

Its 08-full run passed 268 controls against 245 inputs, at source-freeze SHA256
`ad63d5d9acb090bad75f1d405cefa650c631e14741b4f60cabd08b29ff14b8e1`.
Its HANDOFF.json is `b973598552819a1f769b42855621d7d9639e04a06e491b34b2c446cba1d921a1`.
The successor rechecked all 3,070 original regular files and 119 symlink targets unchanged.
Predecessor post-run notes are retained under the successor's `predecessor-notes/`.

| Original run | Actual outcome | Scope |
| --- | --- | --- |
| 01-red | 17 failed | Expected missing selector/parent APIs. |
| 02-focused | 7 passed, 10 failed | Fresh schema counter keys versus inherited zero-delta keys. |
| 03-focused-repair | 17 passed | Exact mathematical comparison plus accounting validation. |
| 04-joint-process | 49 passed | Shared integration and actual process parity. |
| 05-closure-and-custody | 45 passed | Conventional closure/cache and resealed support checks. |
| 06-empty-and-failure | 8 passed | Empty-library, partial work and state/accounting controls. |
| 07-final-targeted | 21 passed | Custody and failed-use-write continuation controls. |
| 08-full | 268 passed | Preserved predecessor, superseded for current accounting. |

The reviewer identified conventional closure/cache costs and earlier missing revalidation counts
by source inspection, followed by repair and controls; no unobserved RED is claimed for those findings.
The original custody inventory records two corrected metadata-helper errors separately from tests.

## Actual process and source scope

The paired control retains 16 completed/reaped process receipts: A, B, C, cold B-history audit,
withdrawal, fresh solve, reinstatement and fresh solve for each adaptive arm.
The two A runs independently mine/select and agree on pool, ranking and selected-library identities.
B uses actual recipes with empty initial caches then warm repeated calls; C starts from A copies.
The conventional child imports neither the ocm nor orion_v2 packages; it uses the fixed raw-loaded
generic ledger/canonical modules. This observed import boundary is not containment qualification.

New helpers separate shared execution/postings/use verification, measured selection/support checking,
pure versus outer inventories, fixed arm dispatch and conventional payload/store/WAL/runtime handling.
The seven planned method adapters changed; pure utility separation also changed the profile import
without changing its interpreter schema, path/hash/flag checks. Current profile controls pass.
Every new helper belongs to its relevant closure; broad launcher custody covers both arms.
No existing semantic solver, core ledger, KSO, OCM runtime or old archive bytes were edited.

The three ADAPTIVE-PARENT notes are post-run descriptions outside both runtime inventories.
They do not rewrite either source freeze. Independent final review precedes commit;
scientific launcher qualification, registered execution and final analysis sealing remain future work.
