# Fixed launch qualification

The current source passes 110 selected authored controls and seven real namespace controls.
These qualify the [fixed launch contract](ASSAY-LAUNCH-CONTRACT.md), not a scientific result.
All execution occurred on billy-laptop with the pinned Python 3.11.14.
No registered master, task partition or scientific STARTED was created.

## Current source and portable controls

External root: /home/billy/orion-director-work/20260907/unary-assay-deadline-qualification-v1.
SOURCE-FREEZE.json SHA256:
`87373941b49ffe267832a817399efb096023d4abe93b41f6b9d7b2b438307be7`.

Run 25-final-selected: 110 tests, no failures/errors/skips, exit0, 15.661416828 s outer span.
Its 376 recorded source inputs match their snapshots, before/after observations and current files.
The manifest is a declared input set, not the whole repository tree.
It comprises 36 deadline controls, 22 namespace/custody controls, 18 cost-data controls,
and 34 selected inherited resource/profile/source-copy controls on the current successor.
The external test recorder excludes its own initial snapshot and final receipt persistence
from that wall observation; it is not a complete execution-lifetime measurement.

The 20 changed/new Python files are individually bound by FINAL-PREFLIGHT-v3.json.
All are at most 200 lines. Staged whitespace was checked before the final run.
The unchanged privileged helper, semantic solver, miner, selector, coordinator and six
normative policies preserve their earlier authority; changed source generations are additive.
Earlier 23-source resource receipts and their archived guard are not silently rebound.

## Current actual enforcement

External root: /home/billy/orion-director-work/20260907/unary-assay-python-profile-qualification-v1.
FROZEN-CONTROLS.json SHA256:
`6f07eb09c2b37724a5002159d90f3c8c7e6d6ea3f863d45f0f8fc3088c6b4ca0`.

Cases 21–27 preserve 375 profile-recorder inputs before/after and in snapshots.
This is the exact 376-input portable set minus historical plan-SOURCE-REFERENCES.json;
all shared bytes agree. The actual launch plan contains 251 runtime source files.
Each preparation independently copied/read back all 734 stdlib files and its empty landmark.

| Authored control | Observed result |
| --- | --- |
| Normal source Python/restart | Parent and fresh child complete; singleton CPU 0 |
| Network | Socket creation raises PermissionError |
| Source write | Read-only filesystem refusal, errno 30 |
| Undeclared native executable | PermissionError, errno 13 |
| Native-library load from writable copy | Authorized original loads; identical copy's executable mapping fails |
| Original 2 s deadline with live descendant | RESOURCE_STOP / EXTERNAL_DEADLINE_EXPIRED; observed returncode -15 |
| Aggregate memory | RESOURCE_STOP / MEMORY_LIMIT; observed returncode 137, kernel OOM 1, peak1,073,741,824 bytes |

Native-library load refusal does not imply that ordinary read-only data mappings are denied.
The deadline's final profile custody is POST_DISPATCH_CUSTODY_FAILED after expiry;
its nested resource stop, original cause and observed return code remain available.
Enforcement is sampled; these are observed outcomes rather than zero-overshoot promises.
Every case retains actual limits/raw streams, empty members, reaped process and removed
controllers. Readback also observed the recorded host PID and controller paths absent.

## Preserved development and corrections

Portable histories 01–25 retain all failed, repaired and inherited generations.
Recursion/escaping-validation observation, post-write clock custody, parent fsync,
native denial verdict and bounded cost-reader failures were reproduced then repaired.
Run 18's two inherited-test failures came from the new fixture's module-registry pollution;
new fixture teardown restored prior modules. The unchanged production control semantics
passed in successor 19 and final 25.
Cost controls retain physically compatible clock spans, pre-open input bounds,
complete required reads before wall promotion, and unchanged semantic-analysis binding.

Actual histories 01–27 retain all earlier profile work.
Run 09 reached and persisted both acquisitions, then the existing strict stderr gate refused
Python's missing-platform-directory warning. Run 10's nested landmark mount was read-only;
run 11's separate-file workaround lacked directory-read access. The approved assembled
source-only stdlib fixed that layout without relaxing the policy or modifying the snapshot.

Historical runs 12 and 20 each completed the same 52-process, one-episode authored lifecycle.
Their episode is CHECKED_CAUSAL_REUSE_APPARATUS with PARENT_SUFFICIENT;
whole apparatus remains CANNOT_CHECK because complete_count 1 is less than expected 4.
They precede the final isolated cost-reader changes and retain that historical source scope.

HISTORY-READBACK.json in each external root binds every actual process/XML/source history.
RAW-MEMBERS.json records the regular files and literal links present before its own creation:
portable 45,492 files/852,151,478 bytes plus 357 links; profile 31,161 files/446,862,368 bytes.
These maps are raw readback snapshots, not compressed archives or claims about future files.
The readback-script corrections are retained separately and changed no product result.

## External observer and empirical boundary

The [cost protocol](ASSAY-COST-RECEIPT.md) has separate PREPARATION/LAUNCH authority.
Its fixed external wrapper and exact authored lifecycle are bound separately from this
source/test set. A future unused scientific seal requires its own binding before dispatch. No arbitrary command or input-driven entrypoint is added.
GNU time process wall has 0.01 s resolution; cgroup CPU and outside/waited CPU stay separate.
Full CPU/lifetime economics remains ECONOMICS_CANNOT_CHECK and earliest crossing UNAVAILABLE.
No data-only accounting supplement reruns or promotes the retained semantic analysis.

## Current fixed capture lifecycle

The final separate roots are /home/billy/orion-director-work/20260907/
unary-assay-final-capture-{qualification,prepared,observer}-v1.
CONFIG-v2 binds the reviewed wrapper dcd566d30e33bf3d9b95104901c74a5cc1dc9b8e7f211d80ce2617a37f951aba.
The original undispatched CONFIG-v1 remains retained.

Both fixed phase commands exited 0. The same authored one-episode input produced 52 distinct
fresh process lifetimes, all with singleton CPU/thread affinity 0 and unchanged 251 source maps.
No coordinator abort occurred; all cleanup paths were empty/removed and observed host PID absent.
The retained episode is CHECKED_CAUSAL_REUSE_APPARATUS / PARENT_SUFFICIENT;
whole apparatus remains CANNOT_CHECK (1 of 4 expected episodes), as do its economics.

GNU time observes PREPARATION 2.12 s and LAUNCH 27.45 s at 0.01 s resolution.
Their measured phase sum is 29.57 s; observer retention and between-phase work are separate.
Cgroup CPU is 22,782,779,450 ns and peak memory 116,072,448 bytes.
Those CPU observations are not added to outside/waited CPU as a complete total.
COST-CLOSURE.json SHA256 118ebd59bbac9be888dbf34dd066c790064d17b7be5548700f7312cf138697be
passes its real bound-data no-alarm while preserving ECONOMICS_CANNOT_CHECK.
READBACK.json is bound below; it checks every current source, all 52 child/result/raw bindings,
the 734-file copy lineage and retained analysis SHA256 5bd398041aaa026becc102ac73c54ada32046e7de1c007c540af2d4d0d5504fb.
READBACK.json SHA256: `ff92a7d112e973b61a91ecc640c66a6c28954dcb9596cfbbf4474e1f20c60561`.
