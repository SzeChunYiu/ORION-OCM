# Runtime evidence — read first

**213 authored controls pass** on 213 source/test bindings. They exercise checked rule admission, actual process exit/restore, OCMRuntime.solve invocation, independent checking, knockout and revision. This is not a registered scientific study or matched-parent advantage.

- [Result](RESULT.json), [archive index](INDEX.json) and [current source map](SOURCE_FREEZE.json.gz).
- [Costs](COSTS.json), [retention limits](OMISSIONS.json), [readback](READBACK.json) and [seal](SEAL.json).

historical.tar.gz preserves the 18-run 206-control generation. profile.tar.gz preserves three later runs, explicit interpreter profiles, relocation and inventory-helper correction. These overlapping generations are not added into a larger test count.

independent-controls.tar.gz retains the actual predecessor missing-use defect and clean control. independent-reviews.tar.gz retains both cleared source/raw audits. release-custody.tar.gz retains source commit and integrated rebase records.

All 5,531 regular original members are included; 141 symlinks are literal metadata in compressed tree maps, never followed. Present case ledgers and child records remain exact. Archived scripts are data and were not executed during packaging. Omitted interpreter/host bodies are not revalidated.

The former semantic and first-learning packages remain unchanged, with their own historical scope. This package does not establish a live restored store merely by archiving its bytes.
