# Authored coordinator qualification

The current combined run passed **424 tests**, with zero failures, errors or skips.
It qualifies the internal coordinator and retained analysis with authored inputs.
It does not execute the four-episode registered comparison or qualify its external entry.

Read the [contract](ASSAY-COORDINATOR-CONTRACT.md) for APIs and claim boundaries.
The exact six [v2.1 policies](../../docs/plans/unary-adaptive-parent-v2-1/CORE.md) are unchanged.

## Current source and evidence

Current formatting-successor records are on billy-laptop under:

`/home/billy/orion-director-work/20260907/unary-assay-coordinator-formatting-qualification-v1/`

- `01-combined/`: command, explicit fresh pytest basetemp, source snapshot, before/after
  inventory, stdout/stderr, XML, process observation and complete current authored cases.
- `SOURCE-FREEZE.json`: 281 exact current source/test/policy inputs.
  SHA256 `b907aab6e1b360ed9df775c440102eea57e896e6d82eaa6cbde41cc59949cba2`.
- `FINAL-HANDOFF.json`: current source/result/process readback, exact 14-file Python
  EOF delta and 267 unchanged source identities relative to the prior 281-input freeze.
  SHA256 `a7dd1b80c0ba0f2080b4631d19adbd1122f50fe7b7f577c62f120b6665ea87ae`.
- `FORMATTING.json`: all 21 preserved preimages, staged-byte checks, 19 identical Python
  ASTs and the passing staged whitespace check before qualification.
  SHA256 `c16c2a31f39b19c8fca4482c1eca16c14aabae4e7f9d7b27505d78f895e1f5d1`.

The earlier 15 generations remain under sibling `unary-assay-coordinator-qualification-v1/`.
Its `HISTORY-BEFORE-FINAL.json` binds the first 14 histories and explicit historical omission:
SHA256 `7af1b94c878796d119b6bc31339f2eca9476bb616b9218507e37a7e193b29a3c`.
Its final handoff retains the 262 unchanged inherited Git source identities. The common outer
source enumerator remains the sole changed inherited file.

The combined recorder observed 154.924101593 seconds wall, 116.805862 seconds waited-child
user CPU and 7.689240 seconds waited-child system CPU. Source bytes matched before/after.
Its wall span includes dispatch/wait and final source readback; initial snapshot work is
outside that span. It is engineering-test cost, not a scientific arm's complete lifetime.

The independent full review of the pre-formatting 424-test generation is retained at:

`/home/billy/orion-director-work/20260907/unary-assay-coordinator-independent-review-v1/FINAL-CLEARANCE-v1.json`

SHA256 `c51a7185044c3be5b8db7462a6fdb33a50cb0736742579935c1e6508ff64e340`.
This predecessor remains historical authority; the current EOF successor has its own run.
This note is post-run documentation, outside the runtime source inventory.

## Actual authored execution

The varied A capacity fixture supplies 32 training and 16 development tasks before either
arm runs. It exercises four distinct universal core schemas, both query polarities,
multiple semantic supports and Boolean development context.

Both adaptive arms physically acquired and selected four rules, each supported by at
least two semantic groups. Pool, ranking and selected-library identities matched.
Current conventional/OCM child result documents were 591,424/599,820 bytes, respectively.
The unchanged individual JSON size/node bounds were sufficient for this fixture.
These tasks are authored controls; no registered stream or prospective partition was read.

The complete small lifecycle used **52 distinct actual child processes**:
two A acquisitions, four B batches, four C batches and 42 role mutation/solve calls.
All returned zero, were reaped, had absent process groups and ran sequentially.
B/C and role stores were independent copies with audited source/pre-dispatch identities.

Retained analysis rechecked all 52 calls, actual issuer/core state, answers and uses.
One rule was causally used on two distinct semantic authored rows by both adaptive arms.
The episode reports `CHECKED_CAUSAL_REUSE_APPARATUS` and `PARENT_SUFFICIENT`.
Knockout recorded zero recipe invocations. No required obligation was unavailable.

This is one authored episode, so the four-episode aggregate correctly remains
`CANNOT_CHECK`. Complete external cost closure is absent, so economics remains
`ECONOMICS_CANNOT_CHECK`. Endpoint observations do not establish earliest crossing.
The inherited actual B64/C128 process controls also pass in the combined run.

## Retained development history

| Generation | Actual controls | Interpretation |
|---|---:|---|
| 01 / 02 | 5 red / 5 green | Create-only ledger/copy API |
| 03 | 2 green | Actual varied A32/16 acquisitions |
| 04 / 05 | 1 failed, 2 passed / 3 green | Corrected mutation REVOKED versus OCM warrant DEAD |
| 06 / 07 | 6 red / 11 green | Reached use, replay cost, custody, cause, generation and root-mode fixes |
| 08 / 09 | 6 failed, 2 passed / 1 failed, 13 passed | Whole promised-artifact and negative-exit ordering; old test double corrected |
| 10 | 31 green | Analysis, precedence, economics and operational continuation |
| 11 | 3 green | Actual retained-lifecycle analysis; historical tamper omission below |
| 12 / 13 / 14 | 11 failed, 3 passed / 28 green / 30 green | Strict positive rows, required calls, lineage and episode failure origin |
| 15 | 424 green | Prior complete combined source generation |
| EOF successor 01 | 424 green | Exact whitespace-corrected current-source qualification |

Generation 11's final tamper assertion overwrote its first call report with `{}` without
retaining the preimage. That exact historical state remains; no artifact is reconstructed.
The current test saves TAMPER-BEFORE/TAMPER-AFTER and restores the exact original in finally.
Both generation 15 and the current EOF successor retain the original first report,
matching their saved preimage.

The first scoped commit stopped at its staged whitespace check: 14 Python files and the
contract had one redundant final blank line. The successor removes exactly those 15 LF
bytes. All owned Python ASTs are identical; no control or semantic implementation changed.
The staged 21-path whitespace check passed before the new combined run.

The retained-analysis obligation controls use explicit synthetic authenticated facts to
isolate their decision boundary. They are supplemented by the actual 52-process no-alarm.
Resealed exact-child malformed-result controls test the new semantic-auth boundary.
Absent/malformed promised positive structures refuse custody; invalid accepted answers
fail the semantic control; operational rows without results remain unavailable.

## Remaining entry seam

No source in this tranche issues the immutable external STARTED record, enforces the
aggregate resource profile, or supplies complete setup/cleanup/outer-lifetime cost closure.
Those are concrete next integration duties under the same original absolute deadline.
There is no scientific selection outcome, learned-grammar result, cost superiority,
broad transfer, novelty or corpus-proof claim in these authored controls.
