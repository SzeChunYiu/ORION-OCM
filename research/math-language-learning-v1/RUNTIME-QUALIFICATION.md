# Authored runtime qualification

## Current interpreter-profile successor

The current suite passes **213 tests, zero failures/errors/skips** on billy-laptop:
153 prior semantic/learning controls, 53 runtime controls and seven profile controls.
The launcher accepts a strict, separately configured Python identity and retains the
original laptop identity as default. Actual restart/process tests explicitly supply
their current interpreter observation; this is portable test support, not a hosted
CI result or host/scientific qualification. Parent and child mismatches refuse.
The local observed interpreter resolves to the same physical binary as the default;
another hosted binary is not claimed tested here. Current A/B/C records each show
126 imported origins, distinct reaped processes, and recipe counts absent/1/0.
B restores zero prior uses; C restores one, with zero callbacks before explicit bind.

Raw root: /home/billy/orion-director-work/20260907/unary-runtime-profile-qualification-v1.
19-profile-red records seven missing-profile failures; 20-profile-green passes all
15 profile/process/restart controls; 21-combined passes 213 with exit0 and empty stderr.
Final outer pytest wall is 9.146840950 s, waited child user 5.340880 s/system 0.647874 s.
The sum of these three recorded pytest spans is 11.994536048 s, not lifetime cost.
Final 213-source before/after freeze is unchanged, SHA256
b4b83038ea72e18edc80c6cbd9f748791d541a94b560e8a6696570996466e6a0.
SUMMARY.json SHA256 d4541e1046af6df87ece218eed00fe29ed39d8d7a5ac41b4d554dbe1307a48d6
binds 940 other regular files / 9,974,032 bytes and 40 symlink metadata entries.

19/20 completed before the separate-root instruction. Only those additions were
relocated; RELOCATION.json preserves original execution paths and byte identities.
Their raw command/path bytes were not rewritten. The original 18 runs and SUMMARY
retain their exact file set and bytes. A metadata-only inventory helper correction
(Path.readlink unavailable on host system Python) is retained separately in the new
root; it changed no test, runtime result or source qualification.

The 206-generation independent review remains historical:
/home/billy/orion-director-work/20260907/unary-runtime-independent-review-v1/REVIEW.json,
SHA256 718cd126ee93e700abc4ad883545212cf64d1e951165807a13374f1b1eb107ae.

The historical pre-profile combined suite passed **206 tests, zero failures/errors/skips**, on
billy-laptop. This includes the unchanged 153 semantic/learning controls and 53 new
runtime controls. No existing OCM or semantic/learning production module changed.
This is an engineering result, not a registered acquisition/transfer study.

## Historical 206-control record

- Base: 8eb24db059fb4e3982262bf4daf75f61eca47169.
- External raw root: /home/billy/orion-director-work/20260907/unary-runtime-qualification-v1.
- Historical run: 18-combined; tests.xml records 206/0/0/0 and 8.334 seconds.
- PROCESS.json records exit 0, empty stderr, unchanged before/after sources,
  outer wall 8.493122141 s, waited child user 4.808908 s and system 0.557033 s.
- SOURCE-BEFORE.json binds 211 historical Python source/test files:
  SHA256 71cb61cecd111328bf1b5862e02a518ed634c775a1b18b2224b3400274e05e42.
- SUMMARY.json is a raw inventory of 4,555 regular files / 39,801,192 bytes,
  all 18 recorded test invocations and their actual outcomes:
  SHA256 8473700e87b10f2c9e9fae9181c279940f10d7515ff653aeec59f8fff9e28415.
- The source snapshots, request commands, external basetemp cases, original stdout,
  stderr, XML and process records remain outside the worktree for later packaging.
  Existing parent receipts have not been rebound to these new sources.

The sum of recorded test process wall times is 26.316044898 s. This is not the
whole development lifetime, and it must not be added to nested process times.

## Actual A/B/C process evidence

Under 18-combined/cases/test_real_acquisition_exit_res0:
A/acquire runs the existing miner on two semantically distinct authored training
tasks, checks the acquired schema and admits rule data through actual KSO operations.
The parent waits for A to exit. B/solve starts a new pinned isolated Python
interpreter, restores the data without host callbacks, and constructs a checked
answer with a Boolean-expression substitution through actual OCMRuntime.solve.
C starts another interpreter, restores one recorded use, disables invocation and
returns a checked exact-parent answer with zero recipes.

B's actual method is
unary:method:3692392eead67cba2d5acefadda64eef0d651d7c9ac0016877bc8f9d0cbf55f4,
with cover [0,1]; C's method is null. Both yield the same semantic status.
Each child records 125 imported module origins, source before/after identities,
raw request/result/streams, interpreter/flags, PID and process timing. The launcher
records nonoverlapping lifetimes, exit and reaped/absent process-group observations.
The historical child launcher wall is its dispatch/wait span: initial/final parent source
scans and receipt assembly sit outside that span but inside the encompassing pytest
process observation. Nested times must not be added. These are authored process
facts, not a network/filesystem isolation qualification.

## Controls and retained corrections

| Recorded generation | Actual outcome and correction |
| --- | --- |
| 01 → 02 → 03 | Missing store API; then 9/11 failures from exact edge replay normalization; explicit Fraction(1) head weight fixed it, 11/11 passed. |
| 04 → 05 → 06 | Missing bridge; then constructor mismatch omitted the predicate registry; corrected call, 11/11 passed. |
| 07 → 08 → 09 | Missing process path; then one failed registry-observation accessor; corrected read, 3/3 passed. |
| 10 → 11 | 2/19 failures: registered callback replacement and unavailable backend detail; both repaired, 19/19 passed. |
| 12 | 3/3 recorder controls: spawn failure, actual harmless timeout and nonzero child. |
| 13 → 14 | Missing use intent/return timing, three failures; repaired persistence controls passed. |
| 14 → 15 | Lost preparation/attempt observations on refused matching; explicit partial-work retention repaired, 4/4 passed. |
| 16 → 17 | Post-child source-read failure lost PROCESS.json; retained failure/exit/cleanup repaired, 5/5 passed. |
| 18 | One combined pre-profile suite, 206/206 passed. |

Directory names containing green are historical intent labels, not substituted
success verdicts; the table and raw XML preserve their actual failures.

Independent actual crash reproduction is retained separately at
/home/billy/orion-director-work/20260907/unary-runtime-independent-controls-v1/RESULT.json,
SHA256 3672cf56f10f55bfc674de45522da2d80144813f21baf54da422b592247f1e9e.
The predecessor could commit core checking, fail before USE append and cold-restore
zero uses. The successor records USE_PREPARE before dispatch and refuses pending
cold use; completed refusals receive USE_REFUSED. No cross-ledger atomicity is claimed.

Other controls distinguish method UNKNOWN from query UNKNOWN, remove and restore
actual matched premises, separate discovery/utility/checker revocation, reject
forged EXACT_CHECKER-only atoms and changed task/rule/certificate/use/core references,
and verify cold history without implicit solving or callback rebinding.

## Warrant boundary and next work

The [runtime contract](RUNTIME-CONTRACT.md) defines the exact API, authority and costs.
The independent verifier checks each final answer. Discovery supports and an authored
selection policy are not new mathematical axioms or learned utility evidence.
The implementation shares the prepared exact parent and existing miner/matcher.

Still separate: the equally adaptive conventional persistence/dispatch arm, prospective
registered task stream, full launcher containment qualification and a scored causal
assay. This result establishes an executable checked-method loop only.

## Independent current review

The profile successor was independently checked without rerunning tests:
 /home/billy/orion-director-work/20260907/unary-runtime-profile-independent-review-v1/REVIEW.json,
SHA256 e5b10e1ba4922417991aa593dd25355dd02c9fd6bbfddf6e503a05e20b264360
(8,786 bytes). It verifies all 213 current bindings, the three profile histories,
940 raw members, actual A/B/C and mismatch refusals, and the unchanged historical set.
