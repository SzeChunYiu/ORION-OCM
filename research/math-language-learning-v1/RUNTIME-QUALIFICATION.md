# Authored runtime qualification

This page and its sealed package describe PR141’s historical 213-control generation.
The [adaptive-parent successor](ADAPTIVE-PARENT-QUALIFICATION.md) qualifies the current
shared modules with 277 authored controls; the original runtime package stays unchanged.

**213 tests pass with zero failures, errors or skips** on billy-laptop.
This is authored checked-method persistence and OCM invocation, not a registered
scientific assay, adaptive-parent advantage, scaling or broad language result.

Read the [runtime contract](RUNTIME-CONTRACT.md) for authority and cost boundaries,
and the [sealed evidence index](runtime-records/CORE.md) for exact original bytes.

## Current profile and source generation

The current suite comprises 153 prior semantic/learning controls, 53 runtime controls
and seven interpreter-profile controls. Both launcher and child check the explicit
path/hash/exact Python 3.11.14 profile; the original laptop profile remains the default.
Restart/process tests explicitly supply their observed current interpreter.
The observed local profile resolves to the default's same physical binary;
a different hosted interpreter was not tested by these local records.

The current three histories are under
/home/billy/orion-director-work/20260907/unary-runtime-profile-qualification-v1:

| Generation | Recorded outcome |
| --- | --- |
| 19-profile-red | Seven missing-profile failures |
| 20-profile-green | 15 profile/process/restart controls pass |
| 21-combined | 213 controls pass; exit 0, empty stderr |

The final outer pytest wall is 9.146840950 s, waited child user 5.340880 s and
system 0.647874 s. The three recorded pytest spans total 11.994536048 s.
These are process observations, not whole development cost; nested child times are not added.

The [compressed current map](runtime-records/SOURCE_FREEZE.json.gz) preserves the exact
213-file SOURCE-BEFORE bytes, identical to SOURCE-AFTER and their retained source copies:
SHA256 b4b83038ea72e18edc80c6cbd9f748791d541a94b560e8a6696570996466e6a0.
The source set includes OCM/orion_v2 and both research packages; it is not an imported-module count.
Original source commit 8995864e22174088d67d616fd638a357d697892f was rebased onto
main 9dabe10112c145c3e91243308afd540be1d20384 as c937431f3c83e46ce6a18dafbfd9b895215a2084.
All 213 qualified bytes remain unchanged. These are source integration identities,
not an assertion that the runtime release has merged.

## Actual process and use evidence

In 21-combined/cases/test_real_acquisition_exit_res0, A mines and independently checks
rules from two semantically distinct authored training tasks, admits the rule data
through actual KSO operations, persists and exits. B starts a new interpreter and
restores the actual ledgers with zero host callbacks and zero prior uses.
B then binds the generic dispatcher and constructs a checked Boolean-substitution answer
inside actual OCMRuntime.solve. C starts another interpreter, restores one recorded use,
disables invocation and returns a checked exact-parent answer with zero recipes.

Current A/B/C each record 126 imported origins, distinct reaped processes, raw
request/result/streams, source before/after and matching interpreter profiles.
B applies one recipe; C applies none. Their semantic results agree.
The launcher observes nonoverlapping lifetimes and absent process groups.
These observations do not establish filesystem, network or arbitrary-Python containment.

## Historical 206-control generation

historical.tar.gz preserves unary-runtime-qualification-v1's original 18 histories
and SUMMARY unchanged: 4,556 complete regular members / 40,652,032 bytes.
SUMMARY itself indexes the other 4,555 files / 39,801,192 bytes.
Its final 18-combined run passed 206 controls, with 211 equal before/after bindings:
SHA256 71cb61cecd111328bf1b5862e02a518ed634c775a1b18b2224b3400274e05e42.
The authoritative historical map is inside that archive; the current profile map is separate.

The historical final XML wall was 8.334 s; outer process wall 8.493122141 s,
waited child user 4.808908 s and system 0.557033 s.
All 18 pytest process spans total 26.316044898 s. Historical A/B/C recorded 125 imports.
The earlier 153-test learning and 110-test semantic releases retain their own scope.

| Historical generations | Retained correction |
| --- | --- |
| 01 → 02 → 03 | Missing store API, then exact edge replay normalization; 11/11 pass after Fraction(1) head weight |
| 04 → 05 → 06 | Missing bridge, then constructor predicate registry; 11/11 pass |
| 07 → 08 → 09 | Missing process entry, then registry-observation accessor; 3/3 pass |
| 10 → 11 | Callback replacement and unavailable-backend detail; 19/19 pass |
| 12 | Spawn refusal, actual harmless timeout and nonzero child; 3/3 pass |
| 13 → 14 → 15 | Use intent, return timing and lost refusal work; 4/4 pass |
| 16 → 17 | Post-child source-read failure lost PROCESS; 5/5 pass |
| 18 | Combined 206/206 pass |

“Green” directory names are historical intentions, not substitutes for actual XML outcomes.
These overlapping generations are not added into a larger distinct-test count.

## Retention and independent review

The [archive index](runtime-records/INDEX.json) binds five archives with all 5,531
regular original members / 51,140,565 bytes, including present cases and ledgers.
Literal targets for 141 symlinks are retained in compressed tree maps without traversal.
profile.tar.gz has 941 complete regular members / 10,157,412 bytes.
Its SUMMARY indexes 940 other members / 9,974,032 bytes; 40 links are metadata.
Profile runs 19/20 were relocated after execution. RELOCATION.json preserves their
original absolute paths and exact regular bytes; dangling aliases were not rewritten.
The separate Path.readlink inventory-helper failure/correction remains raw evidence.

independent-controls.tar.gz retains the actual predecessor orphan-use defect and clean
control: core checking could commit, USE append fail, and cold restore silently see zero uses.
The repair writes USE_PREPARE before dispatch and refuses incomplete cold use.
USE or USE_REFUSED completes that intent. No cross-ledger atomicity is claimed.

independent-reviews.tar.gz retains both audits and their read-only audit sources.
The current cleared review SHA256 is
e5b10e1ba4922417991aa593dd25355dd02c9fd6bbfddf6e503a05e20b264360;
the 206-generation review remains independently bound as historical evidence.
release-custody.tar.gz retains exact commit/rebase records.
[Readback](runtime-records/READBACK.json), [seal](runtime-records/SEAL.json),
[costs](runtime-records/COSTS.json) and [omissions](runtime-records/OMISSIONS.json)
define byte custody and its limits. Archived code was not executed while packaging.

## Hosted checks and remaining scope

The [shared workflow](../../.github/workflows/unary-language.yml) selects both complete
research packages with src and both package import roots, Python 3.11.14 and pytest 8.3.5.
It records actual hosted counts, including real restart through the explicit observed
interpreter profile. This workflow change alone is not evidence of a hosted pass.

Independent final answer checking, support revision, matched-premise removal/restoration,
cold history and failure retention are authored engineering controls.
Still separate: the equally adaptive conventional persistence/dispatch arm, a frozen
scientific task stream, full launch-profile qualification and a scored causal assay.

The independent [runtime archive and CI review](runtime-release-review-records/README.md)
checked the sealed package and workflow without repeating the authored tests.
