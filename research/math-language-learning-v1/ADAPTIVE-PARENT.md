# Adaptive conventional parent

The authored tranche adds measured development selection and a conventional checked-rule
store/direct dispatcher sharing OCM's actual proposal computation and independent checkers.
The combined suite passed 277 controls. This is engineering qualification, not a registered assay result.

Read [the contract](ADAPTIVE-PARENT-CONTRACT.md) for APIs, authority and cost boundaries,
then [qualification](ADAPTIVE-PARENT-QUALIFICATION.md) for exact generations and retained records.
The prospective six-file policy starts at [CORE](../../docs/plans/unary-adaptive-parent-v2-1/CORE.md).

Both adaptive arms physically solve/check training, mine, measure development trials and select.
The conventional arm uses one existing generic LedgerStore WAL and immutable content files.
It does not instantiate KSO/navigation/operator machinery. The OCM arm retains the actual registered
dispatcher, conditional request admission and SV checker path.

The service route shares warm expression-mask caches and immutable posting contents.
Each new task still receives a fresh, issuer-bound Prepared object and a full independent answer check.
Source/payload/revision checks remain real work; cached preparation never hides a completed query answer.

No semantic solver/core source, old sealed evidence, CI, corpus/materialization machinery or generator
was changed. No scientific task generation, outcome selection, economic comparison or assay run occurred.
The larger 32/16/32 prospective workload, process containment and final analysis seal remain later work.
