# AB crosswalk extension — v1 (`gmi-833-ab-terminology-harness-v1`)

The parent crosswalk
`research/gmi-833-tranche-ab-ac-lit/GMI_TERMINOLOGY_CROSSWALK_V2.md`
(blob `9f4d25a5f59cdf83f86bc484cda7efb46a8bc054`) **owns** the AB terminology
audit and covers **149 of the 153** comparison terms the AB rows themselves
name. This file supplies exactly the **3 residual comparison terms** the harness
measured as absent, in the parent's own 9-column format so they merge into the
same audit. The parent file is not edited by this package.

| # | legacy GMI term | proposed paper term | academic field | canonical term(s) | match | citations | definition | migration rule |
|---|---|---|---|---|---|---|---|---|
| 49 | machine species | model family; architecture family; algorithm class; configuration class; behavioral phenotype; equivalence class | ML; metaheuristics; algorithm selection | algorithm class; configuration class (the algorithm/configuration-space partition of Rice's algorithm-selection framework) | PARTIAL | Rice, "The algorithm selection problem", *Advances in Computers* 15, 1976, doi:10.1016/S0065-2458(08)60520-3 (algorithm space `A`); Kotthoff, "Algorithm selection for combinatorial search problems: a survey", *AI Magazine* 35(3), 2014, doi:10.1609/aimag.v35i3.2460 (algorithm/configuration classes as the selection domain) | an **algorithm class** is a set of procedures closed under a stated equivalence on behaviour; a **configuration class** is a set of parameter settings of one procedure. Both are the deflationary readings of "machine species": neither requires speciation, heredity or reproductive isolation, which the biological term smuggles in. Use them whenever the intended object is "the set of candidates the selector ranges over". | PAPER-RENAME: prefer algorithm class / configuration class when the object is a selection domain; keep model family / architecture family for representational families; use behavioral phenotype only with the equivalence relation stated |
| 50 | novel intelligence | novel implementation; novel architecture; novel algorithmic mechanism; novel model class; novel computational paradigm; novel capability profile | ML; philosophy of AI | **novel implementation** — the weakest rung of the novelty ladder: a new realization of an already-registered mechanism | PARTIAL | Lehman & Stanley, "Exploiting open-endedness to solve problems through the search for novelty", *ALIFE XI*, 2008 (novelty as distance in behaviour space, not in code); Rice 1976 (a new member of an existing algorithm class is not a new algorithm space) | **novel implementation** = the mechanism, model class and capability profile are all already registered; only the realization differs. It is the rung that a parent-reduction argument must *exclude* before any higher rung may be claimed, and on its own it licenses no scientific novelty claim. | QUALIFY: state the ladder rung; `novel implementation` is never sufficient for a novelty claim |
| 51 | verification | formal verification; empirical validation; evaluation; testing; certification; verifier feedback | formal methods; program synthesis; software engineering | **verifier feedback** — a counterexample or witness returned by a checker and consumed by a search loop | PARTIAL | Solar-Lezama, "Program synthesis by sketching", PhD thesis, UC Berkeley, 2008 (counterexample-guided inductive synthesis: the verifier's counterexample is the feedback signal); Alur et al., "Syntax-guided synthesis", *FMCAD* 2013, doi:10.1109/FMCAD.2013.6679385 | **verifier feedback** is the CEGIS signal — a concrete counterexample or certificate handed back to a synthesizer. It is neither formal verification (which discharges a specification) nor evaluation (which scores behaviour): it is an *oracle query inside a search loop*, and a result obtained with it is only as strong as the specification the verifier checks. | RENAME-in-place: say "verifier feedback (CEGIS-style counterexample)" and never let it stand for having verified anything |
| 52 | hypothesis class / possibility space | hypothesis class; search space; program space | learning theory; program synthesis | **possibility space** (the vague legacy phrase this row is compared against); hypothesis class; program space | NON | Vapnik & Chervonenkis, "On the uniform convergence of relative frequencies of events to their probabilities", *Theory Probab. Appl.* 16(2), 1971, doi:10.1137/1116025 (the hypothesis class is the object with measurable capacity); Gulwani, Polozov & Singh, "Program synthesis", *Foundations and Trends in Programming Languages* 4(1-2), 2017, doi:10.1561/2500000010 (program space) | **possibility space** has no established academic referent: it names neither a measurable capacity class nor a syntactic search domain, so nothing can be proved about it. It is a NON-match and must always be replaced by the object actually meant - hypothesis class when capacity is at stake, search space when the search domain is at stake, program space when the candidates are programs. | RENAME: never use `possibility space` in paper-facing text |

## Coverage after extension

153 of 153 AB-row-named comparison terms are covered: **149 by the parent
crosswalk, 4 by this table**. The split is recomputed by
`ab_harness_v1.py` on every run and asserted in `test_ab_harness_v1.py`, so it
cannot drift silently.
