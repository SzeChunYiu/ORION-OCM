# Issue 165: production construction learning and shared-lineage integration v1

Status: prospective **engineering/development protocol**, not independently reviewed or protected E3 research. Does not supersede frozen #203, #205, or #206. No issue automatically closes.

## Motivation and parent

Source-pinned #206 review (`1eb80592b25be08e3e451bee3464d363b2908fba`) found that the L1 fixture drops number/prefix tokens, ignores the skill pattern and lexical readings, hashes only part of persisted state, and equates ordinary-parent parity with self-reload. Retain those original observations. Replace the mechanism, not the exposed outcomes.

Use main `4853a5d0f0d3ca5170aaa2010e4919ae5b88551d`: existing VersionSpaceLearner/construction acquisition, lexicon, chart parser, enumerating matcher, meaning canonicalizers, OCMRuntime admission/learning/solve/revoke/reinstate/replay, and existing goal-only native proof bridge. No new cognitive core or constitutional change. Register domain data and typed adapters outside Pi.

## Ordered stages on one persistent OCM root

1. Seed declared lexical meanings and an explicitly AUTHORED/IMPORTED native proof fixture. No learned status for either.
2. Acquire modifier/NP order from aligned demonstrations, selecting between prefix and postfix form hypotheses using the existing construction learner. Persist learned selection, full typed payload, support and source identity in the OCM field.
3. Acquire number/NP order separately, using the previously acquired modifier construction as a declared dependency. The learned number must contribute a HAS_COUNT value; recursive modifier use must preserve every property and consume the entire utterance.
4. Execute later utterances and formal proofs in separate OS processes, reopening the same complete OCM root. Revoke and restore modifier support, number support, and an unrelated lexical support separately. Old formal competence must remain intact under language-only revision. These are acquisition/revision stages, **not three G6 self-evolution generations**.

Learned hypothesis selection, taught semantic templates and lexical priors have distinct origin tags. No training-label leakage through TEST word lists in an executable recognizer. No externally stored bundle acts as a second truth store; records used by the OCM arm come from the admitted field, with exact content and support validation. A process request identifies its expected previous field/event/evidence state and may not silently reset it.

## Frozen domain/data rule

Lexical prior: adjectives red/blue/green/yellow, nouns cube/sphere/pyramid/cone, and numeric determiners two=2/three=3. Meanings use entity-rooted HAS_PROPERTY and HAS_COUNT edges. The noun-to-NP base and semantic template language are taught priors. Learned form alternatives are ADJ NP vs NP ADJ and NUM NP vs NP NUM. The modifier constructor may recursively produce NP; the number constructor produces counted NP.

Modifier demonstrations: red cube, blue sphere. Number demonstrations: two red cube, three blue sphere. All demonstration meanings are retained. No successful held-out utterance is a training example.

Evaluation: all 16 adjective/noun pairs minus the two demonstrated pairs; each of those 14 pairs prefixed by each numeric determiner (28 rows); two recursive modifier sequences with counts; two long modifier sequences requiring exact tree canonicalization beyond seven nodes. Also evaluate unlicensed-prefix, reversed-order, repeated-number and unknown-lexeme controls. Keep every row. No changing examples after looking at results; fixes to the implementation remain labeled development.

The formal proof fixture is the existing authored goal-only bridge control, not the historical 22-method cohort or fresh N4 science. Retain exact native verification archives and actual consumption. Its purpose here is noninterference and same-field continuity across language development, not a new proof-learning result.

## Comparators and checks

Execute an ordinary persistent parent with the SAME existing learner, lexical priors, template language, parser, exact checker and data. Run reset/no-construction, modifier-removed, number-removed, restored and unrelated-revocation controls. Do not replace this parent with a training-pair lookup. Compare complete populations and canonical meanings, not zipped shared prefixes. A parent tie is parent sufficiency, not architectural advantage.

The production chart parser generates candidate meanings. The existing independently structured enumerating matcher rechecks full consumption and meaning agreement; a separate typed graph rubric verifies preserved lexical/count content. Neither ranking nor partial consumption licenses a committed answer. Missing/ambiguous/contradictory training, non-live dependencies, stale lineage leases, altered payloads and source changes must refuse. UNKNOWN is not grammatical impossibility.

## Resource/evidence boundary

Record whole child-process wall and CPU, worker sections, Linux process peak RSS, state/artifact bytes, chart work where available, native calls, outcomes and retained source hashes. Nested wall intervals are not additive; RSS is a maximum, not a sum. Acquisition, rechecking, persistence and restart remain separately visible. No artificial work counter is called physical time, no acquisition/payback or strongest-neural-parent claim without the corresponding measured comparator.

No independent expert authorship is claimed for the four review perspectives (learning/theory, runtime/persistence, measurement/parents, skeptical inference) applied by this author. #38/#144/#49 protected replication remains locked. The intended terminal is a bounded integration disposition based on actual outputs, never ORION_OCM_PROGRAMME_SCIENTIFICALLY_CLOSED.
