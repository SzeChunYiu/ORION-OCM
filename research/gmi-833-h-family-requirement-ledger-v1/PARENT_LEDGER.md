# Parent ownership disclosure

Assimilation-first: every parent below is absorbed, its claim ceiling carried forward
verbatim, and the residual contribution of this tranche stated after.

## What this package does NOT claim as novel

- **The eleven-requirement gate list.** It is the Section H preamble sentence of Issue #833
  itself. The ten-gate operationalization is
  `research/gmi-833-h-neutral-four-family-v1/FAMILY_GATE_LEDGER_V1.json`; the eleven-coordinate
  reading is PR #997 `FGS-1`.
- **The 43-row registry and the row-to-contract map.**
  `research/gmi-833-h-obstruction-census-v1/FROZEN_FAMILY_REGISTRY_V1.json` (#987 / #986). This
  package reads it verbatim and adds no row and no contract.
- **The obstruction theorems** `H-EXP`, `H-RES`, `H-ID`, `H-POS`, `H-REMINT`, `H-CENSUS`.
  Same package.
- **The four-family neutral recovery** and its frozen predictions, lower bounds, resource
  crossovers, remints and source-separated oracle. `gmi-833-h-neutral-four-family-v1`.
- **The soundness theorems** `FGS-1`..`FGS-6` — fail-closed conjunction, scope-gluing no-go,
  finite-evidence non-promotion, observational family non-identifiability, remint/oracle
  controls, reconciliation consequence. PR #997
  `research/gmi-833-h-family-gate-soundness-v1`, open, owned by another lane, cited at
  blob-pinned commit `4abe5c4f` and otherwise untouched. `FGS-1`/`FGS-2` are owned upstream
  by conjunction introduction/elimination and indexed predicates; `FGS-3` by
  underdetermination of theory by finite data and elementary diagonal extension; `FGS-4` by
  observational equivalence, automata minimization (Moore 1956; Hopcroft) and latent-variable
  non-identifiability.
- **The 11/11 blind recovery** of `K01..K11` with labels and fingerprints hidden, two
  materially independent routes per family, one generic primitive basis, priors disclosed.
  #931–#937 / #951, `research/gmi-833-aj9a..aj9h`. Its parent anchors are the families'
  own literature: McCulloch & Pitts 1943; Rumelhart, Hinton & Williams 1986
  (doi:10.1038/323533a0); LeCun, Bottou, Bengio & Haffner 1998 (doi:10.1109/5.726791); Elman
  1990 (doi:10.1207/s15516709cog1402_1); Vaswani et al. 2017 (arXiv:1706.03762);
  Knuth & Bendix 1970 term rewriting.
- **The `M0..M6` maturity ladder** and the corpus fact that it holds no `EV4`/`EV5` evidence.
  `research/gmi-833-maturity-rescore-v2-v1` / `-v3-w4-v1`.
- **The post-hoc clustering and non-forcing map** `MAP-1` / `UNKNOWN-1` of #999 / #998,
  `research/gmi-833-finite-posthoc-clustering-v1`, which demonstrate that a scoped,
  prior-disclosed post-hoc family mapping is performable — the constructive counterpart to
  `FGS-4`'s prohibition on identity-from-equivalence.

## Residual contribution of this tranche

1. A **total, scope-indexed status matrix** over all `43 x 11` (row, requirement) pairs —
   649 cells, each citing a primary artifact or declaring itself unadjudicated. No prior
   artifact covers more than 4 of the 43 rows.
2. The **split of the collapsed grammar/no-macro gate** into two separately witnessed
   coordinates, resolving the 11-versus-10 discrepancy between `FGS-1`'s prose and the
   ten-gate ledger from the ledger's own evidence pointers.
3. The **evidence-backed 43-to-10 contract collapse**, making explicit that census evidence
   is contract-specific rather than row-specific.
4. The **verified absence** of any primary K-family-to-named-row binding, and an explicitly
   agent-constructed, unadjudicated candidate map with 10 rows uncovered and two rows
   multiply covered.
5. The **`R10`-versus-`M5` correction**: independent *search* is met everywhere; independent
   *replication* is the zero-instance tier. Conflating them mislocates the residual.
6. The observation that at both evidence scopes **`R01` and `R08` share one evidence key**
   and are not independently witnessed.
