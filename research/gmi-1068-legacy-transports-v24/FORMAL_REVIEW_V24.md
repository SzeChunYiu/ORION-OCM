# V24 — independent formal review

The paper author independently read all13 new Lean modules, the seven immutable
import interfaces, explicit typed contract, replay checker and final formal scope.
This is independent implementation/proof review, not a second paper authorship.

## Actual fresh replay

Independent isolated Lean4.19.0 SOURCE then AUDIT replay: PASS.
It compiled20 sources and checked138 exact typed registrations with
warningAsError and printed axiom inspection; no placeholder proof was accepted.
The isolated rebuild avoids reliance on previously produced object files.
Detailed local replay record: /tmp/gmi-v24-independent-kernel.json on billy-laptop.

Audit SHA256:
9d050135400b4992aaa31debe3ddd4157aedd9afc306dade237e52d765d92a69
proof_contract_v24.py SHA256:
748d00727038270f7702691854d4f9a63288f1be1f880d8ad561c7bf66259a36
check_lean_v24.py SHA256:
e01ef87525aa794e0f71ca42c60cdc2f94d0a13c740102b8e816f157825a8cea
FORMAL_SCOPE_V24.md SHA256,156 lines actually read:
95eb0cbb489d824bd5455d5af1a3bf5364718c8c061a80239ee3519c3c0fb4b2

## X1: images, witnesses and actual profile Contexts

ImageTransport unfolds actual V20 Attained with actual V15 Context. Its iff
extracts an active selected witness from image equality and reconstructs equality
from those witnesses. It does not assume that selector B already includes only
active histories; P and evaluator definedness remain explicit.
Terminal sufficiency uses both active factorization and active terminal coverage.
The final injective_boundary needs injectivity only on active P intersect E.

ProfileContexts binds every five-field profile coordinate and proves history
identity. Its code/decoded Context equation is actual V17 postcomposition, with
illegal/undefined/value transport. AFErasure binds List.map erasure and the actual
provenance union/presence operations; repeated labels share the tag function.
The repeated-presence and erasure-collision controls are actual kernel statements.

AFContextBridge.fullContext denotes the ERASED profile evaluated on raw histories.
Its image is the old-profile image of the erased active domain. It does not claim
an inverse for erasure, a valid-path recognizer or a BFS algorithm proof.
RichAFContexts separately retains (raw endpoint/edge-word,old profile), proves
identity retention and actual postcomposition/image/observation projection.
Its final order is equality on the whole rich value.

A construction-stage review found the initial rich order was pulled back from the
erased profile and therefore identified distinct raw paths that Python distinguishes.
The author repaired this to equality; corrected source and exact registration were
reread and are included in the successful final replay. Postcomposition supplies
the old order explicitly. No order-reflection theorem is claimed for erasure.

## X2: full identities, reverse orders and prices

CandidateContexts derives reflexive/transitive reverse-coordinate order, actual
(ID,vector) values, full maximal-ID/Pareto equivalence, and nondominance equivalence.
The latter uses total scalar order at the failed converse coordinate, not an
assumed equivalence between syntactically different frontier predicates.
Equal vectors preserve all distinct active IDs. FinitePreference uses actual
predicate filtering and immutable V20 frontier membership; complete active-roster
coverage is an explicit premise. It does not prove canonical list serialization.

PriceContexts uses actual finite dot products and reversed scalar comparison.
The all-ID maximal/argmin iff retains ties. Strict positive-price Pareto efficiency
uses immutable dot_strict with a witnessed strict coordinate. The Int controls
supply actual zero/negative-price failures, aliases and an unattained coordinate
minimum. Imported scalar laws are ordinary primitive ordered-ring assumptions;
the actual Int instance and operations are bound, so no assumed desired result
is hidden in an abstract interface. Rational/Real models remain paper specializations.

Generic dimension0/negative-resource mathematics does not authorize those inputs
in the faithful legacy interface. Arbitrary infinite candidate carriers need not
attain a scalar minimum. Finite nonempty attainment is a paper-level finite-order
argument here; the kernel iff does not silently assume existence of a winner.

## X3 and registration integrity

PlanContexts constructs genuinely partial pair-valued Contexts, with actual domain,
loss evaluator, reverse-loss order and all three observation equations. The image
proof preserves a shared plan identity. Total source loss is a named specialization;
identity_feasible gives only equal feasible sets, not equality of tagged observations.

CommonPlans quantifies ONE plan across ALL selected histories. Empty families
retain U; mapped_intersection obtains a preimage from q(U) and then uses injectivity
to align every witness. This works for arbitrary history families including empty;
no compactness, pairwise-to-global principle or nonempty-plan assumption is hidden.
Concrete three-plan, loss-collision and empty-codomain failures are kernel checked.

The contract binds actual definitions/operations as well as theorem types, rather
than checking names alone. The reviewed hostile design empties all sources or
weakens each of three final proof leaves to True, requiring SOURCE compilation
before AUDIT rejection. Its executed verdict is separately the canonical driver
receipt; this independent replay did not repeat the hostile or primary corpora.

BFS traversal, valid path extraction, exact strings/dictionaries, Python parser
behavior and arbitrary historical-source substitution remain paper/finite/source
review scope. The generic mathematics is classical parent-owned. Final review is
clear at the exact registered scope; no whole R3 or complete-theory promotion.
