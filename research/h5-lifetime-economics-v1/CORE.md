# H5 lifetime economics v1 — polynomial + microworld ledger

Issue [#165](https://github.com/SzeChunYiu/ORION-OCM/issues/165) remaining H5 /
G4.4 lifetime boxes at **registered polynomial + microworld scope**.

No `src/` edits. Frozen predecessors are **inputs**, not overwritten:

- `research/h1-amortized-rewrite-v2/` terminal
  `LIBRARY_ACQUISITION_EXCEEDS_LATER_SAVINGS` (later compute falls; capital
  unrecouped). H1 capital enters this ledger; it is not denied.
- `research/h1-amortized-acquisition-v1/` remains the v1 5-ary negative.
- `research/g2-acquisition-economics-v1/` break-even **2136 vs 41** length-8
  tasks (tournament vs SEARCH_AWARE). The 9,010,526-attempt tournament is
  **cited, not rerun**.
- `research/machine-epistemics-lifetime-v1/` meters stay untouched. Raw
  vectors; no dollar conversion; no scalarization of incommensurable resources.
- `research/g5-physical-denominator-v1/` terminal `DATABASE_PARENT_SUFFICIENT`.
  Physical denominator is **not clean** for cognitive claims if JSONL rewrite
  dominates.

**Terminal:** `LIFETIME_NET_POSITIVE_AFTER_CHEAP_ACQUISITION`
(tournament arm remains `LIFETIME_NET_NEGATIVE_AT_POLYNOMIAL_HORIZON`).

See [`RESULT.json`](RESULT.json).

## Question

After charging, as **separate coordinates**,

```text
acquisition compute   (library/tournament  or  SEARCH_AWARE scan)
later inference compute
update / maintenance  (revocation restore or cheap index update)
state written
```

does later-saving-minus-acquisition stay positive at the frozen H1
16/16/16 length-4/5/6 horizon? Does replacing the validation tournament with
SEARCH_AWARE (zero enumeration) flip that sign? Does the sign survive a
resource price on **one** coordinate at a time?

Do **not** add dollars. Do **not** sum bytes with enumeration attempts.

## World

This capsule does **not** re-search H1 later tasks and does **not** re-run the
#192 / G2 9e6 tournament.

| input | what is used |
|---|---|
| H1 v2 `RESULT.json` | library capital, later K_0 / K_t compute, persist bytes, admitted fragment `square square` |
| G2 acquisition economics | SEARCH_AWARE enumeration = 0, tournament = 9,010,526, break-even 2136 vs 41 |
| this run | zero-enumeration scan over reconstructed H1 training programs (token operations); OCM persist / revoke / restore bytes; miniature posting update vs rebuild; N1 nested-order miniature |

Training reconstruction is length-4 only (cheap). Validation/later rewrite
indexes are **not** rebuilt.

The H1 rewrite-benefit scan (0 enumeration, 512 token operations) does **not**
select H1's admitted `square square`. That is expected: H1 serving never widens
the 4-ary grammar, so G2's SEARCH_AWARE width term is absent. The cheap arm is
therefore a **conditional cost scenario**: G2-licensed zero extra enumeration
on the **same** H1 v2 fragment and later savings, not an executed same-path
acquisition/payback. A G2 result in another selector/serving regime cannot
license the missing H1 selection equivalence. **H1 v3 is a separate actual
acquisition successor** and must not be conflated with this substitution.

## Arms

1. `TOURNAMENT` — H1 library capital as recorded (training search + every
   candidate's validation search). Later inference is H1 K_t vs reset K_0.
2. `SEARCH_AWARE` — same later K_t / fragment, but acquisition enumeration is
   training search only; the validation tournament is replaced by a scan
   (0 extra enumeration), as licensed by G2's
   `CHEAP_SEARCH_AWARE_SELECTION_REPRODUCES_THE_TOURNAMENT_CHOICE`. Scan token
   operations stay on their own coordinate and are never added to enumeration.

Maintenance uses the cheaper of revocation-restore work and an incremental
posting update; full index rebuild is reported, not hidden.

## Pareto

For each charged coordinate *c*, vary a price λ on **c alone** (other
coordinates price 0) and report whether

```text
sign( later_saving − acquisition − maintenance )
```

on the compute comparison flips when that extra cost is priced in. No
cross-coordinate dollar. A flip is `PRICE_REGIME_ONLY`, not a converted score.

## M12 V5 / N1 / N2

Issue L3 asks for **M12 lifetime V5 with N1/N2 acquisition cost included**.
That includes language acquisition at **corpus scale** and is **locked**.

This capsule **does not claim** that box. Corpus status is
`CANNOT_CHECK_N1_N2_ACQUISITION_AT_CORPUS_SCALE`.

A nested-order constraint miniature (`research/ocm-n1/language_lifetime_calibration.py`)
is measured and labelled **`MINIATURE_ONLY`**. Observation counts are not added
to polynomial enumeration.

## Not claimed

Same-path H1 acquisition/payback (the reconstructed scan does not select
`square square`). H1 v3 recoup. Programme-wide H5 close. Matched P6 lifetime
residual. M12 V5 with N1/N2 corpus cost. `PHYSICAL_DENOMINATOR_CLEAN`. OCM
uniqueness over a persistent program-library parent. Salt retune of H1.
Dollars. Neural/Transformer lifetime. Compression savings as serving or
lifetime savings.

## Freeze

Do not retune H1 salts, G2 break-evens, or this ledger's coordinate list to
rescue a sign. Negative and price-regime terminals are first-class.
