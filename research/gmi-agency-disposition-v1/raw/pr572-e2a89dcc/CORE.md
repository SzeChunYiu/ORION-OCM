# Agency and planning disposition: read first

Ledger item 9, filed as a **disposition record rather than a theorem**.

[The record](AGENCY_DISPOSITION_V1.md) names which proofs already on `main`
discharge row 9 — CA-1–5 (including an exact robust Bellman recursion verified
against an independent history-tree recursion over all 390,625 kernels with zero
mismatches), BCR-1–4, the planning semantic-resolution theorem, the decision
finite-score audit, and the master closure ledger's preference boundary — and
states the residue that genuinely remains.

Writing a new "agency theorem" over those results would be a renamed parent,
which the basis-acceptance gate kills as `PARENT_FORMALISM_SUFFICIENT`. The
ledger's own authority line says status words carry no authority and the linked
proofs do; this record makes row 9 readable against those proofs.

Residue: stochastic metalevel value beyond the finite deterministic scope,
preference elicitation where viability supplies no endogenous obligation, and
charged deliberation inside the policy — the last closed separately by
[VOC-1–6](../gmi-value-of-computation-v1/VALUE_OF_COMPUTATION_THEOREM_V1.md).

Status: **NOT REGISTERED IN THE REPLAY CAPSULE**, and deliberately outside
`research/gmi-grand-unification-v1/`.
