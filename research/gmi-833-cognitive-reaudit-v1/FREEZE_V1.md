# GMI #917 cognitive-function re-audit freeze v1

Parent: #833 Section M. Source main:
`36064c956ba24bd85949960dd23d6326656ba3e6`.

## Frozen closure target

This tranche targets exactly three proof-heavy rows:

- re-audit memory differentiation under the upgraded foundation;
- re-audit attention as resource-rational selective processing;
- re-audit concept formation/abstraction.

It does not claim anatomical memory modules, a universal attention architecture,
learning a concept from finite samples, or any of the later Section M rows.

## Frozen parent custody

The audit must import the upgraded #833 foundation and compact axiom core, then
test the strongest relevant historical capsules rather than accepting their
labels as conclusions.

Frozen parent blobs:

- foundation result: `c0c574c4ec6e237d5fdafa694eac131399625a70`;
- axiom-core result: `3366a3bc7236d286f8d123bf53e4e3b2d22ad7b9`;
- historical memory-partition text: `25c6b3c88c07a8cff84d65e573cddbac12781034`;
- historical memory witness: `10390cefbcc6978c0fb1a006aef5c9687ba3ef37`;
- corrected attention theorem: `ed000be608435bc48ca46ea787cc9ca665b8a477`;
- corrected attention witness: `66ab7caeb12d383f3aa25cce3a5c6c9139755ede`;
- concept-admission theorem: `4f77c968a3a83061dbc48201ed31b119c1c015b4`;
- concept implementation: `ba5993d11c0a1a8616c686cc33509f60f65e4bb1`.

## Frozen audit conclusions to test

1. Memory roles are operationally differentiated only relative to a registered
   event/query/intervention/resource contract. Distinct response or charged
   trace profiles can establish functional differentiation; they cannot identify
   anatomical modules or a unique physical partition. A unified-store and a
   separated-store implementation with the same protected profile are a required
   hostile twin.
2. Selective processing is resource-rational only after exactness/admissibility
   is enforced and only when its discovery/selection premium is smaller than the
   materialization cost it avoids. Equal-size blind selection is a required
   negative control; high discovery cost must make full processing rational.
3. At a finite static protected interface, the response-row quotient is the
   coarsest exact abstraction. Partial observations need not identify that
   quotient: observation-equivalent completions with different quotients require
   abstention. Amortization can justify adoption of an already verified
   abstraction, but cannot create its semantics or certify its correctness.

## Frozen falsifiers

The result is red if it derives memory anatomy from functional traces, calls two
observationally equivalent stores differentiated, lets cost legalize an
insufficient attention set, omits discovery cost, calls a partial response table
a complete concept, uses an admission threshold as a formation algorithm,
accepts parent drift, uses binary floating point for decisions, or promotes the
finite registered conclusions to universal cognitive laws.

Allowed terminal only after analytic proof and exhaustive exact replay:

`GMI_833_COGNITIVE_REAUDIT_FUNCTIONAL_DIFFERENTIATION_RESOURCE_RATIONAL_SELECTION_AND_STATIC_QUOTIENT_WITH_ABSTENTION`
