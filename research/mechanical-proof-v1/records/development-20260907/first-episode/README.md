# Exposed F0 development episode 1

Recorded terminal: **REJECTED**.

The proposer returned FOUND; Foundation and Target compiled successfully; candidate elaboration failed. The captured AST is `agreement actual (subset actual member)`. The `sorryAx` diagnostic followed the failed compilation; the candidate AST contains no admit or axiom constructor. The raw terminal remains REJECTED.

`retrospective-source/` is a retrospective hash-matched reconstruction, **not a contemporaneous host snapshot**. All 22 files match the inventory already recorded in `raw/episode.json`; `SOURCE_RECOVERY.json` identifies captured original copies and exact reversed deltas from episode2. Five files required reversal.

- `raw/` preserves original JSON, input, mounted worker and checker source bytes.
  All process fields and absolute paths remain unchanged.
- `ORIGINAL_FILE_INVENTORY.json` records every original regular file.
- `GENERATED_ARTIFACTS.json` records generated artifact names, sizes and hashes.
  No `.olean` binaries or full Lean runtime binaries are copied.
- `SHA256SUMS` binds all archived files except itself.

This is an exposed engineering record, not a paper finding or whole-OCM
no-neural qualification. No tests, search, kernel replay or top-level source
edits were performed for preservation; the original episode directory remains
byte-identical.
