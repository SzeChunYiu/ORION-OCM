# Final exposed F0 commissioning archive

`commission.json` is the unchanged native receipt: all 14 assigned controls
passed, with exact original/restart candidate and counter equality. The terminal
is **MECHANICAL_PROPOSER_COMMISSIONING_PASS** for an exposed finite application
closure apparatus. The Eq-only parent also passes; this is PARENT_SUFFICIENT.

Original relative paths and every copied JSON/Python/Lean byte are retained.
`source-snapshot/` and per-episode snapshots are contemporaneous captures, not
reconstructed source. `runtime-manifest.json` is the exact registered manifest.

- `supporting-evidence/` contains the 117-test JUnit, runtime load control, and
  three prior preparation failures with their small diagnosis/repair records.
  `SOURCES.json` distinguishes existing manifest bindings from added archive bindings.
- `archive-metadata/ORIGINAL_FILE_INVENTORY.json` describes every native file.
- `archive-metadata/GENERATED_ARTIFACTS.json` records the 11 omitted `.olean`
  bodies by original path, size and SHA256. No runtime binaries are archived.
- `SHA256SUMS` binds every archived file except itself.

The independent raw-record review preceded this archive. Original source
folders and supporting files were read only and checked byte-identical after
copying. No tests, proof search, kernel replay or top-level source edits occurred.

This does not grant generic-witness/liveness, learned-method/transfer, FLT,
novelty or whole-OCM no-neural qualification. Runtime preparation includes the
three manifest-recorded failures. Commissioning and per-episode wall costs
overlap; no speedup or complete lifetime-cost claim follows from this receipt.
Archived test files are evidence: exclude `records/` from test collection.
