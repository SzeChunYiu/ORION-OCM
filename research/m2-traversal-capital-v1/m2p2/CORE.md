# M2-P2 independent-authorship replication — CORE (read first)

Lane `LANE_M2_TRAVERSAL_CAPITAL_OPUS`. Owner #165, hardening parent #323.
Delivers the single unmet item of the m2p1 claim ceiling: **replication on an ecology
authored by someone else.**

## What is frozen where

Everything is registered in [M2P2_FREEZE_V1.json](M2P2_FREEZE_V1.json) **before** the
author session and any scored run:

- the **neutral author spec** ([m2p2_author_spec.md](m2p2_author_spec.md), sha256
  bound in the freeze) — hidden-chunk arithmetic worlds in fully neutral vocabulary;
- the **author-unit protocol** — fresh session, spec-only, isolation by instruction,
  mechanically certified by the taxonomy-disjointness hostile
  ([m2p2_taxa.py](m2p2_taxa.py), 219 frozen tokens, P1-E3 tiers imported);
- the **entry gates** — G1/G2/G3, viability floors, and the per-world blocking
  G-SURF surface-derivability gate (fires only if a history-free ordering captures
  ≥ 50% of the oracle headroom; calibrated pre-author against m2p1's recorded
  positives — see [M2P2_GSURF_CALIBRATION.md](M2P2_GSURF_CALIBRATION.md));
  the decisive surface test stays the scored-stage G4, exactly as m2p1;
- the **arm set** (CONTINUED / RESET / LIBRARY_ONLY / SHUFFLED_HISTORY hardened /
  best history-free surface / ORACLE calibration / no-gate parent / depth ladder) on
  the registered units imported unmodified;
- the **statistics** (paired shuffle-equal-n null per world, Holm across worlds) and
  the **terminals**, including AUTHORSHIP_CONTAMINATED and SURFACE_DERIVABLE as
  first-class outcomes;
- the **E8 amortisation-horizon test** as a secondary arm.

## Stage status

| stage | state |
|---|---|
| 1 — freeze (this PR) | design only; no results, no authored content |
| 2 — author session + entry gates | blocked on this PR merging |
| 3 — scored replication run | blocked on stage 2 |

## Hard rules

No m2p1/ artifact is edited; `src/ocm` is untouched; admission is never loosened; no
threshold is tuned; any post-freeze deviation is AUTHORSHIP_CONTAMINATED, never an
amendment. Scored execution runs off the Mac (laptop billy / LUNARC offline). #323,
#277 and #165 stay open regardless of outcome.
