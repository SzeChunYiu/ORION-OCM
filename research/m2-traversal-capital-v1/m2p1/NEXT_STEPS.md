# Status ledger and next steps — refreshed

Every figure below is from an executed run on laptop billy, billy-old or LUNARC; nothing
is projected. Claim ceilings are stated per row.

## The four targets

| target | status | strongest evidence | ceiling |
|---|---|---|---|
| **(1) independent ecology** | 🟢 at lane scope | integrated arm beats the strongest parent on the M1 lane's own partitions (−11 %) and on both ecologies built from its mined vocabulary (FV6 −48 %, FV8 gate neutral vs RESET / −26 % vs parent) | ecologies *derived from* another lane's artefacts; M2-P2's fresh-author worlds are the authoritative test and remain with the other lane |
| **(2) lifetime economics** | 🟢 / 🟡 | 670 executed targets: marginal break-even 403, incremental 215, both pay; conservative **475 ✓ or 878 ✗** depending on whether validation's baseline solves are charged as overhead | the conservative verdict turns on one attribution rule; both readings recorded |
| **(3) OCM vs strongest parent** | 🟢 | integrated arm ≈ **half** the cost of the parent holding the identical library on 6/6 ecologies + 670-target scale; deployment liveness −35 % lifetime under an ecology shift; 18 fresh worlds aggregating | authored ecologies; one deactivation policy |
| **(4) general developmental** | 🟢 at `d = 2` | benefit does not decay with arrangement distance (D1: 5.2 % at `d=1` → 12.9 % at `d=2`); pure-`d=2` replicated on 5 seeds, all positive (−11.5 … −44.7 %); integrated arm −55 … −72 % at `d=2` | `d ≥ 3` empty by counting at this `(m, k)`; P=6 analogue pending |

## The mechanism that made it move

One controller, four parts, each earned by a negative:

```text
CONTINUED_OCM = library by validation (freq vs MDL)   ← FOREIGN_M1 v1 negative
              → probe, depth by expected cost on held-out validation   ← coverage-depth negative
              → task-statement rule on a miss, RESET while stood down   ← shift-test defect 1
              → liveness: counter advances every target, periodic re-probe   ← shift-test defect 2
```

Invariant carried by every part: **reads the task statement, solved history and the
outcomes of charged actions — never the solution.** The retraction of the answer-derived
gate is what forced it.

## RSI spine

| level | status | evidence |
|---|---|---|
| L5 self-diagnosis | 🟡 | 9/9 in-sample → **0.758** on a 33-case sealed packet (C0 9/9, C1 13/13); residual C2→C1 on the ambiguous lifetime worlds |
| L6 improvement of improvement | 🔴 → 🟡 | first **fall** in cost-to-verified-improvement (G4 → G6, zero-cost `admitted` probe); no monotone slope over G0 → G6; a **policy generation** (decision-theoretic stopping) is running |

## Registered next experiments, in order

1. **Policy generation P1** — decision-theoretic stopping for active diagnosis. Running.
   Prediction: active accuracy rises toward exhaustive 0.758 at cost between 131 and 1 023,
   cost/verified below both.
2. **Repair-catalogue revision** — C2's canonical repair becomes depth-aware per-target
   deployment (the probe gate with cost-depth is what actually fixed the lifetime worlds),
   then RSI-6 re-scored. Not started.
3. **18-world integrated-arm aggregate** — auto-queued after the LUNARC array.
4. **9-seed `d=2` aggregate** — auto-armed on laptop billy.
5. **P=6 analogue** at the corrected window — still running; superseded for the `d=2`
   claim by the registered-grammar result, retained as the cleaner cross-check.
6. **M2-P2 stage 2/3** — the other lane's fresh-author worlds, to be run through
   `CONTINUED_OCM` unchanged when they exist.

## What would still falsify the picture

- 18-world aggregate shows the integrated arm losing to the parent on a material fraction
  of fresh worlds → the 6/6 was ecology selection.
- `d=2` aggregate flips sign on later seeds → the 5/5 was early-seed luck.
- Policy generation P1 raises cost without raising accuracy → active diagnosis is
  bottlenecked by the likelihoods, not the policy, and L6 needs a learned likelihood
  layer rather than a better stopping rule.
