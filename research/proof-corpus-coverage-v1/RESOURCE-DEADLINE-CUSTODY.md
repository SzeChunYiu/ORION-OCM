# Deadline source custody

The fixed `resource_deadline_evidence.py` entry authenticates three historical
resource generations and separately checks the 29 deadline/dependency source files.
It uses the existing historical guard with an explicit archived source directory.
The earlier 19-,22- and23-source records and validator bytes remain unchanged.

`resource-deadline-records/SOURCE-FREEZE.json` is the original 376-input qualification
map from commit b835083ec161d070ab937ada8ff2174d6bd171e2. The package preserves its
bytes and seven selected-run records, including the XML it explicitly binds.
The 110 passing controls are historical engineering qualification on that source;
the portable custody command does not rerun them or revalidate host enforcement.

The package includes exact copies of the old23 sources from their original archive
and the separately qualified29 resource/dependency sources. Its existing-format seal
binds60 files. The current check is limited to those29 sources; the other347 entries
in the larger qualification map are not presented as current by this command.
Later unary generator repairs require their own qualification and leave this resource
source generation unchanged. The privileged helper remains the original generation.

The new fixed glue has12 passing focused controls, including a direct real-data
no-alarm check and substitutions between old/current sources, current drift, wrong
seal, receipt drift, unexpected members, invalid paths, duplicate keys and symlinks.
These controls are additional evidence for the glue, not a rebinding of an old pass.
Raw observations remain in unary-assay-release-evidence-qualification-v1 on laptop
billy; the final release binds its exact current source and test receipt separately.

Run the portable custody check with:

    python -I -S research/proof-corpus-coverage-v1/resource_deadline_evidence.py

A pass establishes archive/current-byte custody within this scope. Scientific learning,
performance, original host binaries and controller enforcement have separate evidence.
The first registered unary attempt remains0/4 CANNOT_CHECK after its interface failure.
