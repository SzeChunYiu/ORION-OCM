# GMI #833-F exact finite Pareto-front density

This package closes one Section-F row at a prospectively frozen finite scope:
“Measure Pareto-front density.” It uses the merged #966 exact candidate space,
collapses syntax presentations to complete protected-semantics/raw-resource
morphologies, and applies a disclosed non-scalar five-coordinate benchmark.

Exact morphology-front densities across the complete cumulative budgets are:

| budget | presentations | morphologies | frontier | density |
|---|---:|---:|---:|---:|
| `(1,1)` | 5 | 4 | 2 | `1/2` |
| `(2,1)` | 126 | 22 | 4 | `2/11` |
| `(1,2)` | 14 | 8 | 2 | `1/4` |
| `(2,2)` | 576 | 47 | 4 | `4/47` |

The largest census checks all `47^3 = 103,823` ordered dominance triples. An
independent all-pairs oracle agrees on every frontier. All 120 certified
operation-token remints across 576 presentations preserve the morphology
points (69,120 checks). A duplicate-injection hostile proves why the registered
estimator must be quotient-based: presentation weighting changes from `1/2`
to `2/3` or `1/3` depending on which fiber is duplicated, while quotient
density stays `1/2`.

Claim ceiling:

`GMI_833_FINITE_PARETO_DENSITY_AT_REGISTERED_G0_SCOPE`

The values are conditional on the finite grammar, budgets, protected interface,
step cap, and prospectively declared evaluation objectives. They are not a
universal or objective-neutral density and do not imply large-scale coverage,
reachability, clustering, family recovery, or complete GMI.

## Reproduce

```bash
python3 -I -B research/gmi-833-finite-pareto-density-v1/test_finite_pareto_density_v1.py -v
python3 -I -O -B research/gmi-833-finite-pareto-density-v1/test_finite_pareto_density_v1.py -v
python3 -I -B research/gmi-833-finite-pareto-density-v1/finite_pareto_density_v1.py
```
