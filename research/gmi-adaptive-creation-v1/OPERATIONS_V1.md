# Adaptive creation: read first

Run from this unit directory:

```sh
python3 -I -B test_adaptive_creation_v1.py -v
python3 -I -O -B test_adaptive_creation_v1.py -v
```

The test entrypoint compiles its sibling model from source; exact rational arithmetic only.
