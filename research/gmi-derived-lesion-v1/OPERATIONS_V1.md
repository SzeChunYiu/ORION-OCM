# Derived lesions: read first

Six lesions each knocking out one derived mechanism; the search basis
(T0/T1 transitions) stays intact. Exact enumeration only.

Run from this unit directory:

```sh
python3 -I -B test_derived_lesion_v1.py -v
python3 -I -O -B test_derived_lesion_v1.py -v
```

Sibling source compiled explicitly; exact Fractions; CPython 3.8 safe.
