# G6 intervention-effect laboratory v1

Root-cause repair for `CANNOT_CHECK_MISSING_RAW_TRACES_AND_NO_THIRD_EARNED_SELF_CHANGE`.

Historical M11 `g0 → g1 → g2 → g2` is **not** relabeled. This capsule generates
paid probe transcripts on a tiny governed plant and requires three disjoint
incident generations, restart after adoption, and constitution immutability.

Independent scoring re-simulates hidden stuck modules; the learner never
receives those labels.

Claim ceiling: bounded synthetic plant, not production self-evolution.
