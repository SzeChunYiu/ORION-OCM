# Instrumentation and custody result

Two bounded repairs are integrated without changing frozen V5 measurement
authority or the grand-theory inventory.

- [Parity custody](../gmi-parity-custody-repair-v1/CORE.md): reserve canonical
  attempts before invocation; reconstruct complete packets before aggregation.
- [Opcode instrument revival](../gmi-opcode-instrument-revival-v1/CORE.md):
  one callback assignment restored the recorded first-call opcode witnesses.
- [Integration validation](INTEGRATION_VALIDATION_V1.json): actual local static
  tests, exact receipt comparisons, versions and immutable unit bindings.
- [Linux static CI](../../.github/workflows/gmi-instrument-custody-static.yml):
  hostile controls, complete manifest verification and available static replay.

Only opcode auditor/test V2 is active. V1 remains immutable historical
evidence with its original-location limitation. Neither CI nor this integration
invokes a measurement launcher, collector, candidate body or timing loop.

Parity historical replay requires its exact recorded Python releases. Opcode
replay additionally requires the recorded binary hash. CI records UNVERIFIABLE
when those interpreters are absent, while still checking committed byte bindings
and portable evidence controls. A bound prior result is not newly executed
verification. Test skips are printed and must not be reported as passes.

Local validation used the recorded interpreters for full replay and repeated
focused controls normally and under optimization with GitHub CI variables set.
The recorded first-attempt/host provenance limitations and finite measurement
scope remain explicit in both source units. No global family result follows.

The original parity V1 manifest is copied unchanged. Its versioned V2
extension archives the old adjudicator for replay after upstream changes. The new opcode manifest binds its
exact imported file set; all original freeze, source and raw execution files
remain byte-identical. This index and the integration receipt are new records.

Use [parity countercontrol V2](../gmi-parity-custody-repair-v1/STATIC_REPLAY_V2.md)
for active old-defect replay. The changed upstream aggregator remains separate
authority. Original V1 code and receipts remain byte-identical.

The separately frozen [retained V6 auditor](../gmi-parity-v6-evidence-v1/CORE.md) now
reconstructs all three measured packets. [Integration validation](V6_INTEGRATION_VALIDATION_V1.json)
records the complete static CI script run; [V6 CI](../../.github/workflows/gmi-parity-v6-static.yml)
checks all exact releases and preserves current audit-binary identity separately
from the unauthenticated historical measurement binary.
