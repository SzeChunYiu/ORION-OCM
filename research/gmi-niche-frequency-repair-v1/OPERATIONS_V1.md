# Operations

Use Python3.12 on Linux. Standard library only. No native microscope module,
learning evaluator, campaign or network API is imported by the checker.

    python3.12 -B -m unittest discover -s . -p 'test_*.py' -v
    python3.12 -I -B replay_v1.py > /tmp/gmi-niche-replay.json

Keep all redirected outputs and test artifacts outside this frozen unit.
Repeat with -O to verify optimized execution. Replay verifies complete unit
membership/content, runs the pure-math/full-record checker, compares the complete
canonical receipt and verifies unchanged initial manifest/receipt anchors.

The grid is a NEW exact Cartesian enumeration:83521 coefficient tuples,
49 integer constants directly evaluated per tuple in the independent oracle.
Its complete histogram is retained in raw/EXACT_GRID_V1.json and the full receipt.
The statistical population is explicitly uniform on the entire grid, with no
registry rejection, reweighting or inferred historical sampling chronology.

No confidence claim is attached to the12 reported tasks or the old3000-draw
summary. The literal printed gates and the conditional native-lattice unrounded
interpretation remain distinct. Source bytes are preserved, not repaired in place.
