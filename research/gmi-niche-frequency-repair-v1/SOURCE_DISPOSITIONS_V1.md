# Disposition of the two niche additions at27ac63f3

Pinned branch head:27ac63f3c0121d4866ca5fca6511e3bd51dd17e5 (PR588).
Relative to the FEE-audited8c7b2e4add55a2322d8a1464ecd04b1a461a6198, the complete
Git diff has exactly two additions: the niche note and its retained JSON.
All earlier five FEE source paths are unchanged; their prior corrections stand.
Exact files, hashes, Git blob identities and full delta are in SOURCE_BINDINGS.

## Retained observations

All108 reported cells (12 ecologies×9 named rows) are retained unchanged.
Every printed margin equals round(24*(printed score−printed baseline),3).
Every printed Boolean agrees with printed score≥0.85 and printed margin≥1.
Counts are11/12 for each search row,3/12 for memory,0/12 for each other row.
These are internally consistent observed-record proportions, not population rates.

Threshold-only counts are12/12 for each search row,8/12 memory,6/12 retrieval,
1/12 gradient_h2,0/12 gradient_h4. Thus “gradient ceiling c=.6788<θ” is already
false as a global upper bound for that measured row: E_niche8 reports.8646.
The old table itself calls.6788 and.8524 MEANS across registered ecologies,
not proved family ceilings. Memory scores range across the twelve tasks;
one score above a proposed c is enough to refute its upper-bound premise.

N1's numerical ≥85% and N2's10–40% bands hold on these reported twelve-task
counts; N3 reports no admissible tested learner row in this draw.
N4's literal strict search>memory>retrieval>learners prediction fails at0=0.
Its weaker no-inversion statement holds. This distinction does not require a
new campaign or invalidate the raw table.

## Precision and the actual old interface

The JSON names the six V1 interventions, including extra_unseen_feedback.
The archived ecology source says that old control feeds UNSEEN[:4] and still
scores all8 UNSEEN points. It is a retained V1 record, not V2 leak-free evidence.
No current VM or historical runner is modified or executed by this unit.

The archived native functions return four-decimal capability and best_constant
values. The gate reconstructed from the printed record uses those decimals;
the missing original niche runner prevents identifying its actual decision code. E_niche12's search rows
report.8958 against.8542, margin.998, and are marked false: that is internally
consistent. This does not authenticate the actual native/author-intended gate.
An unrounded-MAE target uses a different score register. If interpreting
the displays as rounded observations of that register, their compatible intervals
straddle the exact margin boundary. This sensitivity is reported separately;
it does not silently change either retained Boolean to true.

Conditional positive refinement: if the archived native unseen-criterion scoring
contract was used, every V1 control scores8 inputs, including the old extra control.
Integer outputs/targets give clipped unrounded score k/192 for k∈0..192.
Four-decimal rounding is injective on that lattice, so all108 displays have a
unique compatible value under this additional contract. Both E_niche12 search
values then have an exact1/24 margin and would pass an UNROUNDED gate.
This locates a possible reporting/gating boundary, not an alteration of observed
results or proof that the missing runner intended an unrounded decision.

The JSON has aggregate min-over-six, margin and Boolean fields. It does not retain
the six separate outputs, genotype hashes, per-call errors/ledgers, interpreter
identity, a runner body or full acquisition costs. These are absent from this
packet schema; execution is reported, not independently authenticated here.
No assertion is made that no execution occurred.

## Sampling and chronology

The documented seed expression reproduces902521130; all12 retained coefficient
tuples lie on the stated grid and are distinct. Git contains prior note
a2efad753e17d32898f07e16adedccbab2be12b3 before the result commit. It supplies a
source chronology, not proof that the outcomes/seed consequences were unseen.

An exact whole-tree search for the seed and seed-string tokens finds only the
niche note and JSON. This establishes no source-bound runner via those identifiers,
not absence of every possible renamed/untracked script. The prose says the runner
was derived from a five-ecology script and retained an obsolete “of5” summary.
That report is preserved; its unretained implementation and retry/rejection trace
remain unknown. The earlier3000-draw summary likewise is not recovered here.

The declared full-target map on k/16, k∈[-8,8]^4 is injective by singleton inputs.
This positive source fact supersedes a generic tuple/many-to-one objection for
this precise map. A complete NEW grid CDF is computed independently; it excludes
no registry members and must not be called the historical rejected-draw law.

## Constructive replacement

Use the same-task joint event of NFR-1. A proved constant ceiling gives NFR-2's
upper bound; actual attainment yields equality under its explicit criterion.
Quantitative ε/γ control yields NFR-3's bounds. The exact analytical CDF supplies
their baseline term when that newly declared population is appropriate.
A mean/stdev or a quartile alone cannot supply any missing attained-score premise.

Claims that search “occupies92% of the space”, memory25%, learners0%, or that
a0.2-percent baseline niche is necessary remain unsupported population/family
upgrades. The matched score-register and correlation countercontrols give the
specific failure stage and the positive condition needed to revive the formula.
