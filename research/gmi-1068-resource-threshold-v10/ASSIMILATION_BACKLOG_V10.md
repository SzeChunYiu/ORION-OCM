# Historical family-tranche assimilation backlog
Read-only audit of #1103 at head
9643e25f3c731193d279bb6adf2a652247ad5d0c.
These findings concern that exact revision, not later edits or all #833 work.
The historical lane is unchanged. No status from it is promoted into #1068.

## Reusable parent
The common Boolean AST enumerator and exact full-cube minimum search are real.
Independent enumeration is useful implementation diversity. Assimilate these
with V6's structural minimum certificate, independent witness validation and
measured discovery/acquisition costs. Credit their source ownership.

## Concrete repair tasks
All source paths below are under research/gmi-833-h-family-tranche-a-v1/.
Every task remains OPEN until checked against a specific successor revision.

1. FAMILY-MECHANISM: family_tranche_a_v1.py lines55–78 supplies eight labels
   but only three truth-table contracts. Require actual temporal lookup,
   acquisition, state update, frontier search, planning or DP recurrences,
   generated from neutral primitives. A coordinate/conjunction predicate
   does not alone recover those mechanisms. More rows of the same cube
   cannot establish the missing architecture behavior. Owner scope R6/R7.

2. CLASS-TRANSPORT: _class_of_tree at lines188–197 interprets x3 as context
   and x4 as persistent state. Transport both across coordinate permutations
   and require an independently defined temporal state contract; include
   swapping x3/x4, which the existing x0/x1/x2 remint control omits. Scope R7.

3. LIFETIME-COST: crossover_block lines236–278 externally supplies the two
   plans. Account for initial read, write, later reads, refresh and stability.
   Under separately priced initial read, regime B at H=1 has stored6 versus
   replay4; first stored win is H=2. If write bundles acquisition, declare and
   justify that accounting explicitly. Replace literal crossed=True/winner
   fields at269–271 with comparisons derived from receipts. Scope R9/R12.

4. ORACLE-CERTIFICATE: independent_search_ok lines556–584 accepts a receipt
   whose every oracle cost is999 and expression is0. Recompute witness
   semantics, grammar membership, node cost and input/source binding before
   comparing minima. Keep the real baseline as a positive control. Scope R15.

5. DEPENDENCY-CANCELLATION: independent_oracle_v1.py line112 says
   XOR(x4,x4) depends on x4 although its truth table is constant0.
   Determine dependency by paired semantic evaluation (or a proved exact
   method), with cancellation controls. Published minimum representatives
   do not use this counterexample; do not declare those particular minima
   false on this evidence alone. Scope R7/R15.

6. FREEZE-NEGATIVE: FREEZE_V1.md line49 registers H06 negative constant1;
   executor lines51,60 use constant0. Reconcile the mismatch in a successor
   and rerun the registered negative, preserving original records. Scope R15.

7. SYNTAX-UNIQUENESS: replace unique minimum spelling with selected canonical
   representative unless uniqueness is proved. AND(AND(x0,x1),x2) and
   AND(AND(x0,x2),x1) are distinct cost5 trees with equal full-cube behavior.
   Enumeration has10050 entries but3290 distinct syntax trees; report unique
   syntax and evaluated work separately. This does not refute minimality.
   Scope R6/R12.

8. FULL-COST-AND-CONTROLS: charge target acquisition, enumeration, evaluation,
   caches, compilation and memory lifetime in any efficiency/selection claim.
   Zero/parity controls distinguish selected descriptors, not necessarily
   named architectures. A family-name substring scan is not evidence of
   semantic architecture neutrality. Scope R6/R7/R12.

## Acceptance boundary
These are bounded, independently executed probes and source-level findings.
No full rerun of the historical executor was performed in this audit.
Four executable probes: duplicate syntax count, AND3 nonuniqueness, XOR
dependency cancellation and oracle corruption. The cost correction is an
explicit conditional calculation under separately priced reads.
Resolve each task using real baseline plus hostile controls. Preserve scoped
positive results; do not translate checker weakness into an unsupported claim
that every committed witness is wrong. #1068 remains the control plane.
