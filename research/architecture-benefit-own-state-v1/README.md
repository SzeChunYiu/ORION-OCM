# Architecture benefit: own-state comparison and statistical evidence

Connects #152 to reopened M12 (#14). Research-only; no production edits, protected
corpus execution, ML training, or general-architecture success claim.

Start with `THEORY_AND_PROTOCOL.md`, then `LITERATURE.md`.

## Convergence with parallel PR161

During publication, PR161 appeared at `a25b39c88ca004fca290dff9b8b10f71f404757d`,
using `research/architecture-net-benefit-v1/`. It develops a related additive
per-demand graph certificate and native correspondence probe. This supplement
uses a separate directory and branch; no PR161 or PR154 files are overwritten.
The shared potential/difference-constraint mathematics is not counted as two
independent scientific discoveries or an independent peer review. Converge the
finite abstractions rather than growing two production executives. Distinct work
here is the complete own-state multiplicative bound including setup and CLOSE,
a practical-margin anytime evidence gate, and a 3-arm persisted native comparison.

## Executable work

- `net_benefit.py`: exact finite relational cost-refinement verification and
  potential synthesis; own-state rollouts; valid-reuse accounting; conditional
  IID complete-lifetime evidence with full phases, all parents and price vertices.
- `test_net_benefit.py`: 27 controls, including reset counterexamples, close debt,
  positive cycles, weak-parent false wins, optional-stopping null arithmetic,
  missing costs and permanently disqualifying invalid records.
- `native_probe.py`: source-bound 3-arm native polynomial-method attribution
  pilot. Runs ordinary fragment learning and OCM's method/KSO path separately with
  the same miner and exact solver. Charges complete child-process time, all search
  slots, final serialized bytes and peak RSS. Includes restart and withdrawal.

```sh
python -m unittest discover -s research/architecture-benefit-own-state-v1 -p 'test_*.py' -v
PYTHONPATH=src python -B research/architecture-benefit-own-state-v1/native_probe.py \
  --out /tmp/NEW-native-benefit.json --repetitions 3
```

Native timings include startup/import and pilot task construction in every arm.
They are descriptive. They must not be sent to the IID evidence gate as though
three repetitions of the same tape were three fresh independently sampled task
environments. Final serialized bytes are not cumulative I/O, and RSS is a peak,
not an additive work coordinate. The ordinary single-writer snapshot parent does
not yet satisfy OCM's full provenance/authority/concurrency/durability contract.

## Source custody and continuation

Base main: `138e90b71e80a0310f65fb2c827c2782e92caa8a`.
Read-only PR154 reference: `94f9f3361cec49c8232049e70fbba8662635902f`.
Native `methods.py` blob: `50323a33418b8ef8bb6500ddeba4b9d1f795e9e3`.

Local validation: 27 tests pass both normally and with `python -O`; native run
requires repository imports and is delegated to this branch's CI, not claimed
from local helper tests. Inspect the actual CI artifact before stating timings.

Next substantive gate: qualify the conventional persistent parent against the
same protected method-lifecycle projection, measure OCM's complete coordination
cost, and bind a finite cost abstraction to that source. Then freeze fresh
workload families and evaluation ownership. Do not reopen outcome-exposed pilot
cases as a protected study or represent source-shaped arithmetic as a proof of
production performance. Preserve PR154's separate non-test-checker census lane.
