# AJ9g — blind K06 probabilistic/evidence-update recovery

The blind phase receives only a finite two-hypothesis evidence problem, prior weights, likelihood weights, generic arithmetic operations and exact normalized-prediction requirements. It receives no K06 label/fingerprint or Bayesian/posterior macro.

Two independent routes recover the same normalized state: (1) multiply generic prior/evidence weights and normalize; (2) enumerate positive integer weight pairs and select the minimum pair satisfying the evidence odds. For `e0` both yield `(3/4,1/4)`; for `e1` both yield `(1/4,3/4)`.

Only after the blind outcome is frozen, the K06 post-hoc fingerprint is applied. The recovered mechanism has a normalized probabilistic state, evidence-dependent update, exact finite likelihood-ratio behavior and predictions read from the updated state. A randomized-action near-neighbor with no maintained belief is rejected.

Strongest parents own the mathematics: Bayes' rule, likelihood-ratio updating, finite Bayesian decision/inference theory, probabilistic graphical-model traditions and generic program synthesis. AJ9g claims only blind holdout recovery at this finite exact-rational scope.

Claim ceiling: `AJ9G_K06_BLIND_PROBABILISTIC_UPDATE_RECOVERY_AT_FROZEN_FINITE_SCOPE`.

Not claimed: general Bayesian intelligence, continuous/high-dimensional inference, learned generative models, `PREDICTED_SELECTED`, K07-K11 recovery, AJ9 closure, or complete GMI.
