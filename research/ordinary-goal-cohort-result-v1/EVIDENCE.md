# Evidence and scope

The [independent outcome review](review/OUTCOME-REVIEW-01.md) is accepted for this recorded joined-library qualification. One Python invocation (PID 2725672) completed with one native call and exit 0. [Reconciled counts](review/OUTCOME-COUNTS-01.json), [caller result](records/qualification-01/RESULT.json), and [actual outer start](records/observation-01/START.json) bind the result to the exact request, gate and source freeze.

The native sequence is exactly the 4,223 old labels followed by `cut-lemma-00` through `cut-lemma-21`: 4,245 proofs. The unchanged 100-axiom order, contracts and raw identities produce a full ordinary inventory of 4,345 assertions. This retains the complete base, not just the 22-member slice.

The 20 A/B/C and two A/B/V physical frames remain intact, now using current native floating declaration order. Eighteen essential occurrences with 11 old local names receive 18 unique scoped names. Only those proof-label names change: targets, ordered essential statements and physical variables remain exact. Transport still records `native_acceptance=false`; the separate fresh native receipt supplies formal acceptance.

All 22 traces match their issued sources and outputs, with 499 total nodes and each within the unchanged 256-node scope. The complete 57-row USED union contains 24 base theorems, 11 axioms, four floating hypotheses and 18 scoped essentials. No cohort theorem appears as a shortcut in these admission proofs.

The existing Library constructor accepted the complete current manifest after native verification. Its receipt binding supplies source/result custody, not automatic future proof authority. No goal was allocated or solved, no method was causally consumed on a fresh task, and no new acquisition, utility, transfer, speed or novelty result follows.

The earlier [caller source review](review/REVIEW.md), [source freeze](records/SOURCE-FREEZE.json), [request](records/RUN-REQUEST.json), [gate](records/EXECUTION-GATE.json) and [reuse bindings](records/SOURCE-ORIGINS.json) are preserved. The original pregate/source-only prose remains historical in RAW. Current outcome acceptance is supplied by the outcome review above; no source controls or native checks were rerun during publication.

Actual source custody covers 19 freeze entries, 17 prior local manifest members, the base/input pins, pinned Python and observation binaries. The process records seven project-module paths and 52 standard-library paths; three dynamic native sources have separate byte pins. This is not an independent whole-standard-library byte freeze or complete performance instrumentation.

The next separate step is a registered goal-only study with matched ordinary-derived-lemma parents and exact generated-proof checking. This capsule stops at joined native qualification. [Recovery and original data](RECOVERY.md); [measured costs](COSTS.md).
