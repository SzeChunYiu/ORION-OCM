# Bounded cohort-caller source acceptance

Verdict: SOURCE_READY_FOR_SEPARATE_ROOT_GATED_NATIVE_PASS.
Concrete blockers: none found. This review grants no execution permission.

Reviewed request: 3db39c0ea17589d0701d2582bcd7a784d2e039343528f8d0f191888ed5e13edd.
Source freeze: af20bdf6fc3c74ddc31c64421610b32b11b71e5ab8377acb4daa71e50d052785.
Run request: 393cbcebaa105ca4b2952e3b045386658bb819b309ca89f3ba96c48cfb9ad6e8.
All 19 freeze entries and the 17-member local FILES manifest reconcile.
The seven reused source/license copies are byte-identical to the accepted bridge;
its acceptance e3ea0fd2... remains prior source/control evidence, not an actual
qualification of this new caller.

## Exact input and transport

cohort_input.py:7–40 requires the exact 22 ordered labels and recorded indices,
canonical cut/admission identities, original target/essential/proof metadata
and source-root agreement. Canonical body variables are translated through the
retained context; no new target, candidate or member-selection rule is introduced.

Current base floating rows supply the physical variable/type declarations.
Sorting those rows by source span establishes native mandatory declaration
order, instead of assuming the retained canonical parameter order is native
order. All three variables must occur in the target/essential statements.
The independently reconstructed original proposal suffix must match its pinned
bytes. Physical A/B/V stays A/B/V; the caller does not relabel its statements.

The unchanged scoped constructor preserves original member and proof identities
and refuses collisions/unsupported frames. cohort_input.py:43–57 independently
derives each expected cohort-e-INDEX-SLOT mapping from the original members,
rechecks bound origin/member/proof identities, then requires exact serialized
transported claims. Normal proof occurrences change only at these essential
labels. Final current frame equality remains a native-wrapper obligation.

The permitted historical packet metadata contains 22 distinct labels,
20 A/B/C and two A/B/V three-class contexts, all with empty retained DV.
It has 18 essential occurrences across members, including repeated local names.
This metadata read establishes input shape only; neither current floating
scope nor native proof validity was inferred from it.

## One complete current native authority

cohort_check.py:6–14 binds indexed base labels and the complete axiom contract/raw
inventory to the exact prior metadata: 4,223 ordered proofs and 100 axioms.
The caller still requires fresh verification of base plus every transported
claim. Prior admission flags do not supply that joined authority.

run_cohort.py:40–55 prepares and preserves members, transport, claims and joined
bytes, then makes exactly one unchanged native-wrapper invocation.
cohort_check.py:16–48 requires one successful native pass, exact issued claim
and source digests, all 4,245 ordered verified labels, the unchanged trust order,
all 22 current traces and the complete union of current USED contracts.
Each trace's source/frame is the exact indexed issued claim. Used hypotheses
must belong to that claim's current mandatory context, all used spans precede
the claim, and ordinary applied assertions come from the base with empty DV.
The wrapper independently rejects any cohort-claim label in these proofs.

Stored request/result/suffix/log/database bytes are bound to the returned
result and exact issued source. Only then is the complete ordered Library
manifest built and the unchanged Library instantiated for compatibility.
Canonical object identities and newline-terminated file identities remain
distinct. There is no claim that the Library's opaque receipt binding alone
confers native authority.

## Launch, failures, costs and limits

run_cohort.py:4–11 explicitly exposes both siblings and vendor imports for the
planned isolated entry. Lines 19–29 require the separately issued exact
request/freeze gate, pinned Python with -I -S -B and no optimization, registered
cwd, and an exclusive output directory. The request/output pair prevents a
second native invocation by this unchanged create-only caller.

Main failures preserve intermediate files and available wrapper records.
A wrapper call that fails to return leaves its native-call count unresolved,
not zero. Post-custody drift forces CANNOT_CHECK. Pre-output refusals or an
outer timeout may leave no final RESULT; the existing GNU-time/timeout plan
must retain START, stdout/stderr and actual exit status. No new observer is needed.

Main timing excludes imports/final RESULT serialization; the outer process
window includes them. Native and Library windows are nested. RSS is cumulative
process highwater. Completed caller/constructor indexing counters do not claim
all partial-stage work or complete lifetime cost. The frozen interpreter and
project sources are bound; whole-standard-library byte closure is not claimed.

The exact source was read/AST-inspected and its metadata hashes checked.
No new controls were run or claimed: the new three modules have source review
only. The base corpus/prefix was not opened, even for a new hash check; its
expected identity was reconciled through already qualified authority metadata.
No H1, newer 32-member cohort, held-out or other new data was inspected.
Actual runtime behavior, the full native result, and future goal-only causal
use remain pending and must be assessed from their separately retained records.
