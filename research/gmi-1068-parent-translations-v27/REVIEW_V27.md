# V27 independent production and claim-scope review

This reviewer authored the paper derivations and parent ledgers. This is an
independent review of root-owned production/driver and oracle-owned test code,
not a claim of a second independent paper author. Formal review is separate.
No primary exhaustive corpus was repeated by this reviewer.

## Actual source review

Read all five production modules, check_parents_v27.py, custody_v27.py,
audit_parents_v27.py, oracle_v27.py, support and the LTS/event/test/task/control/
hostile test modules. The independent oracle uses set images, state walks,
trajectory products and tuple-set relation composition without production imports.
The primary test loops increment their own counters; corpus cardinalities do not
stand in for execution. Final outcomes belong to the separately generated RESULT.

Actual LTS endpoint/path operations call the immutable V11 library. Labels live
in the supplied LTS/edge registry, not the unlabeled endpoint graph alone. Paths
validate all IDs before connectivity; Forward and Back are distinct predicates;
the Hom implementation compares image sets. Lift enumeration tracks the actual
target edges and intermediate states, retaining all distinct source edge words.
Unused label-set cardinality remains validated even for an empty graph.

Event composition retains exact Fraction coefficients, with no conditioning or
requantization. Normalized adapters use the actual cached V14 Kernel class.
Test preserves each nested outcome pair and zero component; aggregate is an
intentional projection, not the original labelled test. Task composition invokes
actual V14 total relations and retains declared source/target labels. Its regular
bridge is an explicit inclusion, and an image shrink is never inserted into it.

Input checks reject aliases before deduplication and validate complete containers.
Frozen dataclass constructor invariants and exact accepted class identities are
the API boundary; deliberate object-level mutation bypasses are not certified.
No generic finite whole-coalgebra constructor is claimed by the small system
fixture. It uses actual V26 category/functor/tree bridges for the named witness.

## Independently executed frozen named controls

Executed /tmp/gmi-v27-independent-probe.py on billy-laptop in normal and -O modes.
Both returned PASS with byte-identical JSON. Script SHA256:
73b071f0205dcac257e1338585a30be45139102a05afedf262b5aeb8f16d753c.
Receipt /tmp/gmi-v27-independent-normal.json SHA256:
b77acdb25c7c8cffcc6f6524099e78e226224598878fc985f9e5468d2a074e2d.
The receipt also binds all five inspected production source hashes.

The seven separate review control groups were: nonvacuous Back failure; four
exact same-target lifts; empty-graph label-set mismatch; half-event composition
to one-quarter; labelled zero outcomes and equal-aggregate collision; the actual
five-label inferred-interface failure with declared-target retention; empty-domain
Event/Kernel/Test/Task distinctions. Assertions used explicit exceptions and
therefore remained active under -O. These are frozen named controls, not new
primary population members or additional exhaustive coverage claims.

## Custody, ledger and corrected wording

Actually executed custody.verify on the current repository:610 source bindings;
expanded inherited input counts600(V26),43(V16 science),14(V16 governance).
The immutable qualification IDs remain003@r1 and007@r1; no latest-ledger scan or
scope broadening occurs. Preregistration bytes/ancestry and freeze-only inventory
are checked separately from present source hashes. Missing-input classification
remains distinct from checked-invalid evidence in the driver wrapper.

Actually executed audit_parents.verify:all seven original ordered rows reconciled.
The guard preserves exact name/parents/native/additional/status fields and strict
schema/source binding. Nonempty new prose fields are only a structural condition;
the guard does not claim they are mathematically true. Current receipt custody
binds the reviewed prose bytes; it does not replace scientific review.

One driver prose correction was requested and verified: arbitrary Rel permits
an input with no related output; TotalRel forbids that case, but empty inputs
remain valid for both. The earlier shorthand could wrongly imply empty-domain
TotalRel arrows were prohibited. No production semantic change was required.
Paper and parent row also explicitly retain the label registry/cardinality
rather than claiming an unlabeled endpoint-graph inverse.

## Verdict and remaining evidence boundary

No unresolved scientific/API defect was found in the inspected production,
independent certificates, named probes or driver AA1–AA5 scope.
Exact Fraction calibration, generic ordered-weight Lean proofs and paper Real/Rat
interpretations remain distinct. Standard monoidal reduct extraction, general
physical/operational interpretation and computational complexity are not silently
mechanized. Only original R1-009 is eligible; all other statuses are preserved.
The final formal replay/registration and integrated normal/-O outcomes remain
separate records. This review does not assert an unobserved canonical run passed.
