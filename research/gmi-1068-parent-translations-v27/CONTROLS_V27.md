# V27 falsifiers and evidence boundaries

The frozen A–C counts are preregistered populations, not observed outcomes here.
RESULT and independent review record actual counts; this file states what each
control can detect. Missing tooling/inputs is CANNOT_CHECK, not a valid result.

## AA1 and AA2 controls

A checks every one-label relation-LTS and total map at state sizes0–2:1143
candidate maps, with empty-function conventions explicit. Classify Forward and
Back independently before checking their conjunction against actual image equality.
For Forward maps, compare actual endpoints, labels, path edges and concatenation.
For Hom maps, retain ALL lifts of each actual target path, not only a matching word.
No bounded cyclic path corpus is claimed to be a finite category.

Dead→loop and loop→dead separate the two square inclusions. An extra target
successor is a nonvacuous Back failure. Two-label swap needs an explicit label
adapter. Equal-trace branching systems reject identifying trace and bisimulation.
Duplicate triples would be multigraph provenance, outside the relation interface.

Two Bool-output singleton systems distinguish faithful Hom forgetting from object
injectivity. Actual V26 evaluation must revive the unequal Empty join after carrier
collapse and retain failure under the structured-tag target. These fixtures check
actual bridges; they do not constitute a generic finite coalgebra implementation.

## AA3 and AA4 controls

B enumerates59 subnormalized matrices,2117 composable pairs and79401 triples
with dimensions0–2 and generating entries0,1/2,1. Exact outputs can leave that grid.
Trajectory-product sums independently check each coefficient; identities,
associativity, row bounds and normalized-slice roundtrips have separate meanings.
Zero-output Events are valid; zero-output normalized Kernels with inputs are not.

The125 two-outcome Tests and12271 pairs retain four outcome pairs per composite.
Zero matrices and nested ordered IDs remain observable even when aggregates agree.
Half-event composition produces a quarter, detecting silent conditional normalization.
Basis preparation/effect checks bind observed coefficients to actual composition.
Support equality must not imply matrix equality; use the actual V14 rational witness.
No general nonnegative Real/Rat Lean instance follows from Python Fraction checks.

## AA5 controls

C checks16 Bool relations,256 pairs and4096 triples, plus the separate five-label
counterexample. Raw exact-range regularity and typed interface retention have
independent expected values; neither can be implemented by calling the other.
For regular pairs, certify actual inclusion graphs and decode equality, including
source equality and potentially unused declared target points. For adjacent
regular triples, check lawful relational associativity at the stated interfaces.

All16 subsets of the four Bool functions receive identity and composition tests.
The two named subsets distinguish closure from possible-factor inference.
Actual V26 resource forgetting/revival retains declared costs/balances; the control
must call the inherited implementation, not repeat an expected output literal.

## Validation, certificates and custody

Validate whole inputs before semantic short circuit: every later path ID, map
entry, coefficient, label and interface member matters even after an earlier failure.
Reject bool/int and float/Fraction aliases, duplicate IDs, wrong dimensions,
negative values, oversized rows and unnormalized test aggregates.
Structural validity is not semantic correctness: mutate actual paths/lift sets,
coefficients, paired labels and task endpoints on valid real baselines.
Do not infer correctness from a well-formed seven-row ledger.

The source-keyed ledger preserves all AJ1 name/parents/native/additional/status
fields and their order. Strict schema guards reject omissions and coupled changes;
independent source/theory review assesses the new maps and scope prose.
AJ1 RESULT is historical evidence, not newly bound proof authority.
Freeze and SOURCE_PINS are immutable; inherited receipts require dereferencing
actual bound inputs. Do not bless a stale inherited receipt after source mutation.

Four source-valid Lean controls weaken all sources or the three frozen leaves:
Hom square iff Forward/Back; test aggregate plus normalization; regular-task decode.
Each changed source must compile before the exact typed audit rejects it.
Fresh proof replay, semantic finite tests and custody checks are separate layers.
They do not mechanize general Python/Lean implementation equivalence.
No unavailable or merely planned result is described as passing.
