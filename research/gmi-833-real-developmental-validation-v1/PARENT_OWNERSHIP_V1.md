# Parent ownership — read this before any result in this package

Assimilation-first: every parent below is absorbed, then the residual is stated.
Nothing here is claimed novel where a parent already owns it.

## 1. #903 `gmi-833-real-transition-receipts-v1` (merged) — the real-system bar

Owns: the fail-closed real-system receipt protocol. Its shape is copied
wholesale and is **not** claimed novel here: real trained systems on
sha256-bound real data sources; a registered rule applied to real bits as the
task, with the data real and the intervention registered; protected train/eval
splits; registered seeds; family-blind classification over opaque candidate
ids; predictions frozen in their own commit before any training code exists;
deterministic receipts reproduced from committed artifacts in CI; and the
discipline of predicting a licensed band on **both** sides (nine systems
predicted to transition, three predicted not to, all twelve correct).

**Residual here:** none on the protocol. This package is a *consumer* of it.
The two real-system rows it earns are different rows, on different laws.

DOI/citation: no external DOI; internal artifact
`research/gmi-833-real-transition-receipts-v1/FREEZE_V1.md`, blob
`43497544080d3dc63492a22cd035ba795fbec7c9` at `origin/main`.

## 2. #1018 `gmi-833-update-law-regimes-v1` (PR, unmerged) — the thresholds and UL-11

Owns: `beta*`, `chi*`, `pistar*`, `tau*`, `disc*`, `psi*`, `s*`, the seven
signature representatives, the accounting function `tuple_vector`, the
crossover-hyperplane partition UL-10b, the non-redundancy lemma UL-9, and the
frozen five-step prospective selector **UL-11**. Its own freeze pre-committed
that `Test on realistic learning systems` closes **only** on real trained
systems with sha256-bound real data sources and predictions frozen before
outcomes, following #903, and that synthetic data may not close it; the row is
listed there as expected-to-remain-open and `REAL_SYSTEM_VALIDATION` is one of
its nineteen forbidden promotions.

**Nothing about those thresholds, the selector, or the accounting is claimed
novel here.** UL-11 is reimplemented from its frozen five-step specification
solely so it can be *applied*; the pinned blob
`a5ae95b27a5de1e0508ab61330e5f54a886c8a3b` is the text that was read.

**Residual here:** the *instantiation* of the ecology objects
`E = (Z, H, prior, eps, target, train, Qset, retr, RS, nbr, Vfun, starts, Fam)`
from **real byte sources** rather than declaration, so that every threshold
input is computable from (registered design) x (real data) with no trained
artifact as input; and the first execution of UL-11 against **real trained
systems**. #1018's environments are declared; these six are derived from files
with recorded sha256. `cover` is measured here rather than hardcoded at `2*T`.

## 3. #909/#908 `gmi-833-developmental-potential-evolvability-v1` (merged) — DP-1, EV-1, HIST-1, CAPITAL-1

Owns: `DP-1` (`C_now`, `C_pot(B)`, `H_dev(B)`, budget monotonicity DP-1A,
non-identification DP-1B); `EV-1` (`Ev_Q(U) = Q(U)`) and `EV-1A`
(`P(T=k) = (1-p)^(k-1) p`, `E[T] = 1/p`, and the exact terminal
`UNREACHABLE_ZERO_USEFUL_MASS`); `HIST-1A` (history strictly lowers expected
priced discovery burden iff `a + c/pH < c/p0`, with `c = pi . r > 0`,
`a = pi . h`, both kernels on one carrier and `U` disjoint from the stored
solution set) and its four boundary terminals; and `CAPITAL-1A` with the exact
2x2 assay on `solution_capital = bool(useful ∩ stored)` and
`law_changed = (baseline != history)`, the contaminated cell failing closed to
`CANNOT_IDENTIFY_STORED_SOLUTION_CONTAMINATION`.

**No part of any of those four theorems is claimed novel here**, and none is
amended by any outcome in this package. The inequality, the 2x2, the band and
the terminals are used exactly as delivered.

**Residual here:** their first instantiation on **real** continual-learning
systems — a real body carried across a sequence of four real tasks on a
sha256-bound real stream, with `Q0`/`QH` realized as two registered proposal
laws, `p0`/`pH` as exact rational proposal masses, `c` and `h` as registered
priced step counts, the CAPITAL-1 disjointness gate **measured** by running
every stored solution on `U` with no further training, and matched POS/NEG
sequence families giving the licensed band on both sides.

## 4. #897 `gmi-833-g0-grammar-growth-v1` (merged) — the built library mechanism

Owns: `GRW-1` monotone grammar growth; `INV-1` deterministic corpus-only
invention with admission gain `o(b-1) - b - kappa > 0`, the tie order, the
three fail-closed invariants and `RECURSIVE_LIBRARY_CYCLE`; `REC-1` the
two-generation witness `m1 -> a b` then `m2 -> m1 m1`; the derived depth bound
`D_reg = S_0 = 23`; the `kappa` ablation deriving `kappa = 1`; `HLD-1`
(`50,052 -> 1,307`, `K_total = 6`, net `-48,739`, control `+1,178`); `THR-1`
(`H_eff . Delta > K`); `NULL-1` (`0/200`, with its disclosed count-metric
conditionality); and the burden identity `T3`
`B_G(w) = Phi(n, l*-1) + rank + 1`, `rank in [0, n^{l*} - 1]`.

Its reconciliation anchors **all three** of its replacements at
`# E. Universal architecture-neutral machine grammar`. Its verb is *Implement*.
It never targeted a Section L row.

**Residual here:** the *conditions*. #897 measured that a library helps; it did
not derive when admitting an operator pays its discovery and maintenance
charge, nor whether its corpus-symbol admission rule agrees with the burden
frame at all. `T3` is the identity this package rests on; the invention
mechanism, the ablation and the held-out measurement are **not** claimed novel.

## 5. #1019 `gmi-833-developmental-reuse-v1` (PR, unmerged) — concurrent prior art, disclosed

Owns: the bracket lemma `L0`; `REP-1`, the rank-free band deciding the sign of
`B1 - B0` for a representation change `G1 = G0 u L` without running the search,
with `REP-1b` its partial converse and `REP-3` its EARNED-BY-COUNTEREXAMPLE;
`REP-2`, the portfolio net in which `K_total(L)` is decisive; `NOV-1`/`NOV-2`;
and the `SD-*` search-dynamics family. Its reconciliation claims exactly three
Section L rows: representation change, dynamics comparison, and P4 growth
novelty. Its forbidden promotion #10 reads
`"No claim on 'Derive operator invention.' or 'Derive library formation.'"`.

**This package claims no part of those three rows.** No step of its derivation
section depends on #1019: the burden identity used is #897's `T3`, on
`origin/main`. #1019 is pinned at blob `a34c3ec337f90fbdfd36029e1c86d680b9ffa012`
so the text disclosed here is recoverable.

**Residual here, stated so it cannot be confused with REP-1 or REP-2:**

1. REP-1/REP-2 compare a library against **no** library. They never compare one
   **shared** library of `k` members against `k` **separate** single-member
   grammars. That comparison — `Phi(n+k, l_L)` versus `Phi(n+1, l_j)` — is what
   `Derive library formation.` asks for, including the interaction between
   members, and it is disjoint from `Phi(n', l1)` versus `Phi(n, l0)`.
2. Under #897's own `K_total(L)`, the maintenance charge is **identical** in
   both regimes, so it cancels: library-versus-separate is a **charge-free**
   decision. This is exactly where REP-2, in which `K_total` is decisive, has
   nothing to say.
3. The corpus-frame / burden-frame divergence of #897's admission rule is a
   statement about a **merged** parent that neither parent makes.

## 6. #837 `gmi-833-foundation-v1` (merged) — the contract everything is stated inside

Owns the realization contract `M = (X, x0, Q, U, Chi, rho)`, the developmental
law and `Reach_Delta(M_0, B)`, the 14-coordinate lifecycle accounting inherited
from PR #805, the scalarization results `R-1`/`R-2` (complete raw vectors are
primary; absent a prospectively frozen price vector, report Pareto sets), the
prior-disclosure tuple and `P0..P4` levels, the no-go results `PF-1`/`PF-2`, the
mandatory domain tags, the EV0-EV5 / M0-M6 ladders and the thirteen
`result_required_fields`. All of that is inherited, none of it claimed.

## 7. What this package does NOT claim

No universal best update law. No vindication of UL-11 — its own parent records
a held-out MISS, disclosed before any outcome here. No attribution of
`select_v2`'s numbers to UL-11. No claim that self-modification is favoured by
any ecology: `SIG-S`'s loss is invariant to the `mut` coordinate in the
inherited accounting, so `s* = 0` identically and any `SIG-S` cell is an
artifact of the seven-way ranking, not evidence. No general claim about
continual learning beyond the registered sequences. No open-ended evolvability.
No prediction on genuinely future task families. No frontier- or large-model
validation. No completion of Section I or Section L.
