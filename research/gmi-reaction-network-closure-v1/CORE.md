# Reaction-network closure v1

This bundle closes the three Issue #602 molecular/reaction-network tasks at
bounded finite scope: formal native semantics, exact parent compilers, and
explicit precision/latency/physical accounting.

Run:

```bash
python3 reaction_network_closure_v1.py
python3 -m unittest -v test_reaction_network_closure_v1.py
```

No unmatched physical-substrate advantage is claimed.
