tree bf6b0c90c6363b14add11e949c4ba4cf91c7fbe9
parent 592001faaf808b98176f86bcd7803aa3825add87
author Sze Chun YIU <52784502+SzeChunYiu@users.noreply.github.com> 1789336932 +0200
committer Sze Chun YIU <52784502+SzeChunYiu@users.noreply.github.com> 1789336932 +0200

Items 8 and 10 turn positive: deferred predictions held 3/3, and K2 is supported at a registered scope

Item 8 is no longer a recomputation. Three predictions were registered and
deliberately left unevaluated, and that state merged to main: no price table for
the deferred ecologies existed in any module of the sector, so no checker could
compute an answer and no edit could backfill one without changing frozen text.
The executor parses the merged document for both the price tables and the
prediction rows rather than hardcoding them. All three held:

  D-P1 D1_MEMORY_BOUND      -> MIRROR_DESCENT      (gradient step 12, mirror 3)
  D-P2 D2_ORACLE_CHEAP      -> BAYES_UPDATE        (bayes 8/7, mirror 4)
  D-P3 D3_ENUMERATION_TAXED -> ORDINAL_HILL_CLIMB  (exact search 97, climb 2)

Terminal ALL_DEFERRED_PREDICTIONS_HELD against frozen digest 38493781de864856.
Each prediction had three registered ways to fail and none occurred. Holding
confirms the registered charge table's consequences were correctly derived; it
does not show the table describes a physical cost, and it does not claim the
hosted parity workflow's status.

A parser defect found during execution is recorded rather than hidden: an
unscoped pattern read a prediction row's law cell as a price clause, the same
over-matching error as a substring classifier. Both parsers are now scoped to
their own section, with a test that feeds the wrong section in and requires a
raise.

Item 10 is run rather than argued, with the standing verdict untouched. In an
exact enumeration microworld charging one unit per candidate examined, K2 holds
on 148 of 148 held-out reuse targets for one library. Semantic reuse - the
library shortens the minimal description - is necessary: 1,707 no-reuse
library-target pairs across four libraries and two disjoint populations, zero K2
successes. It is not sufficient, rates running 45 to 100 percent, so an iff
would be false. Where reuse is absent retention is actively harmful rather than
neutral, strictly worse on 195/195, 300/300, 161/161 and 448/448 held-out cases,
because macros inflate the search alphabet with nothing to shorten. Ties are
kept as a third category; the exploratory population contains three.

An earlier syntactic classifier was corrected to a semantic one after it
mislabelled programs that are semantically equal but share no substring.

Library learning is parent-owned and no novelty is claimed for it. K2 at the
#323 grammar remains NOT_ESTABLISHED: this is a microworld result and a
falsifiable prediction about that negative, not a measurement of it.

Audit rows 49-50 added; pre-existing rows byte-identical, zero removed and two
added. The audit file is a sha256-bound capsule input and its hash is rebound in
this same commit; input count unchanged at 190.

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>
