# OCM self-application ledger — the build treated as a problem the finished machine would solve

Operator directive (2026-09-04): *treat the development of OCM like a problem the finished OCM will
tackle; play the OCM yourself; the obstructions met while building are the leads for improving it.*

Every row is written in the machine's own vocabulary: what was navigated, where the gated walk
stopped, the four-valued outcome, which lower-level dispositions were tried, what was composed,
which checker now guards it, and the parent that already owned the fix. A row whose outcome is
`OBSTRUCTION_WITNESSED` at a level above J1 is a candidate architecture change and must go through
the Jump interface (`src/ocm/kso/jump.py`), never be adopted silently. Rows are append-only.

| # | stage | task atomised | outcome | witness / lower-level dispositions | composed fix | checker | parent | J-level |
|---|---|---|---|---|---|---|---|---|
| S1 | M1 warrant | make `UNKNOWN` compose under ⊗/⊕ | `OBSTRUCTION_WITNESSED` for the single completeness bit: `P=⟨∅,incomplete⟩ ⊗ Q=⟨{{2}},complete⟩` under `R={2}` reads UNKNOWN, `∧₃` says DEAD | BUDGET: irrelevant (algebraic); WARRANT: irrelevant; RESTART: irrelevant — no re-labelling of the bit repairs it; the object itself was the wrong one | warrant **interval** `⟦L,U⟧` (exhibited / possible); liveness is then an exact Kleene homomorphism (KS-T21) | `warrant.check_three_valued_reduction` (168 intervals, 225,792 checks); ORION-V2 `kso_three_valued_warrant_exact.py` counterexample | Pawlak 1982 lower/upper approximation; Kleene 1938; Belnap 1977 | J3 (representation of the label) — proposed and adopted at M1 because the incumbent was witnessed insufficient on an exact counterexample |
| S2 | M1 revocation | prove prune–solve equivalence on random spaces | `GAP_NOT_FOUND → FAIL` at `R={1}` on seeded space #2: `v1→{v4,v3}` with `v3` DEAD; gated `v1→v4 = 5/13`, pruned `3/13` | BUDGET/WARRANT/RESTART: irrelevant; the pruned object dropped a whole edge because it touched a dead head | prune keeps the edge, deletes the dead head, **retains its original γ share** (dissipation), never renormalises survivors (KS-T04c head clause); the renormalising version is now a planted mutant | `revocation.prune_equivalence` on 30 random spaces × 7 revocations; `mutant_prune_renormalize_heads` differs | KS-T04 (contract) — the reference's independent prune already did this implicitly | J1 (local repair) |
| S3 | M1 admission | replay the inherited F4 admission cases | `FOUND` with a mismatch: reference case 1 admits a COMPOSITION head whose warrant ≠ bridge ⊗ tails, which KS-S2 forbids once the space is governed | lower level available: enforce S2 at admission (typed rejection) instead of only at genome check | `COMPOSITION_WARRANT_MISMATCH` rejection; documented tightening in `tests/m1/test_equivalence_reference.py` | `checks.check_admission_channels` | KS-S2 (#203 S2) | J1 |
| S4 | M0 → M1 migration audit | resolve the immune-system row's dependency | `GAP_NOT_FOUND:TARGET_ABSENT` — six frozen artifacts audit against a root `FAILURE_LEDGER.md` that was never migrated (a *document* dependency outside the runtime-only closure policy) | hook = acquisition channel: materialise from the frozen source | `docs/provenance/ORION_V2_FAILURE_LEDGER_42b1b0d.md` byte-bound in CI; dependency-audit policy widened to document dependencies | CI job `ledger-materialization` (blob `4394f4b6…`) | — | J0 |
| S6 | M2 extraction | recover decisive atoms the walk reaches but surprise hides | `FOUND` after revival: 47/50 (was 38/50) | BUDGET: irrelevant (walk reaches them); WARRANT: irrelevant (live); RESTART: the defect — background self-teleport counted as reaction | registered `SurpriseModel.PROPAGATED`; seed-count lemma proves the receipt's lever inert | `evaluation.m21_surprise_revival` with four pre-registered guards | PPR contribution vectors (Andersen, Chung & Lang 2006) | J1 (local repair; model is a registered parameter, default unchanged) |
| S7 | M2 extraction (theory) | test the receipt's named lever (seed-count-conditioned background) | `FOUND`: the lever is a **no-op** — a* is linear in the seed, so the expected fixed point over random seed supports of any size equals the uniform background | — | seed-count lemma (`surprise.check_seed_count_lemma`, exact over ℚ); the effective lever is removing the background's self-teleport term | KS-T28 | linearity of the Neumann series | J0 |
| S8 | M2 packaging | vendor Apache-2.0 parents into an unlicensed repository | `GAP_NOT_FOUND:TARGET_ABSENT` — no LICENSE/NOTICE; and the PEP 639 `license` string form is rejected by the pinned setuptools 75.8 | hook = acquisition (add LICENSE/NOTICE); lower level: PEP 621 table form | LICENSE + NOTICE + `license = {text = "Apache-2.0"}`; `VENDORED_SOURCE_MANIFEST_V1.json` + `tools/m2_vendor_check.py` | CI `Vendored-source drift` | — | J0 |
| S9 | M2 runtime | keep replay digests identical after an eager registry write | `FAIL`: `StaleEvidenceEpoch` on replay because `admit_evidence` mutated the registry before emitting the event | attribution: writer discipline, not the digest | the reducer is the single writer: emit (expectation = state before) then apply; outcome read back from the registry log | `test_ocm_runtime` restart tests | event sourcing | J1 |
| S10 | theory lane → runtime | prove the ≤1-day atlas gaps on the canonical objects | `FOUND` with two **runtime defects** surfaced by the proofs' mutants: (a) `compose` omitted the operator's authority factor, so two commit-bearing tails composed to commit authority (T1/MEG-04 mutant shape); (b) `navigate` iterated from `s`, whose iterates overshoot `a*`, so a budgeted FOUND was not sound (T2/MEG-06 chain-17 witness) | lower level: both are J1 repairs, no representation change | `types.internal_authority` (commit undeclared ⇒ 0) applied in compose/summarize/compose_candidate; `navigate` starts at `α·s` and reports `BUDGET_BRACKET_EXCLUDES_THRESHOLD` when the bracket decides negatively; `Meter.charge` refuses a zero delta (T8) | `tests/m2/test_theory_batch_defects.py`; ORION-V2 `kso_one_day_theorems_batch1_exact.py` | Biba/Denning; Neumann series | J1 |
| S11 | M3 interpretation | interpret "the robot opened the door" with the seed transitive construction | `AMBIGUOUS` where INTERPRETED was expected: the productive past-tense and past-participle rules both analyse *opened*, and the transitive verb slot accepted either, yielding two non-isomorphic meanings (one without TENSE) | lower level: the *construction* under-specified its slot, not the morphology | `Slot.requires` / `Slot.forbids` feature constraints (finite verb slot requires `tense`; bare-verb slots after an auxiliary forbid `tense`/`participle`; aux carries the tense) | `tests/m3/test_interpretation.py` | construction grammar (form constraints are part of the construction) | J1 |
| S12 | M3 morphology | analyse *goed* while the irregular *went* is warranted | `FOUND` with a defect: the productive rule was blocked only when the exception *matched the token*; the override law must block the productive rule for the lemma whenever the exception is LIVE | attribution: the override predicate keyed on the token instead of the lemma | live-exception set keyed on (lemma, category, features); same-analysis derivations merged as alternative support (⊕) | `tests/m3/test_lexicon.py` | inherited L0 override rule | J1 |
| S13 | M3 evaluation | first microworld receipt: transitive_adj 0/26 exact on the protected split while every other family was exact | `GAP` at construction level: the learned transitive pattern was a *flat* S-V-O token pattern with no place for a modifier; the seed pattern only worked because it hard-coded an optional adjective slot | attribution: the construction grammar lacked recursion — phrases were not constructions | `Phrase`/`Slot.phrase`: noun phrases are their own `produces="NP"` construction built bottom-up (`phrase_table`); clause constructions bind NP phrases and merge their fragments (`_arg`); the transitive family is learned over {NP, V, NP} orders with NP as a helper → 26/26 | `research/ocm-m3/M3_MICROWORLD_EVAL_V1.json` | construction grammar (recursive composition) | J1 |
| S14 | M3 evaluation tooling | the second receipt run silently reported the *stale* first receipt (the script crashed on JSON serialisation: a local `seed` dict shadowed the corpus seed string) | `CANNOT_CHECK` mis-read as a result: the receipt file predated the run | attribution: the evaluator's own custody — no freshness binding between run and file | the receipt is deleted before each run and the run's exit code is checked; renamed variable | `tests/m3` + this row | freeze-before-outcome applies to receipts too | J1 |
| S15 | M4 correction | "correction, the robot did not open the door" after two statements on the same topic | `GAP`: the correction superseded the speaker's *latest* commitment on the topic (the cat/box one), not the proposition being corrected | attribution: supersession target chosen by recency instead of by proposition identity | a correction supersedes the speaker's latest active commitment on the same canonical proposition (either polarity); topic recency only as fallback | `tests/m4/test_dialogue_runtime.py` | supersession is proposition-indexed (B5) | J1 |
| S16 | M4 clarification | a question isolating one candidate scored the same as a menu pinning all candidates; a single relevant cell never covered the interaction cost | `GAP`: the value measure counted a cell once if *any* block pinned it, so it could not rank questions, and its unit was not commensurate with cost | attribution: value defined as a cardinality instead of an expectation | value = expected number of query cells determined after the answer (uniform prior over candidates), cost in the same unit (batch-3 C1 / MEG-33) | `tests/m4/test_clarify_gate.py` | clarification value is an expectation over the ambiguity set | J1 |
| S17 | M5 regime E1 | first acquisition receipt: E1 reached 68/134 and 31/97 on the held-out lexemes while E0 reached 134/134 | `GAP`: the held-out lexemes never occur in the dev split *by construction*, so E1 had no aligned example with exactly one unknown token; the only candidates carried two unknowns (TOO_MANY_UNKNOWN) and the held-out verb was never taught | attribution: the regime's information supply, not the learner | leakage-checked teacher examples (one unknown each, excluded if present in the protected set, count and utterances recorded in the receipt); aligned verbs carry the meaning's tense → E1 115/134, held-out 78/97; the remaining gap (participle reading of *found*) is a disclosed limit | `research/ocm-m5/M5_ACQUISITION_EVAL_V1.json` | a regime is defined by its information channel; the receipt must record what was supplied | J1 |
| S5 | M1 admission (design) | compose an atom with undeclared authority | `FOUND` with a design decision: an undeclared authority coordinate is **bottom**; composing with it yields no authority (conservative, non-amplifying) | alternative (missing = top) rejected: it would let an unlabelled component launder authority | `Authority.meet` treats missing ranks as 0 | `checks.check_summary_no_authority` (explicit case) | Denning 1976 lattice flow | J0 |

## Underperforming modules — revival backlog (never tuned to outcome; improved by mechanic)

| module | evidence of underperformance | attribution (one stage) | lever | test that must move | status |
|---|---|---|---|---|---|
| reaction surprise `ρ_Q` | M2 receipt: on 12/50 dev instances the decisive live request atom scores surprise 0 and drops out of `G_Q` although live and one hop from a seed — `results/KSO_M2_SOLVE_OUTCOME_V1.md` | EXTRACT (background model), not NAVIGATE | **tried:** PROPAGATED model (propagated mass vs propagated background; the receipt's seed-count lever is a no-op by linearity — lemma checked). **Result (dev split, pre-registered, all guards held): FOUND_BY_NAVIGATION 38→47/50, misses 12→3**, hub theorem kept, translator invariance kept, no atom lost — `research/ocm-m2/M2_1_SURPRISE_REVIVAL_OUTCOME_V1.md`. Remaining 3 misses are `piece:cN` atoms in family I (two background sources vs one query source) → next lever: per-source fan-out-aware background. Default model unchanged until the M2 receipt is re-run with the model as a declared parameter | mechanic column 38/50 on the dev split; must not lose the hub witness | `POSITIVE_ON_DEV — second lever OPEN` |
| exact PCST extraction | bounded to ≤ 12 free atoms; greedy arm has no guarantee | EXTRACT | bounded-treewidth or ILP-free branch-and-bound; report approximation ratio on the instances where exact is available | tie/optimum agreement on all exact-feasible instances | `OPEN — M2` |
| navigation scale | exact rational solver is O(n³); float power iteration exists but no sparse path | NAVIGATE | **done:** `kso.navigation_sparse` (agrees with exact to 1e-14; 10⁴ atoms in 0.66 s); admission above 200 atoms uses it | 10²…10⁴ sweep landed (`research/ocm-m2/M2_SCALING_BASELINE_V1.json`); 10⁵ in progress | `POSITIVE — sparse path landed (KS-T32)` |
| RWR comparator walk | comparator is undirected while the mechanic is directed (`kso_m2_comparator_v1.py`) — a comparator mechanism gap, not a KSO advantage | PARENT | directed RWR arm added alongside, budgets matched (F8) | comparator table re-run on the dev split | `OPEN — M2` |

## How to add a row

1. Name the stage in the machine's pipeline (atomise → navigate → fire → extract → compose → check → learn → revoke/reopen → jump).
2. Give the four-valued outcome and, for an obstruction, the witness and the three lower-level dispositions (budget, warrant, restart).
3. Compose the fix as a registered object with a checker and a planted mutant.
4. Name the parent. If none exists, the row is a candidate residual — it goes to the parent-subtraction table before any claim.
5. Assign the minimum sufficient J-level; J3+ rows require a `JumpProposal` with preservation obligations and falsifiers.
