# OCM self-application ledger V2 (rows S39 onward)

`OCM_SELF_APPLICATION_LEDGER_V1.md` (rows S1–S38) is archived byte-for-byte by the v4 custody regime (`docs/provenance/runtime_revision_20260905_v4/`, `ParentSnapshot`): any change to it makes every current receipt refuse. Rows from S39 on are therefore appended here, in the same vocabulary and the same columns. Row S40 records the finding itself.

| # | stage | task atomised | outcome | witness / lower-level dispositions | composed fix | checker | parent | J-level |
|---|---|---|---|---|---|---|---|---|
| S39 | N1 phase C (first real parse of web English) | the M3 bottom-up span matcher over ~4,700 UD-derived constructions on UD EWT protected sentences | `OOM_KILLED`: the phrase table enumerates every attachment of every span (11 GB resident before the kernel killed it) — the M3 matcher was designed for a 7-construction bounded world | attribution: architecture ceiling of the matcher (D3/D7 in the M11 vocabulary: exhaustive phrase enumeration instead of a packed chart), not a defect of the induced grammar | parse bounded by declaration (≤ 8 lexical tokens, rules attested ≥ 5, time budget, address-space limit) and every skipped sentence counted as LENGTH_CANNOT_CHECK; a packed-forest chart parser with exact ambiguity counting is the next N1 obligation | a coverage number must state the parser's declared bounds; an exhaustive enumerator is not a parser for open text |
| S40 | custody v4 (this branch) | appending ledger row S39 and registry rows KS-T118–T121 to files archived by the v4 custody regime | `REFUSED`: `immutable predecessor evidence changed` from `runtime_revision_receipts_v4.ParentSnapshot` (every archived `docs/`/`research/` path must keep its archived bytes; the full suite failed 1/953 and no successor receipt could be written) | (1) declare a historical replacement — not applicable, the mechanism is for upstream merges only; (2) amend the regime — rejected, it is the lane's frozen design and the freeze is what makes the receipts addressable; (3) restore the archived bytes and continue the ledger and the registry in successor files | successor files: this ledger V2 and `OCM_SELF_OBLIGATION_REGISTRY_V2.json` (extends V1); the same pattern as ORION-V2's sealed gap atlas and its addenda file | append-only evidence under a byte seal (event sourcing; write-once archives) | file-level: no runtime change |
| S41 | N1 phase E (web English) | evidence-ranked derivation selection over the induced grammar on UD EWT | `NEGATIVE`: the ranking is right whenever the gold tree is among the unpacked derivations (10/10) and blind otherwise (10/68 ambiguous sentences); raising the chart cap to 1M items (789 MB) still leaves 22% of sentences at the cap — the obstruction is invariant under every evidence state of the registered representation (single-order families admit every attachment) | (1) rank by evidence — done, orders but does not refute; (2) raise the cap — done, moves the budget, not the outcome; (3) representation change: lexicalised attachment evidence (per-head/dependent attestations from the demonstrations) so attachments are refuted, through the Jump interface | Jump proposal for N1 phase F: `attachment evidence` as a new registered node/relation class with its own checker and mutant; until adopted, N1's terminal stays NEGATIVE and N2 stays locked | lexicalised PCFG / dependency attachment preferences (parent-owned: Collins 1997; Klein & Manning 2004) | representation-level (D3): a new evidence class, not a parser fix |
| S42 | N1 phase F (parser packing, theory batch 11 K1 ix / H4) | exact derivation counts of the packed-forest chart | `DEFECT_FOUND`: the phase C–E chart packed nodes by sub-derivation identity (one node per derivation, the predicted cause of the cap) and did not propagate a packed node's count growth after first use, so `AMBIGUOUS_COUNT_TOTAL` in `N1_UD_INDUCTION_V1.json` and `_V2_RANKING.json` is an undercount (the INTERPRETED/AMBIGUOUS verdicts and the sentence-level counts are unaffected: a total of 1 cannot arise from an undercount of ≥ 2 only when every derivation is reached, which the old keying guaranteed) | (1) pack by (span, type, lexical readings) with delta-propagated counts — done; (2) a Catalan-number fixture (1, 2, 5, 14, 42) as the exactness test — done, it fails on the old chart; (3) index waiting items and filter prediction so the exact forest costs less — done, but the exact forest is still slower per sentence than the undercounting chart (358 vs 1,606 test sentences in the 600 s budget; cap share 21% vs 25%): terminal PARSER_EXACT__THROUGHPUT_WORSE, reported as such | `tests/n1/test_chart_parser.py::test_exact_derivation_counts_follow_the_catalan_numbers`; receipt `N1_UD_INDUCTION_V3_PACKING.json` supersedes the derivation-count fields of V1/V2 | Earley packed forests with inside counts (parent-owned: Earley 1970; Billot & Lang 1989) | parser-level (D2): no representation change |
| S43 | N1 phase G (Jump proposal from S41) | attachment ambiguity of the induced category-level inventory on UD EWT | `JUMP_ADOPTED (D3, representation)`: a new evidence class ATTACHMENT — (head lemma, relation, dependent lemma) attested by demonstration sentences, with two registered backoff classes (head lemma + dependent category; head category + dependent lemma) — enters the induction receipt, and the chart takes an admit gate that REFUTES a completion whose attachment is attested at no class; nothing is ranked (batch 11 K1 ii–iv: a unique reading needs a finer class or a negative channel; this is the finer class) | (1) rank — done, S41, orders only; (2) cap/prediction — done, S42/phase F, not the cause; (3) finer evidence class with refutation — this row | checker: `tests/n1/test_ud_induction.py::test_attachment_evidence_classes_and_gate`, `tests/n1/test_chart_parser.py::test_admit_gate_refutes_completions_instead_of_ranking_them`; planted mutant: a gate that admits everything (phase F behaviour) or that scores instead of refusing; result receipt `N1_UD_INDUCTION_V4_ATTACHMENT.json` (block `protected_chart_attachment`) | lexicalised attachment preferences / bilexical dependency models (parent-owned: Collins 1997; Eisner 1996) — used as an evidence class with refutation, never as a probability | representation-level (D3): a new evidence class, adopted through this ledger; the warrant of a refuted reading is not lowered — it is not built |
