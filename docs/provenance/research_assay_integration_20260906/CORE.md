# Research assay integration qualification

**Engineering integration qualified; no new native research experiment.**

The corrected integrated source at `b95b2329d59b0b29a94a312366eb710fcbc91e60`
passed 57 mocked assay/phase/decoder controls and five original full-grammar
fixture controls, with zero failures, errors or skips. All twelve current
engineering wrappers also verified their existing source-bound receipt.

The V4 core source ID remains
`0d114c3087855886242611c623f7ccd60061c140e7b5de4033e6df5644b94948`.
V4 still covers only its historical source scope. This additive record does
not change its inventory, predecessor, selector or authority.

## Read first

- [Decoder successor](DECODER_SUCCESSOR.md): the two-line presentation fix;
  historical captures retain their original bytes and source identities.
- [Executions](EXECUTIONS.json): exact commands, exits, elapsed times and scope.
- [Historical custody audit](HISTORICAL_CUSTODY_AUDIT.json): independent byte,
  subtree, seal and eleven-commit replay checks.
- [Original fixture identities](ORIGINAL_FIXTURE_IDENTITIES.json): all five
  original task identities and exact original SyGuS hashes.

## Actual execution boundary

| Check | Executed | Failures / errors / skips |
|---|---:|---:|
| Mocked assay, phase and successor decoder controls | 57 | 0 / 0 / 0 |
| Original full-grammar request/fixture controls | 5 | 0 / 0 / 0 |
| Existing engineering wrapper verifications | 12 | all passed |

The 12 decoder controls are included in the 57, not additional successes.
The initial decoder regression reproduced exactly three failures; its separate
focused green run passed all 12. Neither run invoked native donors.

The integration pytest runs used the recorded [guard](MOCK_GUARD.json).
Their process logs contain 13 read-only Git lineage calls and one Python child
that flushes a phase event then exits. Real cvc5/Z3 imports and unlisted child
launches were refused; there were no refused attempts. **Zero native synthesis,
checker or induction executions occurred in this qualification.** Temporary
mock fixture packets were constructed by the existing tests; no registered
study was prepared, frozen or rerun.

## Exact bindings and raw records

[Before](SOURCE_BEFORE.json) and [after](SOURCE_AFTER.json) bind the complete
V4 inventory, all 3,363 tracked research files, engineering selector, Python
binary/version and dependency versions. All bindings stayed equal. Git HEAD
was the same successor for both snapshots; later record-only commits do not
change that execution identity.

The two `*.execution.json`, `*.stdout`, `*.stderr`, `*.xml`,
`*.case-inventory.json` and `*.process-audit.jsonl` groups preserve exact pytest
execution evidence. `decoder-red.*` and `decoder-green.*` retain the focused
regression. `m1-verify.*` through `m12-verify.*` retain wrapper outputs;
[the verified selector result](CURRENT_ENGINEERING_VERIFIED.json) identifies
the existing receipt. [Artifact hashes](ARTIFACTS.json) bind this record set.

Historical later-consumption and phase records keep their original source
identities and outcomes. This replay does not establish learned-method use,
causal search benefit, OCM novelty, lifetime improvement or native qualification
of the corrected successor. The separate 29-test text-task donor qualification
is owned by its own recorder and is not counted here.
