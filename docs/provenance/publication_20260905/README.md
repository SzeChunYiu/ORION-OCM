# Publication custody and engineering closeout

The connected GitHub publication path creates a new commit identity. This bundle
preserves the original local continuation through `64993a1`, including the exact
`09f8c95` runtime source anchor and the `3ab75d6` theory integration. It is an
original Git object archive, not reconstructed authorship or a replacement history.
The published snapshot and original local ancestry must not be conflated.

`HISTORY_V1.json` records the complete digest, required commits, ref and tip.
Both prerequisite commits are ancestors of the inspected remote main `5352984`.
The dedicated integration workflow verifies all these values and restores the
bundle into `refs/custody/local-continuation-20260905` before running controls.
The theory is independently checked out at ORION-V2 `b15abb4`. No editable install
runs in that job; generated package metadata is outside the packet's source scope.

The existing `REPLAY_V1.json`, source packet and historical receipts are unchanged.
A fresh CI replay is uploaded separately and compared on deterministic fields;
it cannot promote independent semantic validation, scientific truth or adoption.

For a local fresh clone, perform the restoration steps in
`.github/workflows/theory-runtime-integration.yml` before the packet's replay
instructions. Run `git bundle verify` before fetching, and retain the custody ref
so ordinary Git pruning cannot discard the source anchor.

The pre-publication full runtime suite freshly passed **944 tests** in 466.21
seconds on Python 3.12. Runtime source, tests, tools and packaging are byte-identical
to the previously reviewed local runtime. The publication adds CI and custody
artifacts outside that sealed source surface. Remote CI is separate evidence.

The merged scope is engineering repair and scoped method-theory integration.
Historical M11 adoption conclusions remain reopened and M12 requires fresh
protected reevaluation. The programme issue remains open. Follow-up evaluation
must correct unmatched transfer pairs and persistent-language grading, use
stronger matched parents, and separately establish broader learning and a real
proof-kernel route before any unsolved-mathematics claim.
