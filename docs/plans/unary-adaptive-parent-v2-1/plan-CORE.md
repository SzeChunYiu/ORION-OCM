# Adaptive unary parent: implementation entry

Status: prospective implementation plan; no tasks, selections, tests, or study outcomes were generated.
Read against assay head `c937431f3c83e46ce6a18dafbfd9b895215a2084`.
The complete observation scope and hashes are in [SOURCE-REFERENCES.json](plan-SOURCE-REFERENCES.json).

## Decision
Implement an ordinary content-addressed checked-rule library, dependency revision map,
kind postings, and direct dispatcher. Reuse the current miner, schema checker, matcher,
certificate assembler, semantic result verifier, and cached RegionSolver.
Use the existing generic LedgerStore as a conventional durable log; do not require the
parent to reproduce KSO objects, navigation, registered operators, or two-ledger admission.
This is the strongest matched adaptive parent within the supplied unary fragment,
not a claim to have surveyed or beaten every conventional prover.

Both adaptive arms physically acquire and select from identical training/development
inputs in their own process A. Compare candidate-pool and selected-library digests before
paired use. Charge each actual execution to its arm; no duplicated accounting of one run.
Store both training discovery and development selection receipts separately from the
universal schema certificate and current answer warrant.

Keep one RegionSolver per exact ordered predicate registry in each arm process.
Re-prepare every task; retain ordinary expression-mask caches across that process's queries.
Use identical task order and cold/warm boundaries. Never complete a hidden query before
attempting the learned route. No premise-only or persisted-cache extension is needed here.

The fixed 32 training tasks have two–three premises. Only two-premise proper fragments
are reachable in this assay. Three-premise software support remains outside its claims.
Two discovery supports mean distinct training semantic keys, not merely renamed syntax.

## Work order
1. Implement the shared proposal/check boundary and matched warm engine lifetime.
2. Add the shared real-selection acquisition boundary and two persistence adapters.
3. Add conventional direct use, revision, and fixed-profile A/B/C process support.
4. Qualify authored controls before any registered task generation or scientific use.

[IMPLEMENTATION.md](plan-IMPLEMENTATION.md) fixes the minimal APIs and source changes.
[CONTROLS.md](plan-CONTROLS.md) fixes authored falsifiers, process evidence, and cost scopes.

## Current contract and remaining qualification
Root's prospective unary-assay-spec-v2/SELECTION.md now resolves the selector:
rank positive summed query-constraint reduction, then matching work, inner bytes, rule_id;
select at most eight, route by rule_id. Development uses fresh engines per trial;
service batches share warm expression caches. Both adaptive A runs physically repeat acquisition.
EXECUTION.md fixes B/C, role interventions and cost allocation. Exact observed hashes are recorded.
These decisions still require independent spec clearance, authored implementation qualification,
and the final source/launcher/analysis seal. There is no remaining parent algorithm choice here.
The current authored-selected.v1 path cannot stand in for measured selection.
Generator implementation and actual study execution remain outside this implementation tranche.
