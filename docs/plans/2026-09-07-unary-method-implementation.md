# Unary learned-rule tranche implementation plan

> **For Codex:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** expose an exact prepared parent and acquire checked universal certificate
rules from permitted mathematical episodes, then match/assemble fresh proposals.
**Architecture:** immutable session-bound preparation; plain rule data; independent
pointwise schema checker; training-only semantic identity; generic proper-subcover
mining and structural matching. No persistent store, OCM dispatcher or scored run.
**Tech:** existing Python3.11 stdlib, pytest on billy-laptop only.

1. Add failing prepared-state and counter/equivalence controls in
   `research/math-language-v1/test_unary_prepared.py`.
2. Refactor only `unary_solver.py`: prepare once; reuse universal aggregate and
   existential obligations; preserve solve/result/counter contracts; refuse forged,
   stale, cross-engine or changed prepared state before use.
3. In `research/math-language-learning-v1/` add failing authored tests, then
   `unary_rule_contract.py`, `unary_rule_check.py`, `unary_rule_identity.py`,
   `unary_rule_acquire.py` and `unary_rule_apply.py`; keep each file below200lines.
4. Extract only proper universal subcovers of independently checked training
   results; require two different semantic tasks, never renamed demonstrations.
   Enumerate all255 nonempty worlds for three predicates; this corrects127 in the
   historical external proposal. Generalization needs its separate schema proof.
5. Test substitution, quantifier polarity, witness misuse, missing support,
   metadata tamper, canonical/semantic duplicates, all-true-region distinctions,
   real matching and constructed certificates, and no hidden complete solve.
6. Preserve targeted RED/repair generations and exact source/command/raw/XML/cost
   records outside the worktree. Run the combined package suite once after fixes.
7. Read/review current bytes, document limits, commit owned files only. No push.

Commands from the worktree root, with both package paths on PYTHONPATH:
`<engineering-python> -m pytest -q <targeted-new-tests> --basetemp=<new-external>/cases --junitxml=<new-external>/tests.xml`
Final combined command selects `research/math-language-v1 research/math-language-learning-v1`.
The recorder freezes Python sources before/after and keeps all failed generations.

The new contract clarifies that training-only equivalence computation is permitted
and charged; evaluator-held final fingerprints never enter acquisition. A later
used-support intervention invalidates one exact binding, not legitimate alternates.
The original external design and all earlier semantic receipts remain historical.
