# Current A/E comparator correction

Scope: issue #38 and execution programme #115, R0. This is an engineering
correction on exposed tasks. It establishes no new protected result or
whole-system adequacy.

## Adopted mechanisms

The default lifetime parent uses conventional JSON records plus the same
bounded lexicon, morphology, constructions, world-question parser, lexical
lesson parser and interpreter as OCM. Its records retain named-speaker
attribution, polarity, same-proposition correction lineage, separate repeated
lessons and lexical revocation/reinstatement. Language session restart preserves
those records. The parent constructs no OCM runtime or ChatSession.

Report IDs and observation support identities are separate. The existing OCM
donor content-addresses attributed observations: repeating identical input bytes
shares support; a distinct paraphrase does not. The parent matches this policy
with an ordinary content key, including supersession lineage. Withdrawing one
observation invalidates every report backed by that key; replaying its identical
bytes after restart does not silently reinstate it.

Both lifetime Phase A entry points forward named speakers to both arms.
Phase E gives the current parent the same explicit transfer maps, role contracts,
operators and task checker as OCM. Fixed role-contract checking is an adopted
donor, not an OCM-specific capability. The science donor checks declared roles;
it does not prove the semantics of a transferred program.

The old regex/similarity parent remains available as LegacyWholeSystemParent;
its four-case transfer route is an ablation. Frozen historical receipts remain
unchanged.

## Boundaries

- Lexical lessons and bounded interpreted statements/questions are covered.
- Topic-only correction, contextual reference, clarification answers, spelling
  interactions, construction/aligned teaching, planning/style, memory commands,
  nonlexical revocation and arithmetic-method chat routes are not qualified.
  Unsupported attempts return CANNOT_CHECK in last_frontend; cumulative
  unsupported-route counts survive language session restart.
- An initial ambiguity can share the donor's candidate proposal. Full contextual
  clarification-policy parity is not established.
- Work acquisition/routing, science, domain-wide revision and repair retain
  historical comparator gaps. Whole-system parity is CANNOT_CHECK.
- Restart here means reopening the language session while the lifetime wrapper
  remains alive; persistent reconstruction of every work/science component in a
  fresh process is outside this correction.
- No current comparison inherits an earlier source qualification receipt.

## Qualification

tests/m12/test_current_comparator_parity.py exercises named reports, opposed
speakers, correction, role reversal, lessons, restart, revocation, reinstatement,
relearning, equal semantic meanings, equal supplied transfer contracts,
unsupported-route accounting and runtime independence. A failed transferred
execution cannot be scored as TRANSFER.

The initial seven controls on unmodified main 179ad15 produced six failures
and one passing OCM lexical lifecycle control. New implementation tests and the
related M3–M6/M10/M12 suites are run on laptop billy. This document is a scope
contract; final-head CI remains the publication gate.
