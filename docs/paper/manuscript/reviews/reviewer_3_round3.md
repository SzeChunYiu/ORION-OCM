# Reviewer 3, round 3 (reproducibility, artifacts, release)

Blind to the other round-3 reports. Claim verification: 281 rows OK once the phase-F receipt is on main (two rows read it). Release binding: 97 files, receipt PACKAGE_BOUND__IDENTIFIER_PENDING, workflow green. **R3-r3-1 (major, release action):** the custody regime changed under the manuscript (engineering revisions recorded on the laptop; twelve wrappers verify-only). The data-availability statement should name the current engineering pointer file and say that the historical V4 successors are frozen, so a reader can locate what CI verifies today. **R3-r3-2 (minor):** the two N1 receipts whose derivation totals are undercounts (V1, V2) are cited by the claims map only for sentence-level fields; add a one-line note in `claims_map.md` naming the superseded fields so a future reader does not read the totals. **R3-r3-3 (unchanged):** archived release with a persistent identifier remains a pre-submission action.

## Recommendation
Minor revision; one release-facing sentence and one claims-map note.
