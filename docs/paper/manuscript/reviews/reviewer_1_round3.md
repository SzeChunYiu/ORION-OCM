# Reviewer 1, round 3 (validity, methods, data, inference)

Written against draft V3.4 with the V5 study, the acceptance lane's authority correction, the N1 phase E and F results and the theory batches 9–13 in the text; blind to the other round-3 reports.

## Assessment

**V5 (5.9).** The pre-registration is frozen before the run in the repository record, the manifest regenerates identically, the replication is byte-identical and the primary family rejects with three distinct differences; the categorical families are named as such and never tested. The manuscript now also reports the acceptance lane's correction — current scientific promotion NOT_ESTABLISHED — beside the pre-registered decision. That is the right disclosure. **R1-r3-1 (major, closable by wording):** the sentence "V5 therefore replaces V4 as the study of record" is stronger than the two labels together support; say that V5 is the pre-registered study on the corrected runtime and that the repository's acceptance ledger has not promoted it, and let the reader weigh both. **R1-r3-2 (major, not closable by editing):** batch 9's attribution theorem (FDX-15) makes the ablation arm OCM−Δ a condition for attributing the residual to the declared difference; V4 and V5 declare it and do not run it. The manuscript must say that the residual is over the matched parent and that its attribution to the machine's epistemic layer is declared, not ablated — this is a scope statement, and it belongs in 5.9 and in the limitations.

**N1 (5.10).** The phase E and F paragraphs are honest negatives with exact numbers and the undercount is disclosed. Accept. **R1-r3-3 (minor):** give the exact derivation total once as an order of magnitude only; the manuscript already does so.

**Theory loop (8).** The counts come from the field map's checker. Accept.

## Recommendation
Minor revision on wording (R1-r3-1) and one scope statement (R1-r3-2), which changes no number.
