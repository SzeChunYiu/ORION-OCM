# Positioning against contemporary systems (section draft for merge into main.md; closes R2-1)

Draft 2026-09-05. Citations are author–year keys resolved in positioning_refs.md, where every entry was verified online this round. Every difference stated below names the receipt field that supports it; no sentence claims novelty or superiority, and where a parent family was not built or not run the text says so. The stance is assimilation-first: each parent family is named for what the OCM takes from it.

## 1.1 Related systems

**Language-model agents with tools, memory and self-reflection.** ReAct interleaves reasoning traces with tool calls (Yao et al. 2023); Toolformer teaches a model to insert tool calls by self-supervision (Schick et al. 2023); Reflexion keeps verbal self-critiques in an episodic buffer and retries (Shinn et al. 2023); Self-Refine iterates on the model's own feedback (Madaan et al. 2023); Voyager grows a library of verified skill programs from environment feedback (Wang et al. 2023); Generative Agents and MemGPT give an agent a long-term memory with retrieval and paging (Park et al. 2023; Packer et al. 2023). The OCM takes from this family the operator-with-role pattern, the skill library with per-domain adapters (Section 3.2), memory that survives restart, and retry after a diagnosed failure. The difference the receipts support is where authority sits: in these systems the reflection text, the retrieved memory and the adopted skill are accepted by the agent itself, whereas in the OCM feedback is admitted with the certified-zero interval and a self-change is adopted only by an external adopter. The reflection-retry parent built here in that style repaired 1 of 7 planted faults and the parameter-search parent 2 of 7 (`docs/provenance/M11_RECEIPT_V1.json`, `deterministic_results.summary.parent_reflection_retry_solves`, `parent_parameter_search_solves`), values now under a reopened gate (Section 5.8). No frontier language-model agent was matched: the frontier target is `CANNOT_CHECK_FRONTIER_TARGET` (`M10_RECEIPT_V1.json`, `deterministic_results.external.frontier_target`), five external agent benchmarks are `CANNOT_CHECK` (`M9_RECEIPT_V1.json`, `deterministic_results.external_benchmarks`), and the local Qwen2.5 reference (Qwen Team 2024) is bound as `UNBOUND_PRETRAINING (F8): reference only` with 0 of 20 honest unknowns (`M12_REFERENCE_RECEIPT_V1.json`, `information_binding`, `summary.honest_unknown`).

**Neurosymbolic program synthesis and library learning.** DreamCoder alternates wake and sleep phases to grow a library of reusable abstractions under a description-length objective (Ellis et al. 2021); Stitch makes the compression step a top-down search (Bowers et al. 2023); LILO adds a language model that names and documents the learned abstractions (Grand et al. 2024). The OCM takes the library as the parent object for consolidation and the two-part code as the keep-or-split rule, recorded as parent-owned in theory batch 7 (G4). What the OCM adds is a liveness gate: a macro's warrant is the conjunction of its exported constituents (KS-T23), so a summary over dead children is dead, and exceptions count only while live. The receipt supports only the gate, not any library-learning advantage: in every organisation cell the count of live macros over dead children is 0 (`M8_RECEIPT_V1.json`, `deterministic_results.worlds.*.*.macro_live_over_dead_children`), and the study terminal is `M8_PARENT_SUFFICIENT_AT_THIS_SCALE` (`terminal`). A DreamCoder-class library learner was not built as a parent.

**Truth maintenance, belief revision, provenance and knowledge graphs.** The justification-based and assumption-based truth maintenance systems (Doyle 1979; de Kleer 1986) own dependency-directed retraction and antichain labels; AGM contraction and revision (Alchourrón et al. 1985) and its iterated form (Darwiche & Pearl 1997) own revision of belief sets; provenance semirings, why-and-where provenance and the PROV data model (Green et al. 2007; Buneman et al. 2001; Moreau & Missier 2013) own the algebra and vocabulary of derivation records; knowledge graphs (Hogan et al. 2021), Wikidata's referenced statements (Vrandečić & Krötzsch 2014), Cyc (Lenat 1995) and NELL's never-ending learner with periodic human correction (Mitchell et al. 2018) are the running systems nearest to a persistent typed store. The OCM's KnowledgeSpace is an ATMS label lifted to a lower–upper interval, composed by the provenance semiring, revised by the over-deletion and changed-derivability sets of incremental materialisation (Gupta et al. 1993; Motik et al. 2019), with an authority coordinate that AGM does not type. The receipts support the reopening behaviour, not a comparison with these systems: on the dialogue microworld corrections reopened 9 of 9 dependent answers and 0 of 51 user assertions became machine belief (`M4_RECEIPT_V1.json`, `deterministic_results.dialogue_eval.correction.dependent_answer_reopened`, `epistemic_integrity.assertion_to_belief_leakage`), and in the V4 lifetimes the revision-integrity family reads 1.000 against 0.000 in 8 of 8 lifetimes as a secondary family flagged collapsed one coin (`M12_PAIRED_RECEIPT_V4.json`, `deterministic_results.v4.tests.F_integrity`).

**Continual and lifelong learning and its evaluation.** Catastrophic interference (McCloskey & Cohen 1989) and its regularisation and replay remedies (Kirkpatrick et al. 2017; Lopez-Paz & Ranzato 2017), the survey vocabulary of forgetting, transfer and intransigence (Parisi et al. 2019), the continual reinforcement-learning review (Khetarpal et al. 2022) and task-sequence benchmarks such as Continual World (Wołczyk et al. 2021) fix how a lifelong learner is measured. The OCM takes the measurement vocabulary directly: new gain, old loss and unrelated change after each acquisition, forward transfer as acquisition cost, and orderings of a task sequence. The difference the receipts support is what the vocabulary measures: OCM atoms are append-only, so loss is possible only through revocation, and the reported quantity is retention plus the exact reopening set. After the demonstration regime new gain was 93 of 112 with 0 of 22 old loss and 0 of 22 unrelated change (`M5_RECEIPT_V1.json`, `retention_after_E1`); the later work domain cost 7 operator applications with transfer against 12 for the fresh-start, trajectory-memory and skill-library parents (`M9_RECEIPT_V1.json`, `summary.*.mean_later_domain_acquisition_cost`), a descriptive value under the minimum n. None of the neural continual-learning methods was a parent, because no arm here has trainable weights, and no continual-learning benchmark was run.

**Matched comparison, pre-registration and equivalence testing.** Pre-registration (Nosek et al. 2018), the NeurIPS reproducibility checklist and its follow-up report (Pineau et al. 2021), budget-matched reporting (Dodge et al. 2019), tuned baselines (Melis et al. 2018), variance accounting and small-sample interval estimates (Henderson et al. 2018; Bouthillier et al. 2021; Agarwal et al. 2021), leakage audits (Kapoor & Narayanan 2023), exact paired tests and their pitfalls (McNemar 1947; Dietterich 1998; Demšar 2006), equivalence by two one-sided tests (Schuirmann 1987; Lakens 2017) and the pseudoreplication warning (Hurlbert 1984) are the parents of Section 4; theory batch 4 (D1) and batch 6 (F2) adopt them and record them as parent-owned. The OCM adds engineering, not method: the pre-registration hash is bound into the receipt before any protected outcome is read (`M7_RECEIPT_V1.json`, `study.preregistration_sha256`; `M12_PAIRED_RECEIPT_V4.json`, `deterministic_results.preregistration_sha256`), the parent is matched in acceptance discipline as well as information (ledger row S27), and equivalence names its scale (row S28). The one inferential value these parents license is the primary family of V4, 8 of 8 lifetimes at one-sided p = 0.0039, replicated (`M12_PAIRED_RECEIPT_V4.json`, `deterministic_results.v4.tests.A_conversations`; `M12_PAIRED_REPLICATION_RECEIPT_V4.json`).

**Formal verification and autoformalisation.** Lean 4 (de Moura & Ullrich 2021) is the kernel; miniF2F is the cross-system benchmark (Zheng et al. 2022); GPT-f, LeanDojo's retrieval-augmented prover, autoformalisation with language models and Lean Copilot are the systems that search for proofs or translate statements into the kernel's language (Polu & Sutskever 2020; Yang et al. 2023; Wu et al. 2022; Song et al. 2024). The OCM takes the kernel verdict as its only exact-checker channel and takes from the autoformalisation line the observation that translation is a separate failure point. The difference the receipts support is a typed one: the kernel warrant and the formalisation-correspondence warrant are stored apart, so a mistranslated statement that passes the kernel is dead on the correspondence side (`M10_RECEIPT_V1.json`, `deterministic_results.proof.mistranslation_pass_with_dead_correspondence`), and the kernel returned 8 of 8 correct verdicts on a propositional suite (`deterministic_results.proof.kernel_correct`). The Lean 4 route and miniF2F are `CANNOT_CHECK (no Lean toolchain)` (`deterministic_results.external.miniF2F/Lean4`), so no comparison with any prover exists here.

**Grounded language learning and grammar induction.** Identification limits from positive text (Gold 1967; Angluin 1980), unsupervised induction of constituency and dependency (Klein & Manning 2004; Kim et al. 2019) over treebanks such as Universal Dependencies (de Marneffe et al. 2021), CCG induction from sentences paired with logical forms (Zettlemoyer & Collins 2005; Kwiatkowski et al. 2010), synchronous hyperedge-replacement grammars for graph semantics (Peng et al. 2015; Chen et al. 2018), grounded construction grammar (Steels 2004), interactive language games (Wang et al. 2016), the grounding programme (Bisk et al. 2020) and the BabyLM sample-efficiency challenge (Warstadt et al. 2023) are the parents of Sections 3.1 and 5.2. The OCM takes the construction inventory learned from aligned pairs, the meaning graph, the constructivist grounding loop and the interaction game. What the receipts add is the per-example warrant: revoking the single demonstration that taught the transitive construction reopened that construction and left the passive intact (`M3_RECEIPT_V1.json`, `revocation_locality`), and a raw corpus produced 88 form hypotheses and 0 consultable meanings (`M5_RECEIPT_V1.json`, `E2_raw_corpus.forms`), the outcome Gold's limit predicts. The scale gap is the boundary: coverage of BLiMP (Warstadt et al. 2020) and of the UD English Web Treebank under the frozen protocol is 0 (`research/ocm-m7/M7_BLIMP_UD_V1.json`, `blimp.*.covered`, `ud_ewt.genres.*.interpreted`), and BabyLM was not pinned, so none of these learners was compared on natural data.

**What the positioning does not claim.** Across the seven families the parents own the mechanisms and the measurement vocabulary; the OCM's contribution is their coupling under one warrant algebra, checked by exact checkers and planted mutants, and one replicated residual over a parent assembled from the same components with the same information. The strongest parents not built are named for the next study: a language-model agent with a bounded information channel, a DreamCoder-class library learner, a weight-based continual learner, and a CCG or SHRG learner on natural data.
